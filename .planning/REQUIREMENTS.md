# Requirements: CodeMap

## Current Planning Surface

No active milestone. `v2.1 ux-onboarding-enhancement` requirements are archived at `.planning/milestones/v2.1-REQUIREMENTS.md`.

## Historical Notes

- v2.1 closed with one accepted requirement gap: `INI-01` (`mycodemap init --json`)
- Cosmetic follow-up `F-1` is tracked in `.planning/STATE.md`

## Next Step

Use `$gsd-new-milestone` to define the next requirement set when planning resumes.
