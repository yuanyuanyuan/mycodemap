---
phase: 1000-evaluate-arcadedb-as-alternative-to-k-zudb
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-EVIDENCE.md
  - .planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-ARCHITECTURE-IMPACT.md
  - scripts/experiments/arcadedb-http-smoke.mjs
autonomous: true
requirements:
  - ARC-01
  - ARC-02
  - ARC-03
must_haves:
  truths:
    - 证据包明确写出 ArcadeDB Embedded = JVM only，Node 可行路径来自 client/server 协议
    - 有界实验通过官方 HTTP/JSON API 验证 Node 接入，不修改公开 storage surface
    - 架构影响文档明确量化配置、schema、文档与 fallback blast radius
  artifacts:
    - path: .planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-EVIDENCE.md
      provides: 官方支持矩阵与错误前提清单
      contains: "Embedded | JVM only"
    - path: .planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-ARCHITECTURE-IMPACT.md
      provides: 正式接入 blast radius 分析
      contains: "Blast Radius"
    - path: scripts/experiments/arcadedb-http-smoke.mjs
      provides: 隔离 HTTP smoke experiment
      contains: "ARCADEDB_HTTP_URL"
  key_links:
    - from: 1000-EVIDENCE.md
      to: scripts/experiments/arcadedb-http-smoke.mjs
      via: preferred HTTP experiment path
    - from: 1000-ARCHITECTURE-IMPACT.md
      to: src/cli/config-loader.ts
      via: current public config restrictions
    - from: 1000-ARCHITECTURE-IMPACT.md
      to: mycodemap.config.schema.json
      via: required server-backed fields
---

<objective>
建立 ArcadeDB Node 接入的官方证据基线、隔离 smoke experiment 和正式接入 blast radius 分析。

Purpose: 先证伪错误前提，再用最小可运行实验验证官方支持路径，避免继续按“Node embedded adapter”伪实现。
Output: `1000-EVIDENCE.md`、`1000-ARCHITECTURE-IMPACT.md`、`scripts/experiments/arcadedb-http-smoke.mjs`。
</objective>

<execution_context>
@/data/codemap/.claude/get-shit-done/workflows/execute-plan.md
@/data/codemap/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-CONTEXT.md
@.planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-RESEARCH.md
@.planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-VALIDATION.md

## 关键 repo 约束
- `src/interface/types/storage.ts` 当前只公开 `filesystem` / `kuzudb` / `memory`
- `src/cli/config-loader.ts` 当前只接受 `type / outputPath / databasePath / autoThresholds`
- `src/cli/config-loader.ts` 显式拒绝 `uri / username / password`
- `mycodemap.config.schema.json` 当前只为本地 backend 暴露 `databasePath`

## 官方接入事实
- `https://arcadedb.com/embedded.html`：Embedded = `JVM languages` + `Java API`
- `https://arcadedb.com/client-server.html`：Node 侧可走 HTTP/JSON 或 Bolt 协议

## 执行约束
- 本 plan 不得修改 `storage.type`、schema、README 或 public CLI contract
- 所有结论必须回写到 phase artifact，不允许只留在终端输出里
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create official support evidence pack</name>
  <files>.planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-CONTEXT.md
    - .planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-RESEARCH.md
    - src/cli/config-loader.ts
    - mycodemap.config.schema.json
  </read_first>
  <action>
创建 `1000-EVIDENCE.md`，必须包含以下固定结构和精确内容：

1. 标题：`# Phase 1000 Evidence Pack: ArcadeDB Node Support`
2. `## Official Support Matrix` 表格，至少包含三行：
   - `Embedded | JVM only | Java API | Not usable from current Node runtime`
   - `HTTP/JSON | Client/server | Any language with HTTP support | Preferred experiment path`
   - `Bolt | Client/server | Official Neo4j drivers | Secondary experiment path`
3. `## Invalid Assumptions Removed` 列出以下两条：
   - `No import('arcadedb') assumption`
   - `No Node embedded mode assumption`
4. `## Repo Friction Points` 至少点名：
   - `src/interface/types/storage.ts`
   - `src/cli/config-loader.ts`
   - `mycodemap.config.schema.json`
5. `## Source Links` 包含：
   - `https://arcadedb.com/embedded.html`
   - `https://arcadedb.com/client-server.html`

文档必须直接写清楚：Node.js 当前**不能**按原计划实现 embedded `ArcadeDBStorage`。
  </action>
  <acceptance_criteria>
    - `1000-EVIDENCE.md` contains `Embedded | JVM only`
    - `1000-EVIDENCE.md` contains `HTTP/JSON | Client/server`
    - `1000-EVIDENCE.md` contains `Bolt | Client/server`
    - `1000-EVIDENCE.md` contains `No import('arcadedb') assumption`
    - `1000-EVIDENCE.md` contains `https://arcadedb.com/embedded.html`
  </acceptance_criteria>
  <verify>
    <automated>grep -n "Embedded | JVM only" .planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-EVIDENCE.md && grep -n "HTTP/JSON | Client/server" .planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-EVIDENCE.md</automated>
  </verify>
  <done>官方支持矩阵和错误前提清单已固化到 `1000-EVIDENCE.md`</done>
</task>

<task type="auto">
  <name>Task 2: Quantify first-class ArcadeDB blast radius</name>
  <files>.planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-ARCHITECTURE-IMPACT.md</files>
  <read_first>
    - src/interface/types/storage.ts
    - src/cli/config-loader.ts
    - mycodemap.config.schema.json
    - README.md
    - docs/ai-guide/COMMANDS.md
  </read_first>
  <action>
创建 `1000-ARCHITECTURE-IMPACT.md`，必须包含以下固定结构：

1. `## Current Public Surface`
   - 写明当前 `storage.type` 只支持 `filesystem / kuzudb / memory / auto`
   - 写明当前 schema 只暴露 `databasePath`
   - 写明当前配置解析器拒绝 `uri / username / password`
2. `## Required New Surface For First-Class ArcadeDB`
   - 精确列出候选新增字段：`uri`、`database`、`username`、`password`、`protocol`、`tls`
3. `## Blast Radius`
   - 用表格至少覆盖 `Type Surface`、`Config Parser`、`Schema`、`User Docs`、`AI Docs`、`Fallback/Error Handling`
4. `## Isolation Strategy`
   - 明确写出 `Phase 1000 must not modify these files directly`
5. `## Recommendation`
   - 明确写出 `Do not productize arcadedb in Phase 1000`

文档必须把 server-backed ArcadeDB 视为产品面变化，而不是简单 adapter 替换。
  </action>
  <acceptance_criteria>
    - `1000-ARCHITECTURE-IMPACT.md` contains `## Blast Radius`
    - `1000-ARCHITECTURE-IMPACT.md` contains `uri`
    - `1000-ARCHITECTURE-IMPACT.md` contains `username`
    - `1000-ARCHITECTURE-IMPACT.md` contains `docs/ai-guide/COMMANDS.md`
    - `1000-ARCHITECTURE-IMPACT.md` contains `Do not productize arcadedb in Phase 1000`
  </acceptance_criteria>
  <verify>
    <automated>grep -n "## Blast Radius" .planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-ARCHITECTURE-IMPACT.md && grep -n "uri" .planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-ARCHITECTURE-IMPACT.md</automated>
  </verify>
  <done>正式接入 ArcadeDB 所需的配置与文档 blast radius 已量化</done>
</task>

<task type="auto">
  <name>Task 3: Add isolated ArcadeDB HTTP smoke script</name>
  <files>scripts/experiments/arcadedb-http-smoke.mjs</files>
  <read_first>
    - package.json
    - .planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-EVIDENCE.md
  </read_first>
  <action>
创建 `scripts/experiments/arcadedb-http-smoke.mjs`，使用 Node 原生能力，不导入项目 runtime 代码，也不引入新依赖。脚本必须实现以下具体行为：

1. 支持 `--help`：打印必需环境变量并退出 0
2. 必需环境变量：
   - `ARCADEDB_HTTP_URL`
   - `ARCADEDB_DATABASE`
   - `ARCADEDB_USERNAME`
   - `ARCADEDB_PASSWORD`
   - `ARCADEDB_CYPHER`（可选）
3. 默认 Cypher：
   - `MATCH (n) RETURN count(n) AS total LIMIT 1`
4. 发送请求到：
   - `${baseUrl}/api/v1/command/${database}`
5. 请求体必须是：
   - `{ "language": "cypher", "command": command }`
6. 认证方式：
   - `Authorization: Basic ...`
7. 失败行为：
   - 缺失 env 时输出明确错误并退出非零
   - 非 2xx HTTP 状态输出状态码与响应体并退出非零
   - 成功时打印 `ArcadeDB HTTP smoke succeeded`

脚本必须可通过 `node --check`，且允许在无服务环境下用 `--help` 做离线验证。
  </action>
  <acceptance_criteria>
    - `scripts/experiments/arcadedb-http-smoke.mjs` contains `ARCADEDB_HTTP_URL`
    - `scripts/experiments/arcadedb-http-smoke.mjs` contains `/api/v1/command/`
    - `scripts/experiments/arcadedb-http-smoke.mjs` contains `MATCH (n) RETURN count(n) AS total LIMIT 1`
    - `scripts/experiments/arcadedb-http-smoke.mjs` contains `ArcadeDB HTTP smoke succeeded`
    - `node --check scripts/experiments/arcadedb-http-smoke.mjs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/experiments/arcadedb-http-smoke.mjs</automated>
  </verify>
  <done>有界 HTTP smoke experiment 已可离线校验语法并等待真实服务验证</done>
</task>

</tasks>

## Verification

Run these commands to verify Wave 1 completion:

```bash
grep -n "Embedded | JVM only" .planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-EVIDENCE.md
grep -n "## Blast Radius" .planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-ARCHITECTURE-IMPACT.md
node --check scripts/experiments/arcadedb-http-smoke.mjs
```

## Notes

- 这是 backlog research 的 Wave 1；目标是固化事实，不是提前 productize `arcadedb`
- 若 Wave 1 无法证明最小 Node path 存在，Wave 2 必须输出 `NO-GO`
- 不得在本 plan 中顺手修改 `storage.type`、schema 或 README

---

After completion, create `.planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-01-SUMMARY.md`
