# Roadmap Archive: v1.6 CodeMap CLI dogfood reliability hardening

**Archived:** 2026-04-19
**Status:** ✅ SHIPPED 2026-04-18
**Phases:** 25
**Total Plans:** 3

## Archive Note

This archive freezes the `v1.6` milestone after root planning truth had already advanced to `post-v1.6`.
The canonical evidence source is the combination of:

- `.planning/MILESTONES.md` `v1.6` section
- `.planning/milestones/v1.6-phases/25-fix-codemap-cli-dogfood-gaps-hard-fail-analyze-find-scan-err/25-01-SUMMARY.md`
- `.planning/milestones/v1.6-phases/25-fix-codemap-cli-dogfood-gaps-hard-fail-analyze-find-scan-err/25-02-SUMMARY.md`
- `.planning/milestones/v1.6-phases/25-fix-codemap-cli-dogfood-gaps-hard-fail-analyze-find-scan-err/25-03-SUMMARY.md`
- `.planning/milestones/v1.6-phases/25-fix-codemap-cli-dogfood-gaps-hard-fail-analyze-find-scan-err/25-VERIFICATION.md`
- `.planning/milestones/v1.6-phases/25-fix-codemap-cli-dogfood-gaps-hard-fail-analyze-find-scan-err/25-VALIDATION.md`
- `.planning/v1.6-MILESTONE-AUDIT.md`

## Overview

`v1.6` 收口的是 2026-04-17 eatdogfood 直接暴露的 Agent-facing CLI reliability gap：`analyze -i find` 会在 scanner failure 时静默返回“看起来成功”的空结果，相邻的 `complexity` / `ci assess-risk` / `workflow start` 也缺少稳定的 machine-readable status truth。  
本 milestone 的交付目标不是重做整套 CLI，而是把最阻塞 Agent dogfood 的输出契约与文档真相先固定下来。

## Phases

- [x] **Phase 25: CodeMap CLI dogfood reliability hardening** - 收敛 `analyze find` 静默失败、config-aware scanning 漂移、相邻 JSON contract 缺口与 AI docs truth

### Phase 25: CodeMap CLI dogfood reliability hardening

**Goal**: 基于 `docs/exec-plans/completed/2026-04-17-eatdogfood-codemap-cli.md` 修复 `analyze -i find` 的静默失败风险，并把短期/中期/长期建议收敛成可验证的 CLI 契约改进。  
**Depends on**: `docs/exec-plans/completed/2026-04-17-eatdogfood-codemap-cli.md`  
**Requirements**: `P25-SC1`、`P25-SC2`、`P25-SC3`、`P25-SC4`、`P25-SC5`、`P25-DOGFOOD-COMPLEXITY`、`P25-DOGFOOD-CI-RISK`、`P25-DOGFOOD-WORKFLOW`、`P25-DOCS`  
**Status**: Complete on 2026-04-18 after verification  
**Plans**: 3 completed plans

**Success Criteria** (what must be TRUE):
1. `analyze -i find -k SourceLocation --json --structured` 这类底层扫描失败场景不再伪装成可信 0 结果，必须返回显式 warning / failure / `partialFailure` 诊断之一
2. `analyze find` 的 include/exclude 与 TypeScript 解析策略经过核对，不能绕开 `generate/query` 已使用的配置感知扫描边界
3. JSON 消费方只读 stdout 时，也能区分“真实 0 命中”和“扫描链路部分失败”
4. 相邻 dogfood CLI 契约缺口（`complexity -f --json`、`ci assess-risk --json`、`workflow start --json`）被收口为稳定机器输出或显式状态 truth
5. 若 CLI 输出契约、默认推荐路径或 AI 使用模式发生变化，必须同步更新 `AI_GUIDE.md`、`docs/ai-guide/COMMANDS.md`、`docs/ai-guide/OUTPUT.md` 或明确写出无需更新原因
6. `rtk` 只作为 token 节省 / 命令包装工具处理，不进入 CodeMap 产品依赖、功能说明或修复范围

Plans:

- [x] **25-01** 为 `analyze -i find` 引入 stdout-visible diagnostics，并把 scanner failure / fallback truth 上浮到 machine-readable JSON
- [x] **25-02** 收口 `complexity -f --json`、`ci assess-risk --json`、`workflow start --json` 的最小 machine contract
- [x] **25-03** 同步 `AI_GUIDE.md`、`docs/ai-guide/COMMANDS.md`、`docs/ai-guide/OUTPUT.md` 与 guardrail，固定 docs truth

## Milestone Summary

**Key accomplishments:**

- 为 `analyze -i find` 增加 stdout-visible diagnostics，并把 success / `partialFailure` / failure 显式建模
- 让 `find` discovery boundary 与现有 config-aware scanning truth 对齐
- 收口 `complexity -f --json`、`ci assess-risk --json`、`workflow start --json` 的稳定机器输出
- 同步 `AI_GUIDE.md`、`docs/ai-guide/COMMANDS.md`、`docs/ai-guide/OUTPUT.md` 与 docs guardrail
- 补齐 `25-VERIFICATION.md` 与 `25-VALIDATION.md`，让 milestone audit 不再依赖口头状态判断

**Validation snapshot:**

- requirements: `9/9` satisfied
- phases: `1/1` complete
- integration paths: `3/3` pass
- E2E flows: `4/4` pass
- Nyquist: compliant

**Non-blocking tech debt accepted at archive time:**

- `analyze -i find` 的 dogfood degrade path 仍会把详细 `ast-grep` 错误打印到 `stderr`

---
_For current project status, see `.planning/ROADMAP.md` and `.planning/PROJECT.md`._
