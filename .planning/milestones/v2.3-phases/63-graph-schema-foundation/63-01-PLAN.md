---
phase: 63-graph-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/interface/types/index.ts
  - src/domain/entities/Dependency.ts
  - src/interface/types/storage.ts
  - src/infrastructure/storage/sqlite/schema.ts
  - src/infrastructure/storage/adapters/SQLiteStorage.ts
  - src/infrastructure/storage/sqlite/GovernanceGraphCache.ts
  - src/cli/commands/generate.ts
  - src/infrastructure/repositories/CodeGraphRepositoryImpl.ts
  - src/infrastructure/storage/__tests__/SQLiteStorage.test.ts
  - src/infrastructure/storage/__tests__/SQLiteGovernanceGraph.test.ts
autonomous: true
requirements:
  - GRAPH-01
  - GRAPH-03
must_haves:
  truths:
    - "SQLite 持久化的主真相仍然是 modules、symbols、dependencies，而 traversal projection 只是内部派生层。"
    - "每条持久化 dependency edge 都能表达 `EXTRACTED` / `INFERRED` / `AMBIGUOUS`，并在 save/load round-trip 后保持不变。"
    - "stale schema、缺失 projection、非法 confidence 或 projection drift 不会静默通过，而是返回可诊断的 rebuild/compatibility 错误。"
  artifacts:
    - path: "src/infrastructure/storage/sqlite/schema.ts"
      provides: "Phase 63 graph-optimized SQLite schema version, traversal projection DDL, and indexes"
    - path: "src/infrastructure/storage/adapters/SQLiteStorage.ts"
      provides: "Transactional write/read path that keeps primary truth and internal traversal projection in sync"
    - path: "src/interface/types/index.ts"
      provides: "Shared dependency confidence contract consumed by domain/storage surfaces"
    - path: "src/infrastructure/storage/__tests__/SQLiteStorage.test.ts"
      provides: "Schema gate, confidence round-trip, and stale-schema failure coverage"
  key_links:
    - from: "src/cli/commands/generate.ts"
      to: "src/infrastructure/repositories/CodeGraphRepositoryImpl.ts"
      via: "saveToCodeGraphStorage -> repository.save"
      pattern: "saveToCodeGraphStorage|convertToCodeGraph|repository\\.save"
    - from: "src/infrastructure/repositories/CodeGraphRepositoryImpl.ts"
      to: "src/infrastructure/storage/adapters/SQLiteStorage.ts"
      via: "IStorage.saveCodeGraph/loadCodeGraph"
      pattern: "saveCodeGraph|loadCodeGraph"
    - from: "src/infrastructure/storage/adapters/SQLiteStorage.ts"
      to: "src/infrastructure/storage/sqlite/schema.ts"
      via: "CURRENT_SQLITE_SCHEMA_VERSION + SQLITE_GOVERNANCE_SCHEMA_SQL"
      pattern: "CURRENT_SQLITE_SCHEMA_VERSION|SQLITE_GOVERNANCE_SCHEMA_SQL"
---

<objective>
建立 Phase 63 的 storage foundation：在不改动 `modules / symbols / dependencies` 主领域模型的前提下，把 SQLite 持久化升级为 graph-optimized schema，并补齐 confidence 三态与 fail-closed compatibility gate。

Purpose: 先完成 schema、projection、事务写入和 confidence 契约的底层真相收敛，为后续兼容验证计划提供稳定基础。
Output: 一套可重建的 SQLite schema/version/projection 设计、confidence 三态契约、以及覆盖 stale schema / invalid confidence / projection drift 的自动化验证。
</objective>

<execution_context>
@/home/stark/.codex/get-shit-done/workflows/execute-plan.md
@/home/stark/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/63-graph-schema-foundation/63-CONTEXT.md
@.planning/phases/63-graph-schema-foundation/63-RESEARCH.md
@.planning/phases/63-graph-schema-foundation/63-PATTERNS.md
@.planning/phases/63-graph-schema-foundation/63-VALIDATION.md
@src/interface/types/index.ts
@src/domain/entities/Dependency.ts
@src/domain/entities/CodeGraph.ts
@src/interface/types/storage.ts
@src/infrastructure/storage/sqlite/schema.ts
@src/infrastructure/storage/adapters/SQLiteStorage.ts
@src/infrastructure/storage/sqlite/GovernanceGraphCache.ts
@src/cli/commands/generate.ts
@src/infrastructure/repositories/CodeGraphRepositoryImpl.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 63-01-01: 锁定 confidence 三态契约并先补 RED 测试</name>
  <read_first>
    .planning/phases/63-graph-schema-foundation/63-CONTEXT.md
    .planning/phases/63-graph-schema-foundation/63-RESEARCH.md
    src/interface/types/index.ts
    src/domain/entities/Dependency.ts
    src/infrastructure/storage/__tests__/SQLiteStorage.test.ts
    src/infrastructure/storage/__tests__/SQLiteGovernanceGraph.test.ts
  </read_first>
  <files>src/interface/types/index.ts, src/domain/entities/Dependency.ts, src/infrastructure/storage/__tests__/SQLiteStorage.test.ts, src/infrastructure/storage/__tests__/SQLiteGovernanceGraph.test.ts</files>
  <action>按 D-05、D-06、D-07 先统一 interface/domain 的 confidence 枚举命名，明确 Phase 63 以后不能再写入 `high`。先在 `SQLiteStorage.test.ts` 写 RED 用例，覆盖三类失败/行为：1) 保存并读回三态 confidence；2) 旧 schema version 或缺失 traversal projection 时抛出显式 rebuild-required 错误；3) 非法 confidence 字符串（例如 `HIGH`、空字符串、`unknown`）在读取或写入时被诊断拒绝而不是吞掉。同步在 `SQLiteGovernanceGraph.test.ts` 写 RED 用例，验证主真相表与 projection/query cache 的边数量、源/目标实体类型、confidence 值完全一致，避免后续 projection 变成第二份业务真相。</action>
  <verify>
    <automated>rtk ./node_modules/.bin/vitest run src/infrastructure/storage/__tests__/SQLiteStorage.test.ts src/infrastructure/storage/__tests__/SQLiteGovernanceGraph.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/interface/types/index.ts` 中 `confidence` 字段只允许 `EXTRACTED | INFERRED | AMBIGUOUS`
    - `src/domain/entities/Dependency.ts` 中存在 `export type DependencyConfidence = 'EXTRACTED' | 'INFERRED' | 'AMBIGUOUS';`
    - `src/infrastructure/storage/__tests__/SQLiteStorage.test.ts` 新增针对 `GRAPH_SCHEMA_REBUILD_REQUIRED` 或等价显式错误码的失败场景断言
    - `src/infrastructure/storage/__tests__/SQLiteGovernanceGraph.test.ts` 新增 projection/cache parity 断言，且断言文本包含 `EXTRACTED`、`INFERRED`、`AMBIGUOUS`
  </acceptance_criteria>
  <done>confidence contract 及 RED 测试已落地，测试失败原因直接指向 schema/version/projection/confidence 缺口。</done>
</task>

<task type="auto">
  <name>Task 63-01-02: 实现 graph-optimized schema 与事务化 projection 写入</name>
  <read_first>
    .planning/phases/63-graph-schema-foundation/63-CONTEXT.md
    .planning/phases/63-graph-schema-foundation/63-PATTERNS.md
    src/infrastructure/storage/sqlite/schema.ts
    src/infrastructure/storage/adapters/SQLiteStorage.ts
    src/infrastructure/storage/sqlite/GovernanceGraphCache.ts
    src/cli/commands/generate.ts
    src/infrastructure/repositories/CodeGraphRepositoryImpl.ts
    src/interface/types/storage.ts
  </read_first>
  <files>src/infrastructure/storage/sqlite/schema.ts, src/infrastructure/storage/adapters/SQLiteStorage.ts, src/infrastructure/storage/sqlite/GovernanceGraphCache.ts, src/cli/commands/generate.ts, src/infrastructure/repositories/CodeGraphRepositoryImpl.ts, src/interface/types/storage.ts</files>
  <action>按 D-01、D-02、D-03、D-04 实现 schema foundation。保留 `projects/modules/symbols/dependencies` 为唯一业务真相，新增一个内部 traversal projection 层，例如 `graph_edges` 或等价命名，列至少包含 `dependency_id`、`source_id`、`source_entity_type`、`target_id`、`target_entity_type`、`dependency_type`、`confidence`、`file_path`、`line`，并为 `source_id + source_entity_type`、`target_id + target_entity_type`、`confidence`、`dependency_type` 建索引。`replaceCurrentGraph` 必须在单个事务里按“清空主真相表 -> 重写主真相表 -> 重建 projection -> 更新 metadata/schema version -> refresh cache”完成。按 D-03/D-04 删除“依赖旧 snapshots 自动继续服务”的隐式兼容思路：允许 rebuild-first cutover，但必须在初始化/读取路径上显式识别 stale schema，并给出 `run mycodemap generate to rebuild` 之类 remediation。`src/cli/commands/generate.ts` 里的 confidence 分类必须按证据来源实现：AST/parser 直接证明的 import/call/type-ref 边写 `EXTRACTED`，现有 naming/file-structure/re-export heuristics 推出的边写 `INFERRED`，多目标 unresolved 写 `AMBIGUOUS`。</action>
  <verify>
    <automated>rtk ./node_modules/.bin/vitest run src/infrastructure/storage/__tests__/SQLiteStorage.test.ts src/infrastructure/storage/__tests__/SQLiteGovernanceGraph.test.ts && rtk tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/infrastructure/storage/sqlite/schema.ts` 的 `CURRENT_SQLITE_SCHEMA_VERSION` 不再是 `governance-v3`，并新增 traversal projection table/view/index 的 DDL
    - schema DDL 中仍保留 `projects`、`modules`、`symbols`、`dependencies` 主表，且没有把主真相改名为统一 `nodes` / `edges`
    - `src/infrastructure/storage/adapters/SQLiteStorage.ts` 中 `replaceCurrentGraph` 同一事务里同时重建主表与 projection
    - `src/infrastructure/storage/adapters/SQLiteStorage.ts` 中存在显式 schema/projection compatibility gate；缺失 projection 或版本不匹配时返回 rebuild 诊断
    - `src/cli/commands/generate.ts` 按证据来源而不是按边类型硬编码 confidence
  </acceptance_criteria>
  <done>SQLite schema/version/projection、save/load round-trip、generate 写入 confidence 三态全部转绿，并且主领域模型仍以 modules/symbols/dependencies 为中心。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- parser/generate -> SQLite persistence
- SQLite primary truth -> traversal projection

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | T | `SQLiteStorage` schema gate | mitigate | Reject stale `schema_version` or missing projection with explicit rebuild-required error before serving reads |
| T-63-02 | T | dependency confidence mapping | mitigate | Centralize `EXTRACTED | INFERRED | AMBIGUOUS` in shared types + domain validation + row-mapper checks |
| T-63-03 | I | `replaceCurrentGraph` transaction | mitigate | Rebuild primary tables and traversal projection inside one SQLite transaction; add parity tests proving projection row count/keys/confidence match dependencies |
</threat_model>

<verification>
- Focused storage gate: `rtk ./node_modules/.bin/vitest run src/infrastructure/storage/__tests__/SQLiteStorage.test.ts src/infrastructure/storage/__tests__/SQLiteGovernanceGraph.test.ts`
- Type gate: `rtk tsc --noEmit`
</verification>

<success_criteria>
- Phase 63 foundation keeps `modules/symbols/dependencies` as primary truth and adds traversal-oriented SQLite projection only as an internal layer
- `GRAPH-01` and `GRAPH-03` have direct implementation and automated verification evidence
- stale schema / invalid confidence / projection drift are fail-closed with diagnosable remediation
</success_criteria>

<output>
After completion, create `.planning/phases/63-graph-schema-foundation/63-01-SUMMARY.md`
</output>
