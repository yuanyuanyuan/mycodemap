---
phase: 63-graph-schema-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 63-01
files_modified:
  - src/server/handlers/QueryHandler.ts
  - src/server/mcp/service.ts
  - src/server/handlers/__tests__/QueryHandler.test.ts
  - src/server/mcp/__tests__/CodeMapMcpServer.test.ts
  - src/cli/commands/__tests__/generate.test.ts
  - src/cli/commands/__tests__/analyze-command.test.ts
  - src/cli/commands/__tests__/deps-output.test.ts
  - src/cli/commands/__tests__/query-output.test.ts
  - tests/e2e/graph-schema-foundation.test.ts
  - src/infrastructure/storage/__tests__/SQLiteStorage.test.ts
autonomous: true
requirements:
  - GRAPH-02
must_haves:
  truths:
    - "现有 `generate` / `query` / `deps` / `analyze` 成功路径继续返回稳定成功 envelope，不因内部 schema 切换而改变对外成功形状。"
    - "MCP 的 `codemap_query` 与 `codemap_impact` 都在新 persisted truth 上保持稳定成功 envelope。"
    - "真实 filesystem + 真实 subprocess 证据证明 rebuild-first cutover 成功路径与失败路径都可诊断。"
  artifacts:
    - path: "src/server/handlers/__tests__/QueryHandler.test.ts"
      provides: "handler compatibility-envelope verification on the new persisted truth"
    - path: "src/server/mcp/__tests__/CodeMapMcpServer.test.ts"
      provides: "MCP success-envelope verification for `codemap_query` and `codemap_impact`"
    - path: "tests/e2e/graph-schema-foundation.test.ts"
      provides: "real filesystem + real subprocess proof for generate, direct-execution, and stale-schema remediation"
  key_links:
    - from: "src/server/handlers/QueryHandler.ts"
      to: "src/infrastructure/storage/adapters/SQLiteStorage.ts"
      via: "find* methods and loadCodeGraph compatibility reads"
      pattern: "findDependencies|findDependents|loadCodeGraph"
    - from: "src/server/mcp/service.ts"
      to: "src/interface/types/storage.ts"
      via: "MCP service consumes storage truth through loadGraphMetadata/findCallers/findCallees/calculateSymbolImpact"
      pattern: "loadGraphMetadata|findCallers|findCallees|calculateSymbolImpact"
---

<objective>
在 `63-01` 完成 schema/projection foundation 之后，锁定 Phase 63 的兼容成功包络：handler、MCP、direct-execution 和真实 subprocess 验证全部收口到自动化/可执行 gate。

Purpose: 确保内部 SQLite schema 变化不破坏已发货 CLI/MCP 成功契约，并用真实环境证据证明 rebuild-first cutover 可用。
Output: QueryHandler、MCP、CLI direct-execution 与 e2e subprocess 的兼容验证闭环。
</objective>

<execution_context>
@/home/stark/.codex/get-shit-done/workflows/execute-plan.md
@/home/stark/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/63-graph-schema-foundation/63-CONTEXT.md
@.planning/phases/63-graph-schema-foundation/63-RESEARCH.md
@.planning/phases/63-graph-schema-foundation/63-VALIDATION.md
@.planning/phases/63-graph-schema-foundation/63-01-PLAN.md
@src/server/handlers/QueryHandler.ts
@src/server/handlers/__tests__/QueryHandler.test.ts
@src/server/mcp/service.ts
@src/server/mcp/__tests__/CodeMapMcpServer.test.ts
@src/cli/commands/__tests__/generate.test.ts
@src/cli/commands/__tests__/analyze-command.test.ts
@src/cli/commands/__tests__/deps-output.test.ts
@src/cli/commands/__tests__/query-output.test.ts
@tests/e2e/init-onboarding-verification.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 63-02-01: 锁定 handler、MCP、direct-execution 兼容成功 envelope</name>
  <read_first>
    .planning/phases/63-graph-schema-foundation/63-VALIDATION.md
    src/server/handlers/QueryHandler.ts
    src/server/handlers/__tests__/QueryHandler.test.ts
    src/server/mcp/service.ts
    src/server/mcp/__tests__/CodeMapMcpServer.test.ts
    src/cli/commands/__tests__/generate.test.ts
    src/cli/commands/__tests__/analyze-command.test.ts
    src/cli/commands/__tests__/deps-output.test.ts
    src/cli/commands/__tests__/query-output.test.ts
  </read_first>
  <files>src/server/handlers/QueryHandler.ts, src/server/mcp/service.ts, src/server/handlers/__tests__/QueryHandler.test.ts, src/server/mcp/__tests__/CodeMapMcpServer.test.ts, src/cli/commands/__tests__/generate.test.ts, src/cli/commands/__tests__/analyze-command.test.ts, src/cli/commands/__tests__/deps-output.test.ts, src/cli/commands/__tests__/query-output.test.ts, src/infrastructure/storage/__tests__/SQLiteStorage.test.ts</files>
  <action>按 D-08、D-09 把兼容面收口到“对外成功 envelope 不变，内部失败更显式”。如 `QueryHandler` 需要极小调整才能避免在新 schema 下走到全量旧路径，可做最小改动，但禁止在本 phase 开启 query refactor。同步覆盖 MCP：`src/server/mcp/service.ts` 与 `src/server/mcp/__tests__/CodeMapMcpServer.test.ts` 必须同时证明 `codemap_query` 和 `codemap_impact` 都仍读取正确的 storage truth，并保持当前 `status`、`graph_status`、核心结构字段稳定。再补 direct-execution 兼容：在现有 CLI command tests 中新增断言，锁住 `generate -> codemap.json -> query/deps/analyze` 主成功路径没有因 SQLite schema 改动而回归。不要新增新的 CLI/MCP 输出字段，只允许增加兼容安全的 diagnostics。</action>
  <verify>
    <automated>rtk ./node_modules/.bin/vitest run src/server/handlers/__tests__/QueryHandler.test.ts src/server/mcp/__tests__/CodeMapMcpServer.test.ts src/cli/commands/__tests__/generate.test.ts src/cli/commands/__tests__/analyze-command.test.ts src/cli/commands/__tests__/deps-output.test.ts src/cli/commands/__tests__/query-output.test.ts src/infrastructure/storage/__tests__/SQLiteStorage.test.ts && rtk tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/server/handlers/__tests__/QueryHandler.test.ts` 覆盖新 schema 下的 `searchModules`、`getModuleDetail`、`analyzeImpact` 或现有等价成功路径，且断言输出字段名不发生变化
    - `src/server/mcp/__tests__/CodeMapMcpServer.test.ts` 同时覆盖新 schema 下 `codemap_query` 和 `codemap_impact` 的成功 envelope，且断言 `status`、`graph_status`、主要结构字段名不发生变化
    - `src/cli/commands/__tests__/generate.test.ts`、`src/cli/commands/__tests__/analyze-command.test.ts`、`src/cli/commands/__tests__/deps-output.test.ts`、`src/cli/commands/__tests__/query-output.test.ts` 至少有一处新增断言，证明 Phase 63 的 SQLite schema 变更不会破坏 direct-execution 主成功路径
    - 至少一个测试断言 stale schema / projection drift 不返回空成功结果，错误文本包含 `generate` 或 `rebuild` remediation
  </acceptance_criteria>
  <done>`generate` / `query` / `deps` / `analyze` / MCP 的成功 envelope 稳定性有自动化证据，其中 `codemap_query` 和 `codemap_impact` 两个 MCP 图工具都被显式验证。</done>
</task>

<task type="auto">
  <name>Task 63-02-02: 增加真实 filesystem + subprocess 验证闭环</name>
  <read_first>
    .planning/phases/63-graph-schema-foundation/63-VALIDATION.md
    tests/e2e/init-onboarding-verification.test.ts
    src/cli/commands/generate.ts
    src/cli/config-loader.ts
    src/infrastructure/storage/adapters/SQLiteStorage.ts
  </read_first>
  <files>tests/e2e/graph-schema-foundation.test.ts, src/infrastructure/storage/__tests__/SQLiteStorage.test.ts</files>
  <action>新增 `tests/e2e/graph-schema-foundation.test.ts`，复用 `tests/e2e/init-onboarding-verification.test.ts` 的 `spawnSync + mkdtempSync + readFileSync` 模式，在真实临时目录中创建最小 fixture repo。测试至少分两段：1) 正常路径：运行 `node dist/cli/index.js generate`，断言 `.mycodemap/governance.sqlite` 和 `.mycodemap/codemap.json` 同时存在，然后继续运行一个 direct-execution CLI 读路径（优先 `query`、其次 `deps` 或 `analyze`）并断言 exit code 为 0、stdout 包含稳定字段；2) 失败路径：准备一个缺失 Phase 63 projection/version 的 SQLite 样本，触发读取命令并断言 stderr/stdout 中出现 `generate` 或 `rebuild` remediation。必要时在 `SQLiteStorage.test.ts` 中补充帮助函数，确保 stale-schema 样本构造可复用。</action>
  <verify>
    <automated>rtk ./node_modules/.bin/vitest run tests/e2e/graph-schema-foundation.test.ts --config vitest.e2e.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/graph-schema-foundation.test.ts` 使用 `spawnSync(process.execPath, [CLI_PATH, ...args])` 或等价真实 subprocess 模式运行 `node dist/cli/index.js generate`
    - e2e 测试显式断言 `.mycodemap/governance.sqlite` 与 `.mycodemap/codemap.json` 同时存在
    - e2e 测试在 fresh temp repo 上覆盖 `generate` 后的 direct-execution 读路径成功，至少包含 `query`、`deps` 或 `analyze` 之一
    - 至少一个真实失败场景在 stale-schema 样本上触发，并断言错误文本包含 `generate` 或 `rebuild` remediation
    - `rtk ./node_modules/.bin/vitest run tests/e2e/graph-schema-foundation.test.ts --config vitest.e2e.config.ts` 通过
  </acceptance_criteria>
  <done>Phase 63 有真实 filesystem + 真实 subprocess 的成功/失败证据，执行 closeout 不再依赖手工口头说明。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- SQLite storage -> QueryHandler / CLI / MCP success envelope
- Built CLI subprocess -> temp repo artifacts

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-04 | R | `QueryHandler` / CLI compatibility surface | mitigate | Add handler-level and CLI-level envelope assertions so schema redesign cannot change stable success fields without test failure |
| T-63-05 | R | MCP compatibility surface | mitigate | Add MCP-level envelope assertions so schema redesign cannot change shipped MCP result shapes without test failure |
| T-63-06 | R | direct-execution CLI surface | mitigate | Lock `generate -> codemap.json -> query/deps/analyze` success path in CLI tests so SQLite schema changes cannot silently break the JSON runtime path |
| T-63-07 | D | compatibility fallback path | accept | Rebuild-first cutover may temporarily reject old DB files; acceptable because D-03/D-04 explicitly allow no historical migrator, as long as remediation is executable |
</threat_model>

<verification>
- Compatibility gate: `rtk ./node_modules/.bin/vitest run src/server/handlers/__tests__/QueryHandler.test.ts src/server/mcp/__tests__/CodeMapMcpServer.test.ts src/cli/commands/__tests__/generate.test.ts src/cli/commands/__tests__/analyze-command.test.ts src/cli/commands/__tests__/deps-output.test.ts src/cli/commands/__tests__/query-output.test.ts`
- Type gate: `rtk tsc --noEmit`
- Real-path evidence gate: `rtk ./node_modules/.bin/vitest run tests/e2e/graph-schema-foundation.test.ts --config vitest.e2e.config.ts`
</verification>

<success_criteria>
- `GRAPH-02` 的 handler、MCP、direct-execution 兼容成功包络都有自动化证据
- `codemap_query` 与 `codemap_impact` 两个 MCP 图工具都在新 persisted truth 上被显式验证
- 真实 filesystem + 真实 subprocess 证据证明成功路径与 stale-schema 失败路径都可诊断
</success_criteria>

<output>
After completion, create `.planning/phases/63-graph-schema-foundation/63-02-SUMMARY.md`
</output>
