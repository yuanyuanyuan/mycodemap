# Requirements: CodeMap v2.2 architecture-foundation

**Defined:** 2026-05-06
**Shipped:** 2026-05-07
**Archive:** This is a historical snapshot. Start a new milestone to create fresh active requirements.

## v2.2 Requirements — Final Status

### Parser Foundation

- [x] **PAR-01**: 维护者运行 `mycodemap generate` 时，默认主流程使用 Tree-sitter-based parser path，而不是 `FastParser` 或 `hybrid` 自动切换
- [x] **PAR-02**: 当用户通过 CLI 或配置请求已废弃的 `fast` 或 `hybrid` mode 时，系统返回显式错误码、迁移提示和修复建议
- [x] **PAR-03**: parser 选择逻辑只有一个主入口；`src/core/analyzer.ts` 不再保留基于文件数阈值的 parser 切换逻辑
- [x] **PAR-04**: TypeScript/JavaScript、Python、Go 文件能通过统一 parser 主流程进入分析，而不是只有 TypeScript 走新路径
- [x] **PAR-05**: parser 主流程在 native `tree-sitter` 不可用时仍可通过现有 WASM fallback 完成运行，且失败场景有真实验证证据

### Storage Convergence

- [x] **STOR-01**: `storage.type: "auto"` 只会选择 SQLite family，不再静默切换到 `filesystem` 或 `kuzudb`
- [x] **STOR-02**: 当用户显式配置 `storage.type: "filesystem"` 或 `"kuzudb"` 时，系统返回 `UNSUPPORTED_STORAGE_TYPE` 级别的显式错误，而不是继续执行旧 backend
- [x] **STOR-03**: 当 native `better-sqlite3` 不可用时，系统只在 SQLite family 内尝试 fallback，并返回可执行 remediation，而不是跨 backend 降级
- [x] **STOR-04**: 默认配置、config loader 校验和相关运行时输出都把 SQLite 表述为唯一推荐持久化 backend

### MCP Direct Execution

- [x] **MCP-01**: 至少一组高频 contract-backed MCP tools 在单次调用内返回真实 structured result，而不是 `status: "cli_redirect"`
- [x] **MCP-02**: MCP direct execution 与对应 CLI 命令共享同一执行逻辑或 service seam，而不是复制实现
- [x] **MCP-03**: direct execution 路径在成功和失败两种场景下都返回统一错误语义与结构化输出
- [x] **MCP-04**: direct execution 改造后，新增或改造的 CLI command wrapper 保持薄封装，不再把主要业务逻辑塞在 `src/cli/commands/*.ts` 内

### Context Routing Gate

- [x] **CTX-01**: agent 可以通过 `codemap_context` 请求 `review`、`debug` 或 `default` 三种任务上下文
- [x] **CTX-02**: `codemap_context` 返回最小但足够的 graph stats、risk score 和 `nextToolSuggestions`
- [x] **CTX-03**: `codemap_context` 至少支持 `minimal` 和 `standard` 两级 detail level，并能证明 `minimal` 明显更短
- [x] **CTX-04**: `codemap_context` 支持 tool filtering，避免向 agent 暴露无关工具集合

## Deferred Requirements

### Stretch / Non-blocking

- **PAR-06**: `PythonTypeEnhancer` 提供 docstring-based 类型增强，但不阻塞 v2.2 closeout
- **MCP-05**: SSE transport 支持可作为 MCP direct execution 子任务，但不阻塞 v2.2 committed scope

### Future Milestones

- **INI-01**: `mycodemap init --json` machine-readable receipt gap
- **GRAPH-01**: SQLite schema redesign
- **GRAPH-02**: Incremental update / impact CTE / community detection
- **INT-04**: Auto-Provisioned Agent Skills
- **INT-05**: MCP `verify_contract` Tool

## Traceability — Final

| Requirement | Phase | Status |
|-------------|-------|--------|
| PAR-01 | Phase 59 | Complete |
| PAR-02 | Phase 59 | Complete |
| PAR-03 | Phase 59 | Complete |
| PAR-04 | Phase 59 | Complete |
| PAR-05 | Phase 59 | Complete |
| STOR-01 | Phase 60 | Complete |
| STOR-02 | Phase 60 | Complete |
| STOR-03 | Phase 60 | Complete |
| STOR-04 | Phase 60 | Complete |
| MCP-01 | Phase 61 | Complete |
| MCP-02 | Phase 61 | Complete |
| MCP-03 | Phase 61 | Complete |
| MCP-04 | Phase 61 | Complete |
| CTX-01 | Phase 62 | Complete |
| CTX-02 | Phase 62 | Complete |
| CTX-03 | Phase 62 | Complete |
| CTX-04 | Phase 62 | Complete |

**Coverage:**
- v2.2 requirements: 17 total
- Complete: 17
- Unmapped: 0

---

*Requirements defined: 2026-05-06*
*Shipped: 2026-05-07*
*Archived as milestone snapshot*
