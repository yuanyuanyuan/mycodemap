---
phase: 21-evaluate-arcadedb-node-integration-feasibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-EVIDENCE.md
  - .planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-BLAST-RADIUS.md
  - scripts/experiments/arcadedb-http-smoke.mjs
autonomous: false
requirements:
  - ARC-01
  - ARC-02
  - ARC-03
user_setup:
  - 如果要执行 live smoke，需提前提供可访问的 ArcadeDB server、database 名称和 credentials；但本 plan 的离线产物必须在无服务环境下也可完成
must_haves:
  truths:
    - 官方支持矩阵必须明确区分 `embedded = JVM only`、`HTTP/JSON = preferred Node path`、`Bolt = secondary Node path`
    - blast radius 文档必须把现有 runtime/docs drift 纳入 baseline，而不是只数新增字段
    - 隔离 smoke harness 必须停留在 `scripts/experiments/`，不能接入 shipped storage runtime
  artifacts:
    - .planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-EVIDENCE.md
    - .planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-BLAST-RADIUS.md
    - scripts/experiments/arcadedb-http-smoke.mjs
  key_links:
    - `21-EVIDENCE.md` 固化官方支持矩阵与被移除的错误前提
    - `21-BLAST-RADIUS.md` 量化当前 contract 与 server-backed ArcadeDB 的缺口
    - `scripts/experiments/arcadedb-http-smoke.mjs` 提供不污染 public surface 的最小 Node 验证路径
---

<objective>
建立 `Phase 21` 的第一波硬证据：官方支持面、当前 repo blast radius baseline，以及不进入 shipped runtime 的最小 HTTP smoke harness。

Purpose: 如果 Wave 1 还没有把官方支持拓扑和当前 public surface 缺口写清楚，Wave 2 的验证与决策报告就只能继续建立在“ArcadeDB 好像能接”的弱前提上。
Output: `21-EVIDENCE.md`、`21-BLAST-RADIUS.md`、`scripts/experiments/arcadedb-http-smoke.mjs`。
</objective>

<execution_context>
@/data/codemap/.codex/get-shit-done/workflows/execute-plan.md
@/data/codemap/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-CONTEXT.md
@.planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-RESEARCH.md
@.planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-VALIDATION.md
@src/interface/types/storage.ts
@src/infrastructure/storage/StorageFactory.ts
@src/cli/config-loader.ts
@mycodemap.config.schema.json
@README.md
@docs/ai-guide/COMMANDS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 固化官方支持矩阵与被移除的错误前提</name>
  <files>.planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-EVIDENCE.md</files>
  <read_first>.planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-CONTEXT.md, .planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-RESEARCH.md, .planning/archive/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb-seed/1000-RESEARCH.md, src/interface/types/storage.ts</read_first>
  <action>创建 `21-EVIDENCE.md`，必须包含以下固定结构和精确内容：1) 标题 `# Phase 21 Evidence Pack: ArcadeDB Node Support`；2) `## Official Support Matrix` 表格，至少包含三行：`Embedded | JVM only | Java API | Reject as Node runtime path`、`HTTP/JSON | Client/server | Preferred Node experiment path | Official server-backed route`、`Bolt | Client/server | Secondary Node validation path | Do not equate to restored Neo4j product surface`；3) `## Invalid Assumptions Removed` 列出 `No Node embedded SDK assumption`、`No drop-in replacement claim`、`No public surface mutation in Phase 21`；4) `## Source Links` 包含 `https://arcadedb.com/embedded.html` 与 `https://arcadedb.com/client-server.html`；5) `## Decision Posture` 必须直接写出 `Embedded = NO-GO`, `HTTP/JSON = primary`, `Bolt = secondary`。</action>
  <verify>rg -n "Embedded \| JVM only|HTTP/JSON \| Client/server|Bolt \| Client/server|No Node embedded SDK assumption|https://arcadedb.com/embedded.html|https://arcadedb.com/client-server.html" .planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-EVIDENCE.md</verify>
  <acceptance_criteria>
    - `21-EVIDENCE.md` contains `Embedded | JVM only`
    - `21-EVIDENCE.md` contains `HTTP/JSON | Client/server`
    - `21-EVIDENCE.md` contains `Bolt | Client/server`
    - `21-EVIDENCE.md` contains `No Node embedded SDK assumption`
    - `21-EVIDENCE.md` contains `Embedded = NO-GO`
  </acceptance_criteria>
  <done>官方支持矩阵与失效前提已被明确固化，Phase 21 不再建立在 embedded 幻觉上。</done>
</task>

<task type="auto">
  <name>Task 2: 量化 server-backed ArcadeDB 的正式接入 blast radius</name>
  <files>.planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-BLAST-RADIUS.md</files>
  <read_first>src/interface/types/storage.ts, src/infrastructure/storage/StorageFactory.ts, src/cli/config-loader.ts, mycodemap.config.schema.json, README.md, docs/ai-guide/COMMANDS.md, .planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-CONTEXT.md</read_first>
  <action>创建 `21-BLAST-RADIUS.md`，必须包含以下固定结构：1) `## Current Public Surface`，明确写出当前 `storage.type` 只有 `filesystem / kuzudb / memory / auto`，当前 parser 只接受 `type / outputPath / databasePath / autoThresholds`，schema 不含远端连接字段；2) `## Required New Surface For First-Class ArcadeDB`，精确列出 `uri`、`database`、`username`、`password`、`protocol`、`tls`、`serverLifecycle`；3) `## Existing Drift That Changes The Baseline`，明确写出 runtime `auto -> kuzudb` 与 docs `auto -> filesystem` 的冲突；4) `## Blast Radius` 表格，至少覆盖 `Type Surface`、`Config Parser`、`Schema`、`Runtime Selection`、`User Docs`、`AI Docs`、`Fallback/Error Handling`；5) `## Isolation Strategy` 必须写出 `Phase 21 must not modify these files directly`；6) `## Recommendation` 必须写出 `Treat server-backed ArcadeDB as a product-surface change`。</action>
  <verify>rg -n "storage.type|uri|username|password|protocol|tls|serverLifecycle|auto -> kuzudb|auto -> filesystem|Treat server-backed ArcadeDB as a product-surface change" .planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-BLAST-RADIUS.md</verify>
  <acceptance_criteria>
    - `21-BLAST-RADIUS.md` contains `## Blast Radius`
    - `21-BLAST-RADIUS.md` contains `auto -> kuzudb`
    - `21-BLAST-RADIUS.md` contains `auto -> filesystem`
    - `21-BLAST-RADIUS.md` contains `serverLifecycle`
    - `21-BLAST-RADIUS.md` contains `Treat server-backed ArcadeDB as a product-surface change`
  </acceptance_criteria>
  <done>Phase 21 已量化正式接入 ArcadeDB 需要打开的真实产品面，而不是只看到一个 adapter 缺口。</done>
</task>

<task type="auto">
  <name>Task 3: 添加隔离 HTTP smoke harness</name>
  <files>scripts/experiments/arcadedb-http-smoke.mjs</files>
  <read_first>.planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-EVIDENCE.md, .planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-BLAST-RADIUS.md, package.json</read_first>
  <action>创建 `scripts/experiments/arcadedb-http-smoke.mjs`，只使用 Node 原生能力，不导入项目 runtime 代码，也不新增依赖。脚本必须实现以下具体行为：1) 支持 `--help`，打印必需环境变量、请求端点 `/api/v1/command/{database}`、支持的认证方式和“无服务时仅离线验证”的说明，并退出 0；2) 必需环境变量为 `ARCADEDB_HTTP_URL`、`ARCADEDB_DATABASE`、`ARCADEDB_USERNAME`、`ARCADEDB_PASSWORD`，可选 `ARCADEDB_CYPHER`；3) 默认查询为 `MATCH (n) RETURN count(n) AS total LIMIT 1`；4) 请求体固定为 `{ "language": "cypher", "command": command }`；5) 认证方式固定为 `Authorization: Basic ...`；6) 缺失 env 时输出明确错误并退出非零；7) 非 2xx 状态输出状态码与响应体并退出非零；8) 成功时打印 `ArcadeDB HTTP smoke succeeded`。脚本必须可通过 `node --check`，且在没有 ArcadeDB 服务时仍可通过 `--help` 完成离线验证。</action>
  <verify>node --check scripts/experiments/arcadedb-http-smoke.mjs && node scripts/experiments/arcadedb-http-smoke.mjs --help</verify>
  <acceptance_criteria>
    - `scripts/experiments/arcadedb-http-smoke.mjs` contains `ARCADEDB_HTTP_URL`
    - `scripts/experiments/arcadedb-http-smoke.mjs` contains `/api/v1/command/`
    - `scripts/experiments/arcadedb-http-smoke.mjs` contains `MATCH (n) RETURN count(n) AS total LIMIT 1`
    - `scripts/experiments/arcadedb-http-smoke.mjs` contains `ArcadeDB HTTP smoke succeeded`
    - `node --check scripts/experiments/arcadedb-http-smoke.mjs` exits `0`
  </acceptance_criteria>
  <done>最小 Node 验证路径已被隔离在实验脚本中，没有污染 shipped storage surface。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "Embedded \| JVM only|HTTP/JSON \| Client/server|Bolt \| Client/server" .planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-EVIDENCE.md`
- [ ] `rg -n "auto -> kuzudb|auto -> filesystem|serverLifecycle" .planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-BLAST-RADIUS.md`
- [ ] `node --check scripts/experiments/arcadedb-http-smoke.mjs`
- [ ] `node scripts/experiments/arcadedb-http-smoke.mjs --help`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `ARC-01`、`ARC-02`、`ARC-03` 获得可审计的官方支持面、最小实验路径与 blast radius 事实
- 现有 docs/runtime drift 被显式纳入 baseline，而不是被忽略
- 最小验证脚本保持在实验层，不接入 shipped storage runtime
  </success_criteria>

<output>
After completion, create `.planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-01-SUMMARY.md`
</output>
