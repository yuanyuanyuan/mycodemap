# 06-01 Plan

## Goal

落地共享文件发现模块，让 `analyze` 与 `ci check-headers -d` 复用同一套 `.gitignore` 感知排除规则，并修正默认排除模式的递归匹配问题。

## Tasks

1. 新建 `src/core/file-discovery.ts`，封装默认排除、`.git` worktree 兼容、root 解析与 scoped include。
2. 让 `src/core/analyzer.ts` 改为消费共享模块，不再自带局部排除实现。
3. 让 `src/orchestrator/file-header-scanner.ts` 使用共享模块完成目录扫描。
4. 更新 `src/cli/commands/init.ts` 默认 `exclude`，与共享模块保持一致。
5. 为 `.gitignore` 继承、递归 test exclude 与 branch wildcard / CI env fallback 增加回归测试。

## DoD

- `FILE-01` 满足
- 相关测试通过
- `typecheck` / `build` 通过
