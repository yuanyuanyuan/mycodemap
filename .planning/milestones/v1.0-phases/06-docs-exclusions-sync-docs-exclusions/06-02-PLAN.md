# 06-02 Plan

## Goal

同步 README、AI 文档、输出文档与验证规则，使 workflow 四阶段、ship/ci 边界、共享文件发现契约与当前实现一致。

## Tasks

1. 更新 `README.md` 的默认 `exclude`、共享发现契约与 `ci` 段落说明。
2. 更新 `AI_GUIDE.md`、`docs/ai-guide/COMMANDS.md`、`docs/ai-guide/OUTPUT.md`，补齐文件发现契约与 workflow / ship 边界。
3. 更新 `docs/rules/validation.md`、`docs/rules/engineering-with-codex-openai.md`，对齐 ci-gateway 与 ship 复用边界。

## DoD

- `DOC-01` 满足
- 文档措辞与实现一致
- `npm run docs:check` 通过
