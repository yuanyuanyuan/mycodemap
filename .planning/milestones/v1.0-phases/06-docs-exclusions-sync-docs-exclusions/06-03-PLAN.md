# 06-03 Plan

## Goal

把 Phase 4/5/6 的最终边界编码到 docs guardrail、CLI docs-sync 测试与相关回归测试中，形成脚本级护栏。

## Tasks

1. 扩展 `scripts/validate-docs.js`，固定 workflow 四阶段、ship 复用 ci、共享文件发现契约与验证规则真相。
2. 扩展 `src/cli/__tests__/validate-docs-script.test.ts`，覆盖新的 guardrail 失败场景。
3. 跑 docs check、workflow/ship/scanner/ci 回归、lint 与 build，形成最终 Phase 6 验证证据。

## DoD

- `DOC-02` 满足
- docs guardrail / tests / build 全绿
- Phase 6 可写入 verification report
