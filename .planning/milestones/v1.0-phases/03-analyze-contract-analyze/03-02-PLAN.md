---
phase: 03-analyze-contract-analyze
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/orchestrator/types.ts
  - src/cli/commands/analyze.ts
  - src/orchestrator/confidence.ts
  - src/orchestrator/__tests__/types.test.ts
  - src/orchestrator/__tests__/confidence.test.ts
autonomous: false
requirements:
  - ANL-02
  - ANL-03
  - ANL-04
  - ANL-05
user_setup: []
must_haves:
  truths:
    - `read` 输出统一承接 complexity + impact
    - `link` 输出统一承接 reference + dependency
    - legacy intent 在兼容窗口内返回结构化 warning，且 JSON 不被文本污染
  artifacts:
    - src/orchestrator/types.ts
    - src/cli/commands/analyze.ts
    - src/orchestrator/confidence.ts
    - src/orchestrator/__tests__/types.test.ts
    - src/orchestrator/__tests__/confidence.test.ts
  key_links:
    - `src/orchestrator/types.ts` 定义 `read/link/show` schema 与 warning shape
    - `src/cli/commands/analyze.ts` 负责把底层独立命令结果聚合为新 schema
    - `src/orchestrator/confidence.ts` 与其测试一起切到四意图阈值和 reason
---

<objective>
把四意图入口进一步落实为机器契约：定义 `read/link/show` schema，完成 legacy compat/warning 行为，并同步 confidence 机制。

Purpose: 只有名字变了但 JSON 结果还是旧壳，AI/机器消费者并不会真正得到更稳定的 contract。
Output: 新的 `read/link/show` output schema、兼容期结构化 warning、更新后的 confidence types/tests。
</objective>

<execution_context>
@/data/codemap/.codex/get-shit-done/workflows/execute-plan.md
@/data/codemap/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.0-phases/03-analyze-contract-analyze/03-CONTEXT.md
@.planning/milestones/v1.0-phases/03-analyze-contract-analyze/03-RESEARCH.md
@.planning/milestones/v1.0-phases/03-analyze-contract-analyze/03-01-PLAN.md
@src/orchestrator/types.ts
@src/cli/commands/analyze.ts
@src/orchestrator/confidence.ts
@src/cli/commands/impact.ts
@src/cli/commands/deps.ts
@src/cli/commands/complexity.ts
@docs/ai-guide/OUTPUT.md
@/home/stark/.gstack/projects/yuanyuanyuan-mycodemap/stark-main-design-20260324-022633.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义 `read/link/show` 机器输出 schema 与 warning shape</name>
  <files>src/orchestrator/types.ts, src/orchestrator/__tests__/types.test.ts</files>
  <read_first>src/orchestrator/types.ts, src/orchestrator/__tests__/types.test.ts, .planning/milestones/v1.0-phases/03-analyze-contract-analyze/03-CONTEXT.md, docs/ai-guide/OUTPUT.md</read_first>
  <action>在 `src/orchestrator/types.ts` 中为 `read` / `link` / `show` 定义明确的 machine output 结构，并加入结构化 deprecation warning 字段。保留顶层 `schemaVersion + intent + tool + confidence + metadata` 包装，扩展 payload/metadata，而不是输出多套互不兼容壳。同步更新 `src/orchestrator/__tests__/types.test.ts` 的 type guard 与示例对象，使其覆盖四意图和 warning shape。</action>
  <verify>pnpm exec vitest run src/orchestrator/__tests__/types.test.ts</verify>
  <acceptance_criteria>
    - `types.ts` 中存在 `read` / `link` / `show` 对应的明确 schema 类型
    - 结构化 warning 字段在类型层可表达 legacy deprecation
    - `types.test.ts` 不再把旧 8 intents 当作唯一真相
  </acceptance_criteria>
  <done>machine schema 的类型壳和 warning 形状已经成型。</done>
</task>

<task type="auto">
  <name>Task 2: 在 analyze 中实现 schema aggregation 与 legacy compat</name>
  <files>src/cli/commands/analyze.ts</files>
  <read_first>src/cli/commands/analyze.ts, src/cli/commands/impact.ts, src/cli/commands/deps.ts, src/cli/commands/complexity.ts, src/orchestrator/types.ts</read_first>
  <action>更新 `src/cli/commands/analyze.ts`，让 `read` 聚合 impact + complexity，`link` 聚合 dependency + reference，`show` 承接 overview/documentation 类结果；兼容窗口内 legacy intent 先 normalize 到新 intent，再通过结构化 warning 暴露 deprecated 信息。不要把 warning 直接拼到 JSON 文本中，也不要在这一步引入新的历史关联依赖源。</action>
  <verify>node dist/cli/index.js analyze -i read -t src/cli/index.ts --json && node dist/cli/index.js analyze -i link -t src/cli/index.ts --json</verify>
  <acceptance_criteria>
    - `analyze -i read` 返回的顶层 `intent` 为 `read`
    - `analyze -i link` 返回的顶层 `intent` 为 `link`
    - 旧 intent 在兼容窗口内会产生结构化 warning，而不是污染 JSON
    - `show` 不重新打开旧 `report` public surface
  </acceptance_criteria>
  <done>新的四意图 contract 在 analyze 主路径里真正落地。</done>
</task>

<task type="auto">
  <name>Task 3: 更新 confidence 分支到四意图契约</name>
  <files>src/orchestrator/confidence.ts, src/orchestrator/__tests__/confidence.test.ts</files>
  <read_first>src/orchestrator/confidence.ts, src/orchestrator/__tests__/confidence.test.ts, src/orchestrator/types.ts</read_first>
  <action>把 `src/orchestrator/confidence.ts` 与 `src/orchestrator/__tests__/confidence.test.ts` 从旧 8 intents 收敛到四意图：按 `find/read/link/show` 重定义 thresholds / scenario 分支，并确保与新 schema/normalize 行为一致。不要让 confidence reason 继续输出旧 intent 名称作为主事实。</action>
  <verify>pnpm exec vitest run src/orchestrator/__tests__/confidence.test.ts</verify>
  <acceptance_criteria>
    - `confidence.ts` 不再把旧 8 intents 作为主分支类型
    - `confidence.test.ts` 覆盖 `find/read/link/show`
    - confidence reason 文案不再把旧 intent 名称当成目标态契约
  </acceptance_criteria>
  <done>confidence 层和新 analyze contract 对齐，不再滞留旧 intent 语义。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run src/orchestrator/__tests__/types.test.ts src/orchestrator/__tests__/confidence.test.ts`
- [ ] `node dist/cli/index.js analyze -i read -t src/cli/index.ts --json`
- [ ] `node dist/cli/index.js analyze -i link -t src/cli/index.ts --json`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `read/link/show` 的 JSON contract 已明确定义且可运行
- 兼容期 warning 通过结构化字段暴露
- confidence 机制切到四意图，不再依赖旧 8 intents
  </success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/03-analyze-contract-analyze/03-02-SUMMARY.md`
</output>
