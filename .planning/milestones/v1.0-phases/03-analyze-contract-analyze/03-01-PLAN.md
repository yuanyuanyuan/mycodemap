---
phase: 03-analyze-contract-analyze
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/orchestrator/types.ts
  - src/orchestrator/intent-router.ts
  - src/cli/commands/analyze.ts
  - src/orchestrator/__tests__/intent-router.test.ts
autonomous: false
requirements:
  - ANL-01
  - ANL-05
user_setup: []
must_haves:
  truths:
    - `analyze` 的 public intent/help/参数校验只暴露 `find`、`read`、`link`、`show`
    - 顶层 CLI、`AnalyzeCommand`、`IntentRouter` 不再各自持有冲突的默认 intent
    - `find` 支持 `keywords-only` 或 `targets-only` 的入口，不再被旧的 `targets required` 规则误杀
  artifacts:
    - src/orchestrator/types.ts
    - src/orchestrator/intent-router.ts
    - src/cli/commands/analyze.ts
    - src/orchestrator/__tests__/intent-router.test.ts
  key_links:
    - `src/orchestrator/types.ts` 定义新的 public / legacy intent 类型与 normalize 所需元数据
    - `src/orchestrator/intent-router.ts` 负责从 legacy/public intent 归一到四意图
    - `src/cli/commands/analyze.ts` 的 parse/help/validate 与 router/types 使用同一套 intent 事实
---

<objective>
先把 `analyze` 的最外层契约入口收紧为四意图：统一类型、router、help 与参数校验，并清掉当前的默认值漂移。

Purpose: 如果 `Phase 3` 不先拿下 `types + router + analyze help/validate`，后面的 schema 聚合和兼容策略都会建立在漂移的入口上。
Output: 新的 4-intent 类型定义、legacy normalize 骨架、更新后的 analyze help 与按-intent 参数校验。
</objective>

<execution_context>
@/data/codemap/.codex/get-shit-done/workflows/execute-plan.md
@/data/codemap/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.0-phases/03-analyze-contract-analyze/03-CONTEXT.md
@.planning/milestones/v1.0-phases/03-analyze-contract-analyze/03-RESEARCH.md
@src/orchestrator/types.ts
@src/orchestrator/intent-router.ts
@src/cli/commands/analyze.ts
@src/orchestrator/__tests__/intent-router.test.ts
@/home/stark/.gstack/projects/yuanyuanyuan-mycodemap/stark-main-design-20260324-022633.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义 4-intent public types 与 legacy normalize 元数据</name>
  <files>src/orchestrator/types.ts</files>
  <read_first>.planning/milestones/v1.0-phases/03-analyze-contract-analyze/03-CONTEXT.md, .planning/milestones/v1.0-phases/03-analyze-contract-analyze/03-RESEARCH.md, src/orchestrator/types.ts</read_first>
  <action>在 `src/orchestrator/types.ts` 中把 `IntentType` 收敛到 `find|read|link|show`，并引入承载 legacy compat 的辅助类型（如 `LegacyIntentType`、normalize result、warning metadata）。保持现有 `CodemapOutput` 基础包装形状，但为后续 `read/link/show` schema 聚合预留类型位置。不要在这一步发明具体 payload 细节。</action>
  <verify>rg -n \"find|read|link|show|LegacyIntent|deprecated|warning\" src/orchestrator/types.ts</verify>
  <acceptance_criteria>
    - `src/orchestrator/types.ts` 的 public `IntentType` 不再包含旧 8 intents
    - 文件中存在承接 legacy intent normalize 的显式类型或映射入口
    - `CodemapOutput` 仍保留统一的顶层包装，不被拆碎成多套输出壳
  </acceptance_criteria>
  <done>四意图的类型基线与兼容元数据入口就位。</done>
</task>

<task type="auto">
  <name>Task 2: 重写 IntentRouter 的 4-intent normalize 与兼容入口</name>
  <files>src/orchestrator/intent-router.ts</files>
  <read_first>src/orchestrator/intent-router.ts, src/orchestrator/types.ts, .planning/milestones/v1.0-phases/03-analyze-contract-analyze/03-CONTEXT.md</read_first>
  <action>在 `src/orchestrator/intent-router.ts` 中删除旧 8-intent whitelist 作为 public truth 的角色，改为：1) 接受四个新意图；2) 在兼容窗口内把 legacy intent normalize 到四意图并附带 deprecation metadata；3) 对无效或过期 legacy intent 统一抛出 `E0001_INVALID_INTENT` 所需信息。不要继续保留 `search`/`impact` 这种默认值。</action>
  <verify>pnpm exec vitest run src/orchestrator/__tests__/intent-router.test.ts</verify>
  <acceptance_criteria>
    - `IntentRouter` 不再默认返回 `search`
    - `search` 会 normalize 到 `find`
    - `impact` / `complexity` 会 normalize 到 `read`
    - `dependency` / `reference` 会 normalize 到 `link`
    - `overview` / `documentation` 会 normalize 到 `show`
  </acceptance_criteria>
  <done>router 拥有统一、可测试的 normalize 行为，不再散落默认值。</done>
</task>

<task type="auto">
  <name>Task 3: 更新 analyze help 与按-intent 参数校验</name>
  <files>src/cli/commands/analyze.ts, src/orchestrator/__tests__/intent-router.test.ts</files>
  <read_first>src/cli/commands/analyze.ts, src/orchestrator/intent-router.ts, docs/ai-guide/COMMANDS.md, README.md</read_first>
  <action>更新 `src/cli/commands/analyze.ts`：help 文案只展示 `find/read/link/show`；显式要求 `--intent`；清理 `AnalyzeCommand` 的默认 `impact`；参数校验改为按 intent 分流，至少确保 `find` 支持 `keywords-only`。同步更新 `src/orchestrator/__tests__/intent-router.test.ts`，覆盖新的 public intent、legacy normalize 与 invalid cases。</action>
  <verify>node dist/cli/index.js analyze --help && node dist/cli/index.js analyze -i invalid -t src/cli/index.ts</verify>
  <acceptance_criteria>
    - `node dist/cli/index.js analyze --help` 只列出 `find|read|link|show`
    - `AnalyzeCommand` 不再默认 `impact`
    - `analyze -i find -k UnifiedResult` 不会被 `targets required` 直接拒绝
    - `src/orchestrator/__tests__/intent-router.test.ts` 覆盖新四意图和 legacy normalize
  </acceptance_criteria>
  <done>analyze 的 public 入口从帮助、校验到 router 都切到四意图基线。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node dist/cli/index.js analyze --help`
- [ ] `pnpm exec vitest run src/orchestrator/__tests__/intent-router.test.ts`
- [ ] `node dist/cli/index.js analyze -i invalid -t src/cli/index.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `analyze` 的 public help / validate / router 以四意图为唯一目标态
- 默认 intent 漂移被清掉
- `find` 的基础搜索入口不再被旧验证逻辑卡死
  </success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/03-analyze-contract-analyze/03-01-SUMMARY.md`
</output>
