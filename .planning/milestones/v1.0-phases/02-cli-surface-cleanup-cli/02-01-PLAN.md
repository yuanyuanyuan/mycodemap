---
phase: 02-cli-surface-cleanup-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/index.ts
  - src/cli/tree-sitter-check.ts
  - src/cli/__tests__/index-help.test.ts
autonomous: false
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - CLI-05
user_setup: []
must_haves:
  truths:
    - `node dist/cli/index.js --help` 不再暴露 `server`、`watch`、`report`、`logs`
    - `src/cli/index.ts` 不再把 `server`、`watch`、`report`、`logs` 注册为公共顶层命令
    - 顶层 help 输出有回归测试覆盖，避免 removed commands 被未来改动重新暴露
  artifacts:
    - src/cli/index.ts
    - src/cli/tree-sitter-check.ts
    - src/cli/__tests__/index-help.test.ts
  key_links:
    - `src/cli/index.ts` 的 Commander 注册与 `node dist/cli/index.js --help` 输出一致
    - `src/cli/tree-sitter-check.ts` 的命令集合不再把已移除的 public command 当作正常入口
    - `src/cli/__tests__/index-help.test.ts` 直接断言 help 输出移除四个命令且保留核心命令
---

<objective>
先收紧最外层 public CLI surface：让命令注册与 help 输出停止暴露 `server`、`watch`、`report`、`logs`，并补一个专门的 help 回归测试。

Purpose: 如果 Phase 2 不先拿下 `src/cli/index.ts` 和 help surface，后续兼容策略与文档同步都没有稳定事实源。
Output: 更新 `src/cli/index.ts` 的注册表、清理命令辅助列表，并新增顶层 help 回归测试。
</objective>

<execution_context>
@/data/codemap/.codex/get-shit-done/workflows/execute-plan.md
@/data/codemap/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.0-phases/02-cli-surface-cleanup-cli/02-CONTEXT.md
@.planning/milestones/v1.0-phases/02-cli-surface-cleanup-cli/02-RESEARCH.md
@.planning/milestones/v1.0-phases/01-positioning-baseline/01-VERIFICATION.md
@src/cli/index.ts
@src/cli/tree-sitter-check.ts
@src/cli/__tests__/validate-docs-script.test.ts
@/home/stark/.gstack/projects/yuanyuanyuan-mycodemap/stark-main-design-20260324-022633.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 移除主 CLI 的四个公共命令注册</name>
  <files>src/cli/index.ts</files>
  <read_first>.planning/milestones/v1.0-phases/02-cli-surface-cleanup-cli/02-CONTEXT.md, .planning/milestones/v1.0-phases/02-cli-surface-cleanup-cli/02-RESEARCH.md, src/cli/index.ts, .planning/milestones/v1.0-phases/01-positioning-baseline/01-VERIFICATION.md</read_first>
  <action>在 `src/cli/index.ts` 中移除顶层 Commander 注册：`.command('watch')`、`.command('report')`、`.command('logs')`、`.command('server')` 及其对应 import。保留 `workflow`、`ship`、`export` 与核心分析命令的当前公开状态；不要顺手改 `analyze`、`workflow`、`ship`、`src/server/` 或 `src/cli-new/`。</action>
  <verify>rg -n "\\.command\\('watch'\\)|\\.command\\('report'\\)|\\.command\\('logs'\\)|\\.command\\('server'\\)|import \\{ watchCommand|import \\{ reportCommand|import \\{ logsCommand|import \\{ serverCommand" src/cli/index.ts</verify>
  <acceptance_criteria>
    - `src/cli/index.ts` 不再包含 `.command('watch')`
    - `src/cli/index.ts` 不再包含 `.command('report')`
    - `src/cli/index.ts` 不再包含 `.command('logs')`
    - `src/cli/index.ts` 不再包含 `.command('server')`
    - `src/cli/index.ts` 仍保留 `generate`、`query`、`deps`、`impact`、`complexity`、`cycles`、`analyze`、`ci`、`workflow`、`export`、`ship`
  </acceptance_criteria>
  <done>主 CLI 注册表完成第一层收口，removed commands 不再是公开顶层命令。</done>
</task>

<task type="auto">
  <name>Task 2: 清理与 removed commands 相关的顶层命令辅助列表</name>
  <files>src/cli/tree-sitter-check.ts</files>
  <read_first>src/cli/tree-sitter-check.ts, src/cli/index.ts, .planning/codebase/CONCERNS.md</read_first>
  <action>更新 `src/cli/tree-sitter-check.ts` 中的命令集合，使其与新的 public surface 一致：从 `COMMANDS_REQUIRING_TREE_SITTER` 中移除 `watch`，并检查是否需要把 removed commands 明确列入“不需要 tree-sitter 的已移除命令”或完全不再出现在命令集合里。不要更改 `generate`、`analyze`、`deps`、`cycles`、`impact` 的现有 tree-sitter 行为。</action>
  <verify>rg -n "'watch'|'report'|'logs'|'server'" src/cli/tree-sitter-check.ts</verify>
  <acceptance_criteria>
    - `src/cli/tree-sitter-check.ts` 不再把 `watch` 作为当前 public command 的正常入口处理
    - `src/cli/tree-sitter-check.ts` 不新增 `report`、`logs`、`server` 的错误 tree-sitter 依赖
    - 保留命令的 tree-sitter 需求集合不发生无关变化
  </acceptance_criteria>
  <done>命令辅助集合与新的 public surface 保持一致，不留下死配置。</done>
</task>

<task type="auto">
  <name>Task 3: 新增顶层 help surface 回归测试</name>
  <files>src/cli/__tests__/index-help.test.ts</files>
  <read_first>src/cli/__tests__/validate-docs-script.test.ts, src/cli/index.ts, package.json</read_first>
  <action>新增 `src/cli/__tests__/index-help.test.ts`，通过执行 `node dist/cli/index.js --help` 或等价可测试入口，断言输出中不再包含 `watch`、`report`、`logs`、`server`，同时仍包含 `generate`、`query`、`deps`、`impact`、`complexity`、`cycles`、`analyze`、`ci`、`workflow`、`export`、`ship`。</action>
  <verify>pnpm exec vitest run src/cli/__tests__/index-help.test.ts</verify>
  <acceptance_criteria>
    - 新文件 `src/cli/__tests__/index-help.test.ts` 存在
    - 测试中显式断言 help 输出不包含 `watch`
    - 测试中显式断言 help 输出不包含 `report`
    - 测试中显式断言 help 输出不包含 `logs`
    - 测试中显式断言 help 输出不包含 `server`
    - 测试中显式断言 help 输出仍包含 `workflow` 与 `ship`
  </acceptance_criteria>
  <done>顶层 help surface 有了专门回归测试，不再只靠人工 eyeballing。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node dist/cli/index.js --help`
- [ ] `pnpm exec vitest run src/cli/__tests__/index-help.test.ts`
- [ ] `rg -n "\\.command\\('watch'\\)|\\.command\\('report'\\)|\\.command\\('logs'\\)|\\.command\\('server'\\)" src/cli/index.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 顶层 help surface 不再公开四个 removed commands
- `src/cli/index.ts` 与 help 输出一致
- Phase 2 有专门的 help 回归测试，而不是只靠手工检查
  </success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/02-cli-surface-cleanup-cli/02-01-SUMMARY.md`
</output>
