---
phase: 38-codex-release-entry-surface
plan: 01
type: skill
wave: 1
depends_on: []
files_modified:
  - .agents/skills/release/SKILL.md
  - docs/rules/release.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/phases/38-codex-release-entry-surface/38-VALIDATION.md
autonomous: true
requirements:
  - RELF-01
  - SAFE-01
  - SAFE-02
user_setup: []
must_haves:
  truths:
    - Codex gets a repo-local release entry surface without creating a second release authority chain.
    - `docs/rules/release.md` remains the single workflow source of truth.
    - The Codex wrapper preserves refusal cases plus `Confirmation Gate #1` and `Confirmation Gate #2`.
    - Phase 38 completes without executing `npm publish`, git tag, push, or GitHub Release.
  artifacts:
    - .agents/skills/release/SKILL.md
    - docs/rules/release.md
    - .planning/phases/38-codex-release-entry-surface/38-01-SUMMARY.md
    - .planning/phases/38-codex-release-entry-surface/38-VERIFICATION.md
    - .planning/phases/38-codex-release-entry-surface/38-VALIDATION.md
  key_links:
    - `.agents/skills/release/SKILL.md` routes back to `docs/rules/release.md`
    - `docs/rules/release.md` may mention runtime adapters, but it still owns workflow truth
---

<objective>
Complete Phase 38 by adding the first non-Claude release entry surface for Codex while preserving the existing release authority chain.

Purpose: `v1.9` proved the Claude-side release orchestrator, but `RELF-01` is still open. This phase closes that gap with the smallest possible Codex-first adapter, without reopening release governance drift or triggering real publish actions.
Output: A Codex-discoverable release wrapper, minimal release-doc routing if needed, passing docs validation, and planning truth that moves the milestone to Phase 39.
</objective>

<execution_context>
@.planning/phases/38-codex-release-entry-surface/38-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@docs/rules/release.md
@.claude/skills/release/SKILL.md
@AGENTS.md
@README.md
@docs/AI_ASSISTANT_SETUP.md
@examples/codex/codemap-agent.md
@scripts/release.sh
@.github/workflows/publish.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a repo-local Codex release wrapper</name>
  <files>.agents/skills/release/SKILL.md</files>
  <read_first>docs/rules/release.md, .claude/skills/release/SKILL.md, AGENTS.md, .planning/phases/38-codex-release-entry-surface/38-CONTEXT.md</read_first>
  <action>Create `.agents/skills/release/SKILL.md` as a Codex runtime adapter for release governance. It must point readers back to `docs/rules/release.md`, preserve refusal cases and both confirmation gates, delegate to existing tooling, and explicitly forbid autonomous publish behavior.</action>
  <acceptance_criteria>
    - `.agents/skills/release/SKILL.md` contains `docs/rules/release.md`
    - `.agents/skills/release/SKILL.md` contains `Confirmation Gate #1`
    - `.agents/skills/release/SKILL.md` contains `Confirmation Gate #2`
    - `.agents/skills/release/SKILL.md` contains `scripts/release.sh`
    - `.agents/skills/release/SKILL.md` contains `.github/workflows/publish.yml`
    - `.agents/skills/release/SKILL.md` does not describe itself as the authoritative release workflow
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy rg -n "docs/rules/release\\.md|Confirmation Gate #1|Confirmation Gate #2|scripts/release\\.sh|\\.github/workflows/publish\\.yml" .agents/skills/release/SKILL.md</automated>
    <automated>! rtk proxy rg -n "single authoritative|authoritative workflow document" .agents/skills/release/SKILL.md</automated>
  </verify>
  <done>Codex users now have a repo-local release entry surface that still routes to the same release authority.</done>
</task>

<task type="auto">
  <name>Task 2: Add only the minimum runtime-adapter routing note to release docs</name>
  <files>docs/rules/release.md</files>
  <read_first>docs/rules/release.md, .claude/skills/release/SKILL.md, .planning/phases/38-codex-release-entry-surface/38-CONTEXT.md</read_first>
  <action>If discoverability needs it, add a short runtime-adapter note to `docs/rules/release.md` that names the Claude and Codex wrappers as thin adapters. Keep workflow truth only in `docs/rules/release.md`; do not clone the workflow into router docs.</action>
  <acceptance_criteria>
    - `docs/rules/release.md` still states it is the single authority
    - if a runtime note is added, it references both `.claude/skills/release/SKILL.md` and `.agents/skills/release/SKILL.md`
    - no second workflow copy appears in README, AI guide, or entry docs
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy rg -n "single authoritative|单一权威|\\.claude/skills/release/SKILL\\.md|\\.agents/skills/release/SKILL\\.md" docs/rules/release.md</automated>
    <automated>! rtk proxy rg -n "Confirmation Gate #1|Confirmation Gate #2" README.md AI_GUIDE.md CLAUDE.md</automated>
  </verify>
  <done>Release docs make Codex discoverable without recreating release authority drift.</done>
</task>

<task type="auto">
  <name>Task 3: Verify and advance planning truth</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/PROJECT.md</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/PROJECT.md, .planning/phases/38-codex-release-entry-surface/38-VALIDATION.md</read_first>
  <action>Run the existing docs validation entrypoints, capture verification evidence, and then update planning truth so `RELF-01` is complete and the milestone advances to Phase 39.</action>
  <acceptance_criteria>
    - `rtk npm run docs:check` passes
    - `rtk node dist/cli/index.js ci check-docs-sync` passes
    - `rtk git diff --check` passes
    - Phase 38 is marked complete and `current_phase` advances to `39`
  </acceptance_criteria>
  <verify>
    <automated>rtk npm run docs:check</automated>
    <automated>rtk node dist/cli/index.js ci check-docs-sync</automated>
    <automated>rtk git diff --check</automated>
    <automated>rtk proxy rg -n "Phase 38|Complete on 2026-04-23|\\[x\\] \\*\\*RELF-01|current_phase: 39" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md</automated>
  </verify>
  <done>Phase 38 is green and the milestone is ready to proceed to publish polling.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rtk proxy rg -n "docs/rules/release\\.md|Confirmation Gate #1|Confirmation Gate #2|scripts/release\\.sh|\\.github/workflows/publish\\.yml" .agents/skills/release/SKILL.md`
- [ ] `rtk proxy rg -n "single authoritative|单一权威|\\.claude/skills/release/SKILL\\.md|\\.agents/skills/release/SKILL\\.md" docs/rules/release.md`
- [ ] `! rtk proxy rg -n "Confirmation Gate #1|Confirmation Gate #2" README.md AI_GUIDE.md CLAUDE.md`
- [ ] `rtk npm run docs:check`
- [ ] `rtk node dist/cli/index.js ci check-docs-sync`
- [ ] `rtk git diff --check`
</verification>

<success_criteria>
- `RELF-01` is satisfied with a Codex-first repo-local release entry surface
- `SAFE-01` is satisfied because release workflow authority stays in `docs/rules/release.md`
- `SAFE-02` is satisfied because Phase 38 never executes real publish actions
- Phase 38 does not expand into Kimi parity, publish polling, or readiness gate evaluation
</success_criteria>

<output>
After completion, create `.planning/phases/38-codex-release-entry-surface/38-01-SUMMARY.md`
</output>
