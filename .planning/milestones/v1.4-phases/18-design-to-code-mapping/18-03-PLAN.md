---
phase: 18-design-to-code-mapping
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - src/interface/types/design-mapping.ts
  - src/cli/design-scope-resolver.ts
  - src/cli/commands/design.ts
  - src/cli/__tests__/design-scope-resolver.test.ts
  - src/cli/commands/__tests__/design-map-command.test.ts
  - tests/fixtures/design-contracts/no-match.design.md
  - tests/fixtures/design-contracts/over-broad.design.md
  - tests/fixtures/design-contracts/high-risk.design.md
  - README.md
  - AI_GUIDE.md
  - CLAUDE.md
  - docs/ai-guide/COMMANDS.md
  - docs/ai-guide/OUTPUT.md
  - docs/ai-guide/PATTERNS.md
  - docs/rules/engineering-with-codex-openai.md
  - scripts/validate-docs.js
  - src/cli/__tests__/validate-docs-script.test.ts
  - src/cli/commands/__tests__/ci-docs-sync.test.ts
autonomous: false
requirements:
  - MAP-03
user_setup: []
must_haves:
  truths:
    - no-match / over-broad / high-risk 都是 blocker，而不是 warning
    - Phase 18 至少有 success + 3 类 blocker fixtures
    - 若 `design` public surface 新增 `map`，最小 docs / guardrail 必须同步，但不越界到 Phase 19/20 的 handoff/drift 闭环
  artifacts:
    - src/interface/types/design-mapping.ts
    - src/cli/design-scope-resolver.ts
    - src/cli/commands/design.ts
    - tests/fixtures/design-contracts/no-match.design.md
    - tests/fixtures/design-contracts/over-broad.design.md
    - tests/fixtures/design-contracts/high-risk.design.md
    - README.md
    - AI_GUIDE.md
    - docs/ai-guide/COMMANDS.md
    - docs/ai-guide/OUTPUT.md
    - scripts/validate-docs.js
  key_links:
    - `src/cli/design-scope-resolver.ts` 负责 blocker diagnostics 与 threshold 判定
    - `tests/fixtures/design-contracts/*.design.md` 锁住失败预演
    - `scripts/validate-docs.js` 与相关 tests 锁住 `design map` 的文档 truth
---

<objective>
固定 `MAP-03`：把 no-match / over-broad / high-risk 的失败语义、fixtures、最小 docs sync 一次锁死。

Purpose: Phase 18 的最后一段不是“再多找几个候选”，而是确保系统在 design 不足时会诚实地停下来；同时既然 `design` public surface 已变化，就必须同步 docs/guardrail，避免再次制造 trust drift。
Output: blocker diagnostics、失败夹具、命令失败回归、最小 docs/guardrail 同步。
</objective>

<execution_context>
@/data/codemap/.codex/get-shit-done/workflows/execute-plan.md
@/data/codemap/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-design-to-code-mapping/18-CONTEXT.md
@.planning/phases/18-design-to-code-mapping/18-RESEARCH.md
@.planning/phases/18-design-to-code-mapping/18-VALIDATION.md
@.planning/phases/18-design-to-code-mapping/18-01-PLAN.md
@.planning/phases/18-design-to-code-mapping/18-02-PLAN.md
@scripts/validate-docs.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 no-match / over-broad / high-risk blocker diagnostics</name>
  <files>src/interface/types/design-mapping.ts, src/cli/design-scope-resolver.ts, src/cli/commands/design.ts</files>
  <read_first>src/cli/design-scope-resolver.ts, src/cli/commands/design.ts, .planning/phases/18-design-to-code-mapping/18-CONTEXT.md, .planning/phases/18-design-to-code-mapping/18-RESEARCH.md</read_first>
  <action>在 `src/interface/types/design-mapping.ts` 中新增 blocker diagnostics code：`no-candidates`、`over-broad-scope`、`high-risk-scope`。在 `src/cli/design-scope-resolver.ts` 中落实三条判定：1) `candidates.length === 0` → `no-candidates`；2) 候选集合跨多个无关子系统、或超过明确上限、或主要由 `src/cli/index.ts` / `src/cli/commands/analyze.ts` / `src/orchestrator/workflow/workflow-orchestrator.ts` 等高 blast-radius 文件构成 → `over-broad-scope` / `high-risk-scope`；3) 一旦 diagnostics 中存在 blocker，`DesignMappingResult.ok` 必须为 `false`。在 `src/cli/commands/design.ts` 中，当 `design map` 返回 blocker diagnostics 时设置非零 `process.exitCode`。</action>
  <verify>rg -n "no-candidates|over-broad-scope|high-risk-scope" src/interface/types/design-mapping.ts src/cli/design-scope-resolver.ts src/cli/commands/design.ts</verify>
  <acceptance_criteria>
    - 三个 blocker code 在 `src/interface/types/design-mapping.ts` 中存在
    - `src/cli/design-scope-resolver.ts` 明确判断 `candidates.length === 0`
    - `src/cli/design-scope-resolver.ts` 明确提及 `src/cli/index.ts`、`src/cli/commands/analyze.ts`、`src/orchestrator/workflow/workflow-orchestrator.ts`
    - `src/cli/commands/design.ts` 在 blocker path 设置 `process.exitCode = 1`
  </acceptance_criteria>
  <done>design 不足或风险过高时，mapping 会诚实阻断而不是继续假装可执行。</done>
</task>

<task type="auto">
  <name>Task 2: 编写失败 fixtures 与 blocker regression</name>
  <files>src/cli/__tests__/design-scope-resolver.test.ts, src/cli/commands/__tests__/design-map-command.test.ts, tests/fixtures/design-contracts/no-match.design.md, tests/fixtures/design-contracts/over-broad.design.md, tests/fixtures/design-contracts/high-risk.design.md</files>
  <read_first>tests/fixtures/design-contracts/mapping-basic.design.md, src/cli/__tests__/design-contract-loader.test.ts, src/cli/commands/__tests__/design-map-command.test.ts</read_first>
  <action>新增 3 个失败夹具：`tests/fixtures/design-contracts/no-match.design.md`、`tests/fixtures/design-contracts/over-broad.design.md`、`tests/fixtures/design-contracts/high-risk.design.md`。为 resolver 与 command tests 增加 blocker 回归：`no-match` 断言 diagnostics code 为 `no-candidates` 且 `ok === false`；`over-broad` 断言 diagnostics code 为 `over-broad-scope`；`high-risk` 断言 diagnostics code 为 `high-risk-scope` 且 CLI 退出码为非零。保持 success-path fixture 不回归。</action>
  <verify>pnpm exec vitest run src/cli/__tests__/design-scope-resolver.test.ts src/cli/commands/__tests__/design-map-command.test.ts && (node dist/cli/index.js design map tests/fixtures/design-contracts/no-match.design.md --json >/tmp/phase18-no-match.json 2>&1; test $? -ne 0 && rg -n '"code": "no-candidates"' /tmp/phase18-no-match.json) && (node dist/cli/index.js design map tests/fixtures/design-contracts/over-broad.design.md --json >/tmp/phase18-over-broad.json 2>&1; test $? -ne 0 && rg -n '"code": "over-broad-scope"' /tmp/phase18-over-broad.json) && (node dist/cli/index.js design map tests/fixtures/design-contracts/high-risk.design.md --json >/tmp/phase18-high-risk.json 2>&1; test $? -ne 0 && rg -n '"code": "high-risk-scope"' /tmp/phase18-high-risk.json)</verify>
  <acceptance_criteria>
    - 3 个失败夹具文件都存在
    - resolver tests 明确断言 `no-candidates` / `over-broad-scope` / `high-risk-scope`
    - command tests 明确断言 blocker path 的非零退出码
    - success-path tests 继续断言 `mapping-basic.design.md` 为 `ok: true`
  </acceptance_criteria>
  <done>Phase 18 的失败预演不再停留在口头要求，而被 fixtures 与 tests 锁住。</done>
</task>

<task type="auto">
  <name>Task 3: 最小同步 `design map` 文档与 docs guardrail</name>
  <files>README.md, AI_GUIDE.md, CLAUDE.md, docs/ai-guide/COMMANDS.md, docs/ai-guide/OUTPUT.md, docs/ai-guide/PATTERNS.md, docs/rules/engineering-with-codex-openai.md, scripts/validate-docs.js, src/cli/__tests__/validate-docs-script.test.ts, src/cli/commands/__tests__/ci-docs-sync.test.ts</files>
  <read_first>README.md, AI_GUIDE.md, CLAUDE.md, docs/ai-guide/COMMANDS.md, docs/ai-guide/OUTPUT.md, docs/ai-guide/PATTERNS.md, scripts/validate-docs.js</read_first>
  <action>最小同步所有受 `design map` 影响的 public docs：在 `README.md`、`AI_GUIDE.md`、`CLAUDE.md`、`docs/ai-guide/COMMANDS.md`、`docs/ai-guide/OUTPUT.md`、`docs/ai-guide/PATTERNS.md`、`docs/rules/engineering-with-codex-openai.md` 中新增 `mycodemap design map mycodemap.design.md --json` 入口、success/blocker output 说明，以及 `design validate → design map` 的最小工作流说明。同步扩展 `scripts/validate-docs.js` 与 `src/cli/__tests__/validate-docs-script.test.ts`，把 `design map` 命令与输出字段（至少 `candidates`、`diagnostics`、`unknowns`）纳入 guardrail；保持 `src/cli/commands/__tests__/ci-docs-sync.test.ts` 继续覆盖 CLI docs sync 入口。**不要**在这一任务里提前写 handoff package 或 drift verification 的完整 docs——只更新 `design map` 直接触达的 contract 文档。</action>
  <verify>rg -n "design map|design validate -> design map|candidates|unknowns|diagnostics" README.md AI_GUIDE.md CLAUDE.md docs/ai-guide/COMMANDS.md docs/ai-guide/OUTPUT.md docs/ai-guide/PATTERNS.md docs/rules/engineering-with-codex-openai.md scripts/validate-docs.js && npm run docs:check:human && pnpm exec vitest run src/cli/__tests__/validate-docs-script.test.ts src/cli/commands/__tests__/ci-docs-sync.test.ts</verify>
  <acceptance_criteria>
    - 所有列出的 docs 文件都包含 `design map`
    - `docs/ai-guide/OUTPUT.md` 明确出现 `unknowns` 与 `diagnostics`
    - `scripts/validate-docs.js` 明确检查 `design map`
    - docs guardrail tests 与 CLI docs sync tests 退出码为 `0`
  </acceptance_criteria>
  <done>`design map` 作为 public contract 的最小文档面已同步锁住，不再依赖人工记忆。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run src/cli/__tests__/design-scope-resolver.test.ts src/cli/commands/__tests__/design-map-command.test.ts`
- [ ] `(node dist/cli/index.js design map tests/fixtures/design-contracts/no-match.design.md --json >/tmp/phase18-no-match.json 2>&1; test $? -ne 0 && rg -n '"code": "no-candidates"' /tmp/phase18-no-match.json)`
- [ ] `(node dist/cli/index.js design map tests/fixtures/design-contracts/over-broad.design.md --json >/tmp/phase18-over-broad.json 2>&1; test $? -ne 0 && rg -n '"code": "over-broad-scope"' /tmp/phase18-over-broad.json)`
- [ ] `(node dist/cli/index.js design map tests/fixtures/design-contracts/high-risk.design.md --json >/tmp/phase18-high-risk.json 2>&1; test $? -ne 0 && rg -n '"code": "high-risk-scope"' /tmp/phase18-high-risk.json)`
- [ ] `npm run docs:check:human`
- [ ] `pnpm exec vitest run src/cli/__tests__/validate-docs-script.test.ts src/cli/commands/__tests__/ci-docs-sync.test.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- no-match / over-broad / high-risk 全部为 blocker，且有 fixtures 覆盖
- `design map` 的最小 public contract 文档面已同步
- 本 phase 只同步 `design map` 直接相关 docs，不越界实现 Phase 19/20 的完整 handoff/drift 文档
  </success_criteria>

<output>
After completion, create `.planning/phases/18-design-to-code-mapping/18-03-SUMMARY.md`
</output>
