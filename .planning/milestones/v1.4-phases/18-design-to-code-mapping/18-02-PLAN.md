---
phase: 18-design-to-code-mapping
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/interface/types/design-mapping.ts
  - src/cli/design-scope-resolver.ts
  - src/cli/commands/design.ts
  - src/cli/__tests__/design-scope-resolver.test.ts
  - src/cli/commands/__tests__/design-map-command.test.ts
autonomous: false
requirements:
  - MAP-02
user_setup: []
must_haves:
  truths:
    - mapping 结果必须同时包含 `dependencies`、`testImpact`、`risk`、`confidence`、`unknowns`
    - `test impact` 不能只转抄单个 `testFile`，而要显式表达“零个/一个/多个测试命中”
    - human-readable summary 与 machine-readable JSON 都要指向同一份 mapping truth
  artifacts:
    - src/interface/types/design-mapping.ts
    - src/cli/design-scope-resolver.ts
    - src/cli/commands/design.ts
    - src/cli/__tests__/design-scope-resolver.test.ts
    - src/cli/commands/__tests__/design-map-command.test.ts
  key_links:
    - `src/cli/commands/analyze.ts` 已提供 `confidence`、`risk` 与 `attachTestFiles()` 逻辑
    - `src/orchestrator/test-linker.ts` 是 source→test mapping 的正式 seam
    - `src/interface/types/design-mapping.ts` 负责把 enrichment 后的结果稳定暴露给人类与机器
---

<objective>
在 success-path baseline 上补齐 reviewer 真正需要的信息：dependencies、test impact、risk、confidence 与 unknowns。

Purpose: 只有 candidate 没有上下文仍然不够；Phase 18 的价值在于让人类能判断“为什么会改这里、影响多大、还有哪些未知”。
Output: enriched mapping result、stable JSON/human renderers，以及覆盖 metadata 的 focused regressions。
</objective>

<execution_context>
@/data/codemap/.codex/get-shit-done/workflows/execute-plan.md
@/data/codemap/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-design-to-code-mapping/18-CONTEXT.md
@.planning/phases/18-design-to-code-mapping/18-RESEARCH.md
@.planning/phases/18-design-to-code-mapping/18-01-PLAN.md
@src/cli/commands/analyze.ts
@src/cli/commands/impact.ts
@src/orchestrator/test-linker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 用 analyze/link/impact 丰富 dependencies、risk 与 confidence</name>
  <files>src/interface/types/design-mapping.ts, src/cli/design-scope-resolver.ts, src/cli/__tests__/design-scope-resolver.test.ts</files>
  <read_first>src/cli/commands/analyze.ts, src/cli/commands/impact.ts, src/orchestrator/types.ts, .planning/phases/18-design-to-code-mapping/18-RESEARCH.md</read_first>
  <action>更新 `src/interface/types/design-mapping.ts` 与 `src/cli/design-scope-resolver.ts`：对每个 candidate file 追加 enrichment 流程，至少包含：1) `new AnalyzeCommand({ intent: 'read', targets: [candidatePath], includeTests: true, json: true, structured: true })` 提供 `risk` 与 `confidence` 基础；2) `new AnalyzeCommand({ intent: 'link', targets: [candidatePath], includeTests: true, json: true, structured: true })` 或 `ImpactCommand` 提供 `dependencies` / `importedBy` / blast-radius 证据；3) 将 enrichment 结果写入 candidate 的 `dependencies`、`risk`、`confidence` 字段。更新 `src/cli/__tests__/design-scope-resolver.test.ts`，断言 enriched candidate 至少包含非空 `confidence` 与数组型 `dependencies`。</action>
  <verify>pnpm exec vitest run src/cli/__tests__/design-scope-resolver.test.ts</verify>
  <acceptance_criteria>
    - `src/cli/design-scope-resolver.ts` 同时出现 `intent: 'read'` 与 `intent: 'link'`
    - 每个 enriched candidate 都有 `confidence.score` 和 `confidence.level`
    - 每个 enriched candidate 都有数组型 `dependencies`
    - `src/cli/__tests__/design-scope-resolver.test.ts` 断言 `risk` 为 `high|medium|low`
  </acceptance_criteria>
  <done>mapping 结果已包含与候选范围绑定的依赖、风险与置信度。</done>
</task>

<task type="auto">
  <name>Task 2: 补 test impact 与 unknowns synthesis</name>
  <files>src/interface/types/design-mapping.ts, src/cli/design-scope-resolver.ts, src/cli/__tests__/design-scope-resolver.test.ts</files>
  <read_first>src/orchestrator/test-linker.ts, src/cli/commands/analyze.ts, .planning/phases/18-design-to-code-mapping/18-CONTEXT.md</read_first>
  <action>在 `src/cli/design-scope-resolver.ts` 中加入 `resolveTestFile()` / `resolveTestFiles()` 集成，把每个 candidate 的测试命中写入 `testImpact: string[]`。同时实现 `unknowns` 聚合规则：至少包含以下场景的显式字符串条目 —— 无测试命中、低置信候选、design contract 中存在 `Open Questions` section、以及命中 partial/stubbed implementation 的情况。同步更新 `src/interface/types/design-mapping.ts`，确保 `testImpact` 与 `unknowns` 为正式字段；更新 resolver tests 断言 `testImpact` 总是数组，且低置信/零测试场景会生成 `unknowns[]`。</action>
  <verify>pnpm exec vitest run src/cli/__tests__/design-scope-resolver.test.ts</verify>
  <acceptance_criteria>
    - `src/interface/types/design-mapping.ts` 中 `testImpact` 和 `unknowns` 都是数组字段
    - `src/cli/design-scope-resolver.ts` 调用了 `resolveTestFile(` 或 `resolveTestFiles(`
    - resolver tests 覆盖“零测试命中 → unknowns 非空”
    - resolver tests 覆盖“低置信 candidate → unknowns 非空”
  </acceptance_criteria>
  <done>mapping 结果不再只给范围，还会显式告诉人类测试影响与未知项。</done>
</task>

<task type="auto">
  <name>Task 3: 稳定 machine/human 输出契约并锁定回归</name>
  <files>src/cli/commands/design.ts, src/cli/commands/__tests__/design-map-command.test.ts</files>
  <read_first>src/cli/commands/design.ts, src/interface/types/design-mapping.ts, .planning/phases/18-design-to-code-mapping/18-RESEARCH.md</read_first>
  <action>更新 `src/cli/commands/design.ts` 的 `map` renderer：JSON 模式必须稳定输出 `summary`、`candidates[]`、`diagnostics[]`；human 模式必须明确打印 `Candidates`、`Unknowns`、`Diagnostics` 三段。`summary` 至少包含 `candidateCount`、`blocked`、`unknownCount`。在 `src/cli/commands/__tests__/design-map-command.test.ts` 中新增回归，断言 `runDesignMap(..., { json: true })` 返回的每个 candidate 都有 `dependencies`、`testImpact`、`risk`、`confidence`、`unknowns`；human mode 输出包含 `Candidates` 与 `Unknowns` 标题。</action>
  <verify>pnpm exec vitest run src/cli/commands/__tests__/design-map-command.test.ts && node dist/cli/index.js design map tests/fixtures/design-contracts/mapping-basic.design.md --json | node -e "const fs=require('fs'); const o=JSON.parse(fs.readFileSync(0,'utf8')); if (!o.summary || !Array.isArray(o.candidates) || !o.candidates.every(c => Array.isArray(c.dependencies) && Array.isArray(c.testImpact) && c.confidence && Array.isArray(c.unknowns))) process.exit(1);" </verify>
  <acceptance_criteria>
    - `src/cli/commands/design.ts` human renderer包含 `Candidates`、`Unknowns`、`Diagnostics`
    - JSON output 包含 `summary.candidateCount`
    - JSON output 中每个 candidate 同时包含 `dependencies`、`testImpact`、`risk`、`confidence`、`unknowns`
    - `src/cli/commands/__tests__/design-map-command.test.ts` 覆盖 human + machine 两条输出路径
  </acceptance_criteria>
  <done>Phase 18 的输出已足够给人类 review，也能稳定给后续 agent 消费。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run src/cli/__tests__/design-scope-resolver.test.ts src/cli/commands/__tests__/design-map-command.test.ts`
- [ ] `node dist/cli/index.js design map tests/fixtures/design-contracts/mapping-basic.design.md --json | node -e "const fs=require('fs'); const o=JSON.parse(fs.readFileSync(0,'utf8')); if (!o.candidates?.length) process.exit(1); if (!o.candidates.every(c => Array.isArray(c.dependencies) && Array.isArray(c.testImpact) && c.confidence && Array.isArray(c.unknowns))) process.exit(1);"`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- mapping 输出同时具备 dependencies、test impact、risk、confidence、unknowns
- `test impact` 与 `unknowns` 不再依赖隐式推断，而成为正式字段
- human 与 machine 两条输出面围绕同一份 mapping truth
  </success_criteria>

<output>
After completion, create `.planning/phases/18-design-to-code-mapping/18-02-SUMMARY.md`
</output>
