---
phase: 19-handoff-package-human-gates
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/cli/design-handoff-builder.ts
  - src/cli/__tests__/design-handoff-builder.test.ts
  - tests/fixtures/design-contracts/handoff-pending-review.design.md
autonomous: false
requirements:
  - HOF-02
  - HOF-03
user_setup: []
must_haves:
  truths:
    - handoff builder 只消费 validated design contract 与 successful mapping truth，不从失败 scope 猜测
    - approvals、assumptions、openQuestions 都有 provenance，且保持分离建模
    - unresolved assumptions / open questions 会让 artifact 标记 `readyForExecution=false`，mapping blocker 则阻断 handoff
  artifacts:
    - src/cli/design-handoff-builder.ts
    - src/cli/__tests__/design-handoff-builder.test.ts
    - tests/fixtures/design-contracts/handoff-pending-review.design.md
  key_links:
    - `src/cli/design-handoff-builder.ts` 从 design + mapping truth 组装 canonical handoff payload
    - `src/cli/design-scope-resolver.ts` 提供 touched files / tests / risks / unknowns 的上游数据
    - `src/interface/types/design-handoff.ts` 定义 trace items 与 gate state
---

<objective>
把 Phase 19 的骨架升级为真实 handoff builder：消费 design contract 与 mapping result，产出 approvals / assumptions / openQuestions 可追踪、带 readiness gate 的 canonical handoff artifact。

Purpose: 如果 Phase 19 只有模板和路径，没有真实 traceability / gate semantics，它仍然只是“漂亮的摘要”，无法真正支撑后续 agent handoff。
Output: `buildDesignHandoff()` canonical builder、gate-state logic、traceability rules、focused regressions 与 review-needed fixture。
</objective>

<execution_context>
@/data/codemap/.codex/get-shit-done/workflows/execute-plan.md
@/data/codemap/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-handoff-package-human-gates/19-CONTEXT.md
@.planning/phases/19-handoff-package-human-gates/19-RESEARCH.md
@.planning/phases/19-handoff-package-human-gates/19-01-PLAN.md
@src/interface/types/design-handoff.ts
@src/cli/design-contract-loader.ts
@src/cli/design-scope-resolver.ts
@src/interface/types/design-mapping.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 canonical handoff builder</name>
  <files>src/cli/design-handoff-builder.ts</files>
  <read_first>.planning/phases/19-handoff-package-human-gates/19-CONTEXT.md, src/cli/design-contract-loader.ts, src/cli/design-scope-resolver.ts, src/interface/types/design-contract.ts, src/interface/types/design-mapping.ts, src/interface/types/design-handoff.ts</read_first>
  <action>在 `src/cli/design-handoff-builder.ts` 中新增 `export async function buildDesignHandoff(options)`。该函数必须：1) 通过 `loadDesignContract({ filePath })` 获取 validated / normalized design contract；2) 调用 `resolveDesignScope({ filePath })` 获取 mapping truth；3) 从 contract sections 提取 `goal`、`constraints`、`acceptanceCriteria`、`nonGoals`、可选 `openQuestions`；4) 从 mapping candidates / dependencies / testImpact / risk / unknowns 归并出 `touchedFiles`、`supportingFiles`、`tests`、`risks`；5) 把 `acceptanceCriteria` 映射为 `validationChecklist` 条目；6) 返回完整 `DesignHandoffResult`，供 markdown renderer 与 command 共用。</action>
  <verify>pnpm exec vitest run src/cli/__tests__/design-handoff-builder.test.ts</verify>
  <acceptance_criteria>
    - `src/cli/design-handoff-builder.ts` 包含 `buildDesignHandoff`
    - builder 同时调用 `loadDesignContract` 与 `resolveDesignScope`
    - `DesignHandoffResult.handoff` 同时填充 `touchedFiles`、`supportingFiles`、`tests`、`validationChecklist`
    - focused tests 退出码为 `0`
  </acceptance_criteria>
  <done>handoff 不再只是模板，而是建立在真实 design / mapping truth 上的 canonical payload。</done>
</task>

<task type="auto">
  <name>Task 2: 落实 approvals / assumptions / openQuestions 的 provenance 与 gate 规则</name>
  <files>src/cli/design-handoff-builder.ts, src/cli/__tests__/design-handoff-builder.test.ts</files>
  <read_first>.planning/phases/19-handoff-package-human-gates/19-CONTEXT.md, src/interface/types/design-handoff.ts, src/interface/types/design-contract.ts, src/interface/types/design-mapping.ts</read_first>
  <action>在 builder 中落实 traceability / gate semantics：1) `approvals` 至少包含 design contract required sections 的 approved trace item，并在存在 unresolved items 时增加 `needs-review` gate item；2) `assumptions` 只来源于 mapping `unknowns`、builder 必要推导或用户明确留给 the agent discretion 的条目；3) `openQuestions` 继承 design contract `Open Questions` section，并可补充 builder 发现的 review-needed 问题；4) 每个 approval / assumption / openQuestion 都必须包含 `sourceRefs`，格式至少可表达 `design:{section}`、`candidate:{path}`、`diagnostic:{code}`；5) 当 design contract blocker 或 mapping blocker 存在时，`ok=false`，`diagnostics` 带上 `blocked-mapping` 或上游 blocker code，且 `readyForExecution=false`；6) 当只是存在 assumptions / openQuestions 时，允许 `ok=true`，但必须 `readyForExecution=false` 并在 summary 中标记 `requiresReview=true`。</action>
  <verify>pnpm exec vitest run src/cli/__tests__/design-handoff-builder.test.ts</verify>
  <acceptance_criteria>
    - tests 覆盖 `readyForExecution=false` 的 review-needed path
    - tests 覆盖 mapping blocker path 返回 `ok=false`
    - 所有 trace items 都有非空 `sourceRefs`
    - `summary` 同时包含 `approvalCount`、`assumptionCount`、`openQuestionCount`、`requiresReview`
  </acceptance_criteria>
  <done>handoff 的 gate status 与 traceability 已正式落地，而不是靠 prose 解释。</done>
</task>

<task type="auto">
  <name>Task 3: 添加 review-needed fixture 与 traceability regressions</name>
  <files>src/cli/__tests__/design-handoff-builder.test.ts, tests/fixtures/design-contracts/handoff-pending-review.design.md</files>
  <read_first>tests/fixtures/design-contracts/handoff-basic.design.md, tests/fixtures/design-contracts/no-match.design.md, src/cli/design-handoff-builder.ts</read_first>
  <action>新增 `tests/fixtures/design-contracts/handoff-pending-review.design.md`，要求包含至少 1 条 `Open Questions`。扩展 `src/cli/__tests__/design-handoff-builder.test.ts`，至少断言：1) pending-review fixture 的 builder 结果 `ok=true` 且 `readyForExecution=false`；2) `handoff.openQuestions.length > 0` 且 `summary.requiresReview === true`；3) `no-match.design.md` 的 builder 结果 `ok=false`，并返回 `blocked-mapping` 或转发的 mapping blocker diagnostics；4) `approvals`、`assumptions`、`openQuestions` 三类 trace item 都保留 `sourceRefs`。</action>
  <verify>pnpm exec vitest run src/cli/__tests__/design-handoff-builder.test.ts</verify>
  <acceptance_criteria>
    - `tests/fixtures/design-contracts/handoff-pending-review.design.md` 包含 `## Open Questions`
    - builder tests 退出码为 `0`
    - 测试断言 pending-review path 为 `ok=true` 且 `readyForExecution=false`
    - 测试断言 blocked path 为 `ok=false`
  </acceptance_criteria>
  <done>traceability 与 gate semantics 已被 success / review-needed / blocked 三条路径锁住。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run src/cli/__tests__/design-handoff-builder.test.ts`
- [ ] `rg -n "buildDesignHandoff|readyForExecution|sourceRefs|blocked-mapping" src/cli/design-handoff-builder.ts`
- [ ] `rg -n "## Open Questions" tests/fixtures/design-contracts/handoff-pending-review.design.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- handoff builder 已能从 design / mapping truth 生成可追踪的 canonical artifact
- assumptions / openQuestions 不会被 silent promotion 成 approved facts
- review-needed 与 blocker 两类 gate path 都被 focused tests 锁住
  </success_criteria>

<output>
After completion, create `.planning/phases/19-handoff-package-human-gates/19-02-SUMMARY.md`
</output>
