---
phase: 19-handoff-package-human-gates
plan: 03
type: execute
wave: 3
depends_on:
  - 19-02
files_modified:
  - src/cli/commands/design.ts
  - src/cli/commands/__tests__/design-handoff-command.test.ts
  - README.md
  - AI_GUIDE.md
  - CLAUDE.md
  - docs/ai-guide/COMMANDS.md
  - docs/ai-guide/OUTPUT.md
  - docs/ai-guide/PATTERNS.md
  - docs/rules/engineering-with-codex-openai.md
  - scripts/validate-docs.js
  - src/cli/__tests__/validate-docs-script.test.ts
  - src/cli/commands/__tests__/ci-docs-sync.test.ts
autonomous: false
requirements:
  - HOF-01
  - HOF-04
user_setup: []
must_haves:
  truths:
    - `design handoff` 成为正式 public CLI 入口，同时不污染 `analyze` / `workflow`
    - human mode 会写出 `.handoff.md` 与 `.handoff.json` artifact，json mode 保持纯 JSON
    - docs / guardrail 同步 `design validate → design map → design handoff`，且 workflow 仍只保留四阶段
  artifacts:
    - src/cli/commands/design.ts
    - src/cli/commands/__tests__/design-handoff-command.test.ts
    - README.md
    - AI_GUIDE.md
    - docs/ai-guide/COMMANDS.md
    - docs/ai-guide/OUTPUT.md
    - docs/ai-guide/PATTERNS.md
    - scripts/validate-docs.js
  key_links:
    - `src/cli/commands/design.ts` 暴露 `design handoff [file]`
    - `docs/ai-guide/OUTPUT.md` 记录 `DesignHandoffResult` JSON 契约
    - `scripts/validate-docs.js` 与 tests 锁住 handoff docs truth
---

<objective>
把 Phase 19 的 handoff builder 接到正式 CLI 与文档护栏：新增 `design handoff` 命令、最小 docs sync、focused regressions，并明确 handoff 仍是 `design` sidecar 而不是 workflow / analyze 扩展。

Purpose: 如果 Phase 19 只有内部 builder，没有稳定命令面与文档 truth，AI / agent 仍然无法可靠发现或消费 handoff artifact。
Output: `design handoff` command、CLI tests、README / AI docs / rules / docs guardrail 同步。
</objective>

<execution_context>
@/data/codemap/.codex/get-shit-done/workflows/execute-plan.md
@/data/codemap/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-handoff-package-human-gates/19-CONTEXT.md
@.planning/phases/19-handoff-package-human-gates/19-RESEARCH.md
@.planning/phases/19-handoff-package-human-gates/19-02-PLAN.md
@src/cli/commands/design.ts
@src/cli/design-handoff-builder.ts
@src/cli/paths.ts
@docs/ai-guide/COMMANDS.md
@docs/ai-guide/OUTPUT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 暴露 `design handoff` 命令并复用 canonical builder</name>
  <files>src/cli/commands/design.ts</files>
  <read_first>.planning/phases/19-handoff-package-human-gates/19-CONTEXT.md, src/cli/commands/design.ts, src/cli/design-handoff-builder.ts, src/cli/paths.ts</read_first>
  <action>在 `src/cli/commands/design.ts` 中新增 `handoff` 子命令：`design handoff [file]`，支持 `-j, --json` 与 `-o, --output <dir>`。新增 `runDesignHandoff(filePath?, options?)`，内部调用 `buildDesignHandoff({ filePath, outputDir: options.output })`。human 模式下：写出 `{stem}.handoff.md` 与 `{stem}.handoff.json` 到 resolved output dir，并打印 `Ready for execution` / `Needs human review`、candidate / approval / assumption / open question summary 与 artifact paths。JSON 模式下：只打印纯 `DesignHandoffResult` JSON，不写额外 prose。只有当 `ok=false` 或 handoff/blocker diagnostics 存在时才设置非零 `process.exitCode`；review-needed 只通过 `readyForExecution=false` 表达，不应把命令当成 crash path。</action>
  <verify>rg -n "\\.command\\('handoff'\\)|runDesignHandoff|--output <dir>|readyForExecution" src/cli/commands/design.ts</verify>
  <acceptance_criteria>
    - `src/cli/commands/design.ts` 包含 `.command('handoff')`
    - `src/cli/commands/design.ts` 包含 `runDesignHandoff`
    - 命令支持 `--json` 与 `--output`
    - review-needed path 不会直接被当成 `process.exitCode = 1`
  </acceptance_criteria>
  <done>`design handoff` 成为正式 CLI 入口，同时保持 human gate 与 blocker path 语义分离。</done>
</task>

<task type="auto">
  <name>Task 2: 添加 command regression 与 write-to-disk smoke coverage</name>
  <files>src/cli/commands/__tests__/design-handoff-command.test.ts</files>
  <read_first>src/cli/commands/design.ts, src/cli/design-handoff-builder.ts, tests/fixtures/design-contracts/handoff-basic.design.md, tests/fixtures/design-contracts/handoff-pending-review.design.md</read_first>
  <action>新增 `src/cli/commands/__tests__/design-handoff-command.test.ts`，至少断言：1) `designCommand.helpInformation()` 包含 `handoff`；2) `runDesignHandoff(..., { json: true })` 返回 `ok`、`readyForExecution`、`artifacts`、`handoff.approvals`、`handoff.assumptions`、`handoff.openQuestions`；3) human mode 在临时 output dir 下会写出 `.handoff.md` 与 `.handoff.json`；4) pending-review fixture 结果 `readyForExecution=false` 但命令不应被视为 blocker failure；5) `no-match.design.md` 会走 non-zero blocker path。</action>
  <verify>pnpm exec vitest run src/cli/commands/__tests__/design-handoff-command.test.ts</verify>
  <acceptance_criteria>
    - `src/cli/commands/__tests__/design-handoff-command.test.ts` 退出码为 `0`
    - 测试断言 help surface 包含 `handoff`
    - 测试断言 human mode 写出两份 artifact 文件
    - 测试断言 blocked path 为 non-zero / `ok=false`
  </acceptance_criteria>
  <done>CLI surface、write-to-disk 路径与 review-needed / blocker 语义已被 focused command tests 锁住。</done>
</task>

<task type="auto">
  <name>Task 3: 最小同步 handoff 文档与 docs guardrail</name>
  <files>README.md, AI_GUIDE.md, CLAUDE.md, docs/ai-guide/COMMANDS.md, docs/ai-guide/OUTPUT.md, docs/ai-guide/PATTERNS.md, docs/rules/engineering-with-codex-openai.md, scripts/validate-docs.js, src/cli/__tests__/validate-docs-script.test.ts, src/cli/commands/__tests__/ci-docs-sync.test.ts</files>
  <read_first>README.md, AI_GUIDE.md, docs/ai-guide/COMMANDS.md, docs/ai-guide/OUTPUT.md, docs/ai-guide/PATTERNS.md, scripts/validate-docs.js, .planning/phases/19-handoff-package-human-gates/19-CONTEXT.md</read_first>
  <action>最小同步所有直接受 `design handoff` 影响的 public docs：在 `README.md`、`AI_GUIDE.md`、`CLAUDE.md`、`docs/ai-guide/COMMANDS.md`、`docs/ai-guide/OUTPUT.md`、`docs/ai-guide/PATTERNS.md`、`docs/rules/engineering-with-codex-openai.md` 中新增 `mycodemap design handoff mycodemap.design.md --json` 入口、artifact output path 说明、`readyForExecution` / `approvals` / `assumptions` / `openQuestions` JSON contract，以及最小工作流 `design validate → design map → design handoff`。同步扩展 `scripts/validate-docs.js` 与 `src/cli/__tests__/validate-docs-script.test.ts`，把 `design handoff` 命令与关键字段纳入 guardrail；保持 `workflow` 仍只保留 `find/read/link/show` 四阶段，不要在该任务里提前写完整 Phase 20 的 drift / e2e 叙事。</action>
  <verify>rg -n "design handoff|design validate.*design map.*design handoff|readyForExecution|approvals|assumptions|openQuestions" README.md AI_GUIDE.md CLAUDE.md docs/ai-guide/COMMANDS.md docs/ai-guide/OUTPUT.md docs/ai-guide/PATTERNS.md docs/rules/engineering-with-codex-openai.md scripts/validate-docs.js && npm run docs:check:human && pnpm exec vitest run src/cli/__tests__/validate-docs-script.test.ts src/cli/commands/__tests__/ci-docs-sync.test.ts</verify>
  <acceptance_criteria>
    - 所有列出的 docs 文件都包含 `design handoff`
    - `docs/ai-guide/OUTPUT.md` 明确记录 `readyForExecution`、`approvals`、`assumptions`、`openQuestions`
    - `scripts/validate-docs.js` 明确检查 `design handoff`
    - `npm run docs:check:human` 与相关 docs tests 通过
  </acceptance_criteria>
  <done>`design handoff` 的最小 public contract 文档面与 docs guardrail 已同步锁住。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run src/cli/commands/__tests__/design-handoff-command.test.ts`
- [ ] `node dist/cli/index.js design handoff tests/fixtures/design-contracts/handoff-basic.design.md --json`
- [ ] `npm run docs:check:human`
- [ ] `pnpm exec vitest run src/cli/__tests__/validate-docs-script.test.ts src/cli/commands/__tests__/ci-docs-sync.test.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `design handoff` 已成为稳定 public seam，且不污染 `analyze` / `workflow`
- human/json dual output 与 artifact paths 已被 command tests 锁住
- 直接相关 docs / guardrail 已同步，不再依赖人工记忆发现 handoff surface
  </success_criteria>

<output>
After completion, create `.planning/phases/19-handoff-package-human-gates/19-03-SUMMARY.md`
</output>
