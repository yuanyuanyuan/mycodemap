---
phase: 20-design-drift-verification-docs-sync
plan: 03
type: execute
wave: 3
depends_on:
  - 20-02
files_modified:
  - src/cli/__tests__/design-verify-e2e.test.ts
  - src/cli/__tests__/design-verification-builder.test.ts
  - src/cli/commands/__tests__/design-verify-command.test.ts
  - src/cli/__tests__/validate-docs-script.test.ts
  - tests/fixtures/design-contracts/verify-ready.design.md
autonomous: false
requirements:
  - VAL-04
  - DOC-07
  - VAL-05
user_setup: []
must_haves:
  truths:
    - 端到端示例必须证明完整链路 `design validate → design map → design handoff → design verify`
    - 至少三类失败模式要被自动化回归覆盖，而不是只写在 UAT 文本里
    - milestone audit 证据优先来自可复现命令和测试，而不是人工 narrative
  artifacts:
    - src/cli/__tests__/design-verify-e2e.test.ts
    - src/cli/__tests__/design-verification-builder.test.ts
    - src/cli/commands/__tests__/design-verify-command.test.ts
    - src/cli/__tests__/validate-docs-script.test.ts
    - tests/fixtures/design-contracts/verify-ready.design.md
  key_links:
    - `src/cli/__tests__/design-verify-e2e.test.ts` 锁住 full-chain success / failure evidence
    - `tests/fixtures/design-contracts/verify-ready.design.md` 提供真正 ready 的演示输入
    - `src/cli/__tests__/validate-docs-script.test.ts` 锁住 verify docs drift 与 workflow boundary drift
---

<objective>
把 Phase 20 的 verify 能力变成可审计的闭环证据：补零未决项 ready-path E2E、补三类失败预演、把 docs drift regression 纳入自动化，给后续 UAT / milestone audit 留下硬证据。

Purpose: 如果 Phase 20 只交 command 和 docs，而不交 success/failure E2E 证据，最终依然无法证明“设计到实现再到验证”的链路真正闭环。
Output: full-chain E2E tests、ready fixture、failure rehearsal coverage、verify-specific docs drift regressions。
</objective>

<execution_context>
@/data/codemap/.codex/get-shit-done/workflows/execute-plan.md
@/data/codemap/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-design-drift-verification-docs-sync/20-CONTEXT.md
@.planning/phases/20-design-drift-verification-docs-sync/20-RESEARCH.md
@.planning/phases/20-design-drift-verification-docs-sync/20-02-PLAN.md
@src/cli/design-verification-builder.ts
@src/cli/commands/design.ts
@scripts/validate-docs.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 zero-unresolved ready-path E2E 证明</name>
  <files>src/cli/__tests__/design-verify-e2e.test.ts, tests/fixtures/design-contracts/verify-ready.design.md</files>
  <read_first>.planning/phases/19-handoff-package-human-gates/19-UAT.md, src/cli/commands/design.ts, src/cli/design-verification-builder.ts, tests/fixtures/design-contracts/handoff-basic.design.md</read_first>
  <action>新增 `src/cli/__tests__/design-verify-e2e.test.ts`，通过真实命令或近真实 command helper 串起 `design validate → design map → design handoff → design verify`。ready-path 使用 `tests/fixtures/design-contracts/verify-ready.design.md`，断言：1) validate / map / handoff / verify 全链路可运行；2) 最终 verify 结果至少一条 checklist item 为 `satisfied`；3) `readyForExecution=true` 且 `drift.length === 0`（或等价零漂移状态）。如果需要，可在测试中预先生成 deterministic handoff artifact，再验证 verify 会消费该 truth。</action>
  <verify>pnpm exec vitest run src/cli/__tests__/design-verify-e2e.test.ts</verify>
  <acceptance_criteria>
    - `src/cli/__tests__/design-verify-e2e.test.ts` 退出码为 `0`
    - ready-path 使用零未决项 fixture，而不是复用 `handoff-basic.design.md`
    - E2E 断言最终 verify 为 ready / zero-drift
    - 测试链路显式包含 handoff 步骤
  </acceptance_criteria>
  <done>Phase 20 拥有真正的 success-path 闭环证据，而不是口头说明。</done>
</task>

<task type="auto">
  <name>Task 2: 把三类失败模式落成自动化回归</name>
  <files>src/cli/__tests__/design-verify-e2e.test.ts, src/cli/__tests__/design-verification-builder.test.ts, src/cli/commands/__tests__/design-verify-command.test.ts</files>
  <read_first>tests/fixtures/design-contracts/missing-acceptance.design.md, tests/fixtures/design-contracts/no-match.design.md, tests/fixtures/design-contracts/over-broad.design.md, tests/fixtures/design-contracts/high-risk.design.md, src/cli/design-verification-builder.ts</read_first>
  <action>扩展 verify tests，至少覆盖三类失败预演：1) 缺失 design sections（如 `missing-acceptance.design.md`）会在 verify 阶段直接 blocked；2) scope blocker（`no-match.design.md` / `over-broad.design.md` / `high-risk.design.md`）会保留上游 blocker diagnostics，不被 verify 洗掉；3) ready-path handoff 不存在或 not-ready 时，verify 会降级为 `needs-review` 或 blocker，而不是伪装成 success。优先复用已有 fixtures，避免为失败样本重复造轮子。</action>
  <verify>(pnpm exec vitest run src/cli/__tests__/design-verification-builder.test.ts src/cli/commands/__tests__/design-verify-command.test.ts src/cli/__tests__/design-verify-e2e.test.ts) && (node dist/cli/index.js design verify tests/fixtures/design-contracts/no-match.design.md --json >/tmp/phase20-no-match.json 2>&1; test $? -ne 0 && rg -n '"code": "blocked-mapping"|"code": "no-candidates"' /tmp/phase20-no-match.json)</verify>
  <acceptance_criteria>
    - 三类失败模式都有 automated coverage
    - verify 不会吞掉上游 blocker diagnostics
    - handoff missing / not-ready path 不会 false-pass
    - 失败预演优先复用现有 fixtures
  </acceptance_criteria>
  <done>`VAL-05` 不再停留在文档描述，而成为自动化事实。</done>
</task>

<task type="auto">
  <name>Task 3: 增加 verify-specific docs drift regressions 与 milestone gate</name>
  <files>src/cli/__tests__/validate-docs-script.test.ts</files>
  <read_first>scripts/validate-docs.js, src/cli/__tests__/validate-docs-script.test.ts, docs/ai-guide/PATTERNS.md, docs/ai-guide/COMMANDS.md, docs/ai-guide/OUTPUT.md</read_first>
  <action>在 `src/cli/__tests__/validate-docs-script.test.ts` 中增加 verify-specific drift fixtures，例如：README/COMMANDS/PATTERNS/OUTPUT 任一处删除 `design verify` 示例、删掉 checklist/drift contract 关键字段，或重新把 workflow 写成包含非分析阶段时，`scripts/validate-docs.js` 会失败。确保 `npm test && npm run docs:check:human` 可以作为 Phase 20 的 milestone gate，直接产出可复现 audit evidence。</action>
  <verify>pnpm exec vitest run src/cli/__tests__/validate-docs-script.test.ts -t "design verify" && npm run docs:check:human</verify>
  <acceptance_criteria>
    - docs drift regressions 至少覆盖 verify 命令入口与 output contract
    - tests 能检测 workflow boundary 被重新扩写
    - `npm run docs:check:human` 保持 green
    - full suite 可作为 milestone audit 的可复制证据
  </acceptance_criteria>
  <done>verify-specific docs drift 被 guardrail 正式锁住，Phase 20 获得稳定的 audit gate。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run src/cli/__tests__/design-verify-e2e.test.ts`
- [ ] `pnpm exec vitest run src/cli/__tests__/design-verification-builder.test.ts src/cli/commands/__tests__/design-verify-command.test.ts`
- [ ] `pnpm exec vitest run src/cli/__tests__/validate-docs-script.test.ts -t "design verify"`
- [ ] `npm test && npm run docs:check:human`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 20 拥有 full-chain success evidence，而不只是局部单测
- 三类失败模式都被 automated regressions 锁住
- verify docs drift 与 workflow boundary drift 都纳入 milestone gate
  </success_criteria>

<output>
After completion, create `.planning/phases/20-design-drift-verification-docs-sync/20-03-SUMMARY.md`
</output>
