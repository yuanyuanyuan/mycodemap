---
phase: 31-release-governance-contract
plan: 01
type: docs
wave: 1
depends_on: []
files_modified:
  - docs/rules/release.md
  - docs/rules/deployment.md
  - docs/rules/pre-release-checklist.md
  - docs/rules/README.md
  - AGENTS.md
  - CLAUDE.md
autonomous: true
requirements:
  - REL-01
  - REL-02
  - DOC-01
  - DOC-02
  - DOC-03
user_setup: []
must_haves:
  truths:
    - `docs/rules/release.md` is the single authoritative `/release` workflow document for Phase 31.
    - `/release` is documented as a thin orchestrator that delegates to existing GSD closeout, `scripts/release.sh`, and `.github/workflows/publish.yml`.
    - Release operations remain L3 and require explicit user confirmation before version bump, tag creation, push, or publish-triggering action.
    - Milestone `vX.Y` maps to npm `X.Y.0`, with `v1.9` mapping to `1.9.0`.
  artifacts:
    - docs/rules/release.md
    - docs/rules/deployment.md
    - docs/rules/pre-release-checklist.md
    - docs/rules/README.md
    - AGENTS.md
    - CLAUDE.md
  key_links:
    - `AGENTS.md` defines L3 release boundaries and points to `docs/rules/release.md` for the operational workflow.
    - `CLAUDE.md` routes release questions and release-rule edits to `docs/rules/release.md`.
    - `docs/rules/README.md` includes `release.md` in the rules index for release workflow changes.
---

<objective>
Complete Phase 31 by turning the source `/release` proposal into the live documentation contract for release governance.

Purpose: CodeMap currently has separate GSD closeout and npm release paths. Phase 31 must define the single authoritative workflow and route all release-governance readers to it, without implementing the Phase 32 skill or executing a real release.
Output: Updated release/rules/entry documentation that clearly describes milestone ↔ npm version binding, L3 safety gates, readiness checks, and the thin-orchestrator boundary.
</objective>

<execution_context>
@.planning/phases/31-release-governance-contract/31-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@/home/stark/.claude/plans/ticklish-sprouting-church.md
@docs/rules/release.md
@docs/rules/deployment.md
@docs/rules/pre-release-checklist.md
@docs/rules/README.md
@AGENTS.md
@CLAUDE.md
@package.json
@scripts/release.sh
@.github/workflows/publish.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize the authoritative release workflow document</name>
  <files>docs/rules/release.md</files>
  <read_first>docs/rules/release.md, .planning/phases/31-release-governance-contract/31-CONTEXT.md, package.json, scripts/release.sh, .github/workflows/publish.yml</read_first>
  <action>Update `docs/rules/release.md` so it is explicitly the single authoritative `/release` workflow. Preserve the source plan's structure, but make these concrete corrections: 1) state that `vX.Y` maps to npm `X.Y.0`, including `v1.9 -> 1.9.0`; 2) state that current `package.json` version is `0.5.2-beta.1` and `1.9.0` is a major jump that must be highlighted; 3) state `/release` is a thin orchestrator delegating to `$gsd-complete-milestone`, `scripts/release.sh`, and `.github/workflows/publish.yml`; 4) define refusal cases for missing target version, dirty working tree, non-main branch, milestone not ready, existing remote/local tag conflict, and missing release prerequisites; 5) define Confirmation Gate #1 after closeout summary and Confirmation Gate #2 before `npm version`, git commit, git tag, `git push`, or any publish-triggering remote action; 6) correct any text that implies AI may autonomously publish; 7) keep `NPM_TOKEN` documented only as GitHub secret / environment input, never as repo content.</action>
  <acceptance_criteria>
    - `docs/rules/release.md` contains the exact string `vX.Y → X.Y.0`
    - `docs/rules/release.md` contains the exact string `v1.9 → 1.9.0`
    - `docs/rules/release.md` contains the exact string `0.5.2-beta.1 → 1.9.0`
    - `docs/rules/release.md` contains the exact string `Confirmation Gate #1`
    - `docs/rules/release.md` contains the exact string `Confirmation Gate #2`
    - `docs/rules/release.md` contains the exact string `thin orchestrator`
    - `docs/rules/release.md` contains the exact string `scripts/release.sh`
    - `docs/rules/release.md` contains the exact string `.github/workflows/publish.yml`
    - `docs/rules/release.md` does not contain `NPM_TOKEN=` followed by a concrete token value
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy rg -n "vX\\.Y → X\\.Y\\.0|v1\\.9 → 1\\.9\\.0|0\\.5\\.2-beta\\.1 → 1\\.9\\.0|Confirmation Gate #1|Confirmation Gate #2|thin orchestrator|scripts/release\\.sh|\\.github/workflows/publish\\.yml" docs/rules/release.md</automated>
    <automated>! rtk proxy rg -n "NPM_TOKEN=.+[A-Za-z0-9_\\-]{12,}" docs/rules/release.md</automated>
  </verify>
  <done>`docs/rules/release.md` can be read alone to understand how `/release` safely connects closeout and npm publication.</done>
</task>

<task type="auto">
  <name>Task 2: Sync deployment and pre-release rules with milestone-bound release semantics</name>
  <files>docs/rules/deployment.md, docs/rules/pre-release-checklist.md</files>
  <read_first>docs/rules/deployment.md, docs/rules/pre-release-checklist.md, docs/rules/release.md, package.json, scripts/release.sh, .github/workflows/publish.yml</read_first>
  <action>Update `docs/rules/deployment.md` with a short section named `Milestone-bound releases` that states: every milestone maps to one npm release, milestone `vX.Y` maps to npm `X.Y.0`, `/release` is the unified entry point, and direct release without GSD closeout is forbidden. Update `docs/rules/pre-release-checklist.md` so its critical principles include `Milestone readiness`, and add a new check section named `Milestone 归档状态检查` that requires a completed milestone audit/summary, closed or deferred requirements, clean working tree, version mapping review, and explicit confirmation gates before publication.</action>
  <acceptance_criteria>
    - `docs/rules/deployment.md` contains the heading `## Milestone-bound releases`
    - `docs/rules/deployment.md` contains the exact string `/release`
    - `docs/rules/deployment.md` contains the exact string `vX.Y → X.Y.0`
    - `docs/rules/deployment.md` contains the exact string `不得绕过 GSD milestone closeout`
    - `docs/rules/pre-release-checklist.md` contains the exact string `Milestone readiness`
    - `docs/rules/pre-release-checklist.md` contains the heading `### 12. Milestone 归档状态检查`
    - `docs/rules/pre-release-checklist.md` contains the exact string `Confirmation Gate #1`
    - `docs/rules/pre-release-checklist.md` contains the exact string `Confirmation Gate #2`
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy rg -n "Milestone-bound releases|/release|vX\\.Y → X\\.Y\\.0|不得绕过 GSD milestone closeout" docs/rules/deployment.md</automated>
    <automated>rtk proxy rg -n "Milestone readiness|### 12\\. Milestone 归档状态检查|Confirmation Gate #1|Confirmation Gate #2" docs/rules/pre-release-checklist.md</automated>
  </verify>
  <done>Deployment and pre-release docs no longer describe release as only a package operation; they require milestone readiness and `/release` routing.</done>
</task>

<task type="auto">
  <name>Task 3: Add minimal release routing to entry and rules indexes</name>
  <files>docs/rules/README.md, AGENTS.md, CLAUDE.md</files>
  <read_first>docs/rules/README.md, AGENTS.md, CLAUDE.md, docs/rules/release.md, .planning/phases/31-release-governance-contract/31-CONTEXT.md</read_first>
  <action>Make minimal routing updates only. In `docs/rules/README.md`, add `release.md` as the document for `/release` workflow / milestone-bound publication and keep `deployment.md` focused on package/deployment mechanics. In `CLAUDE.md`, add one route row for `/release` and one edit-destination bullet for release workflow rules. In `AGENTS.md`, extend the L3 release boundary with a short pointer to `docs/rules/release.md` and explicitly state that `/release` requires the documented confirmation gates. Do not paste the full release workflow into `AGENTS.md` or `CLAUDE.md`.</action>
  <acceptance_criteria>
    - `docs/rules/README.md` contains the exact string `release.md`
    - `docs/rules/README.md` contains the exact string `/release workflow`
    - `CLAUDE.md` contains the exact string `docs/rules/release.md`
    - `CLAUDE.md` contains the exact string `/release`
    - `AGENTS.md` contains the exact string `docs/rules/release.md`
    - `AGENTS.md` contains the exact string `/release`
    - `AGENTS.md` does not contain the exact string `Confirmation Gate #1` more than once
    - `CLAUDE.md` does not contain the exact string `Confirmation Gate #1`
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy rg -n "release\\.md|/release workflow" docs/rules/README.md</automated>
    <automated>rtk proxy rg -n "docs/rules/release\\.md|/release" CLAUDE.md AGENTS.md</automated>
    <automated>test "$(rtk proxy rg -n "Confirmation Gate #1" AGENTS.md | wc -l | tr -d ' ')" -le 1</automated>
    <automated>! rtk proxy rg -n "Confirmation Gate #1" CLAUDE.md</automated>
  </verify>
  <done>Readers can discover release governance from `docs/rules/README.md`, `CLAUDE.md`, and `AGENTS.md` without creating duplicate workflow truth.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rtk proxy rg -n "vX\\.Y → X\\.Y\\.0|v1\\.9 → 1\\.9\\.0|Confirmation Gate #1|Confirmation Gate #2|thin orchestrator" docs/rules/release.md`
- [ ] `rtk proxy rg -n "Milestone-bound releases|不得绕过 GSD milestone closeout" docs/rules/deployment.md`
- [ ] `rtk proxy rg -n "### 12\\. Milestone 归档状态检查|Milestone readiness" docs/rules/pre-release-checklist.md`
- [ ] `rtk proxy rg -n "release\\.md|/release" docs/rules/README.md CLAUDE.md AGENTS.md`
- [ ] `rtk npm run docs:check`
- [ ] `rtk node dist/cli/index.js ci check-docs-sync`
</verification>

<success_criteria>
- `REL-01` and `REL-02` are satisfied by the release workflow contract and version mapping documentation
- `DOC-01` is satisfied by `docs/rules/release.md`
- `DOC-02` is satisfied by `deployment.md` and `pre-release-checklist.md`
- `DOC-03` is satisfied with routing-only changes in `AGENTS.md` and `CLAUDE.md`
- Phase 31 does not create release skill files or execute release commands
</success_criteria>

<output>
After completion, create `.planning/phases/31-release-governance-contract/31-01-SUMMARY.md`
</output>
