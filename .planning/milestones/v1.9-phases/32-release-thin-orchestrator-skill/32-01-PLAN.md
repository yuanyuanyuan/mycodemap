---
phase: 32-release-thin-orchestrator-skill
plan: 01
type: skill
wave: 1
depends_on:
  - .planning/phases/31-release-governance-contract/31-VERIFICATION.md
files_modified:
  - .claude/skills/release/SKILL.md
autonomous: true
requirements:
  - REL-03
  - SAFE-01
  - SAFE-02
  - SKILL-01
  - SKILL-02
user_setup: []
must_haves:
  truths:
    - `.claude/skills/release/SKILL.md` exists and can guide `/release v{X.Y}` as a thin orchestrator.
    - The skill refuses to continue when target version, worktree, branch, milestone readiness, or tag state is invalid.
    - The skill defines Confirmation Gate #1 and Confirmation Gate #2, with Gate #2 accepting only explicit `y` / `Y`.
    - The skill highlights the current major jump example `0.5.2-beta.1 → 1.9.0`.
  artifacts:
    - .claude/skills/release/SKILL.md
  key_links:
    - The skill points back to `docs/rules/release.md` as the authoritative workflow.
    - The skill delegates to `$gsd-complete-milestone`, `scripts/release.sh`, and `.github/workflows/publish.yml`.
---

<objective>
Implement Phase 32 by creating the Claude `/release` skill as a thin safety orchestrator.

Purpose: The repository now has a release governance contract, but Claude runtime still lacks a concrete skill surface that can enforce the same L3 boundary, refusal cases, version mapping, and double-confirmation behavior.
Output: A `.claude/skills/release/SKILL.md` that routes to the authoritative rules doc, performs preflight refusal logic, displays both confirmation gates, highlights major version jumps, and delegates irreversible work to existing tooling.
</objective>

<execution_context>
@.planning/phases/32-release-thin-orchestrator-skill/32-CONTEXT.md
</execution_context>

<context>
@docs/rules/release.md
@docs/rules/deployment.md
@docs/rules/pre-release-checklist.md
@AGENTS.md
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@scripts/release.sh
@.github/workflows/publish.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the release skill skeleton and authority boundary</name>
  <files>.claude/skills/release/SKILL.md</files>
  <read_first>docs/rules/release.md, AGENTS.md, CLAUDE.md, package.json, scripts/release.sh, .github/workflows/publish.yml</read_first>
  <action>Create `.claude/skills/release/SKILL.md` with frontmatter plus sections for scope, authority, L3 boundary, preflight checks, confirmation gates, delegation boundary, failure handling, and references. The skill must explicitly point readers back to `docs/rules/release.md` as the single authoritative workflow.</action>
  <acceptance_criteria>
    - `.claude/skills/release/SKILL.md` contains the exact string `docs/rules/release.md`
    - `.claude/skills/release/SKILL.md` contains the exact string `thin orchestrator`
    - `.claude/skills/release/SKILL.md` contains the exact string `不得自主发布`
    - `.claude/skills/release/SKILL.md` contains the exact string `$gsd-complete-milestone`
    - `.claude/skills/release/SKILL.md` contains the exact string `scripts/release.sh`
    - `.claude/skills/release/SKILL.md` contains the exact string `.github/workflows/publish.yml`
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy rg -n "docs/rules/release\\.md|thin orchestrator|不得自主发布|\\$gsd-complete-milestone|scripts/release\\.sh|\\.github/workflows/publish\\.yml" .claude/skills/release/SKILL.md</automated>
  </verify>
  <done>Claude runtime has a concrete `/release` skill surface that still defers authority to the rules doc.</done>
</task>

<task type="auto">
  <name>Task 2: Encode refusal cases, version mapping, and confirmation gates</name>
  <files>.claude/skills/release/SKILL.md</files>
  <read_first>.claude/skills/release/SKILL.md, docs/rules/release.md, package.json</read_first>
  <action>Add refusal conditions for missing target version, dirty worktree, wrong branch, missing releasable milestone, tag conflict, and missing version truth. Add the version-mapping section with `vX.Y → X.Y.0`, `v1.9 → 1.9.0`, and `0.5.2-beta.1 → 1.9.0`. Add full templates for `Confirmation Gate #1` and `Confirmation Gate #2`, and make Gate #2 accept only explicit `y / Y`.</action>
  <acceptance_criteria>
    - `.claude/skills/release/SKILL.md` contains the exact string `Confirmation Gate #1`
    - `.claude/skills/release/SKILL.md` contains the exact string `Confirmation Gate #2`
    - `.claude/skills/release/SKILL.md` contains the exact string `y / Y`
    - `.claude/skills/release/SKILL.md` contains the exact string `vX.Y → X.Y.0`
    - `.claude/skills/release/SKILL.md` contains the exact string `v1.9`
    - `.claude/skills/release/SKILL.md` contains the exact string `0.5.2-beta.1 → 1.9.0`
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy rg -n "Confirmation Gate #1|Confirmation Gate #2|y / Y|vX\\.Y → X\\.Y\\.0|v1\\.9|0\\.5\\.2-beta\\.1 → 1\\.9\\.0" .claude/skills/release/SKILL.md</automated>
  </verify>
  <done>The skill now encodes the actual safety behavior rather than just pointing at the docs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rtk proxy rg -n "docs/rules/release\\.md|thin orchestrator|不得自主发布|\\$gsd-complete-milestone|scripts/release\\.sh|\\.github/workflows/publish\\.yml" .claude/skills/release/SKILL.md`
- [ ] `rtk proxy rg -n "Confirmation Gate #1|Confirmation Gate #2|y / Y|vX\\.Y → X\\.Y\\.0|0\\.5\\.2-beta\\.1 → 1\\.9\\.0" .claude/skills/release/SKILL.md`
</verification>

<success_criteria>
- `REL-03` is satisfied by explicit refusal cases in the skill
- `SAFE-01` and `SAFE-02` are satisfied by Gate #1 / Gate #2 templates
- `SKILL-01` and `SKILL-02` are satisfied by the created `SKILL.md` and its L3 boundary language
- Phase 32 does not execute any release command
</success_criteria>

<output>
After completion, create `.planning/phases/32-release-thin-orchestrator-skill/32-01-SUMMARY.md`
</output>
