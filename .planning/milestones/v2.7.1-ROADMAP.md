# Milestone v2.7.1: agent-hook-protocol-hardening

**Status:** ✅ SHIPPED 2026-05-12  
**Phases:** 84-85 shipped, 85.1 follow-up tracked  
**Total Plans:** 2 completed, 1 follow-up closeout plan

## Overview

`v2.7.1` 把 CodeMap 的 installable `git commit` hooks 收口成更适合 AI Agent 的控制协议层。它先在 `Phase 84` 建立 fail-fast structured blocker protocol，再在 `Phase 85` 继续降低 mixed output 噪音、补齐 protocol-only mode、显式 log-path fallback 和更清晰的 status taxonomy。

Milestone shipped 之后，又出现了一组紧邻同一 agent-facing toolchain surface 的未提交改动：把 TypeScript CLI 构建与 typecheck 切到 `tsgo`，并同步修正 Node 端 JSON import 语法与 Claude 编辑后 TS 检查。这批改动作为 post-ship follow-up 记录为 `Phase 85.1`，继续挂在 `v2.7.1` 归档下维护，但不重开根路线图的 active milestone 状态。

## Phases

### Phase 84: AI Agent Hook Control Protocol ✓ COMPLETE (2026-05-12)

**Goal:** 把 `pre-commit` / `commit-msg` 从人类可读阻断提示升级为机器可读控制协议。  
**Depends on:** shipped hook/env-contract baseline (`v2.1` Phase 58, `v2.5` Phase 74)  
**Requirements:** `HOOK-AI-01`, `HOOK-AI-02`, `HOOK-AI-03`  
**Plans:** 1 — complete

**Success Criteria:** ALL MET
1. `pre-commit` fail-fast blocker path emits `codemap.precommit.v1`. ✓
2. `commit-msg` emits `codemap.commitmsg.v1` with rewrite routing. ✓
3. related-tests remediation stays framework-aware and installable-template-first. ✓

### Phase 85: Hook Protocol Noise Reduction ✓ COMPLETE (2026-05-12)

**Goal:** 继续降低 autonomous agent 的解析成本：补 protocol-only mode、显式 log-path、`not_applicable` 状态和低误导 report-only wording。  
**Depends on:** Phase 84 structured protocol baseline  
**Requirements:** `HOOK-AI-04`  
**Plans:** 1 — complete

**Success Criteria:** ALL MET
1. `CODEMAP_PROTOCOL_ONLY=1` suppresses human logs while preserving protocol lines. ✓
2. Every hook emits `CODEMAP_PRECHECK_LOG_PATH` and writes complete JSON to the attempt log. ✓
3. report-only limit-reached path no longer reads like an exception and exposes structured `warn` details. ✓

### Phase 85.1: tsgo Build and Typecheck Flow (POST-SHIP FOLLOW-UP)

**Goal:** 把当前 TypeScript CLI/tooling 变更作为紧邻 `v2.7.1` agent-facing workflow 的后续 phase 来维护：切换 `build` / `typecheck` 到 `tsgo`，修正 Node JSON import 语法，并让 Claude 的 TS 编辑后检查跟随新工具链。  
**Depends on:** Phase 85 shipped hook/agent ergonomics baseline  
**Requirements:** `TOOLCHAIN-TS-01`  
**Plans:** 1 — closeout complete

**Current tracked changes:**
1. `package.json` 把 `build` / `typecheck` 从 `tsc` 切到 `tsgo`，并新增 `@typescript/native-preview`。
2. `package-lock.json` 锁定 `@typescript/native-preview` 及平台包。
3. `src/cli-new/index.ts` 把 JSON import 从 `assert` 语法切到 `with` 语法，匹配新工具链。
4. `.claude/settings.json` 增加 TS 文件编辑后的 `tsgo --noEmit` 快速检查 hook。

**Validation status:**
1. `npm run build` 已通过，构建链路走 `tsgo` + `postbuild`。
2. `npm run typecheck` 已通过，路径为 `tsgo --noEmit`。
3. `.claude/settings.json` 的 PostToolUse 命令与 `package.json` typecheck 真相一致，并在 `file_path` 缺失时保持 no-op。
4. `npm run prepublishOnly` 已通过，覆盖 docs guardrail、build 和全量 tests（150 files / 1465 passed / 11 skipped）。

## Milestone Summary

**Key Decisions:**

- Hook truth remains in `scripts/hooks/templates/*`; managed `.mycodemap/hooks/*` copies are parity targets.
- Human-readable diagnostics remain useful by default, but `CODEMAP_PROTOCOL_ONLY=1` is the preferred mode for fully autonomous agents.
- Status semantics matter to agents: `not_applicable` is distinct from `skipped`, and report-only limit-reached is a `warn`, not a failure.
- Post-ship toolchain changes that still belong to the same agent workflow surface can stay attached to the shipped milestone archive as decimal follow-up phases, without reopening the root active roadmap.

**Issues Resolved:**

- AI agents no longer need to re-translate every hook failure from natural language into actions.
- Commit-message failures now provide the same machine-readable contract quality as pre-commit failures.
- Mixed human/protocol output now has an explicit low-noise escape hatch and log-path recovery story.
- Shared hook protocol source now lives in `scripts/hooks/templates/protocol-contract.json`, so tests and AI docs no longer need to hardcode schema/env tokens independently.

**Issues Deferred:**

- Broader architecture-intelligence work remains future scope (`v3.0+`).
- Contract migration and shared-output follow-ups (`AGENT-10`, `AGENT-11`) stay outside this milestone.
- `Phase 85.1` closeout 已完成；其范围仍然是 shipped milestone archive 下的 post-ship follow-up，不改变根路线图的 between-milestones 状态。

**Technical Debt Incurred:**

- Shell runtime still renders protocol JSON inline; the shared contract source now exists, but future work may still want generated shell fragments if more hook families appear.

---

_For current project status, see `.planning/PROJECT.md`, `.planning/STATE.md`, and `.planning/MILESTONES.md`._
