---
phase: 28-entry-doc-authority-and-destination-map
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/28-entry-doc-authority-and-destination-map/28-ENTRY-DOC-MIGRATION-MAP.md
autonomous: true
requirements:
  - DEST-01
  - DEST-02
  - ROUTE-04
user_setup: []
must_haves:
  truths:
    - `AGENTS.md`、根 `CLAUDE.md`、`.claude/CLAUDE.md` 中所有计划移出的 section 都有明确的 `source file + source section + destination file + why`
    - 每个 destination 都是当前仓库已存在的 live doc 或 machine truth 文件
    - 迁移图同时标明各入口文档哪些 section 保留在原位，避免 Phase 29 误删宪法 / 路由 / adapter 必留内容
    - 交付物显式声明 `docs/rules/README.md` / `AI_GUIDE.md` 只能做最小导航同步，且不新增治理中间层
  artifacts:
    - .planning/phases/28-entry-doc-authority-and-destination-map/28-CONTEXT.md
    - .planning/phases/28-entry-doc-authority-and-destination-map/28-ENTRY-DOC-MIGRATION-MAP.md
  key_links:
    - `CLAUDE.md` 的执行/验证/dogfood 内容分别回到 `docs/rules/validation.md`、`docs/rules/engineering-with-codex-openai.md`、`AI_GUIDE.md`
    - `.claude/CLAUDE.md` 只保留 Claude adapter 关系说明，不再持有通用政策
    - `AGENTS.md` 的 RTK 长表与工程执行细节不得回流到宪法层
---

<objective>
完成 Phase 28 的唯一结构工件：写出三份入口文档的 section 级迁移图与 destination ownership 合同，为 Phase 29/30 提供不可争议的 rewrite 输入。

Purpose: 当前真正缺的不是更多规则，而是“哪些段落该留、哪些必须离开、离开后去哪改”的精确映射。没有这张图，Phase 29 很容易把入口文档重写成另一套新摘要。
Output: 一份可执行的 `28-ENTRY-DOC-MIGRATION-MAP.md`，包含 source-section inventory、destination matrix、keep/move guardrails 与 discoverability/no-middle-layer 约束。
</objective>

<execution_context>
@.planning/phases/28-entry-doc-authority-and-destination-map/28-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@docs/brainstorms/2026-04-22-rules-entry-docs-phase1-structure-consolidation-requirements.md
@AGENTS.md
@CLAUDE.md
@.claude/CLAUDE.md
@docs/rules/README.md
@docs/rules/validation.md
@docs/rules/engineering-with-codex-openai.md
@AI_GUIDE.md
@RTK.md
@.claude/rule-system.config.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 审计三份入口文档并产出 section inventory</name>
  <files>.planning/phases/28-entry-doc-authority-and-destination-map/28-ENTRY-DOC-MIGRATION-MAP.md</files>
  <action>通读 `AGENTS.md`、根 `CLAUDE.md`、`.claude/CLAUDE.md`，按 section 级粒度把每个标题归类为 `keep in place` / `move out` / `compress into navigation only`。在 `28-ENTRY-DOC-MIGRATION-MAP.md` 中先写三段 source inventory：1) `AGENTS.md` 保留宪法级 section，显式标出需迁出的操作性 section（至少覆盖 `3.2 Commit 策略`、详细文档同步触发表 / AI 友好细则、`11 RTK 使用规范` 的长表）；2) 根 `CLAUDE.md` 明确保留 `加载顺序` 与最小路由，迁出 `执行回路`、`修改后必须执行`、`Rule-system 默认值`、`CodeMap CLI Dogfood`、`交付清单`；3) `.claude/CLAUDE.md` 明确仅保留 Claude adapter 关系说明，迁出 `强制执行规则`、`禁止行为`、`Commit 策略`、`快速参考`、`任务初始化模板`。每条 inventory 都要附一句 why，并引用对应 locked decision（如 `per D-01`、`per D-13`）。</action>
  <verify>
    <automated>rtk proxy rg -n "AGENTS\\.md|CLAUDE\\.md|\\.claude/CLAUDE\\.md|执行回路|Rule-system 默认值|CodeMap CLI Dogfood|强制执行规则|Commit 策略|RTK 使用规范" .planning/phases/28-entry-doc-authority-and-destination-map/28-ENTRY-DOC-MIGRATION-MAP.md</automated>
  </verify>
  <done>`28-ENTRY-DOC-MIGRATION-MAP.md` 明确列出三份入口文档哪些 section 留下、哪些迁出、哪些只允许压缩成导航表达。</done>
</task>

<task type="auto">
  <name>Task 2: 锁定 destination ownership 与 discoverability guardrails</name>
  <files>.planning/phases/28-entry-doc-authority-and-destination-map/28-ENTRY-DOC-MIGRATION-MAP.md</files>
  <action>在同一工件中补全 destination matrix，强制使用列：`source file`、`source section`、`destination file`、`why / maintenance owner`。映射至少覆盖：验证顺序、post-edit 检查、rule-system defaults → `docs/rules/validation.md` + `.claude/rule-system.config.json`；执行回路、TDD/commit/verification 行为、任务模板、交付 checklist → `docs/rules/engineering-with-codex-openai.md`；dogfood / 工具发现 / 命令速查 → `AI_GUIDE.md` 与 `docs/ai-guide/*`；“改哪类文件先读哪份规则” → `docs/rules/README.md`；RTK 速查 → `RTK.md`。在工件末尾再写 `Guardrails for Phase 29/30`：明确禁止新增治理中间层、禁止把迁出内容回灌到 `AGENTS.md`、仅允许对 `docs/rules/README.md` / `AI_GUIDE.md` 做最小导航同步。若当前导航文档已足够，也要显式写出 `no sync required yet`。</action>
  <verify>
    <automated>rtk proxy rg -n "source file|source section|destination file|docs/rules/validation\\.md|docs/rules/engineering-with-codex-openai\\.md|AI_GUIDE\\.md|docs/rules/README\\.md|RTK\\.md|no new governance middle layer|no sync required yet|minimal navigation sync" .planning/phases/28-entry-doc-authority-and-destination-map/28-ENTRY-DOC-MIGRATION-MAP.md</automated>
  </verify>
  <done>迁移图既能回答“内容迁到哪”，也能回答“后续 phase 什么不能做”，满足 `DEST-01`、`DEST-02`、`ROUTE-04`。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rtk proxy rg -n "AGENTS\\.md|CLAUDE\\.md|\\.claude/CLAUDE\\.md" .planning/phases/28-entry-doc-authority-and-destination-map/28-ENTRY-DOC-MIGRATION-MAP.md`
- [ ] `rtk proxy rg -n "docs/rules/validation\\.md|docs/rules/engineering-with-codex-openai\\.md|AI_GUIDE\\.md|docs/rules/README\\.md|RTK\\.md" .planning/phases/28-entry-doc-authority-and-destination-map/28-ENTRY-DOC-MIGRATION-MAP.md`
- [ ] `rtk npm run docs:check`
</verification>

<success_criteria>
- 三份入口文档所有计划移出的 section 都有明确 destination
- 所有 destination 都是现有 live docs / machine truth
- 工件明确标出 entry docs 的 keep vs move 边界，Phase 29 不需要重新猜
- 工件显式写明“最小导航同步”与“禁止新增治理中间层”
</success_criteria>

<output>
After completion, create `.planning/phases/28-entry-doc-authority-and-destination-map/28-01-SUMMARY.md`
</output>
