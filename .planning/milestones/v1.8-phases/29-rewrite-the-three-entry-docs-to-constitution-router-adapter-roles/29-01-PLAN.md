---
phase: 29-rewrite-the-three-entry-docs-to-constitution-router-adapter-roles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - CLAUDE.md
  - .claude/CLAUDE.md
  - docs/rules/engineering-with-codex-openai.md
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - DEST-03
user_setup: []
must_haves:
  truths:
    - "`AGENTS.md` 只保留仓库级治理协议、证据协议、改动边界、验证/交付底线，不再夹带 RTK 长表、详细 docs-sync 细则或 mem payload"
    - "根 `CLAUDE.md` 只保留入口路由，不再出现执行回路、验证命令、rule-system 默认值、dogfood 命令或交付 checklist"
    - "`.claude/CLAUDE.md` 只保留 Claude adapter 说明，不再形成第二套执行手册"
    - "`docs/rules/engineering-with-codex-openai.md` 能承接被迁出的工程执行模板与文档 authoring 细节"
  artifacts:
    - .planning/phases/29-rewrite-the-three-entry-docs-to-constitution-router-adapter-roles/29-CONTEXT.md
    - .planning/phases/29-rewrite-the-three-entry-docs-to-constitution-router-adapter-roles/29-01-PLAN.md
  key_links:
    - "根 `CLAUDE.md` 必须把执行协议路由到 `docs/rules/engineering-with-codex-openai.md`"
    - "验证协议继续由 `docs/rules/validation.md` 定权"
    - "RTK 细节继续由 `RTK.md` 定权"
---

<objective>
把三份入口文档真正改成 `constitution / router / adapter`，并把被移出的执行性细节稳妥接入现有 live docs，而不是再留下压缩版副本。
</objective>

<execution_context>
@.planning/phases/29-rewrite-the-three-entry-docs-to-constitution-router-adapter-roles/29-CONTEXT.md
</execution_context>

<context>
@.planning/phases/28-entry-doc-authority-and-destination-map/28-ENTRY-DOC-MIGRATION-MAP.md
@AGENTS.md
@CLAUDE.md
@.claude/CLAUDE.md
@docs/rules/engineering-with-codex-openai.md
@docs/rules/validation.md
@RTK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重写三份入口文档到单一职责</name>
  <files>AGENTS.md, CLAUDE.md, .claude/CLAUDE.md</files>
  <action>按 `Phase 28` 迁移图重写 `AGENTS.md`、根 `CLAUDE.md`、`.claude/CLAUDE.md`。要求：`AGENTS.md` 只保留治理协议与底线；根 `CLAUDE.md` 只做路由；`.claude/CLAUDE.md` 只做 Claude adapter。删除入口文档中的执行回路、验证命令、rule-system 默认值、dogfood、commit 策略、任务模板、RTK 长表和任何运行时 mem payload。</action>
  <verify>
    <automated>rtk proxy rg -n "执行回路|修改后必须执行|Rule-system 默认值|CodeMap CLI Dogfood|交付清单|Commit 策略|任务初始化模板|<claude-mem-context>" AGENTS.md CLAUDE.md .claude/CLAUDE.md</automated>
  </verify>
  <done>三份入口文档的职责边界清晰，不再承载第二套执行规则。</done>
</task>

<task type="auto">
  <name>Task 2: 补写目标工程规则文档</name>
  <files>docs/rules/engineering-with-codex-openai.md</files>
  <action>对 `docs/rules/engineering-with-codex-openai.md` 做 targeted supplement：把根 `CLAUDE.md` 的角色修正为入口路由；补上任务初始化最小模板；补上 AI 友好文档 authoring / doc-sync 细则；修正文档同步触发表中仍把 `CLAUDE.md` 当执行手册的表述。</action>
  <verify>
    <automated>rtk proxy rg -n "入口路由|任务初始化最小模板|AI 友好文档|docs/ai-guide/OUTPUT\\.md|docs/ai-guide/PATTERNS\\.md" docs/rules/engineering-with-codex-openai.md</automated>
  </verify>
  <done>被入口文档移出的工程执行细节在现有 live doc 中可维护，不留下规范性空洞。</done>
</task>

<task type="auto">
  <name>Task 3: 文档守卫与重复面验证</name>
  <files>AGENTS.md, CLAUDE.md, .claude/CLAUDE.md, docs/rules/engineering-with-codex-openai.md</files>
  <action>运行 docs-focused 验证，证明根 `CLAUDE.md` / `.claude/CLAUDE.md` 不再出现执行政策正文，并为 Phase 30 留下 discoverability sweep 基线。</action>
  <verify>
    <automated>rtk npm run docs:check && rtk node dist/cli/index.js ci check-docs-sync && rtk git diff --check</automated>
  </verify>
  <done>Phase 29 交付有静态 guardrail 与失败预演证据。</done>
</task>

</tasks>

<success_criteria>
- `AGENTS.md` 保持宪法级边界
- 根 `CLAUDE.md` 只保留路由，不再携带执行政策正文
- `.claude/CLAUDE.md` 只保留 Claude adapter 差异
- moved operational content 已落到现有 live docs
</success_criteria>
