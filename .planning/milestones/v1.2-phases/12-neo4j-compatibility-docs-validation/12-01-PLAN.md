# Plan 12-01: Neo4j Persistence / Query Contract / Error Semantics

## Goal

让 Neo4j 达到与 KùzuDB 对齐的最小 contract，并把连接失败显式暴露出来。

## Tasks

- 初始化 Neo4j driver / session。
- 持久化和读取 snapshot。
- 实现更新、删除、查询与分析接口。
- 锁住连通性失败路径。

