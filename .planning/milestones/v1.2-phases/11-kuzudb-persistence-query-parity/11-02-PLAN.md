# Plan 11-02: KùzuDB Query / Update / Analysis Parity

## Goal

让 KùzuDB 满足 `IStorage` 规定的增删改查与分析合同。

## Tasks

- 通过共享 helper 支持 `updateModule` / `deleteModule`。
- 支持 `findModuleById`、`findSymbolById`、dependencies/callers/callees。
- 支持 cycles / impact / statistics。

