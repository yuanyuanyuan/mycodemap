# Plan 11-03: KùzuDB Contract Tests & Failure Validation

## Goal

为 KùzuDB 补齐成功路径和失败路径测试，证明它是正式 backend，而不是存在于代码里的占位能力。

## Tasks

- 覆盖 snapshot 成功路径。
- 覆盖 query/update/delete/analysis 路径。
- 覆盖初始化失败路径。

