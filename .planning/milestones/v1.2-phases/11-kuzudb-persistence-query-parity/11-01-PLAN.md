# Plan 11-01: KùzuDB Schema & Full-Graph Persist/Load

## Goal

让 `kuzudb` 后端可以初始化真实存储并持久化完整 CodeGraph snapshot。

## Tasks

- 初始化 Kùzu Database / Connection。
- 创建 snapshot 表并写入 `graph` 字段。
- 支持从 snapshot 表重新加载 CodeGraph。

