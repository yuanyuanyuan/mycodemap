# Plan 10-02: Main Path Activation

## Goal

让主路径真正写入 / 读取所选 backend，而不是只在抽象层保留 storage 接缝。

## Tasks

- 让 `generate` 保存到配置的 storage backend。
- 让 `export` 与内部 `Server Layer` handler 通过统一 runtime helper 读取同一 backend。
- 让内部 `cli-new` query/export/server 路径复用相同配置入口。

