# Plan 10-03: Shared Graph Helper / Contract Tests

## Goal

为 graph backend 固定一层共享行为，避免 KùzuDB / Neo4j 再各自维护分叉实现。

## Tasks

- 抽取 CodeGraph snapshot 的共享增删改查 / callers / callees / cycles / impact / statistics helper。
- 让 graph backend 在 helper 上实现最小 contract parity。
- 补共享 helper 回归测试。

