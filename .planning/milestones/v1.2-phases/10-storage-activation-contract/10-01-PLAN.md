# Plan 10-01: Storage Config / Backend Selection / Diagnostics

## Goal

把 `storage` 提升为正式配置面，并让非法字段、非法后端类型和 legacy 场景都给出明确反馈。

## Tasks

- 在 CLI config loader 中增加 `storage` 默认值、白名单字段和类型归一化。
- 让 `init` 输出文件、schema 与 config 类型一致。
- 用测试锁住默认值、显式字段和坏配置报错。

