---
phase: 26-implement-symbol-level-graph-and-experimental-mcp-thin-slice
plan: 03
type: execute
wave: 2
depends_on:
  - 26-01
  - 26-02
files_modified:
  - src/interface/types/storage.ts
  - src/interface/types/index.ts
  - src/infrastructure/storage/interfaces/StorageBase.ts
  - src/infrastructure/storage/graph-helpers.ts
  - src/infrastructure/storage/adapters/MemoryStorage.ts
  - src/infrastructure/storage/adapters/FileSystemStorage.ts
  - src/infrastructure/storage/adapters/KuzuDBStorage.ts
  - src/infrastructure/storage/adapters/SQLiteStorage.ts
  - src/server/mcp/index.ts
  - src/server/mcp/server.ts
  - src/server/mcp/service.ts
  - src/server/mcp/types.ts
  - src/server/mcp/__tests__/CodeMapMcpServer.test.ts
  - src/cli/commands/mcp.ts
  - src/cli/commands/__tests__/mcp.test.ts
  - src/cli/__tests__/index-help.test.ts
  - src/cli/index.ts
  - README.md
  - AI_GUIDE.md
  - CLAUDE.md
  - docs/ai-guide/COMMANDS.md
  - docs/ai-guide/INTEGRATION.md
  - docs/ai-guide/OUTPUT.md
  - docs/rules/engineering-with-codex-openai.md
  - .planning/ROADMAP.md
  - .planning/milestones/post-v1.6-phases/26-implement-symbol-level-graph-and-experimental-mcp-thin-slice/26-03-SUMMARY.md
autonomous: false
requirements:
  - P26-NOW-MCP-STDIO
  - P26-NOW-SYMBOL-IMPACT
user_setup: []
must_haves:
  truths:
    - `mycodemap mcp start` / `mycodemap mcp install` 必须显式标记 experimental
    - `codemap_query` / `codemap_impact` 必须复用现有只读 storage 语义，不在 tool 层二次 graph walk
    - `stdout` 只能承载 MCP 协议消息，首次运行欢迎信息与其他 CLI 启动 side-effect 不能污染 transport
    - `GRAPH_NOT_FOUND` / `SYMBOL_NOT_FOUND` / `AMBIGUOUS_EDGE` 都必须返回结构化状态，而不是静默空结果
    - `docs/ai-guide/INTEGRATION.md` 必须改成真实 stdio MCP server 为主、旧 wrapper 为 fallback
  artifacts:
    - .planning/milestones/post-v1.6-phases/26-implement-symbol-level-graph-and-experimental-mcp-thin-slice/26-03-PLAN.md
    - src/server/mcp/server.ts
    - src/cli/commands/mcp.ts
    - docs/ai-guide/INTEGRATION.md
  key_links:
    - 设计稿锁定首期 MCP 为本地只读 stdio first experimental surface
    - test plan 把 `mcp install` / `mcp start` / `codemap_query` / `codemap_impact` / stdio smoke 列为关键路径
---

<objective>
完成 Phase 26 的第三刀：在现有 symbol-level graph truth 之上，打通 experimental MCP stdio 薄切片。

Purpose: 26-01/26-02 已经把 symbol-level truth 与 partial graph truth 写进 generate / SQLite，但 AI 还没有真实可调用的 MCP server。26-03 要把最小 query / impact 读路径、experimental CLI surface 和 canonical integration doc 一次打通。
Output: symbol impact storage contract、MCP server / tool contract、`mcp start` / `mcp install`、focused tests、docs truth、phase summary。
</objective>

<execution_context>
@/data/codemap/.planning/milestones/post-v1.6-phases/26-implement-symbol-level-graph-and-experimental-mcp-thin-slice/26-CONTEXT.md
@/data/codemap/.planning/milestones/post-v1.6-phases/26-implement-symbol-level-graph-and-experimental-mcp-thin-slice/26-02-SUMMARY.md
@/home/stark/.gstack/projects/yuanyuanyuan-mycodemap/stark-main-design-20260418-011843.md
@/home/stark/.gstack/projects/yuanyuanyuan-mycodemap/stark-main-eng-review-test-plan-20260418-191311.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: 下沉 symbol impact 与 graph metadata 读能力</name>
  <files>src/interface/types/storage.ts, src/interface/types/index.ts, src/infrastructure/storage/interfaces/StorageBase.ts, src/infrastructure/storage/graph-helpers.ts, src/infrastructure/storage/adapters/MemoryStorage.ts, src/infrastructure/storage/adapters/FileSystemStorage.ts, src/infrastructure/storage/adapters/KuzuDBStorage.ts, src/infrastructure/storage/adapters/SQLiteStorage.ts</files>
  <action>在 storage contract 中新增 graph metadata 与 symbol-level impact 返回 shape；memory / filesystem / kuzudb 走共享 graph helper，SQLite 用只读 relation table 直查 callers 链，确保 impact 最终 shape 由 storage/service 一次返回。</action>
  <verify>npm run test -- src/infrastructure/storage/__tests__/SQLiteStorage.test.ts</verify>
</task>

<task type="auto">
  <name>Task 2: 实现 experimental MCP stdio server 与 tool contract</name>
  <files>src/server/mcp/index.ts, src/server/mcp/server.ts, src/server/mcp/service.ts, src/server/mcp/types.ts</files>
  <action>新增 MCP server 工厂与只读 symbol query service，注册 `codemap_query` / `codemap_impact` 两个工具，统一返回 `status` / `confidence` / `graph_status` / `generated_at` / `error.code`。symbol resolution 按 symbol + 可选 filePath，覆盖 graph missing、symbol missing、ambiguous 三类显式错误。</action>
  <verify>npm run test -- src/server/mcp/__tests__/CodeMapMcpServer.test.ts</verify>
</task>

<task type="auto">
  <name>Task 3: 接入 CLI experimental surface</name>
  <files>src/cli/commands/mcp.ts, src/cli/commands/__tests__/mcp.test.ts, src/cli/__tests__/index-help.test.ts, src/cli/index.ts</files>
  <action>新增 `mycodemap mcp start` / `mycodemap mcp install`；`mcp start` 走纯 stdio path，绕开会向 stdout 打印欢迎信息的 CLI 启动 side-effect；`mcp install` 最小化更新仓库根 `.mcp.json` 并显式提示 experimental。</action>
  <verify>npm run test -- src/cli/commands/__tests__/mcp.test.ts src/cli/__tests__/index-help.test.ts</verify>
</task>

<task type="auto">
  <name>Task 4: 同步文档与 phase 工件</name>
  <files>README.md, AI_GUIDE.md, CLAUDE.md, docs/ai-guide/COMMANDS.md, docs/ai-guide/INTEGRATION.md, docs/ai-guide/OUTPUT.md, docs/rules/engineering-with-codex-openai.md, .planning/ROADMAP.md, .planning/milestones/post-v1.6-phases/26-implement-symbol-level-graph-and-experimental-mcp-thin-slice/26-03-SUMMARY.md</files>
  <action>把真实 stdio MCP server 设为 canonical integration path，明确 `mcp start` / `mcp install` 为 experimental；补 tool 输入输出契约、使用步骤、fallback wrapper 说明，并在 roadmap / summary 中记录 26-03 完成状态。</action>
  <verify>npm run docs:check</verify>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- src/infrastructure/storage/__tests__/SQLiteStorage.test.ts src/server/mcp/__tests__/CodeMapMcpServer.test.ts src/cli/commands/__tests__/mcp.test.ts src/cli/__tests__/index-help.test.ts`
- [ ] `npm run typecheck`
- [ ] `npm run docs:check`
- [ ] `npm run build`
- [ ] `git diff --check`
</verification>

<success_criteria>
- `mycodemap mcp start` 能启动真实 stdio MCP server，且 `stdout` 不被欢迎信息或运行日志污染
- `codemap_query` / `codemap_impact` 返回 symbol-level definition / callers / callees / impact，并带显式 `graph_status`
- `GRAPH_NOT_FOUND` / `SYMBOL_NOT_FOUND` / `AMBIGUOUS_EDGE` 都能被测试命中
- `mcp install` 能以 experimental server 方式更新 `.mcp.json`
- `docs/ai-guide/INTEGRATION.md` 以真实 stdio path 为主，旧 wrapper 只保留 fallback 说明
</success_criteria>
