---
phase: 26-implement-symbol-level-graph-and-experimental-mcp-thin-slice
plan: 02
type: execute
wave: 1
depends_on:
  - 26-01
files_modified:
  - src/interface/types/index.ts
  - src/domain/entities/CodeGraph.ts
  - src/core/analyzer.ts
  - src/core/__tests__/analyzer.test.ts
  - src/cli/commands/generate.ts
  - src/cli/__tests__/generate.test.ts
  - src/infrastructure/storage/graph-helpers.ts
  - src/infrastructure/storage/adapters/SQLiteStorage.ts
  - src/infrastructure/storage/sqlite/GovernanceGraphCache.ts
  - src/infrastructure/storage/__tests__/SQLiteStorage.test.ts
  - README.md
  - AI_GUIDE.md
  - CLAUDE.md
  - docs/ai-guide/COMMANDS.md
  - docs/ai-guide/OUTPUT.md
autonomous: false
requirements:
  - P26-NOW-PARTIAL-GRAPH-TRUTH
user_setup: []
must_haves:
  truths:
    - `generate` / `codemap.json` 必须显式暴露 `graphStatus: complete | partial`
    - 单文件解析掉队时，analyze 不得伪装成完整图，必须产出 `failedFileCount` 与可选 `parseFailureFiles`
    - `generate --symbol-level` 与默认 generate 都要透传同一套 partial graph truth
    - SQLite 保存与读回后，后续 MCP / query 读路径仍能看见 `graphStatus` 与失败文件信息
  artifacts:
    - .planning/milestones/post-v1.6-phases/26-implement-symbol-level-graph-and-experimental-mcp-thin-slice/26-02-PLAN.md
    - src/core/analyzer.ts
    - src/infrastructure/storage/adapters/SQLiteStorage.ts
    - docs/ai-guide/OUTPUT.md
  key_links:
    - Success Criteria 要求 `graph_status: complete | partial` 可见
    - test plan 把“单文件 parse failure 不得 silent corruption”列为 edge case
---

<objective>
完成 Phase 26 的第二刀：把 partial graph truth 变成正式契约，而不是依赖日志猜测。

Purpose: 26-01 已经把 symbol-level truth 写进 CodeGraph / SQLite，但还缺少“图是不是完整”的护栏。没有 `graphStatus`，后续 MCP query / impact 无法判断当前图能否被可信消费。
Output: `CodeMap` / `CodeGraph` graph integrity 字段、analyzer 失败文件追踪、SQLite metadata 持久化与回读、focused tests、必要 docs 更新。
</objective>

<execution_context>
@/data/codemap/.planning/milestones/post-v1.6-phases/26-implement-symbol-level-graph-and-experimental-mcp-thin-slice/26-CONTEXT.md
@/data/codemap/.planning/milestones/post-v1.6-phases/26-implement-symbol-level-graph-and-experimental-mcp-thin-slice/26-01-SUMMARY.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: 定义 graph integrity contract 并在 analyzer 产出 partial truth</name>
  <files>src/interface/types/index.ts, src/domain/entities/CodeGraph.ts, src/core/analyzer.ts, src/core/__tests__/analyzer.test.ts</files>
  <action>为 `CodeMap` / `CodeGraph` 增加 `graphStatus`、`failedFileCount`、`parseFailureFiles`，并在 `analyze()` 基于 discovered files 与实际成功 materialize 的 modules 计算 `complete | partial`。补测试证明 smart parser 静默跳过文件时，返回结果会显式降级为 `partial`。</action>
  <verify>npm run test -- src/core/__tests__/analyzer.test.ts</verify>
</task>

<task type="auto">
  <name>Task 2: 把 graph integrity 透传到 generate 与 SQLite</name>
  <files>src/cli/commands/generate.ts, src/cli/__tests__/generate.test.ts, src/infrastructure/storage/graph-helpers.ts, src/infrastructure/storage/adapters/SQLiteStorage.ts, src/infrastructure/storage/sqlite/GovernanceGraphCache.ts, src/infrastructure/storage/__tests__/SQLiteStorage.test.ts</files>
  <action>`generate` stdout 与 `convertToCodeGraph()` 透传 partial graph truth；SQLite 用 metadata 表持久化 `graph_status` / `failed_file_count` / `parse_failure_files_json`，并在 `loadCodeGraph()` 与 governance cache 读回时恢复这些字段。补测试证明保存和读回后语义不丢。</action>
  <verify>npm run test -- src/cli/__tests__/generate.test.ts src/infrastructure/storage/__tests__/SQLiteStorage.test.ts</verify>
</task>

<task type="auto">
  <name>Task 3: 同步 AI / 用户文档</name>
  <files>README.md, AI_GUIDE.md, CLAUDE.md, docs/ai-guide/COMMANDS.md, docs/ai-guide/OUTPUT.md</files>
  <action>只在真实暴露给用户和 Agent 的 `generate` / `codemap.json` 文档面补 `graphStatus` 语义：`summary.totalFiles` 仍是成功入图文件数，`graphStatus = partial` 代表发现到的文件没有全部进入最终图，不能当成完整 truth。</action>
  <verify>npm run docs:check</verify>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test -- src/core/__tests__/analyzer.test.ts src/cli/__tests__/generate.test.ts src/infrastructure/storage/__tests__/SQLiteStorage.test.ts`
- [ ] `npm run typecheck`
- [ ] `npm run docs:check`
</verification>

<success_criteria>
- `graphStatus` 在 `generate` 输出与 `codemap.json` 中可见
- 单文件 parse failure 会显式产出 `partial`，而不是 silent corruption
- SQLite 读回的 `CodeGraph` 仍保留完整的 graph integrity metadata
- 文档说明了 `summary.totalFiles` 与 `graphStatus` 的差异
</success_criteria>
