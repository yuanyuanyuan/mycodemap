---
phase: 999.1-mycodemap-init-enhancements
plan: 05
type: execute
wave: 4
depends_on: ["999.1-03", "999.1-04"]
files_modified:
  - README.md
  - AI_GUIDE.md
  - docs/SETUP_GUIDE.md
  - docs/AI_ASSISTANT_SETUP.md
  - docs/ai-guide/COMMANDS.md
  - docs/ai-guide/QUICKSTART.md
  - docs/ai-guide/PATTERNS.md
  - scripts/validate-docs.js
  - src/cli/__tests__/validate-docs-script.test.ts
autonomous: false
requirements:
  - R4
  - R5
  - R23
  - R40
  - docs-sync
  - package-smoke
user_setup: []
must_haves:
  truths:
    - Public docs and AI docs must present `.mycodemap/config.json` as the canonical config path.
    - Docs guardrails must fail if stale root-config guidance returns.
    - Package smoke must prove init assets are available outside the source checkout.
  artifacts:
    - .planning/phases/999.1-mycodemap-init-enhancements/999.1-RESEARCH.md
    - .planning/phases/999.1-mycodemap-init-enhancements/999.1-VALIDATION.md
  key_links:
    - AGENTS.md requires README / AI_GUIDE / docs/ai-guide updates when config or CLI behavior changes.
---

<objective>
Synchronize human docs, AI docs, docs guardrails, and package-level smoke verification with the new `mycodemap init` reconciler contract.

Purpose: Phase 999.1 changes user-visible CLI behavior, config location, generated assets, and AI setup instructions; docs and package validation must close the contract rather than lag behind implementation.
Output: Updated docs, validation script/test expectations, and repeatable package smoke instructions.
</objective>

<execution_context>
@/data/codemap/.planning/phases/999.1-mycodemap-init-enhancements/999.1-RESEARCH.md
@/data/codemap/.planning/phases/999.1-mycodemap-init-enhancements/999.1-VALIDATION.md
@/data/codemap/AGENTS.md
</execution_context>

<threat_model>
- Threat: README/AI docs still tell users to edit root `mycodemap.config.json`, contradicting the CLI
  - Severity: high
  - Mitigation: Update docs and docs guardrail fixtures to enforce `.mycodemap/config.json`
- Threat: Hook/rule templates work from source checkout but are missing from npm package
  - Severity: high
  - Mitigation: Add package smoke that installs the packed tarball into a temp prefix and runs init in a fixture repo
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Synchronize user and AI documentation</name>
  <files>README.md, AI_GUIDE.md, docs/SETUP_GUIDE.md, docs/AI_ASSISTANT_SETUP.md, docs/ai-guide/COMMANDS.md, docs/ai-guide/QUICKSTART.md, docs/ai-guide/PATTERNS.md</files>
  <read_first>README.md, AI_GUIDE.md, docs/ai-guide/README.md, docs/brainstorms/999.1-mycodemap-init-enhancements-requirements.md</read_first>
  <action>Update init, config, workspace, hook, and AI-rule guidance to match the new contract. Mention `.mycodemap/config.json`, `.mycodemap/status/init-last.json`, `.mycodemap/hooks/`, `.mycodemap/rules/`, and the manual `CLAUDE.md` / `AGENTS.md` snippet. Keep docs AI-friendly with decision tables, copy-paste commands, and clear error/recovery guidance.</action>
  <verify>npm run docs:check</verify>
  <acceptance_criteria>
    - README says init creates/reconciles `.mycodemap/config.json`, not root `mycodemap.config.json`
    - AI_GUIDE links or summarizes the new init workflow
    - COMMANDS/QUICKSTART docs include receipt/manual-action behavior
    - Docs state that init does not auto-edit `CLAUDE.md` or `AGENTS.md`
  </acceptance_criteria>
  <done>Human and AI docs describe the same init contract the CLI implements.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs guardrails and package smoke</name>
  <files>scripts/validate-docs.js, src/cli/__tests__/validate-docs-script.test.ts</files>
  <read_first>scripts/validate-docs.js, src/cli/__tests__/validate-docs-script.test.ts, package.json</read_first>
  <action>Update documentation guardrails so stale root-config guidance is caught and required `.mycodemap/config.json` / receipt / AI-rule snippets stay discoverable. Add or document a package smoke command that runs `npm pack`, installs the tarball into a temporary prefix, runs `mycodemap init --yes` in a temp Git repo, and verifies `.mycodemap/status/init-last.json`, hooks, and rules exist.</action>
  <verify>npm run docs:check && npm run check:all</verify>
  <acceptance_criteria>
    - `npm run docs:check` fails if docs revert to root config as canonical
    - Docs guardrail covers the receipt path `.mycodemap/status/init-last.json`
    - Package smoke proves hook/rule assets are present after install
    - `npm run check:all` passes after docs and implementation changes
  </acceptance_criteria>
  <done>Docs and published-package validation close the end-to-end phase contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run docs:check`
- [ ] `npm run check:all`
- [ ] `npm pack` followed by a temp-prefix install and `mycodemap init --yes` smoke in a temp Git repo
</verification>

<success_criteria>
- Public and AI docs agree on canonical `.mycodemap/` workspace behavior
- Documentation guardrails enforce the new init/config truth
- Packaged CLI can initialize a temp project without relying on source-only assets
- Phase verification has both automated and manual smoke evidence
</success_criteria>
