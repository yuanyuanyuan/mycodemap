---
phase: 999.1-mycodemap-init-enhancements
plan: 03
subsystem: cli
tags: [cli, init, hooks, gitignore, package-safe-assets]
requires: [999.1-02]
provides:
  - package-safe hook templates under `scripts/hooks/templates/`
  - `.mycodemap/hooks/*` payload sync with hash-based already-synced detection
  - ownership-preserving `.git/hooks/*` shim install plus `.gitignore` reconciliation
affects: [init, git-hooks, gitignore, package-smoke, phase-999.1-05]
tech-stack:
  added: []
  patterns: [packaged-hook-templates, preserve-user-hook-via-backup, idempotent-gitignore-append]
key-files:
  created:
    - src/cli/init/hooks.ts
    - scripts/hooks/templates/pre-commit
    - scripts/hooks/templates/commit-msg
    - src/cli/commands/__tests__/init-hooks.test.ts
    - .planning/phases/999.1-mycodemap-init-enhancements/999.1-03-SUMMARY.md
  modified:
    - src/cli/init/reconciler.ts
key-decisions:
  - "Hook payloads are shipped from `scripts/hooks/templates/`, not source-only `.githooks/`."
  - "When a user already owns `.git/hooks/<name>`, init preserves it in `.mycodemap/hooks/user-<name>.sh` and installs a shim instead of silent overwrite."
  - "Non-Git projects still receive `.mycodemap/hooks/*` payloads, but shim / gitignore actions are explicitly `skipped`."
patterns-established:
  - "Managed shim pattern: preserved user hook runs first, then CodeMap payload."
  - "Missing packaged template is reported as `manual-action-needed`, never green."
requirements-completed: [R13, R14, R15, R16, R17, R18, R19, R37, R38, R39]
duration: 1 plan
completed: 2026-04-21
---

# Phase 999.1 Plan 03 Summary

**Hooks 现已以 package-safe payload + preserving shim 的方式接入 `init` receipt**

## Accomplishments
- 新增 `src/cli/init/hooks.ts`，统一完成 hook payload 规划、现状扫描、shim 安装、`.gitignore` 追加与冲突判断。
- 新增 `scripts/hooks/templates/pre-commit` 与 `scripts/hooks/templates/commit-msg`，让 npm tarball 安装后的 CLI 也能找到 hook 模板。
- 将 hook 资产接入 `src/cli/init/reconciler.ts`，把 Wave 2 的 placeholder 状态替换为真实 installed / already-synced / skipped / conflict。
- 新增 `src/cli/commands/__tests__/init-hooks.test.ts`，覆盖 Git 仓库、非 Git 仓库、已有用户 hook、重复运行、缺失模板五类场景。

## Task Commits

No commits were created in this session.

## Verification
- `./node_modules/.bin/vitest run src/cli/commands/__tests__/init-hooks.test.ts` — PASS (`5` tests)
- `npm run typecheck` — PASS

## Acceptance Criteria Evidence
- PASS — `.mycodemap/hooks/pre-commit` 与 `.mycodemap/hooks/commit-msg` 会从 packaged templates 生成
- PASS — rerun 会把 hook payload / shim / `.gitignore` 标为 `already-synced`
- PASS — 缺失模板路径会返回 `manual-action-needed`
- PASS — 用户已有 hook 会被保留到 `.mycodemap/hooks/user-*.sh`，shim 明确串联两段逻辑

## Issues Encountered
None inside this plan scope.

## Deviations from Plan
None.

## Next Phase Readiness
- Rules bundle 与 AI context snippet 现在可以复用同一份 receipt/asset contract。
- Package smoke 已有 package-safe hook 模板基础，不再依赖源码 checkout 下的 `.githooks/`。

---
*Phase: 999.1-mycodemap-init-enhancements*
*Completed: 2026-04-21*
