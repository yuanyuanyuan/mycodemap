---
phase: 27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/capability-report.py
  - scripts/tests/test_capability_report.py
autonomous: false
requirements:
  - P27-NOW-CAPABILITY-REPORT
user_setup: []
must_haves:
  truths:
    - capability report 只能报告环境与可执行性，不能阻断 git hook、CI 或正常开发流程
    - required checks 失败必须显式标为 `REQUIRED_FAIL`，optional checks 不可用必须显式标为 `OPTIONAL_DISABLED`
    - strategy checks 必须记录 `duration_ms`，为后续 hard-gate 放置决策提供真实基线
    - `scripts/validate-rules.py` 若尚不存在，只能标记 unavailable，不能伪装成已通过
  artifacts:
    - .planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-RESEARCH.md
    - .planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-VALIDATION.md
    - /home/stark/.claude/plans/harness-claude-openai-github-openharness-parallel-alpaca.md
    - /home/stark/.gstack/projects/codemap/stark-main-eng-review-test-plan-20260418-200710.md
  key_links:
    - 主计划 Phase 0 要求先做 capability report，再决定 hard gate 放置策略
    - QA checklist 把 capability report 列为第一条 critical path
---

<objective>
实现 Phase 27 的 capability baseline：新增 repo-local `scripts/capability-report.py`，把 Python/Node/hooks/build/validator 可执行性与关键命令耗时写成结构化 JSON 报告，但不改变任何 hook / CI 的阻断行为。

Purpose: 后续 `validate-rules.py`、pre-commit 与 CI backstop 的策略取决于真实能力基线；如果先接 gate 再测环境，会把“缺能力”误判成“代码失败”。
Output: `scripts/capability-report.py`、可重复运行的 Python 单测、稳定的 JSON contract 与 `/tmp` 级 smoke 命令。
</objective>

<execution_context>
@/data/codemap/.planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-RESEARCH.md
@/data/codemap/.planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-VALIDATION.md
</execution_context>

<threat_model>
- Threat: 环境缺少 `python3`、`node` 或 `dist/cli/index.js` 时被脚本当成通过，导致后续 hook / CI 建在错误前提上
  - Severity: high
  - Mitigation: required items 统一输出 `REQUIRED_FAIL`，optional items 统一输出 `OPTIONAL_DISABLED`，JSON `summary` 统计 required/optional 通过数
- Threat: capability report 默认写回仓库、污染提交内容或阻塞正常开发
  - Severity: medium
  - Mitigation: 支持 `--output <path>`；验证命令默认写 `/tmp/codemap-capability-report.json`
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 新建 capability report 脚本与 JSON contract</name>
  <files>scripts/capability-report.py</files>
  <read_first>scripts/capability-report.py, .githooks/pre-commit, .github/workflows/ci-gateway.yml, .planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-RESEARCH.md, /home/stark/.claude/plans/harness-claude-openai-github-openharness-parallel-alpaca.md</read_first>
  <action>新建 `scripts/capability-report.py`。实现 CLI：`python3 scripts/capability-report.py --output <path>`。输出 JSON 顶层键必须包含 `version`、`generated_at`、`summary`、`items`。`items` 至少检查 7 项：`python3 --version`、`node --version`、`git config core.hooksPath`（若为空则退回检查 `.githooks/` 目录）、`npm run typecheck`、`npm test`、`npm run lint`、`node dist/cli/index.js --version`。若 `scripts/validate-rules.py` 已存在，再追加 `python3 scripts/validate-rules.py code --report-only`；若不存在，将该项写为 `OPTIONAL_DISABLED`。required 项状态只能是 `PASS` 或 `REQUIRED_FAIL`；optional 项状态只能是 `PASS` 或 `OPTIONAL_DISABLED`；strategy 项状态只能是 `STRATEGY_PASS` 或 `STRATEGY_FAIL`，并写 `duration_ms`。禁止输出 `SKIP`、`ok-ish`、空字符串之类的模糊状态。</action>
  <verify>python3 scripts/capability-report.py --output /tmp/codemap-capability-report.json</verify>
  <acceptance_criteria>
    - `scripts/capability-report.py` 包含字符串 `REQUIRED_FAIL`
    - `scripts/capability-report.py` 包含字符串 `OPTIONAL_DISABLED`
    - 运行 `python3 scripts/capability-report.py --output /tmp/codemap-capability-report.json` 后，`/tmp/codemap-capability-report.json` 存在
    - `/tmp/codemap-capability-report.json` 包含键 `"summary"`
    - `/tmp/codemap-capability-report.json` 包含键 `"items"`
    - `/tmp/codemap-capability-report.json` 中存在 `"name": "python3"` 与 `"name": "node"`
  </acceptance_criteria>
  <done>repo 有了不阻断流程的 capability baseline 事实来源。</done>
</task>

<task type="auto">
  <name>Task 2: 为 capability report 添加 Python 单测</name>
  <files>scripts/tests/test_capability_report.py</files>
  <read_first>scripts/tests/test_capability_report.py, scripts/capability-report.py, .planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-VALIDATION.md</read_first>
  <action>新增 `scripts/tests/test_capability_report.py`，使用 Python `unittest` 覆盖 3 条核心路径：1) required command 失败时状态为 `REQUIRED_FAIL`；2) optional command 缺失时状态为 `OPTIONAL_DISABLED`；3) `--output <tempfile>` 会写出含 `summary` 和 `items` 的 JSON 文件。若为便于测试需要抽出 `run_check()`、`classify_status()` 或 `build_report()` 等纯函数，可在 `scripts/capability-report.py` 中实现，但不得引入第三方 Python 依赖。</action>
  <verify>python3 -m unittest scripts/tests/test_capability_report.py</verify>
  <acceptance_criteria>
    - `scripts/tests/test_capability_report.py` 包含 `test_required_fail_status`
    - `scripts/tests/test_capability_report.py` 包含 `test_optional_disabled_status`
    - `scripts/tests/test_capability_report.py` 包含 `test_output_file_written`
    - `python3 -m unittest scripts/tests/test_capability_report.py` 退出码为 0
  </acceptance_criteria>
  <done>capability report 的核心 contract 有可回归验证。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m py_compile scripts/capability-report.py scripts/tests/test_capability_report.py`
- [ ] `python3 -m unittest scripts/tests/test_capability_report.py`
- [ ] `python3 scripts/capability-report.py --output /tmp/codemap-capability-report.json`
</verification>

<success_criteria>
- capability report 能生成结构化 JSON
- required / optional / strategy 状态语义显式可区分
- 缺少 `scripts/validate-rules.py` 时不会伪装成 pass
- 后续计划可据此决定 report-only 与 hard-gate 的默认路径
</success_criteria>
