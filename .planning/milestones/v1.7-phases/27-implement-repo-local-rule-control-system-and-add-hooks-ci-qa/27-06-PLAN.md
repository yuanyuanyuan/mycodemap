---
phase: 27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa
plan: 06
type: execute
wave: 4
depends_on:
  - 27-04
  - 27-05
files_modified:
  - scripts/qa-rule-control.sh
  - scripts/tests/test_rule_control_workflow.py
  - docs/rules/validation.md
autonomous: false
requirements:
  - P27-NOW-HOOKS-CI-QA
  - P27-NOW-WORKFLOW-VALIDATION
user_setup: []
must_haves:
  truths:
    - QA 不能只证明 happy path，必须覆盖 block / warn / unavailable / disabled / bypass-backstop 场景
    - QA 脚本必须在 `/tmp` 下创建临时 repo 或临时文件，不能污染工作树
    - validation 文档必须包含可复制的 QA 命令
  artifacts:
    - scripts/qa-rule-control.sh
    - scripts/tests/test_rule_control_workflow.py
    - docs/rules/validation.md
    - /home/stark/.gstack/projects/codemap/stark-main-eng-review-test-plan-20260418-200710.md
  key_links:
    - QA checklist 已列出 capability、P0 block、P1/P2 warn、disabled soft gate、subagent injection、CI backstop 等关键路径
    - 研究结论要求“不要只靠 agent 自述已验证”
---

<objective>
补齐 Phase 27 的可执行 QA 验证面：新增 `scripts/qa-rule-control.sh` 与自动化回归测试，覆盖 capability、validator、hooks、soft gate、subagent route helper、`--no-verify` backstop 文档化路径。

Purpose: 如果没有成体系 QA，Phase 27 很容易停留在“文档说会这样、代码大概如此”，而不是可重复证据。
Output: 一键 QA 脚本、针对 rule-context / workflow contract 的自动化测试、同步后的 validation 文档。
</objective>

<execution_context>
@/data/codemap/.planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-RESEARCH.md
@/data/codemap/.planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-VALIDATION.md
</execution_context>

<threat_model>
- Threat: QA 只验证 happy path，P0 block、disabled soft gate、unavailable dependency 与 `--no-verify` backstop 没有证据
  - Severity: high
  - Mitigation: `scripts/qa-rule-control.sh` 至少覆盖 `capability`, `p0-block`, `p1-warn`, `unavailable`, `disabled-soft-gate`, `rule-context`, `no-verify-backstop` 七个 scenario
- Threat: QA 过程污染工作树，导致 hook / CI 结果和真实开发态串味
  - Severity: medium
  - Mitigation: 所有 fixture 一律在 `/tmp/codemap-rule-control-*` 创建并在脚本退出时清理
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 新增 rule-control QA 脚本覆盖关键场景</name>
  <files>scripts/qa-rule-control.sh</files>
  <read_first>scripts/qa-rule-control.sh, scripts/validate-rules.py, scripts/capability-report.py, scripts/rule-context.mjs, .githooks/pre-commit, .github/workflows/ci-gateway.yml, .planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-VALIDATION.md</read_first>
  <action>新增 `scripts/qa-rule-control.sh`。CLI 形态固定为 `bash scripts/qa-rule-control.sh --scenario <name|all>`。脚本必须在 `/tmp/codemap-rule-control-*` 创建临时目录，并覆盖 7 个 scenario：`capability`（运行 capability report 到 `/tmp`）、`p0-block`（构造会触发 P0 的输入并断言 validator exit 1）、`p1-warn`（断言 exit 2 不被当作 blocker）、`unavailable`（缺少 `dist/cli/index.js` 时断言 exit 4）、`disabled-soft-gate`（将 `.claude/rule-system.config.json` 复制到临时目录并设置 `enabled:false`，断言 hook 无 advisory 输出）、`rule-context`（断言 helper 只返回相关规则）、`no-verify-backstop`（grep CI workflow 中 `Rule validation backstop` 与 `python3 scripts/validate-rules.py code`）。脚本输出末尾必须打印 `RULE_CONTROL_QA: PASS` 或 `RULE_CONTROL_QA: FAIL`。</action>
  <verify>bash scripts/qa-rule-control.sh --scenario all</verify>
  <acceptance_criteria>
    - `scripts/qa-rule-control.sh` 包含 `--scenario`
    - `scripts/qa-rule-control.sh` 包含 `p0-block`
    - `scripts/qa-rule-control.sh` 包含 `disabled-soft-gate`
    - `scripts/qa-rule-control.sh` 包含 `Rule validation backstop`
    - `scripts/qa-rule-control.sh` 包含 `RULE_CONTROL_QA: PASS`
  </acceptance_criteria>
  <done>Phase 27 有了可重复的一键 QA 脚本。</done>
</task>

<task type="auto">
  <name>Task 2: 为 workflow contract 与 QA 命令补自动化回归与文档</name>
  <files>scripts/tests/test_rule_control_workflow.py, docs/rules/validation.md</files>
  <read_first>scripts/tests/test_rule_control_workflow.py, docs/rules/validation.md, scripts/qa-rule-control.sh, scripts/rule-context.mjs, .codex/get-shit-done/workflows/execute-phase.md, .claude/get-shit-done/workflows/execute-phase.md</read_first>
  <action>新增 `scripts/tests/test_rule_control_workflow.py`，使用 Python `unittest` 做 3 类断言：1) `node scripts/rule-context.mjs --files src/cli/index.ts --format json` 返回 `code-quality-redlines.md` 且不返回 `docs/rules/testing.md`；2) `.codex/get-shit-done/workflows/execute-phase.md` 与 `.claude/get-shit-done/workflows/execute-phase.md` 都包含 `rule-context.mjs --files` 与 `<rule_context>`；3) `.github/workflows/ci-gateway.yml` 包含 `Rule validation backstop`。同步更新 `docs/rules/validation.md`，新增 `## Rule Control QA` 小节，列出 `bash scripts/qa-rule-control.sh --scenario all`、`python3 -m unittest scripts/tests/test_rule_control_workflow.py` 两条命令与各自目的。</action>
  <verify>python3 -m unittest scripts/tests/test_rule_control_workflow.py && grep -n "Rule Control QA" docs/rules/validation.md</verify>
  <acceptance_criteria>
    - `scripts/tests/test_rule_control_workflow.py` 包含 `test_rule_context_is_scoped`
    - `scripts/tests/test_rule_control_workflow.py` 包含 `test_execute_workflows_include_rule_context`
    - `scripts/tests/test_rule_control_workflow.py` 包含 `test_ci_contains_rule_backstop`
    - `docs/rules/validation.md` 包含 `Rule Control QA`
    - `docs/rules/validation.md` 包含 `bash scripts/qa-rule-control.sh --scenario all`
    - `python3 -m unittest scripts/tests/test_rule_control_workflow.py` 退出码为 0
  </acceptance_criteria>
  <done>workflow QA contract 与运行命令被自动化与文档同时锁住。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash scripts/qa-rule-control.sh --scenario all`
- [ ] `python3 -m unittest scripts/tests/test_rule_control_workflow.py`
- [ ] `npm run docs:check`
</verification>

<success_criteria>
- Phase 27 的关键失败模式都有可重复 QA 路径
- rule-context / workflow injection / CI backstop 都有自动化断言
- validation 文档给出复制即用的 QA 命令
- QA 过程不污染工作树
</success_criteria>
