---
phase: 27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validate-rules.py
  - scripts/tests/test_validate_rules.py
autonomous: false
requirements:
  - P27-NOW-VALIDATE-RULES
user_setup: []
must_haves:
  truths:
    - `validate-rules.py` 必须使用仓库现有命令：`npm run typecheck`、`npm run lint`、`npm test`
    - report-only 模式必须总是退出 0，但要把 P0/P1/P2/unavailable 明确打印在 JSON 与 summary 里
    - gate 模式退出码必须稳定：`0 pass`、`1 P0`、`2 P1`、`3 P2`、`4 unavailable`
    - 不允许出现 `SKIP`、`silent pass`、`|| true` 这类把依赖缺失伪装成通过的路径
  artifacts:
    - scripts/validate-rules.py
    - .planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-RESEARCH.md
  key_links:
    - 主计划 Phase 3 要求 repo-local hard gate 基于代码事实
    - QA checklist 把 `validate-rules.py` 的 exit-code contract 列为关键交互
---

<objective>
实现 Phase 27 的核心 validator：新增 `scripts/validate-rules.py`，统一 code/arch/all 三类检查的 report-only 与 gate 行为，并用 Python 单测锁住退出码语义。

Purpose: hooks、CI、soft-gate 和 workflow QA 都要引用同一份 validator contract；如果脚本本身的退出码不稳定，后续所有 gate 都会漂移。
Output: `scripts/validate-rules.py`、可回归的 exit-code tests、明确的 JSON report 结构。
</objective>

<execution_context>
@/data/codemap/.planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-RESEARCH.md
@/data/codemap/.planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-VALIDATION.md
</execution_context>

<threat_model>
- Threat: unavailable dependency 被写成 pass，导致 pre-commit / CI 在缺能力机器上给出虚假绿色
  - Severity: high
  - Mitigation: unavailable 统一走退出码 `4`，JSON `status` 统一为 `unavailable`
- Threat: warn-level (P1/P2) 被 CI 当成 blocker，或 P0 被当成 warn-only
  - Severity: high
  - Mitigation: 固定 exit code contract，并用单测覆盖 `0/1/2/3/4`
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 新建 repo-local validate-rules 脚本</name>
  <files>scripts/validate-rules.py</files>
  <read_first>scripts/validate-rules.py, package.json, .githooks/pre-commit, .github/workflows/ci-gateway.yml, docs/rules/validation.md, .planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-RESEARCH.md</read_first>
  <action>新建 `scripts/validate-rules.py`。CLI 形态固定为 `python3 scripts/validate-rules.py [code|arch|all] [--report-only]`。`code` 目标至少运行 3 项：`npm run typecheck`（P0）、`npm test`（P0）、`npm run lint`（P1）。`arch` 目标运行 `node dist/cli/index.js deps -m src/domain`（P0），但只有当 `dist/cli/index.js` 存在时才运行；否则该项状态为 `unavailable`。`all` 组合 `code + arch`。JSON 输出顶层必须包含 `checks` 数组；每个 check 必须包含 `name`、`level`、`status`、`command`、`output`。`--report-only` 必须总是退出 0；gate 模式必须严格返回 `0/1/2/3/4`。禁止使用 `npx eslint --rule`、`|| echo SKIP`、静默吞掉 timeout 或异常。</action>
  <verify>python3 scripts/validate-rules.py code --report-only</verify>
  <acceptance_criteria>
    - `scripts/validate-rules.py` 包含字符串 `status\": \"unavailable` 或 `status': 'unavailable'`
    - `scripts/validate-rules.py` 包含字符串 `sys.exit(4)`
    - `scripts/validate-rules.py` 包含字符串 `npm run typecheck`
    - `scripts/validate-rules.py` 包含字符串 `npm test`
    - `scripts/validate-rules.py` 包含字符串 `npm run lint`
    - 运行 `python3 scripts/validate-rules.py code --report-only` 时退出码为 0
  </acceptance_criteria>
  <done>hooks / CI 有了统一可调用的 repo-local validator。</done>
</task>

<task type="auto">
  <name>Task 2: 为 validator 锁定 exit-code 与 unavailable 语义</name>
  <files>scripts/tests/test_validate_rules.py</files>
  <read_first>scripts/tests/test_validate_rules.py, scripts/validate-rules.py, .planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-VALIDATION.md</read_first>
  <action>新增 `scripts/tests/test_validate_rules.py`。使用 Python `unittest` 对内部纯函数或可 patch 的 runner 做 5 条断言：1) no findings → exit 0；2) P0 failed → exit 1；3) only P1 failed → exit 2；4) only P2 failed → exit 3；5) any unavailable → exit 4；6) report-only regardless of findings → exit 0。测试还要断言 JSON checks 中 `status` 值只使用 `passed`、`failed`、`unavailable` 三种实现态，severity 由 `level` 表达。</action>
  <verify>python3 -m unittest scripts/tests/test_validate_rules.py</verify>
  <acceptance_criteria>
    - `scripts/tests/test_validate_rules.py` 包含 `test_exit_code_for_p0`
    - `scripts/tests/test_validate_rules.py` 包含 `test_exit_code_for_p1`
    - `scripts/tests/test_validate_rules.py` 包含 `test_exit_code_for_unavailable`
    - `scripts/tests/test_validate_rules.py` 包含 `test_report_only_exits_zero`
    - `python3 -m unittest scripts/tests/test_validate_rules.py` 退出码为 0
  </acceptance_criteria>
  <done>validator 的核心退出语义被测试锁住。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m py_compile scripts/validate-rules.py scripts/tests/test_validate_rules.py`
- [ ] `python3 -m unittest scripts/tests/test_validate_rules.py`
- [ ] `python3 scripts/validate-rules.py code --report-only`
</verification>

<success_criteria>
- validator 有稳定的 report-only / gate contract
- unavailable 情况不再伪装成 pass
- hooks 与 CI 可以基于 exit code 做明确决策
- 不引入新的 Python 依赖
</success_criteria>
