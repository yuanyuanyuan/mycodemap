---
phase: 27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa
plan: 03
type: execute
wave: 2
depends_on:
  - 27-02
files_modified:
  - CLAUDE.md
  - docs/rules/README.md
  - docs/rules/code-quality-redlines.md
  - docs/rules/architecture-guardrails.md
  - docs/rules/validation.md
  - .claude/rule-system.config.json
autonomous: false
requirements:
  - P27-NOW-SOFT-GATE-DEFAULTS
  - P27-NOW-WORKFLOW-VALIDATION
user_setup: []
must_haves:
  truths:
    - 规则加载必须按编辑文件路径路由，不能只依赖 AI 是否输出任务分析模板
    - `CLAUDE.md` 必须明确写出 post-edit report-only 验证命令
    - soft gate 默认开启 advisory 路径，但 hard gate 默认不能直接变成强阻断
    - 规则文档要短、可执行、可 grep，不再保留大段无法验证的描述
  artifacts:
    - CLAUDE.md
    - docs/rules/validation.md
    - .claude/rule-system.config.json
    - .planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-RESEARCH.md
  key_links:
    - 主计划修订稿明确要求 `enabled: true` 且至少 `change_analyzer: true`
    - 研究结论要求路径驱动规则加载与 post-edit 可核实验证
---

<objective>
把 Phase 27 的规则入口文档与默认配置收敛成“短、可执行、按路径路由”的 repo-local contract：更新 `CLAUDE.md`、压缩 `docs/rules/*`、新增 `.claude/rule-system.config.json` 默认值。

Purpose: 没有明确入口 contract，后面的 hook / subagent 注入只会再次退化成“希望 AI 自觉遵守”。先把入口和默认配置锁定，后续执行面才有统一事实来源。
Output: path-based rule routing、post-edit report-only contract、soft/hard gate 默认值、精简后的规则索引与核心规则文档。
</objective>

<execution_context>
@/data/codemap/.planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-RESEARCH.md
@/data/codemap/.planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-VALIDATION.md
</execution_context>

<threat_model>
- Threat: 文档说“按路径强制加载”，但默认配置仍为 disabled，导致 soft gate 实际上不存在
  - Severity: high
  - Mitigation: `.claude/rule-system.config.json` 默认 `enabled: true`、`change_analyzer: true`
- Threat: 规则文档继续冗长、不可验证，执行代理只能凭印象工作
  - Severity: medium
  - Mitigation: 每份规则文档改成表格化命令 / 阈值 / 级别 / 恢复路径
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 新增默认 rule-system config 并写清入口契约</name>
  <files>.claude/rule-system.config.json, CLAUDE.md</files>
  <read_first>.claude/rule-system.config.json, CLAUDE.md, .claude/settings.json, .planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-RESEARCH.md</read_first>
  <action>新建 `.claude/rule-system.config.json`，顶层键固定为 `version`、`enabled`、`route_by_edit_path`、`soft_gate`、`hard_gate`。默认值写成：`enabled: true`、`route_by_edit_path: true`、`soft_gate.change_analyzer: true`、`soft_gate.rule_injector: false`、`soft_gate.human_escalation: false`、`hard_gate.mode: \"report-only\"`。同步更新 `CLAUDE.md`：新增一段明确说明“每次 Edit/Write 后必须运行 `python3 scripts/validate-rules.py code --report-only`”；新增按文件路径路由表，至少覆盖 `src/cli/*`、`src/domain/*`、`src/server/*`、`src/infrastructure/*`、`src/interface/*`、`*.test.ts`、`docs/*`、`.githooks/*`、`.github/workflows/*`。</action>
  <verify>grep -n "python3 scripts/validate-rules.py code --report-only" CLAUDE.md && grep -n "\"enabled\": true" .claude/rule-system.config.json</verify>
  <acceptance_criteria>
    - `.claude/rule-system.config.json` 包含 `"enabled": true`
    - `.claude/rule-system.config.json` 包含 `"route_by_edit_path": true`
    - `.claude/rule-system.config.json` 包含 `"mode": "report-only"`
    - `CLAUDE.md` 包含 `python3 scripts/validate-rules.py code --report-only`
    - `CLAUDE.md` 包含 `.githooks/*`
    - `CLAUDE.md` 包含 `.github/workflows/*`
  </acceptance_criteria>
  <done>repo-local rule control 有了清晰默认值与入口契约。</done>
</task>

<task type="auto">
  <name>Task 2: 把 rules 文档压缩为可执行表格与索引</name>
  <files>docs/rules/README.md, docs/rules/code-quality-redlines.md, docs/rules/architecture-guardrails.md, docs/rules/validation.md</files>
  <read_first>docs/rules/README.md, docs/rules/code-quality-redlines.md, docs/rules/architecture-guardrails.md, docs/rules/validation.md, CLAUDE.md, package.json</read_first>
  <action>新增或重写 `docs/rules/README.md` 为纯索引，列出每个规则文档的适用场景与主命令。把 `docs/rules/code-quality-redlines.md`、`docs/rules/architecture-guardrails.md`、`docs/rules/validation.md` 收敛为短文档：每条规则都要以表格给出 `命令`、`阈值/级别`、`失败后果`、`恢复方式`。`docs/rules/validation.md` 必须新增 `validate-rules.py` exit-code 表，明确 `0 pass / report-only`、`1 P0`、`2 P1`、`3 P2`、`4 unavailable`，并注明 `git commit --no-verify` 不能绕过 CI backstop。</action>
  <verify>npm run docs:check</verify>
  <acceptance_criteria>
    - `docs/rules/README.md` 包含 `code-quality-redlines.md`
    - `docs/rules/README.md` 包含 `validation.md`
    - `docs/rules/validation.md` 包含 `1 P0`
    - `docs/rules/validation.md` 包含 `4 unavailable`
    - `docs/rules/code-quality-redlines.md` 包含 `命令`
    - `docs/rules/architecture-guardrails.md` 包含 `失败后果`
    - `npm run docs:check` 退出码为 0
  </acceptance_criteria>
  <done>规则文档变成执行代理与人类都可直接消费的短合同。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run docs:check`
- [ ] `grep -n "python3 scripts/validate-rules.py code --report-only" CLAUDE.md`
- [ ] `grep -n "\"enabled\": true" .claude/rule-system.config.json`
</verification>

<success_criteria>
- path-based rule routing 写进入口文档
- soft gate 默认开启且 hard gate 默认 report-only
- 规则文档具备可执行命令与 exit-code truth
- docs guardrail 保持通过
</success_criteria>
