---
phase: 27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa
plan: 04
type: execute
wave: 3
depends_on:
  - 27-01
  - 27-02
  - 27-03
files_modified:
  - .githooks/pre-commit
  - .githooks/commit-msg
  - .github/workflows/ci-gateway.yml
  - docs/rules/engineering-with-codex-openai.md
autonomous: false
requirements:
  - P27-NOW-HOOKS-CI-QA
  - P27-NOW-NO-VERIFY-BACKSTOP
user_setup: []
must_haves:
  truths:
    - pre-commit 与 commit-msg 不能再重复做 file-count enforcement
    - `git commit --no-verify` 可以跳过本地 hooks，但不能绕过 CI backstop
    - CI 里的 rule validation 必须只在 P0 或 unavailable 时 fail，P1/P2 只给 warn-only / notice
    - hook / CI 行为要与 `validate-rules.py` exit code 完全一致
  artifacts:
    - .githooks/pre-commit
    - .githooks/commit-msg
    - .github/workflows/ci-gateway.yml
    - docs/rules/engineering-with-codex-openai.md
  key_links:
    - QA checklist 明确要求 pre-commit / commit-msg / CI 的职责拆分
    - 研究结论要求 `--no-verify` 只能绕过本地，不能绕过 CI
---

<objective>
把 `validate-rules.py` 接到真实执行面：更新 pre-commit、commit-msg、CI gateway，并写明 `--no-verify` 只会跳过本地 hooks、不会跳过 CI backstop。

Purpose: Phase 27 的价值不在“有一个脚本”，而在脚本真的进入 commit 与 CI 链路，且不制造新的职责重叠。
Output: hook/CI 调用 validator 的明确 shell 分支、commit-msg 精简为格式检查、`--no-verify` backstop 文档化。
</objective>

<execution_context>
@/data/codemap/.planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-RESEARCH.md
@/data/codemap/.planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-VALIDATION.md
</execution_context>

<threat_model>
- Threat: 本地 `git commit --no-verify` 绕过所有新规则，CI 也没有再跑 validator
  - Severity: high
  - Mitigation: `.github/workflows/ci-gateway.yml` 新增 `python3 scripts/validate-rules.py code` backstop，并对 `1/4` 明确 fail
- Threat: commit-msg 与 pre-commit 继续重复 file-count 检查，用户收到双重、甚至相互矛盾的错误
  - Severity: medium
  - Mitigation: file-count 只保留在 `.githooks/pre-commit`，`.githooks/commit-msg` 只保留消息格式
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 接入 pre-commit 并清理 commit-msg 职责漂移</name>
  <files>.githooks/pre-commit, .githooks/commit-msg</files>
  <read_first>.githooks/pre-commit, .githooks/commit-msg, .claude/rule-system.config.json, scripts/validate-rules.py, docs/rules/validation.md</read_first>
  <action>编辑 `.githooks/pre-commit`，新增一个 `validate-rules.py` 分支：读取 `.claude/rule-system.config.json` 的 `hard_gate.mode`。当 mode 为 `report-only` 时执行 `python3 scripts/validate-rules.py code --report-only` 并继续；当 mode 为 `enforce` 时执行 `python3 scripts/validate-rules.py code`，然后用 `case` 处理退出码：`0` 通过、`1` 阻断、`2/3` 警告不阻断、`4` 作为依赖缺失阻断。编辑 `.githooks/commit-msg`，删除 file-count 相关常量、`MAX_FILES_PER_COMMIT`、`MAX_FILES_WITH_DOCS`、`COMMIT_FILE_COUNT` 分支，只保留 commit message tag/scope/message 格式检查。</action>
  <verify>grep -n "validate-rules.py code" .githooks/pre-commit && ! grep -n "MAX_FILES_PER_COMMIT" .githooks/commit-msg</verify>
  <acceptance_criteria>
    - `.githooks/pre-commit` 包含 `validate-rules.py code`
    - `.githooks/pre-commit` 包含 `case $VALIDATE_EXIT in` 或等效 `case "$VALIDATE_EXIT" in`
    - `.githooks/commit-msg` 不再包含 `MAX_FILES_PER_COMMIT`
    - `.githooks/commit-msg` 不再包含 `COMMIT_FILE_COUNT`
    - `.githooks/commit-msg` 仍包含 `Commit message must start with an uppercase tag.` 或等效格式错误提示
  </acceptance_criteria>
  <done>本地 hooks 的职责拆分清晰，validator 已进入 pre-commit。</done>
</task>

<task type="auto">
  <name>Task 2: 在 CI gateway 增加 no-verify backstop 并同步工程文档</name>
  <files>.github/workflows/ci-gateway.yml, docs/rules/engineering-with-codex-openai.md</files>
  <read_first>.github/workflows/ci-gateway.yml, docs/rules/engineering-with-codex-openai.md, scripts/validate-rules.py, .planning/milestones/v1.7-phases/27-implement-repo-local-rule-control-system-and-add-hooks-ci-qa/27-RESEARCH.md</read_first>
  <action>在 `.github/workflows/ci-gateway.yml` 中新增独立 step，命名固定为 `Rule validation backstop`。step 运行 `python3 scripts/validate-rules.py code`，捕获退出码并只在 `1` 或 `4` 时 `exit 1`；若是 `2` 或 `3`，打印 `warn-only` / `notice-only` 说明后 `exit 0`。同步更新 `docs/rules/engineering-with-codex-openai.md`：明确写出“禁止依赖 `git commit --no-verify` 作为解决方案；即使本地绕过，CI 仍会运行 `Rule validation backstop`”。</action>
  <verify>grep -n "Rule validation backstop" .github/workflows/ci-gateway.yml && grep -n "no-verify" docs/rules/engineering-with-codex-openai.md</verify>
  <acceptance_criteria>
    - `.github/workflows/ci-gateway.yml` 包含 `Rule validation backstop`
    - `.github/workflows/ci-gateway.yml` 包含 `python3 scripts/validate-rules.py code`
    - `.github/workflows/ci-gateway.yml` 包含对退出码 `1` 或 `4` 的失败处理
    - `docs/rules/engineering-with-codex-openai.md` 包含 `CI` 与 `no-verify`
    - `docs/rules/engineering-with-codex-openai.md` 包含 `Rule validation backstop`
  </acceptance_criteria>
  <done>CI 成为 `--no-verify` 绕过场景的最终 backstop。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -n "validate-rules.py code" .githooks/pre-commit`
- [ ] `! grep -n "MAX_FILES_PER_COMMIT" .githooks/commit-msg`
- [ ] `grep -n "Rule validation backstop" .github/workflows/ci-gateway.yml`
- [ ] `npm run docs:check`
</verification>

<success_criteria>
- pre-commit 调用 validator 且按 exit code 决策
- commit-msg 只保留消息格式职责
- CI 对 `--no-verify` 有明确 backstop
- warn-only 与 blocking 行为不再混淆
</success_criteria>
