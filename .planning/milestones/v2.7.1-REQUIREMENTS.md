# Requirements Archive: CodeMap v2.7.1 agent-hook-protocol-hardening

**Shipped:** 2026-05-12  
**Core Value:** 为人类与 AI / Agent 提供可信的代码上下文、设计交接边界与后续演化决策依据。

## Final Outcome

`v2.7.1 agent-hook-protocol-hardening` 已完成 closeout。它把原先 between-milestones 的 special `Phase 84` 与本轮 `Phase 85` 统一收口为一个正式 milestone，主题是让 installable `git commit` hooks 更像机器控制协议，而不是半结构化的人类阻断通知。

## Requirements

- [x] **HOOK-AI-01**: `pre-commit` 必须先处理 O(1) blocker，并把阻断结果输出为结构化 `codemap.precommit.v1` 协议  
  Outcome: validated by Phase 84
- [x] **HOOK-AI-02**: `commit-msg` 必须与 `pre-commit` 保持协议 parity，为 commit message blocker 直接提供 `rewrite_commit_message` 路由  
  Outcome: validated by Phase 84
- [x] **HOOK-AI-03**: installable template 真相源必须保持 framework-agnostic remediation 与 managed hook parity，不能只修当前仓库本地副本  
  Outcome: validated by Phase 84
- [x] **HOOK-AI-04**: autonomous agent 场景需要更低噪音协议，包括 `CODEMAP_PROTOCOL_ONLY=1`、显式 `CODEMAP_PRECHECK_LOG_PATH`、`not_applicable` 状态以及非误导性的 report-only limit-reached 文案  
  Outcome: validated by Phase 85

## Traceability

| Requirement | Phase | Final Status |
|-------------|-------|--------------|
| HOOK-AI-01 | Phase 84 | Validated |
| HOOK-AI-02 | Phase 84 | Validated |
| HOOK-AI-03 | Phase 84 | Validated |
| HOOK-AI-04 | Phase 85 | Validated |

## Notes

- `v2.7.1` 是对 hook protocol hardening 的正式范围化补记；它不代表产品发布动作，也不触发 `/release` 流程。
- 当前 milestone closeout 后，planning 会重新回到 `between-milestones` 状态。
