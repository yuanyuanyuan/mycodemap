---
phase: 22-real-arcadedb-server-live-smoke-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/22-real-arcadedb-server-live-smoke-gate/22-LIVE-SMOKE-RUNBOOK.md
  - .planning/phases/22-real-arcadedb-server-live-smoke-gate/22-GATE-CHECKLIST.md
autonomous: false
requirements:
  - PROTO-01
user_setup:
  - 需要一个可访问的 ArcadeDB server、database 名称和 credentials；若缺失这些前置条件，本 phase 允许输出 `blocked`，不得伪造 smoke success
must_haves:
  truths:
    - runbook 必须显式记录 `ARCADEDB_HTTP_URL`、`ARCADEDB_DATABASE`、`ARCADEDB_USERNAME`、`ARCADEDB_PASSWORD`、`Authorization: Basic` 与 TLS 前置条件
    - gate rubric 必须把结果限定为 `pass`、`blocked`、`fail`，且 `public-surface change required` 不能被归类为 `pass`
    - Wave 1 只允许写 planning/evidence artifacts，不得改 `src/interface/types/storage.ts`、`src/infrastructure/storage/StorageFactory.ts` 或 public config/schema
  artifacts:
    - .planning/phases/22-real-arcadedb-server-live-smoke-gate/22-LIVE-SMOKE-RUNBOOK.md
    - .planning/phases/22-real-arcadedb-server-live-smoke-gate/22-GATE-CHECKLIST.md
  key_links:
    - `22-LIVE-SMOKE-RUNBOOK.md` 固化真实 live smoke 的 env/auth/TLS/preflight contract
    - `22-GATE-CHECKLIST.md` 固化 `pass / blocked / fail` gate 语义与 stop conditions
---

<objective>
把 `Phase 22` 的 live smoke 从“脚本存在”升级为“可执行、可审计、可失败”的 gate：先固化 runbook 和 gate rubric，再允许后续真的去跑真实 server smoke。

Purpose: 如果 Wave 1 不先写清楚 env/auth/TLS 前置条件与 gate 规则，Wave 2 很容易把 blocked/fail 写成模糊叙事，导致 `PROTO-01` 失真。
Output: `22-LIVE-SMOKE-RUNBOOK.md`、`22-GATE-CHECKLIST.md`。
</objective>

<execution_context>
@/data/codemap/.codex/get-shit-done/workflows/execute-plan.md
@/data/codemap/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-real-arcadedb-server-live-smoke-gate/22-RESEARCH.md
@.planning/phases/22-real-arcadedb-server-live-smoke-gate/22-VALIDATION.md
@.planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-VALIDATION-DESIGN.md
@.planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-NEXT-STEPS.md
@scripts/experiments/arcadedb-http-smoke.mjs
@src/interface/types/storage.ts
@src/infrastructure/storage/StorageFactory.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 固化真实 live smoke runbook</name>
  <files>.planning/phases/22-real-arcadedb-server-live-smoke-gate/22-LIVE-SMOKE-RUNBOOK.md</files>
  <read_first>.planning/phases/22-real-arcadedb-server-live-smoke-gate/22-RESEARCH.md, .planning/phases/21-evaluate-arcadedb-node-integration-feasibility/21-VALIDATION-DESIGN.md, scripts/experiments/arcadedb-http-smoke.mjs, src/interface/types/storage.ts</read_first>
  <action>创建 `22-LIVE-SMOKE-RUNBOOK.md`，必须包含以下固定结构和精确内容：1) 标题 `# Phase 22 Live Smoke Runbook`；2) `## Preconditions`，列出 `ARCADEDB_HTTP_URL`、`ARCADEDB_DATABASE`、`ARCADEDB_USERNAME`、`ARCADEDB_PASSWORD`，并明确 `A reachable ArcadeDB server is required`；3) `## Command Contract`，至少写出 `node --check scripts/experiments/arcadedb-http-smoke.mjs`、`node scripts/experiments/arcadedb-http-smoke.mjs --help`、以及完整的 live smoke 执行命令；4) `## Auth And TLS Notes`，必须写出 `Authorization: Basic`、`TLS preconditions must be recorded explicitly`、`No insecure bypass is allowed as a hidden default`；5) `## Isolation Guardrails`，必须写出 `No shipped runtime/config changes` 和 `Do not modify StorageType / StorageFactory / public schema to make smoke pass`；6) `## Sanitization Rules`，要求 evidence 中不得写入明文密码或完整敏感 endpoint。</action>
  <verify>rg -n "ARCADEDB_HTTP_URL|ARCADEDB_DATABASE|ARCADEDB_USERNAME|ARCADEDB_PASSWORD|Authorization: Basic|No shipped runtime/config changes|Do not modify StorageType / StorageFactory / public schema" .planning/phases/22-real-arcadedb-server-live-smoke-gate/22-LIVE-SMOKE-RUNBOOK.md</verify>
  <acceptance_criteria>
    - `22-LIVE-SMOKE-RUNBOOK.md` contains `# Phase 22 Live Smoke Runbook`
    - `22-LIVE-SMOKE-RUNBOOK.md` contains `ARCADEDB_HTTP_URL`
    - `22-LIVE-SMOKE-RUNBOOK.md` contains `Authorization: Basic`
    - `22-LIVE-SMOKE-RUNBOOK.md` contains `No shipped runtime/config changes`
    - `22-LIVE-SMOKE-RUNBOOK.md` contains `Do not modify StorageType / StorageFactory / public schema`
  </acceptance_criteria>
  <done>真实 live smoke 的前置条件、执行命令与隔离护栏已被写死，不再依赖口头解释。</done>
</task>

<task type="auto">
  <name>Task 2: 定义 pass / blocked / fail 的 gate rubric</name>
  <files>.planning/phases/22-real-arcadedb-server-live-smoke-gate/22-GATE-CHECKLIST.md</files>
  <read_first>.planning/phases/22-real-arcadedb-server-live-smoke-gate/22-RESEARCH.md, .planning/phases/22-real-arcadedb-server-live-smoke-gate/22-LIVE-SMOKE-RUNBOOK.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</read_first>
  <action>创建 `22-GATE-CHECKLIST.md`，必须包含以下固定结构：1) `# Phase 22 Gate Checklist`；2) `## Pass Criteria`，明确只有在真实 server live smoke 成功、auth mode 与 TLS 前提被记录、且没有 public-surface change required 时才能写 `Gate outcome: pass`；3) `## Blocked Criteria`，至少包含 `No reachable server`、`Missing credentials`、`TLS preconditions unresolved` 三类，并要求输出 `Gate outcome: blocked`；4) `## Fail Criteria`，至少包含 `public-surface change required`、`endpoint/auth contract mismatch`、`script cannot complete the documented command` 三类，并要求输出 `Gate outcome: fail`；5) `## Required Evidence Fields`，至少列出 `Live smoke command`、`Observed outcome`、`Sanitized endpoint`、`Auth mode`、`TLS mode`、`Failure reason`；6) `## Stop Rule`，必须直接写出 `Phase 23 remains locked unless Gate outcome: pass`。</action>
  <verify>rg -n "Gate outcome: pass|Gate outcome: blocked|Gate outcome: fail|No reachable server|Missing credentials|TLS preconditions unresolved|public-surface change required|Phase 23 remains locked unless Gate outcome: pass" .planning/phases/22-real-arcadedb-server-live-smoke-gate/22-GATE-CHECKLIST.md</verify>
  <acceptance_criteria>
    - `22-GATE-CHECKLIST.md` contains `Gate outcome: pass`
    - `22-GATE-CHECKLIST.md` contains `Gate outcome: blocked`
    - `22-GATE-CHECKLIST.md` contains `Gate outcome: fail`
    - `22-GATE-CHECKLIST.md` contains `public-surface change required`
    - `22-GATE-CHECKLIST.md` contains `Phase 23 remains locked unless Gate outcome: pass`
  </acceptance_criteria>
  <done>Phase 22 的 gate 语义已经固定，后续执行不能再把 blocked/fail 包装成乐观进展。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "ARCADEDB_HTTP_URL|Authorization: Basic|No shipped runtime/config changes" .planning/phases/22-real-arcadedb-server-live-smoke-gate/22-LIVE-SMOKE-RUNBOOK.md`
- [ ] `rg -n "Gate outcome: pass|Gate outcome: blocked|Gate outcome: fail|Phase 23 remains locked unless Gate outcome: pass" .planning/phases/22-real-arcadedb-server-live-smoke-gate/22-GATE-CHECKLIST.md`
- [ ] `node --check scripts/experiments/arcadedb-http-smoke.mjs`
- [ ] `node scripts/experiments/arcadedb-http-smoke.mjs --help`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `PROTO-01` 的前置条件、gate 语义与 stop rule 可被 reviewer 在 30 秒内读懂
- Wave 1 没有为了 smoke 去修改 shipped runtime / public config surface
  </success_criteria>

<output>
After completion, create `.planning/phases/22-real-arcadedb-server-live-smoke-gate/22-01-SUMMARY.md`
</output>
