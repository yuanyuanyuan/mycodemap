---
phase: 09-plugin-docs-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - scripts/validate-docs.js
  - src/cli/__tests__/validate-docs-script.test.ts
  - src/cli/commands/__tests__/generate.test.ts
autonomous: true
requirements:
  - DOC-04
  - VAL-01
must_haves:
  truths:
    - docs guardrail 把插件配置文件名、Plugin Summary、pluginReport 契约和 diagnostics 写成脚本级断言
    - docs drift 会通过失败用例直接暴露，而不是依赖人工发现
    - built-in plugin 与 user plugin 都有真实 CLI 证据，证明插件产品面在 runtime 下成立
  artifacts:
    - path: scripts/validate-docs.js
      provides: 插件文档 guardrail
      contains: "`pluginReport.diagnostics[]`"
    - path: src/cli/__tests__/validate-docs-script.test.ts
      provides: 文档漂移失败用例
      contains: "fails when OUTPUT guide drops the pluginReport contract"
    - path: src/cli/commands/__tests__/generate.test.ts
      provides: built-in / user plugin CLI 证据
      contains: "good-plugin"
  key_links:
    - from: scripts/validate-docs.js
      to: src/cli/__tests__/validate-docs-script.test.ts
      via: guardrail failure fixtures
    - from: src/cli/commands/__tests__/generate.test.ts
      to: pluginReport
      via: built-in and user plugin runtime evidence
    - from: src/cli/commands/__tests__/generate.test.ts
      to: scripts/validate-docs.js
      via: docs facts must match runtime evidence
---

<objective>
把插件文档事实锁进 guardrail，并用真实 CLI built-in/user plugin 证据证明插件产品面不是“只在文档里成立”。

Purpose: 让插件产品面同时被脚本级文档护栏和端到端 CLI 证据固定，避免以后再次出现“文档说可以、真实 CLI 不行”的漂移。
Output: 更新后的 `scripts/validate-docs.js`、`src/cli/__tests__/validate-docs-script.test.ts`、`src/cli/commands/__tests__/generate.test.ts`。
</objective>

<execution_context>
@/data/codemap/.claude/get-shit-done/workflows/execute-plan.md
@/data/codemap/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.1-phases/09-plugin-docs-validation/09-CONTEXT.md
@.planning/milestones/v1.1-phases/09-plugin-docs-validation/09-RESEARCH.md
@.planning/milestones/v1.1-phases/09-plugin-docs-validation/09-VALIDATION.md
@.planning/milestones/v1.1-phases/09-plugin-docs-validation/09-01-SUMMARY.md

## Required runtime evidence
- built-in plugin 场景必须证明 `complexity-analyzer` 被记录进 `pluginReport`
- user plugin 场景必须证明 `good-plugin` 被加载，`broken-plugin` 被拒绝，且生成 `good.txt`

## Guardrail boundary
- 只收口 plugin docs drift 与 CLI evidence
- 不扩展插件 marketplace / 远程安装 / 新命令
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend documentation guardrail for plugin facts</name>
  <files>scripts/validate-docs.js</files>
  <read_first>
    - scripts/validate-docs.js
    - README.md
    - AI_GUIDE.md
    - docs/ai-guide/COMMANDS.md
    - docs/ai-guide/OUTPUT.md
  </read_first>
  <action>
扩展 `scripts/validate-docs.js` 的插件相关断言，必须显式校验以下事实：

1. 文档使用 `mycodemap.config.json` 作为配置文件名
2. 文档包含 `AI_MAP.md` 的 `Plugin Summary`
3. 文档包含 `codemap.json.pluginReport`
4. 文档包含 `` `pluginReport.diagnostics[]` 用统一结构暴露 `load / initialize / analyze / generate` 四个阶段 ``
5. 文档说明 `generate` 不提供独立 `--plugin` flags

断言必须是脚本级失败条件，而不是注释说明；缺失任一事实时，guardrail 应返回失败。
  </action>
  <acceptance_criteria>
    - `scripts/validate-docs.js` contains `` `AI_MAP.md` 的 `Plugin Summary` ``
    - `scripts/validate-docs.js` contains `` `codemap.json.pluginReport` ``
    - `scripts/validate-docs.js` contains `` `pluginReport.diagnostics[]` 用统一结构暴露 `load / initialize / analyze / generate` 四个阶段 ``
    - `scripts/validate-docs.js` contains `没有独立`
  </acceptance_criteria>
  <verify>
    <automated>grep -n "Plugin Summary" scripts/validate-docs.js && grep -n "pluginReport.diagnostics\\[\\]" scripts/validate-docs.js</automated>
  </verify>
  <done>插件产品面的关键文档事实已进入脚本级 guardrail</done>
</task>

<task type="auto">
  <name>Task 2: Add docs drift failure fixtures</name>
  <files>src/cli/__tests__/validate-docs-script.test.ts</files>
  <read_first>
    - src/cli/__tests__/validate-docs-script.test.ts
    - scripts/validate-docs.js
    - .planning/milestones/v1.1-phases/09-plugin-docs-validation/09-02-SUMMARY.md
  </read_first>
  <action>
扩展 `src/cli/__tests__/validate-docs-script.test.ts`，至少覆盖以下两类插件文档漂移失败：

1. `README` 重新写回 `codemap.config.json`
2. `OUTPUT` guide 丢失 `pluginReport?: PluginExecutionReport;`

测试名称必须保留可读、可 grep 的失败意图，例如：
- `fails when README reintroduces the legacy config filename`
- `fails when OUTPUT guide drops the pluginReport contract`

测试断言必须验证 guardrail 返回失败，而不是只 snapshot 文本。
  </action>
  <acceptance_criteria>
    - `src/cli/__tests__/validate-docs-script.test.ts` contains `fails when README reintroduces the legacy config filename`
    - `src/cli/__tests__/validate-docs-script.test.ts` contains `fails when OUTPUT guide drops the pluginReport contract`
    - `pnpm exec vitest run src/cli/__tests__/validate-docs-script.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/cli/__tests__/validate-docs-script.test.ts</automated>
  </verify>
  <done>插件文档漂移已被失败用例直接锁住</done>
</task>

<task type="auto">
  <name>Task 3: Preserve real CLI evidence for built-in and user plugins</name>
  <files>src/cli/commands/__tests__/generate.test.ts</files>
  <read_first>
    - src/cli/commands/__tests__/generate.test.ts
    - scripts/validate-docs.js
    - README.md
  </read_first>
  <action>
更新或保留 `src/cli/commands/__tests__/generate.test.ts` 中的真实 CLI 插件证据，测试必须明确覆盖：

1. built-in plugin 场景：
   - `pluginReport.loadedPlugins` 包含 `complexity-analyzer`
   - `pluginReport.metrics['complexity-analyzer']` 已定义
2. user plugin 场景：
   - `pluginReport.loadedPlugins` 包含 `good-plugin`
   - `pluginReport.loadedPlugins` 不包含 `broken-plugin`
   - `pluginReport.generatedFiles` 包含 `plugins/good.txt`
   - `pluginReport.diagnostics` 记录失败插件信息

这些断言必须来自真实 `generate` fixture 运行，而不是孤立 mock 对象。
  </action>
  <acceptance_criteria>
    - `src/cli/commands/__tests__/generate.test.ts` contains `complexity-analyzer`
    - `src/cli/commands/__tests__/generate.test.ts` contains `good-plugin`
    - `src/cli/commands/__tests__/generate.test.ts` contains `broken-plugin`
    - `src/cli/commands/__tests__/generate.test.ts` contains `good.txt`
    - `pnpm exec vitest run src/cli/commands/__tests__/generate.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/cli/commands/__tests__/generate.test.ts</automated>
  </verify>
  <done>built-in 和 user plugin 的真实 CLI 证据已留在测试中</done>
</task>

</tasks>

## Verification

Run these commands to verify Wave 2 completion:

```bash
pnpm exec vitest run src/cli/__tests__/validate-docs-script.test.ts src/cli/__tests__/validate-ai-docs-script.test.ts src/cli/commands/__tests__/generate.test.ts
npm run docs:check
npm run typecheck
npm run build
```

## Notes

- 本计划重写的是 historical planning artifact，不改变 Phase 09 已 `passed` 的验证结论
- 真实 CLI 证据必须同时覆盖 built-in plugin 与 user plugin
- docs guardrail 必须锁定文件名、Plugin Summary、pluginReport 与 diagnostics 四类事实

---

After completion, create `.planning/milestones/v1.1-phases/09-plugin-docs-validation/09-02-SUMMARY.md`
