# 09-02 Plan

## Goal

把插件文档事实锁进 guardrail，并用真实 CLI built-in/user plugin 证据证明插件产品面不是“只在文档里成立”。

## Tasks

1. 扩展 `scripts/validate-docs.js`，加入配置文件名、插件配置表、`pluginReport` 契约和 Plugin Summary 断言。
2. 扩展 `src/cli/__tests__/validate-docs-script.test.ts`，为插件相关漂移补失败用例。
3. 执行 built-in plugin 与 user plugin 的真实 CLI fixture，记录验证证据。
4. 运行最终 targeted tests、docs:check、typecheck、build，为 v1.1 phase 收口。

## DoD

- `DOC-04`、`VAL-01` 满足
- 插件文档与实现之间存在脚本级护栏
- 至少两条真实 CLI 证据可证明 built-in/user plugin 可用
