# 09-01 Plan

## Goal

把插件配置、运行结果与诊断约定同步到 README 和 AI 文档入口，防止用户继续沿用旧配置名或未知输出字段。

## Tasks

1. 更新 `README.md` 的配置章节、插件表格与使用示例。
2. 更新 `AI_GUIDE.md`，补插件诊断/扩展结果速查与 `PluginExecutionReport`。
3. 更新 `docs/ai-guide/COMMANDS.md`、`OUTPUT.md`、`QUICKSTART.md`、`INTEGRATION.md`，明确 `plugins` 段、`Plugin Summary` 与 `pluginReport`。
4. 保持文档叙事一致：`generate` 没有独立 `--plugin` flags，插件只能通过配置文件声明。

## DoD

- `DOC-03` 满足
- README 与 AI docs 都能指导用户配置并解读插件结果
