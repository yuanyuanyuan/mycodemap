---
phase: 09-plugin-docs-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - AI_GUIDE.md
  - docs/ai-guide/COMMANDS.md
  - docs/ai-guide/OUTPUT.md
  - docs/ai-guide/QUICKSTART.md
  - docs/ai-guide/INTEGRATION.md
autonomous: true
requirements:
  - DOC-03
must_haves:
  truths:
    - README 与 AI 文档统一使用 `mycodemap.config.json` 作为插件配置入口
    - 文档明确说明 `generate` 没有独立 `--plugin` flags，插件只能通过 `plugins` 段声明
    - `AI_MAP.md` 的 `Plugin Summary` 与 `codemap.json.pluginReport` 都进入用户和 AI 文档叙事
  artifacts:
    - path: README.md
      provides: 用户主入口的插件配置与输出说明
      contains: "`pluginReport` - 插件运行摘要"
    - path: AI_GUIDE.md
      provides: AI 速查入口中的插件结果说明
      contains: "Plugin Summary"
    - path: docs/ai-guide/OUTPUT.md
      provides: `pluginReport` 输出契约
      contains: "pluginReport?: PluginExecutionReport;"
  key_links:
    - from: README.md
      to: docs/ai-guide/COMMANDS.md
      via: generate 插件配置入口
    - from: AI_GUIDE.md
      to: docs/ai-guide/OUTPUT.md
      via: Plugin Summary / pluginReport contract
    - from: docs/ai-guide/QUICKSTART.md
      to: docs/ai-guide/INTEGRATION.md
      via: plugin diagnostics triage path
---

<objective>
把插件配置、运行结果与诊断约定同步到 README 和 AI 文档入口，防止用户继续沿用旧配置名或未知输出字段。

Purpose: 让人类入口与 AI 入口都基于同一套插件产品事实，不再把插件 runtime 误读成“有独立 CLI flags”的隐藏能力。
Output: 更新后的 `README.md`、`AI_GUIDE.md`、`docs/ai-guide/COMMANDS.md`、`OUTPUT.md`、`QUICKSTART.md`、`INTEGRATION.md`。
</objective>

<execution_context>
@/data/codemap/.claude/get-shit-done/workflows/execute-plan.md
@/data/codemap/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.1-phases/09-plugin-docs-validation/09-CONTEXT.md
@.planning/milestones/v1.1-phases/09-plugin-docs-validation/09-RESEARCH.md
@.planning/milestones/v1.1-phases/09-plugin-docs-validation/09-VALIDATION.md

## Phase boundary
- 只收口插件产品面的文档、AI guide 与叙事一致性
- 不新增插件 UX 命令
- 不引入 marketplace / 远程安装 / 新 runtime 契约

## Canonical evidence targets
- `README.md:506` — plugin diagnostics / `pluginReport`
- `README.md:541` — plugin report 字段说明
- `AI_GUIDE.md:60` — 读取 `Plugin Summary` / `pluginReport`
- `docs/ai-guide/COMMANDS.md:36` — `generate` + plugins 配置说明
- `docs/ai-guide/OUTPUT.md:56` — `pluginReport` 只在显式配置 `plugins` 时出现
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README plugin configuration narrative</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - .planning/milestones/v1.1-phases/09-plugin-docs-validation/09-CONTEXT.md
    - .planning/milestones/v1.1-phases/09-plugin-docs-validation/09-01-SUMMARY.md
  </read_first>
  <action>
更新 `README.md` 的插件相关章节，必须把以下事实写清楚并使用这些精确字面：

1. 配置入口统一为 `mycodemap.config.json`
2. `generate` 不提供独立 `--plugin` flags
3. 插件通过配置文件中的 `plugins` 段声明
4. 运行结果会写入 `AI_MAP.md` 的 `Plugin Summary`
5. 机器可读结果会写入 `codemap.json` 的 `pluginReport`
6. `pluginReport` 至少说明 `loadedPlugins`、`generatedFiles`、`metrics`、`diagnostics`

README 的示例和表格必须与上述事实一致，不允许再出现 `codemap.config.json` 或“单独 plugin flag”表述。
  </action>
  <acceptance_criteria>
    - `README.md` contains `mycodemap.config.json`
    - `README.md` contains `generate` and `没有独立 \`--plugin\` flags`
    - `README.md` contains `Plugin Summary`
    - `README.md` contains `` `pluginReport` - 插件运行摘要 ``
    - `README.md` does not contain `codemap.config.json` in plugin configuration guidance
  </acceptance_criteria>
  <verify>
    <automated>grep -n "Plugin Summary" README.md && grep -n "\`pluginReport\` - 插件运行摘要" README.md && ! rg -n "codemap.config.json" README.md</automated>
  </verify>
  <done>README 已统一到插件配置、Plugin Summary 与 pluginReport 契约</done>
</task>

<task type="auto">
  <name>Task 2: Update AI guide entry points</name>
  <files>AI_GUIDE.md</files>
  <read_first>
    - AI_GUIDE.md
    - README.md
    - .planning/milestones/v1.1-phases/09-plugin-docs-validation/09-RESEARCH.md
  </read_first>
  <action>
更新 `AI_GUIDE.md`，让 AI 入口直接可见插件运行结果与诊断消费方式。文档必须包含以下精确事实：

1. 当用户“需要插件诊断/扩展结果”时，应读 `AI_MAP.md` 的 `Plugin Summary` 或解析 `codemap.json.pluginReport`
2. `pluginReport.diagnostics[]` 暴露 `load / initialize / analyze / generate` 四个阶段
3. 只有显式声明 `plugins` 段时，插件 runtime 才会参与 `generate`

禁止只写模糊描述，例如“查看插件日志”；必须指向 `Plugin Summary` 和 `pluginReport`。
  </action>
  <acceptance_criteria>
    - `AI_GUIDE.md` contains `需要插件诊断/扩展结果`
    - `AI_GUIDE.md` contains `Plugin Summary`
    - `AI_GUIDE.md` contains `codemap.json.pluginReport`
    - `AI_GUIDE.md` contains `load / initialize / analyze / generate`
  </acceptance_criteria>
  <verify>
    <automated>grep -n "需要插件诊断/扩展结果" AI_GUIDE.md && grep -n "codemap.json.pluginReport" AI_GUIDE.md</automated>
  </verify>
  <done>AI 速查入口已明确插件诊断与输出契约</done>
</task>

<task type="auto">
  <name>Task 3: Sync ai-guide reference docs</name>
  <files>docs/ai-guide/COMMANDS.md, docs/ai-guide/OUTPUT.md, docs/ai-guide/QUICKSTART.md, docs/ai-guide/INTEGRATION.md</files>
  <read_first>
    - docs/ai-guide/COMMANDS.md
    - docs/ai-guide/OUTPUT.md
    - docs/ai-guide/QUICKSTART.md
    - docs/ai-guide/INTEGRATION.md
    - AI_GUIDE.md
  </read_first>
  <action>
同步更新 `docs/ai-guide/COMMANDS.md`、`OUTPUT.md`、`QUICKSTART.md`、`INTEGRATION.md`，每个文件都必须覆盖明确、可 grep 的插件事实：

1. `COMMANDS.md` 必须写出 `AI_MAP.md` 会增加 `Plugin Summary`，`codemap.json` 会增加 `pluginReport`
2. `OUTPUT.md` 必须声明 `pluginReport?: PluginExecutionReport;`
3. `QUICKSTART.md` 必须告诉用户：若显式配置了 `plugins`，要看 `Plugin Summary`
4. `INTEGRATION.md` 必须提供 `pluginReport.diagnostics` 的排查路径

四个文件之间的措辞必须一致：插件配置入口来自 `mycodemap.config.json`，而不是额外 CLI flags。
  </action>
  <acceptance_criteria>
    - `docs/ai-guide/COMMANDS.md` contains `Plugin Summary`
    - `docs/ai-guide/OUTPUT.md` contains `pluginReport?: PluginExecutionReport;`
    - `docs/ai-guide/QUICKSTART.md` contains `若显式配置了 plugins，也要看 Plugin Summary`
    - `docs/ai-guide/INTEGRATION.md` contains `pluginReport.diagnostics`
  </acceptance_criteria>
  <verify>
    <automated>grep -n "Plugin Summary" docs/ai-guide/COMMANDS.md && grep -n "pluginReport?: PluginExecutionReport;" docs/ai-guide/OUTPUT.md && grep -n "若显式配置了 plugins，也要看 Plugin Summary" docs/ai-guide/QUICKSTART.md && grep -n "pluginReport.diagnostics" docs/ai-guide/INTEGRATION.md</automated>
  </verify>
  <done>细分 AI guide 已与 README / AI_GUIDE 对齐到同一插件契约</done>
</task>

</tasks>

## Verification

Run these commands to verify Wave 1 completion:

```bash
npm run docs:check
pnpm exec vitest run src/cli/__tests__/validate-docs-script.test.ts src/cli/__tests__/validate-ai-docs-script.test.ts
grep -n "Plugin Summary" README.md AI_GUIDE.md docs/ai-guide/COMMANDS.md
```

## Notes

- 本计划只重写 planning artifact，不改变 Phase 09 已完成的历史结论
- 文档叙事必须统一为 `mycodemap.config.json`
- 插件输出叙事必须统一围绕 `Plugin Summary` 与 `pluginReport`

---

After completion, create `.planning/milestones/v1.1-phases/09-plugin-docs-validation/09-01-SUMMARY.md`
