# 08-01 Plan

## Goal

把现有 `PluginSystem` 正式接入 `generate` 主流程，并让插件分析结果进入主输出契约，而不是继续停留在孤立骨架层。

## Tasks

1. 在 `src/cli/commands/generate.ts` 中基于 `hasExplicitPluginConfig` 启用插件 runtime。
2. 运行 `initialize / analyze / generate / dispose` 生命周期，并把 `additionalEdges` 合并回主依赖图。
3. 在 interface 层新增 `PluginDiagnostic` / `PluginExecutionReport`，让 `codemap.json` 有正式插件输出契约。
4. 更新 `src/generator/index.ts`，把 `pluginReport` 摘要写入 `AI_MAP.md`。
5. 补 built-in plugin 的主流程可见性回归测试。

## DoD

- `PLG-04`、`PLG-05` 满足
- `generate` 能在显式插件配置下真正运行插件
- built-in plugin 结果可在 `AI_MAP.md` 或 `codemap.json` 中观察到
