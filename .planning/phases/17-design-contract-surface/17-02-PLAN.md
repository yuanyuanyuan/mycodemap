---
phase: 17-design-contract-surface
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/cli/design-contract-loader.ts
  - src/cli/commands/design.ts
  - src/cli/index.ts
  - src/cli/__tests__/design-contract-loader.test.ts
  - src/cli/commands/__tests__/design-command.test.ts
  - src/cli/__tests__/index-help.test.ts
  - tests/fixtures/design-contracts/valid-basic.design.md
  - tests/fixtures/design-contracts/missing-acceptance.design.md
autonomous: false
requirements:
  - DES-02
user_setup: []
must_haves:
  truths:
    - `mycodemap design validate [file] --json` 能读取默认或显式路径的 design contract
    - 缺失 sections、重复 sections、空 sections、未知 sections 都会返回机器可读 diagnostics
    - 新命令进入顶层 help，但不扩写 `analyze` / `workflow`
  artifacts:
    - src/cli/design-contract-loader.ts
    - src/cli/commands/design.ts
    - src/cli/index.ts
    - src/cli/__tests__/design-contract-loader.test.ts
    - src/cli/commands/__tests__/design-command.test.ts
  key_links:
    - `src/cli/design-contract-loader.ts` 负责文件发现、解析、归一化与 diagnostics 组装
    - `src/cli/commands/design.ts` 暴露 `validate` 子命令与 JSON/human 输出
    - `src/cli/index.ts` 只做顶层 command 注册，不在其他 surface 偷渡语义
---

<objective>
实现最小可信的 design 输入验证链：loader / validator / diagnostics baseline + `design validate` CLI。

Purpose: 只有当 design contract 能被稳定读取、验证并返回结构化错误时，它才配成为后续 mapping / handoff 的事实源。
Output: design contract loader、`design validate` 命令、CLI 注册、focused regression tests 与最小 fixtures。
</objective>

<execution_context>
@/data/codemap/.codex/get-shit-done/workflows/execute-plan.md
@/data/codemap/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-design-contract-surface/17-CONTEXT.md
@.planning/phases/17-design-contract-surface/17-RESEARCH.md
@.planning/phases/17-design-contract-surface/17-01-PLAN.md
@src/cli/index.ts
@src/cli/config-loader.ts
@src/cli/__tests__/index-help.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 design contract loader / validator / diagnostics</name>
  <files>src/cli/design-contract-loader.ts, src/cli/__tests__/design-contract-loader.test.ts, tests/fixtures/design-contracts/valid-basic.design.md, tests/fixtures/design-contracts/missing-acceptance.design.md</files>
  <read_first>src/cli/config-loader.ts, .planning/phases/17-design-contract-surface/17-01-PLAN.md, .planning/phases/17-design-contract-surface/17-RESEARCH.md</read_first>
  <action>新增 `src/cli/design-contract-loader.ts`，提供默认路径发现、Markdown section 解析、required/duplicate/unknown/empty diagnostics 与 normalized result。为成功和失败路径编写 focused tests 与 fixtures，确保 loader 是命令层可复用的单一入口。</action>
  <verify>pnpm exec vitest run src/cli/__tests__/design-contract-loader.test.ts</verify>
  <acceptance_criteria>
    - loader 在未传路径时会尝试 `mycodemap.design.md`
    - 成功路径返回 normalized contract 与空 blocker diagnostics
    - 失败路径能区分缺 section、重复 section、空 section、未知 section
  </acceptance_criteria>
  <done>design contract 已能被稳定读取并产出结构化 diagnostics。</done>
</task>

<task type="auto">
  <name>Task 2: 暴露 `design validate` 命令并支持机器输出</name>
  <files>src/cli/commands/design.ts, src/cli/commands/__tests__/design-command.test.ts</files>
  <read_first>src/cli/commands/workflow.ts, src/cli/config-loader.ts, src/cli/design-contract-loader.ts</read_first>
  <action>新增 `src/cli/commands/design.ts`，实现 `design validate [file]` 子命令，支持 `--json`（及必要时 `--structured`）输出稳定 diagnostics/result 结构。命令在 blocker diagnostics 存在时返回失败状态码，但不得在 JSON 模式下混入 prose。</action>
  <verify>pnpm exec vitest run src/cli/commands/__tests__/design-command.test.ts</verify>
  <acceptance_criteria>
    - `design validate` 支持显式路径与默认路径两种入口
    - `--json` 输出可被机器稳定消费
    - diagnostics 存在时命令退出码明确失败
  </acceptance_criteria>
  <done>`design validate` 成为正式的 design 输入校验入口。</done>
</task>

<task type="auto">
  <name>Task 3: 注册顶层命令并补 CLI help 回归</name>
  <files>src/cli/index.ts, src/cli/__tests__/index-help.test.ts</files>
  <read_first>src/cli/index.ts, src/cli/__tests__/index-help.test.ts</read_first>
  <action>在 `src/cli/index.ts` 注册 `design` 命令，并更新 help regression，确保顶层 `--help` 能发现该命令且不影响既有 public surface。不得借此修改 `analyze` / `workflow` 的 contract。</action>
  <verify>pnpm exec vitest run src/cli/__tests__/index-help.test.ts && node dist/cli/index.js design validate --help</verify>
  <acceptance_criteria>
    - 顶层 help 明确列出 `design`
    - 既有 removed-command / public surface 断言仍然通过
    - `design` 只作为独立命令出现，不影响 analyze/workflow 帮助文本
  </acceptance_criteria>
  <done>CLI public surface 已引入 design 入口且 blast radius 被控制。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run src/cli/__tests__/design-contract-loader.test.ts src/cli/commands/__tests__/design-command.test.ts src/cli/__tests__/index-help.test.ts`
- [ ] `node dist/cli/index.js design validate --help`
- [ ] `node dist/cli/index.js design validate tests/fixtures/design-contracts/missing-acceptance.design.md --json`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- design contract 验证链具备默认路径、结构化 diagnostics 与可测试的失败语义
- CLI 通过独立 `design` surface 暴露能力，而不是污染 analyze/workflow
- 至少一个成功 fixture 与一个失败 fixture 被自动化覆盖
  </success_criteria>

<output>
After completion, create `.planning/phases/17-design-contract-surface/17-02-SUMMARY.md`
</output>
