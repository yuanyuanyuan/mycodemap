---
phase: 17-design-contract-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/interface/types/design-contract.ts
  - src/interface/types/index.ts
  - src/cli/design-contract-schema.ts
  - docs/product-specs/DESIGN_CONTRACT_TEMPLATE.md
autonomous: false
requirements:
  - DES-01
user_setup: []
must_haves:
  truths:
    - design contract 有明确的正式类型、必填 sections 与默认路径约定
    - 人类 authoring 的 canonical template 与 CLI/schema 使用同一套字段语义
    - 本计划不引入 design-to-code mapping 或 handoff 输出，只收敛 design input contract
  artifacts:
    - src/interface/types/design-contract.ts
    - src/interface/types/index.ts
    - src/cli/design-contract-schema.ts
    - docs/product-specs/DESIGN_CONTRACT_TEMPLATE.md
  key_links:
    - `src/interface/types/design-contract.ts` 定义正式 `DesignContract` / diagnostics / section 标识
    - `src/cli/design-contract-schema.ts` 持有 CLI 侧 required sections、aliases、默认路径与 schema helper
    - `docs/product-specs/DESIGN_CONTRACT_TEMPLATE.md` 是人类编写 contract 的 canonical sample
---

<objective>
先把 design contract 的“事实源”收口：正式类型、必填 sections、默认文件路径和 canonical authoring template 一次对齐。

Purpose: 如果没有清晰 schema 与 template，后续 loader / validator 只能在漂移输入上工作，Phase 17 会退化成“继续猜需求”。
Output: `DesignContract` 类型、section schema 常量、默认路径约定、可复制的 Markdown template。
</objective>

<execution_context>
@/data/codemap/.codex/get-shit-done/workflows/execute-plan.md
@/data/codemap/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-design-contract-surface/17-CONTEXT.md
@.planning/phases/17-design-contract-surface/17-RESEARCH.md
@src/interface/types/index.ts
@src/cli/config-loader.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义 design contract 正式类型与 section 标识</name>
  <files>src/interface/types/design-contract.ts, src/interface/types/index.ts</files>
  <read_first>.planning/phases/17-design-contract-surface/17-CONTEXT.md, .planning/phases/17-design-contract-surface/17-RESEARCH.md, src/interface/types/index.ts</read_first>
  <action>新增 `src/interface/types/design-contract.ts`，定义 `DesignContract`、`DesignContractSectionId`、`DesignContractDiagnostic`、`NormalizedDesignContract` 等正式类型；在 `src/interface/types/index.ts` 中导出这些类型。类型只表达契约与 diagnostics 形状，不写具体文件读取逻辑。</action>
  <verify>rg -n "DesignContract|DesignContractSectionId|DesignContractDiagnostic|NormalizedDesignContract" src/interface/types</verify>
  <acceptance_criteria>
    - Interface Layer 中存在 design contract 的单一类型源
    - section id 覆盖 `goal`、`constraints`、`acceptanceCriteria`、`nonGoals`
    - diagnostics 类型支持 code / severity / section / message 这类最小机器消费信息
  </acceptance_criteria>
  <done>design contract 的正式语义已能被后续 loader/CLI 复用。</done>
</task>

<task type="auto">
  <name>Task 2: 固定默认路径、required sections 与 alias schema</name>
  <files>src/cli/design-contract-schema.ts</files>
  <read_first>src/cli/config-loader.ts, .planning/phases/17-design-contract-surface/17-RESEARCH.md</read_first>
  <action>新增 `src/cli/design-contract-schema.ts`，集中定义 `DEFAULT_DESIGN_CONTRACT_PATH`、必填 sections、可接受 heading aliases、未知/重复段判定策略与 schema helper。保持其为 CLI-owned schema helper，而不是在多个命令/测试里重复硬编码。</action>
  <verify>rg -n "DEFAULT_DESIGN_CONTRACT_PATH|REQUIRED_DESIGN_CONTRACT_SECTIONS|DESIGN_CONTRACT_SECTION_ALIASES" src/cli/design-contract-schema.ts</verify>
  <acceptance_criteria>
    - 默认输入路径固定为 `mycodemap.design.md`
    - 必填 section 集合只在一个文件中定义
    - alias 策略能覆盖 `Non-Goals` / `Exclusions` 这类常见 human wording
  </acceptance_criteria>
  <done>design contract 的路径与 section 规则已收口成单一事实源。</done>
</task>

<task type="auto">
  <name>Task 3: 发布 canonical Markdown template</name>
  <files>docs/product-specs/DESIGN_CONTRACT_TEMPLATE.md</files>
  <read_first>.planning/phases/17-design-contract-surface/17-CONTEXT.md, .planning/phases/17-design-contract-surface/17-RESEARCH.md</read_first>
  <action>编写 `docs/product-specs/DESIGN_CONTRACT_TEMPLATE.md`，提供可直接复制的 design contract 模板，明确必填 sections、填写说明、最小示例与 non-goals 提示。模板内容必须与 Task 1/2 的 schema 保持一致。</action>
  <verify>test -f docs/product-specs/DESIGN_CONTRACT_TEMPLATE.md && rg -n "^## (Goal|Constraints|Acceptance Criteria|Non-Goals)" docs/product-specs/DESIGN_CONTRACT_TEMPLATE.md</verify>
  <acceptance_criteria>
    - 模板覆盖全部必填 sections
    - 模板强调“显式非目标”和“可验证验收标准”
    - 文档成为后续 README / AI docs 引用的 canonical source，而不是再复制一份细节
  </acceptance_criteria>
  <done>人类 authoring 样板与机器 schema 对齐到同一事实源。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "DesignContract|DEFAULT_DESIGN_CONTRACT_PATH|REQUIRED_DESIGN_CONTRACT_SECTIONS" src/interface/types src/cli`
- [ ] `test -f docs/product-specs/DESIGN_CONTRACT_TEMPLATE.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- design contract 的类型、section 规则与默认路径不再散落在多处
- canonical template 与 schema 使用同一套命名和要求
- 没有提前越界到 mapping / handoff / drift verification
  </success_criteria>

<output>
After completion, create `.planning/phases/17-design-contract-surface/17-01-SUMMARY.md`
</output>
