---
phase: 18-design-to-code-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/interface/types/design-mapping.ts
  - src/interface/types/index.ts
  - src/cli/design-scope-resolver.ts
  - src/cli/commands/design.ts
  - src/cli/__tests__/design-scope-resolver.test.ts
  - src/cli/commands/__tests__/design-map-command.test.ts
  - tests/fixtures/design-contracts/mapping-basic.design.md
autonomous: false
requirements:
  - MAP-01
user_setup: []
must_haves:
  truths:
    - `design map` 能把 design contract 解析成 candidate files / modules / symbols，而不是分散搜索结果
    - 每个 candidate 都有明确的 reason chain，说明来自哪个 design section 与哪类证据
    - 新能力挂在既有 `design` command 下，不扩写 `analyze` / `workflow` public contract
  artifacts:
    - src/interface/types/design-mapping.ts
    - src/interface/types/index.ts
    - src/cli/design-scope-resolver.ts
    - src/cli/commands/design.ts
    - src/cli/__tests__/design-scope-resolver.test.ts
    - src/cli/commands/__tests__/design-map-command.test.ts
    - tests/fixtures/design-contracts/mapping-basic.design.md
  key_links:
    - `src/interface/types/design-mapping.ts` 定义 mapping contract、candidate kind 与 diagnostics
    - `src/cli/design-scope-resolver.ts` 组合 design input 与现有 analysis primitives
    - `src/cli/commands/design.ts` 暴露 `design map [file] --json`
---

<objective>
先交付 Phase 18 的 success-path 骨架：正式 mapping types、scope resolver baseline，以及能输出 candidates + reason chain 的 `design map` 命令。

Purpose: 如果第一步还停留在“内部能搜到一些文件”，Phase 18 就会继续依赖人工猜测，无法满足 `MAP-01` 对 candidate files / modules / symbols + 原因链的要求。
Output: `DesignMappingResult` 类型、`resolveDesignScope()` resolver、`design map` subcommand、happy-path fixture 与 focused tests。
</objective>

<execution_context>
@/data/codemap/.codex/get-shit-done/workflows/execute-plan.md
@/data/codemap/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-design-to-code-mapping/18-CONTEXT.md
@.planning/phases/18-design-to-code-mapping/18-RESEARCH.md
@src/interface/types/design-contract.ts
@src/cli/design-contract-loader.ts
@src/cli/commands/design.ts
@src/cli/commands/analyze.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义 design mapping 正式类型与 blocker diagnostics</name>
  <files>src/interface/types/design-mapping.ts, src/interface/types/index.ts</files>
  <read_first>.planning/phases/18-design-to-code-mapping/18-CONTEXT.md, .planning/phases/18-design-to-code-mapping/18-RESEARCH.md, src/interface/types/design-contract.ts, src/interface/types/index.ts</read_first>
  <action>新增 `src/interface/types/design-mapping.ts`，定义 `DesignMappingCandidateKind`（至少包含 `file`、`module`、`symbol`）、`DesignMappingReason`、`DesignMappingCandidate`、`DesignMappingDiagnosticCode`、`DesignMappingDiagnostic`、`DesignMappingResult`。`DesignMappingCandidate` 必须至少包含 `kind`、`path`、`symbolName?`、`moduleName?`、`reasons`、`dependencies`、`testImpact`、`risk`、`confidence`、`unknowns`；`DesignMappingResult` 必须包含 `ok`、`filePath`、`summary`、`candidates`、`diagnostics`。在 `src/interface/types/index.ts` 中导出这些类型。</action>
  <verify>rg -n "export type DesignMappingCandidateKind|export interface DesignMappingCandidate|export interface DesignMappingResult|DesignMappingDiagnosticCode" src/interface/types/design-mapping.ts src/interface/types/index.ts</verify>
  <acceptance_criteria>
    - `src/interface/types/design-mapping.ts` 包含 `DesignMappingCandidateKind`
    - `src/interface/types/design-mapping.ts` 包含 `DesignMappingResult`
    - `src/interface/types/design-mapping.ts` 中 `DesignMappingCandidate` 同时出现 `reasons`、`dependencies`、`testImpact`、`unknowns`
    - `src/interface/types/index.ts` 导出 `./design-mapping.js`
  </acceptance_criteria>
  <done>Phase 18 的输出契约不再借用 analyze contract，而是拥有自己的正式类型。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 scope resolver baseline 与 reason chain</name>
  <files>src/cli/design-scope-resolver.ts, src/cli/__tests__/design-scope-resolver.test.ts</files>
  <read_first>src/cli/design-contract-loader.ts, src/cli/design-contract-schema.ts, src/cli/commands/analyze.ts, .planning/phases/18-design-to-code-mapping/18-RESEARCH.md</read_first>
  <action>新增 `src/cli/design-scope-resolver.ts`，实现 `export async function resolveDesignScope(options)`。该函数必须：1) 通过 `loadDesignContract({ filePath })` 读取 normalized contract；2) 从 `goal`、`constraints`、`acceptanceCriteria`、`nonGoals` 抽取 explicit anchors（路径片段、模块名、符号名、关键词）；3) 对显式 path/module anchors 走 exact candidate lookup，对其余关键词使用 `new AnalyzeCommand({ intent: 'find', keywords, includeTests: true, json: true, structured: true })` 产生 broad candidates；4) 为每个 candidate 记录 `reasons[]`，字段至少包含 `section`、`matchedText`、`evidenceType`；5) 在 baseline 中输出去重后的 `candidates[]`，并按 relative path 排序。为 success-path 写 `src/cli/__tests__/design-scope-resolver.test.ts`，覆盖 explicit anchor 命中、non-goals negative filtering 与 reason chain 存在性。</action>
  <verify>pnpm exec vitest run src/cli/__tests__/design-scope-resolver.test.ts</verify>
  <acceptance_criteria>
    - `src/cli/design-scope-resolver.ts` 包含 `export async function resolveDesignScope(`
    - `src/cli/design-scope-resolver.ts` 中同时出现 `matchedText`、`evidenceType`、`section`
    - `src/cli/__tests__/design-scope-resolver.test.ts` 退出码为 `0`
    - success-path 测试断言至少 1 个 candidate 拥有非空 `reasons`
  </acceptance_criteria>
  <done>resolver 已能把 design contract 解释成具备 reason chain 的 candidate scope。</done>
</task>

<task type="auto">
  <name>Task 3: 暴露 `design map` 命令并锁住 success path</name>
  <files>src/cli/commands/design.ts, src/cli/commands/__tests__/design-map-command.test.ts, tests/fixtures/design-contracts/mapping-basic.design.md</files>
  <read_first>src/cli/commands/design.ts, src/cli/design-scope-resolver.ts, tests/fixtures/design-contracts/valid-basic.design.md</read_first>
  <action>在 `src/cli/commands/design.ts` 下新增 `map` 子命令：`design map [file]`，支持 `-j, --json`。JSON 模式输出 `DesignMappingResult`；human 模式输出最少包含 `Candidates:` 与每个 candidate 的 `kind/path/reasons`。新增 `tests/fixtures/design-contracts/mapping-basic.design.md` 作为 success-path fixture，并在 `src/cli/commands/__tests__/design-map-command.test.ts` 中断言：`designCommand.helpInformation()` 包含 `map`；`runDesignMap(..., { json: true })` 返回 `ok: true` 且 `candidates.length > 0`；JSON 中每个 candidate 至少有 1 条 `reasons[]`。</action>
  <verify>pnpm exec vitest run src/cli/commands/__tests__/design-map-command.test.ts && node dist/cli/index.js design map tests/fixtures/design-contracts/mapping-basic.design.md --json</verify>
  <acceptance_criteria>
    - `src/cli/commands/design.ts` 包含 `.command('map')`
    - `src/cli/commands/__tests__/design-map-command.test.ts` 退出码为 `0`
    - `node dist/cli/index.js design map tests/fixtures/design-contracts/mapping-basic.design.md --json` 输出包含 `"candidates"`
    - success-path fixture 明确包含 `Goal`、`Constraints`、`Acceptance Criteria`、`Non-Goals`
  </acceptance_criteria>
  <done>`design map` 成为正式 CLI 入口，并能交付带 reason chain 的 success-path scope。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "DesignMappingResult|DesignMappingCandidate|DesignMappingDiagnostic" src/interface/types/design-mapping.ts src/interface/types/index.ts`
- [ ] `pnpm exec vitest run src/cli/__tests__/design-scope-resolver.test.ts src/cli/commands/__tests__/design-map-command.test.ts`
- [ ] `node dist/cli/index.js design map tests/fixtures/design-contracts/mapping-basic.design.md --json`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `design map` 已能返回 candidate files / modules / symbols 与 reason chain
- 新能力只扩展 `design` surface，不污染 `analyze` / `workflow`
- success-path fixture 与 focused tests 能回归锁住 `MAP-01`
  </success_criteria>

<output>
After completion, create `.planning/phases/18-design-to-code-mapping/18-01-SUMMARY.md`
</output>
