---
phase: 20-design-drift-verification-docs-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/interface/types/design-verification.ts
  - src/interface/types/index.ts
  - src/cli/design-verification-builder.ts
  - src/cli/__tests__/design-verification-builder.test.ts
  - tests/fixtures/design-contracts/verify-ready.design.md
autonomous: false
requirements:
  - VAL-04
  - VAL-05
user_setup: []
must_haves:
  truths:
    - `Acceptance Criteria` 被映射成正式 checklist item，而不是手写 prose 清单
    - verification 只在有直接 evidence 时标记 `satisfied`，否则保守地保持 `needs-review`
    - reviewed handoff truth 是 scope boundary；artifact 缺失或上游 blocker 必须显式诊断
  artifacts:
    - src/interface/types/design-verification.ts
    - src/interface/types/index.ts
    - src/cli/design-verification-builder.ts
    - src/cli/__tests__/design-verification-builder.test.ts
    - tests/fixtures/design-contracts/verify-ready.design.md
  key_links:
    - `src/interface/types/design-verification.ts` 定义 checklist / drift / diagnostic 正式契约
    - `src/cli/design-verification-builder.ts` 聚合 design + handoff truth，构建 verification result
    - `tests/fixtures/design-contracts/verify-ready.design.md` 提供 zero-unresolved ready-path baseline
---

<objective>
先交付 Phase 20 的 verification core：正式 output contract、conservative checklist/drift builder，以及一份真正 ready 的 baseline fixture。

Purpose: 如果 Phase 20 一开始没有正式 verify schema 和 conservative builder，后续 command/docs/E2E 只能继续围绕“模糊验收”打补丁，最终还是无法稳定回答 drift 是否存在。
Output: `DesignVerification*` 类型、`buildDesignVerification()`、ready-path fixture 与 focused builder tests。
</objective>

<execution_context>
@/data/codemap/.codex/get-shit-done/workflows/execute-plan.md
@/data/codemap/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-design-drift-verification-docs-sync/20-CONTEXT.md
@.planning/phases/20-design-drift-verification-docs-sync/20-RESEARCH.md
@src/interface/types/design-contract.ts
@src/interface/types/design-handoff.ts
@src/cli/design-handoff-builder.ts
@src/cli/design-scope-resolver.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义正式 verification / drift contract</name>
  <files>src/interface/types/design-verification.ts, src/interface/types/index.ts</files>
  <read_first>.planning/phases/20-design-drift-verification-docs-sync/20-CONTEXT.md, .planning/phases/20-design-drift-verification-docs-sync/20-RESEARCH.md, src/interface/types/design-contract.ts, src/interface/types/design-handoff.ts, src/interface/types/index.ts</read_first>
  <action>新增 `src/interface/types/design-verification.ts`，至少定义：`DesignVerificationStatus`（`satisfied` / `needs-review` / `violated` / `blocked`）、`DesignVerificationChecklistItem`、`DesignDriftItem`、`DesignVerificationSummary`、`DesignVerificationDiagnostic`、`DesignVerificationResult`。`DesignVerificationChecklistItem` 必须至少包含 `id`、`text`、`status`、`evidenceRefs`；`DesignDriftItem` 必须至少包含 `kind`、`severity`、`message`、`sourceRefs`；`DesignVerificationResult` 必须至少包含 `ok`、`filePath`、`readyForExecution`、`summary`、`checklist`、`drift`、`diagnostics`，并在 `src/interface/types/index.ts` 中导出 `./design-verification.js`。</action>
  <verify>rg -n "DesignVerificationResult|DesignVerificationChecklistItem|DesignDriftItem|DesignVerificationStatus" src/interface/types/design-verification.ts src/interface/types/index.ts</verify>
  <acceptance_criteria>
    - `src/interface/types/design-verification.ts` 包含 `DesignVerificationResult`
    - `DesignVerificationChecklistItem` 同时包含 `text`、`status`、`evidenceRefs`
    - `DesignDriftItem` 同时包含 `kind`、`severity`、`message`
    - `src/interface/types/index.ts` 导出 `./design-verification.js`
  </acceptance_criteria>
  <done>Phase 20 不再依赖 ad-hoc verify 对象，而拥有正式共享 contract。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 conservative checklist / drift builder</name>
  <files>src/cli/design-verification-builder.ts</files>
  <read_first>.planning/phases/20-design-drift-verification-docs-sync/20-CONTEXT.md, src/cli/design-contract-loader.ts, src/cli/design-handoff-builder.ts, src/cli/design-scope-resolver.ts, src/interface/types/design-contract.ts, src/interface/types/design-handoff.ts, src/interface/types/design-verification.ts</read_first>
  <action>新增 `src/cli/design-verification-builder.ts`，提供 `buildDesignVerification(options)`。该函数必须：1) 通过 `loadDesignContract({ filePath })` 获取 validated design input；2) 优先读取 deterministic `.handoff.json` truth（可通过 `resolveDesignHandoffArtifacts()` 解析路径）；若 artifact 缺失，则回退到 `buildDesignHandoff()`，但必须添加 `handoff-missing` / `needs-review` 级别 diagnostics；3) 把 `acceptanceCriteria` 逐条映射为 checklist item；4) 只在存在直接 evidence refs（approved handoff scope、tests、diagnostics-free probes 等）时标记 `satisfied`，否则保持 `needs-review`；5) 当上游 design/handoff/mapping blocker 存在时返回 `ok=false` 与 `blocked` 状态；6) 产出 drift items，至少覆盖 `scope-extra`、`acceptance-unverified`、`handoff-missing` 三类。</action>
  <verify>pnpm exec vitest run src/cli/__tests__/design-verification-builder.test.ts</verify>
  <acceptance_criteria>
    - `src/cli/design-verification-builder.ts` 包含 `buildDesignVerification`
    - builder 同时调用 `loadDesignContract` 与 handoff truth（artifact 或 builder fallback）
    - checklist item 不会在缺证据时自动标成 `satisfied`
    - drift items 至少覆盖 scope / acceptance / handoff 三个方向
  </acceptance_criteria>
  <done>verification result 已经建立在 design + handoff truth 上，而不是自由推断。</done>
</task>

<task type="auto">
  <name>Task 3: 添加 zero-unresolved fixture 与 builder regressions</name>
  <files>src/cli/__tests__/design-verification-builder.test.ts, tests/fixtures/design-contracts/verify-ready.design.md</files>
  <read_first>tests/fixtures/design-contracts/handoff-basic.design.md, tests/fixtures/design-contracts/missing-acceptance.design.md, tests/fixtures/design-contracts/no-match.design.md, src/cli/design-verification-builder.ts</read_first>
  <action>新增 `tests/fixtures/design-contracts/verify-ready.design.md`，要求：包含 `Goal`、`Constraints`、`Acceptance Criteria`、`Non-Goals`，且不含 `Open Questions`，用于 ready-path。新增 `src/cli/__tests__/design-verification-builder.test.ts`，至少断言：1) ready fixture 在 handoff truth ready 时可得到 `readyForExecution=true` 与至少一个 `satisfied` checklist item；2) 缺失 handoff artifact 时结果不会 silent pass，而会出现 `needs-review` / `handoff-missing` diagnostics；3) `missing-acceptance.design.md` 或 `no-match.design.md` 会进入 blocked path；4) 所有 checklist / drift 条目都保留 evidence/source refs。</action>
  <verify>pnpm exec vitest run src/cli/__tests__/design-verification-builder.test.ts</verify>
  <acceptance_criteria>
    - `tests/fixtures/design-contracts/verify-ready.design.md` 不包含 `## Open Questions`
    - builder tests 退出码为 `0`
    - 测试断言 ready path 可以 `readyForExecution=true`
    - 测试断言 missing artifact / blocked input 不会 false-pass
  </acceptance_criteria>
  <done>ready-path、needs-review path 与 blocked path 的 verify core 已被 focused tests 锁住。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "DesignVerificationResult|DesignVerificationChecklistItem|DesignDriftItem" src/interface/types/design-verification.ts src/interface/types/index.ts`
- [ ] `pnpm exec vitest run src/cli/__tests__/design-verification-builder.test.ts`
- [ ] `rg -n "buildDesignVerification|handoff-missing|scope-extra|acceptance-unverified" src/cli/design-verification-builder.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 20 拥有正式 verification/drift schema，而不是临时 checklist prose
- builder 会保守地处理证据不足项，不制造 false-positive “已满足”
- ready / needs-review / blocked 三条路径都被 fixture 与 focused tests 锁住
  </success_criteria>

<output>
After completion, create `.planning/phases/20-design-drift-verification-docs-sync/20-01-SUMMARY.md`
</output>
