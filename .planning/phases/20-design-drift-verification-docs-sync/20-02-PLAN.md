---
phase: 20-design-drift-verification-docs-sync
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/cli/commands/design.ts
  - src/cli/commands/__tests__/design-verify-command.test.ts
  - README.md
  - AI_GUIDE.md
  - CLAUDE.md
  - docs/ai-guide/COMMANDS.md
  - docs/ai-guide/OUTPUT.md
  - docs/ai-guide/PATTERNS.md
  - docs/rules/engineering-with-codex-openai.md
  - docs/rules/validation.md
  - scripts/validate-docs.js
  - src/cli/__tests__/validate-docs-script.test.ts
  - src/cli/commands/__tests__/ci-docs-sync.test.ts
autonomous: false
requirements:
  - VAL-04
  - DOC-07
  - VAL-05
user_setup: []
must_haves:
  truths:
    - `design verify` 成为正式 public CLI 入口，同时继续保持 `workflow` analysis-only 与 `analyze` 四意图稳定
    - verify human/json 输出都反映 checklist / drift truth；review-needed 与 blocker 语义分离
    - docs / rules / guardrail 统一到真实链路 `design validate → design map → design handoff → design verify`
  artifacts:
    - src/cli/commands/design.ts
    - src/cli/commands/__tests__/design-verify-command.test.ts
    - README.md
    - AI_GUIDE.md
    - docs/ai-guide/COMMANDS.md
    - docs/ai-guide/OUTPUT.md
    - docs/ai-guide/PATTERNS.md
    - docs/rules/engineering-with-codex-openai.md
    - docs/rules/validation.md
    - scripts/validate-docs.js
  key_links:
    - `src/cli/commands/design.ts` 暴露 `design verify [file]`
    - `docs/ai-guide/OUTPUT.md` 记录 verify checklist / drift JSON contract
    - `scripts/validate-docs.js` 与 docs tests 锁住 verify 文档真相和 workflow 边界
---

<objective>
把 Phase 20 的 verification core 接到正式 CLI 与 docs/guardrail：新增 `design verify` 命令、同步完整 design chain 文档，并用脚本/tests 把 verify 叙事固定下来。

Purpose: 如果 Phase 20 只有内部 builder，没有 discoverable CLI surface 和 docs guardrail，用户与 AI 仍然无法稳定走完整闭环，`DOC-07` 也不会真正成立。
Output: `design verify` command、command regressions、README/AI docs/rules/guardrail 同步。
</objective>

<execution_context>
@/data/codemap/.codex/get-shit-done/workflows/execute-plan.md
@/data/codemap/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-design-drift-verification-docs-sync/20-CONTEXT.md
@.planning/phases/20-design-drift-verification-docs-sync/20-RESEARCH.md
@.planning/phases/20-design-drift-verification-docs-sync/20-01-PLAN.md
@src/cli/commands/design.ts
@src/cli/design-verification-builder.ts
@docs/ai-guide/COMMANDS.md
@docs/ai-guide/OUTPUT.md
@docs/ai-guide/PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 暴露 `design verify` 命令并复用 canonical builder</name>
  <files>src/cli/commands/design.ts</files>
  <read_first>.planning/phases/20-design-drift-verification-docs-sync/20-CONTEXT.md, src/cli/commands/design.ts, src/cli/design-verification-builder.ts, src/cli/design-handoff-builder.ts</read_first>
  <action>在 `src/cli/commands/design.ts` 中新增 `verify` 子命令：`design verify [file]`，至少支持 `-j, --json`。新增 `runDesignVerify(filePath?, options?)`，内部调用 `buildDesignVerification({ filePath })`。human 模式下打印 checklist/drift summary、ready vs needs-review vs blocked 概览以及建议下一步；JSON 模式下只输出纯 `DesignVerificationResult`。当 verify 只是 `needs-review` 时不得直接设置 non-zero `process.exitCode`；只有 `ok=false` 或 blocker diagnostics 才走 non-zero exit。不要把 `verify` 设计成新的 workflow phase，也不要复活 `commit` / `ci` 叙事。</action>
  <verify>rg -n "\\.command\\('verify'\\)|runDesignVerify|DesignVerificationResult|needs-review" src/cli/commands/design.ts</verify>
  <acceptance_criteria>
    - `src/cli/commands/design.ts` 包含 `.command('verify')`
    - `src/cli/commands/design.ts` 包含 `runDesignVerify`
    - verify 命令支持 `--json`
    - review-needed path 不会被硬编码成 `process.exitCode = 1`
  </acceptance_criteria>
  <done>`design verify` 成为正式 public seam，且继续遵守 review-needed / blocker 分离语义。</done>
</task>

<task type="auto">
  <name>Task 2: 添加 command regression 与 CLI smoke coverage</name>
  <files>src/cli/commands/__tests__/design-verify-command.test.ts</files>
  <read_first>src/cli/commands/design.ts, src/cli/design-verification-builder.ts, tests/fixtures/design-contracts/verify-ready.design.md, tests/fixtures/design-contracts/no-match.design.md</read_first>
  <action>新增 `src/cli/commands/__tests__/design-verify-command.test.ts`，至少断言：1) `designCommand.helpInformation()` 包含 `verify`；2) `runDesignVerify(..., { json: true })` 返回 `ok`、`readyForExecution`、`summary`、`checklist`、`drift`、`diagnostics`；3) ready fixture 在命令层可以返回 `readyForExecution=true`；4) review-needed path 不应被视为 blocker failure；5) `no-match.design.md` 或其他 blocked fixture 会走 non-zero / `ok=false` 路径。</action>
  <verify>pnpm exec vitest run src/cli/commands/__tests__/design-verify-command.test.ts</verify>
  <acceptance_criteria>
    - `src/cli/commands/__tests__/design-verify-command.test.ts` 退出码为 `0`
    - 测试断言 help surface 包含 `verify`
    - 测试断言 JSON output 同时包含 `checklist` 与 `drift`
    - 测试断言 blocked path 为 `ok=false`
  </acceptance_criteria>
  <done>CLI surface、输出契约与 exit semantics 已被 focused command tests 锁住。</done>
</task>

<task type="auto">
  <name>Task 3: 完整同步 verify 文档与 docs guardrail</name>
  <files>README.md, AI_GUIDE.md, CLAUDE.md, docs/ai-guide/COMMANDS.md, docs/ai-guide/OUTPUT.md, docs/ai-guide/PATTERNS.md, docs/rules/engineering-with-codex-openai.md, docs/rules/validation.md, scripts/validate-docs.js, src/cli/__tests__/validate-docs-script.test.ts, src/cli/commands/__tests__/ci-docs-sync.test.ts</files>
  <read_first>README.md, AI_GUIDE.md, docs/ai-guide/COMMANDS.md, docs/ai-guide/OUTPUT.md, docs/ai-guide/PATTERNS.md, docs/rules/engineering-with-codex-openai.md, docs/rules/validation.md, scripts/validate-docs.js, .planning/phases/20-design-drift-verification-docs-sync/20-CONTEXT.md</read_first>
  <action>同步所有直接受 `design verify` 与完整 design chain 影响的文档与护栏：在 `README.md`、`AI_GUIDE.md`、`CLAUDE.md`、`docs/ai-guide/COMMANDS.md`、`docs/ai-guide/OUTPUT.md`、`docs/ai-guide/PATTERNS.md`、`docs/rules/engineering-with-codex-openai.md`、`docs/rules/validation.md` 中加入 `mycodemap design verify mycodemap.design.md --json` 入口、checklist / drift output 说明，以及完整链路 `design validate → design map → design handoff → design verify`。扩展 `scripts/validate-docs.js`、`src/cli/__tests__/validate-docs-script.test.ts` 与必要的 `ci-docs-sync` tests，把 verify 命令、关键字段和 workflow 四阶段边界纳入 docs guardrail。不要把 verify 叙述成 workflow phase，也不要在这里提前加入 autonomous executor 文案。</action>
  <verify>rg -n "design verify|design validate.*design map.*design handoff.*design verify|checklist|drift" README.md AI_GUIDE.md CLAUDE.md docs/ai-guide/COMMANDS.md docs/ai-guide/OUTPUT.md docs/ai-guide/PATTERNS.md docs/rules/engineering-with-codex-openai.md docs/rules/validation.md scripts/validate-docs.js && npm run docs:check:human && pnpm exec vitest run src/cli/__tests__/validate-docs-script.test.ts src/cli/commands/__tests__/ci-docs-sync.test.ts</verify>
  <acceptance_criteria>
    - 所有列出的 docs 文件都包含 `design verify`
    - `docs/ai-guide/OUTPUT.md` 明确记录 checklist / drift JSON contract
    - `scripts/validate-docs.js` 显式校验 verify 链路与 workflow 四阶段边界
    - `npm run docs:check:human` 与相关 docs tests 通过
  </acceptance_criteria>
  <done>`design verify` 的 discoverability、output contract 与 docs guardrail 已完整同步。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run src/cli/commands/__tests__/design-verify-command.test.ts`
- [ ] `node dist/cli/index.js design verify tests/fixtures/design-contracts/verify-ready.design.md --json`
- [ ] `npm run docs:check:human`
- [ ] `pnpm exec vitest run src/cli/__tests__/validate-docs-script.test.ts src/cli/commands/__tests__/ci-docs-sync.test.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `design verify` 已成为稳定 public seam，且不污染 `analyze` / `workflow`
- 完整 design chain 文档与 rules/guardrail 已对齐，不再依赖人工记忆发现 verify surface
- verify 的 review-needed / blocker 语义在 CLI 与 docs 两层都保持一致
  </success_criteria>

<output>
After completion, create `.planning/phases/20-design-drift-verification-docs-sync/20-02-SUMMARY.md`
</output>
