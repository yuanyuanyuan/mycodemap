---
phase: 58-subagent-environment-contract-injection
plan: 05
type: execute
wave: 5
depends_on: []
files_modified:
  - .planning/phases/58-subagent-environment-contract-injection/58-HUMAN-UAT.md
  - .planning/phases/58-subagent-environment-contract-injection/58-UAT.md
  - .planning/phases/58-subagent-environment-contract-injection/58-VERIFICATION.md
  - .planning/phases/58-subagent-environment-contract-injection/58-VALIDATION.md
  - scripts/verify-subagent-env-contract.mjs
  - .claude/agents/env-contract-verifier.md
  - .codex/agents/env-contract-verifier.toml
  - docs/generated/phase-58/subagent-evidence/verification-manifest.json
  - docs/generated/phase-58/subagent-evidence/claude-hook-example.json
  - docs/generated/phase-58/subagent-evidence/codex-agent-example.toml
  - docs/generated/phase-58/subagent-evidence/claude-subagent.json
  - docs/generated/phase-58/subagent-evidence/codex-subagent.json
  - docs/generated/phase-58/subagent-evidence/claude-session.md
  - docs/generated/phase-58/subagent-evidence/codex-session.md
  - .planning/STATE.md
autonomous: false
requirements:
  - SDC-05
  - VER-03
user_setup:
  - Authenticated Claude Code session with Agent tool enabled for this repository
  - Optional authenticated Codex session if Codex parity evidence is attempted
must_haves:
  truths:
    - "Phase 58 no longer treats `claude -p`, bare `codex exec`, or blocker-only JSON files as valid subagent proof"
    - "`scripts/verify-subagent-env-contract.mjs` prepares agent fixtures and evidence templates instead of pretending to execute real subagents"
    - "Repo-local Claude and Codex verification agent definitions require env-contract retrieval before any substantive work"
    - "`58-HUMAN-UAT.md` defines a Claude-required, Codex-optional manual protocol with pass, fail, and waiver rules"
    - "`58-VERIFICATION.md` cites real evidence files and marks SDC-05 passed only after retrieval-before-work is visible in the transcript"
  artifacts:
    - path: ".planning/phases/58-subagent-environment-contract-injection/58-HUMAN-UAT.md"
      provides: "Canonical manual verification protocol"
      contains: "Claude required"
    - path: "scripts/verify-subagent-env-contract.mjs"
      provides: "Preparation workflow for manual subagent verification"
      contains: "verification-manifest.json"
    - path: ".claude/agents/env-contract-verifier.md"
      provides: "Claude verification agent fixture"
      contains: "mycodemap env-contract --for explore --json"
    - path: ".codex/agents/env-contract-verifier.toml"
      provides: "Codex verification agent fixture"
      contains: "codemap_env_contract(agentType=\"worker\")"
    - path: ".planning/phases/58-subagent-environment-contract-injection/58-VERIFICATION.md"
      provides: "Phase closure report with real-evidence gate"
      contains: "claude-subagent.json"
  key_links:
    - from: "scripts/verify-subagent-env-contract.mjs"
      to: "docs/generated/phase-58/subagent-evidence/verification-manifest.json"
      via: "writes the evidence scaffold and manual instructions"
      pattern: "verification-manifest"
    - from: ".claude/agents/env-contract-verifier.md"
      to: ".planning/phases/58-subagent-environment-contract-injection/58-HUMAN-UAT.md"
      via: "manual protocol references the same agent name and retrieval step"
      pattern: "env-contract-verifier|mycodemap env-contract --for explore --json"
    - from: ".planning/phases/58-subagent-environment-contract-injection/58-VERIFICATION.md"
      to: "docs/generated/phase-58/subagent-evidence/claude-subagent.json"
      via: "closure report must cite captured evidence, not theoretical blockers"
      pattern: "retrievalEvidence|verdict"
---

<objective>
Close the only remaining Phase 58 gap by replacing the fake subagent harness with a manual-evidence workflow: reset stale verification claims, add concrete verification agent fixtures, stage reusable evidence templates, and roll the real Claude/Codex results back into UAT and verification docs.

Purpose: Phase 58's product code is already shipped. The milestone is blocked only by S1-S3 verification rework, so this plan focuses exclusively on truthful closure evidence.

Output: refreshed verification docs, a preparation script, concrete Claude/Codex verification agent files, staged evidence templates, and a blocking human checkpoint for the real Claude path.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-subagent-environment-contract-injection/58-CONTEXT.md
@.planning/phases/58-subagent-environment-contract-injection/58-HUMAN-UAT.md
@.planning/phases/58-subagent-environment-contract-injection/58-UAT.md
@.planning/phases/58-subagent-environment-contract-injection/58-VERIFICATION.md
@.planning/phases/58-subagent-environment-contract-injection/58-VALIDATION.md
@.planning/archive/phases/58-subagent-environment-contract-injection/58-04-SUMMARY.md
@scripts/verify-subagent-env-contract.mjs
@src/cli/commands/env-contract.ts
@src/server/mcp/server.ts
@src/cli/init/assistant-plan.ts
@.claude/agents/ai-generator.md
</context>

<tasks>

<task type="auto">
  <name>Task 58-05-01: Re-baseline Phase 58 verification truth</name>
  <files>.planning/phases/58-subagent-environment-contract-injection/58-HUMAN-UAT.md, .planning/phases/58-subagent-environment-contract-injection/58-UAT.md, .planning/phases/58-subagent-environment-contract-injection/58-VERIFICATION.md, .planning/phases/58-subagent-environment-contract-injection/58-VALIDATION.md</files>
  <read_first>
    - .planning/phases/58-subagent-environment-contract-injection/58-CONTEXT.md (D-11 through D-17 are the authoritative rework decisions)
    - .planning/phases/58-subagent-environment-contract-injection/58-HUMAN-UAT.md (current redesign notes and wrong historical assumptions)
    - .planning/phases/58-subagent-environment-contract-injection/58-UAT.md (S1-S3 pending status)
    - .planning/phases/58-subagent-environment-contract-injection/58-VERIFICATION.md (currently over-claims SDC-05)
    - .planning/phases/58-subagent-environment-contract-injection/58-VALIDATION.md (needs 58-05 rows and manual-gate wording)
  </read_first>
  <action>
Update the four phase documents so they describe the current truth and only the current truth:

1. `58-HUMAN-UAT.md`
   - Rewrite the protocol section so it is explicitly "Claude required, Codex optional".
   - Keep the historical doc-review findings, but mark `claude -p` and nonexistent `codex exec --agent` as superseded evidence paths.
   - Define the required evidence files by exact name:
     - `docs/generated/phase-58/subagent-evidence/claude-session.md`
     - `docs/generated/phase-58/subagent-evidence/claude-subagent.json`
     - `docs/generated/phase-58/subagent-evidence/codex-session.md`
     - `docs/generated/phase-58/subagent-evidence/codex-subagent.json`
     - `docs/generated/phase-58/subagent-evidence/verification-manifest.json`
   - State the pass rule exactly: Claude passes only if the transcript shows an env-contract retrieval call before any substantive work.

2. `58-UAT.md`
   - Keep automated tests 1-10 as historical pass.
   - Replace the S1-S3 section with the new manual protocol status matrix:
     - S1 Claude real subagent path: required
     - S2 Codex parity path: optional, waiver allowed with exact blocker
     - S3 Preparation script and evidence scaffolding: automated prerequisite
   - Add explicit fields or notes that distinguish `pass`, `fail`, and `waived`.

3. `58-VERIFICATION.md`
   - Remove the claim that SDC-05 is "SATISFIED (with environment blockers)".
   - Rewrite the human-verification section so it references the real evidence files above.
   - State that blocker-only JSON output is not proof; at most it is a temporary rework status.
   - Keep the already-verified product-code truths intact; only the SDC-05 closure logic changes.

4. `58-VALIDATION.md`
   - Add 58-05 validation rows for doc re-baselining, script preparation, the manual Claude/Codex checkpoint, and the post-checkpoint rollup.
   - Update the manual-only verification rows to match the new required-vs-optional split.

Do not edit `58-01` through `58-04` plan files or summaries. They remain historical record only.
  </action>
  <verify>
    <automated>
      rtk rg -n "Claude required|Codex optional|retrieval call exists" .planning/phases/58-subagent-environment-contract-injection/58-HUMAN-UAT.md .planning/phases/58-subagent-environment-contract-injection/58-UAT.md .planning/phases/58-subagent-environment-contract-injection/58-VERIFICATION.md
      rtk rg -n "58-05-01|58-05-02|58-05-03|58-05-04" .planning/phases/58-subagent-environment-contract-injection/58-VALIDATION.md
      ! rtk rg -n "SATISFIED \\(with environment blockers\\)" .planning/phases/58-subagent-environment-contract-injection/58-VERIFICATION.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `58-HUMAN-UAT.md` names `claude-session.md`, `claude-subagent.json`, `codex-session.md`, `codex-subagent.json`, and `verification-manifest.json`
    - `58-HUMAN-UAT.md` contains the literal phrases `Claude required` and `Codex optional`
    - `58-UAT.md` keeps automated tests 1-10 as historical pass and marks S1-S3 as the remaining rework surface
    - `58-VERIFICATION.md` no longer contains `SATISFIED (with environment blockers)`
    - `58-VALIDATION.md` contains rows `58-05-01`, `58-05-02`, `58-05-03`, and `58-05-04`
  </acceptance_criteria>
  <done>
    - All four phase docs describe the same S1-S3 truth
    - No file claims that a fake harness or blocker-only attempt closes SDC-05
    - Manual evidence file names and verdict semantics are consistent across docs
  </done>
</task>

<task type="auto">
  <name>Task 58-05-02: Replace the fake harness with a preparation workflow and concrete agent fixtures</name>
  <files>scripts/verify-subagent-env-contract.mjs, .claude/agents/env-contract-verifier.md, .codex/agents/env-contract-verifier.toml, docs/generated/phase-58/subagent-evidence/verification-manifest.json, docs/generated/phase-58/subagent-evidence/claude-hook-example.json, docs/generated/phase-58/subagent-evidence/codex-agent-example.toml, docs/generated/phase-58/subagent-evidence/claude-subagent.json, docs/generated/phase-58/subagent-evidence/codex-subagent.json, docs/generated/phase-58/subagent-evidence/claude-session.md, docs/generated/phase-58/subagent-evidence/codex-session.md</files>
  <read_first>
    - scripts/verify-subagent-env-contract.mjs (remove fake subagent execution, keep useful temp-repo/evidence structure only if still needed)
    - .claude/agents/ai-generator.md (Claude agent-file structure)
    - src/cli/commands/env-contract.ts (`--as-hook-config` and `--as-codex-agent` output paths)
    - src/server/mcp/server.ts (`codemap_env_contract` tool name and parameter shape)
    - .planning/phases/58-subagent-environment-contract-injection/58-HUMAN-UAT.md (the protocol this script must prepare, not execute)
  </read_first>
  <action>
Rewrite `scripts/verify-subagent-env-contract.mjs` so it becomes a preparation script and nothing more.

Required behavior:

1. The script must NOT run `claude -p`, `codex exec`, or any other one-shot prompt pretending to be a real subagent path.
2. The script must run `rtk npm run build` first so the repository has a fresh `dist/cli/index.js`.
3. The script must create or refresh `docs/generated/phase-58/subagent-evidence/verification-manifest.json` with at least:
   - `phase: "58"`
   - `preparedAt`
   - `claudeRequired: true`
   - `codexOptional: true`
   - `requiredArtifacts` listing the exact evidence/template files
4. Use the built CLI to generate fresh helper snippets:
   - `rtk node dist/cli/index.js env-contract --as-hook-config > docs/generated/phase-58/subagent-evidence/claude-hook-example.json`
   - `rtk node dist/cli/index.js env-contract --as-codex-agent > docs/generated/phase-58/subagent-evidence/codex-agent-example.toml`
5. Create repo-local verification agents:
   - `.claude/agents/env-contract-verifier.md`
     - Describe it as a verification-only agent
     - Require `mycodemap env-contract --for explore --json` before any other work
     - Allow `codemap_env_contract(agentType="explore")` only as an alternate retrieval path
   - `.codex/agents/env-contract-verifier.toml`
     - Use `developer_instructions`
     - Require `mycodemap env-contract --for worker --json` or `codemap_env_contract(agentType="worker")` before any other work
6. Create or overwrite evidence templates:
   - `claude-subagent.json`
   - `codex-subagent.json`
   Each JSON must include keys: `platform`, `attempted`, `available`, `commandTranscriptPath`, `retrievalEvidence`, `verdict`, `blocker`, `notes`
7. Create empty markdown placeholders:
   - `claude-session.md`
   - `codex-session.md`
   Each file should begin with a heading and a short note telling the human what to paste there.
8. Print a short numbered checklist that points directly to `58-HUMAN-UAT.md` for the human-only step.

This task may reuse helper code from the old script only when it helps write files or invoke the built CLI. Any logic that pretends a single command is a real subagent run must be deleted.
  </action>
  <verify>
    <automated>
      rtk npm run build
      rtk node scripts/verify-subagent-env-contract.mjs
      ! rtk rg -n "claude -p|codex exec --agent|which claude|which codex" scripts/verify-subagent-env-contract.mjs
      rtk rg -n "mycodemap env-contract --for explore --json" .claude/agents/env-contract-verifier.md
      rtk rg -n "codemap_env_contract\\(agentType=\"worker\"\\)|mycodemap env-contract --for worker --json" .codex/agents/env-contract-verifier.toml
      rtk rg -n "claudeRequired|codexOptional|requiredArtifacts" docs/generated/phase-58/subagent-evidence/verification-manifest.json
    </automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-subagent-env-contract.mjs` contains no `claude -p`
    - `scripts/verify-subagent-env-contract.mjs` contains no `codex exec --agent`
    - `.claude/agents/env-contract-verifier.md` exists and contains `mycodemap env-contract --for explore --json`
    - `.codex/agents/env-contract-verifier.toml` exists and contains `codemap_env_contract(agentType="worker")` or `mycodemap env-contract --for worker --json`
    - `docs/generated/phase-58/subagent-evidence/verification-manifest.json` exists and declares `claudeRequired: true`
    - `claude-subagent.json`, `codex-subagent.json`, `claude-session.md`, and `codex-session.md` all exist after the script runs
  </acceptance_criteria>
  <done>
    - The repository contains concrete verification fixtures instead of fake one-shot test commands
    - The preparation script only stages assets and instructions
    - A human can start the real Claude path without inventing filenames or agent definitions
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Run the real Claude verification path from `58-HUMAN-UAT.md`, and optionally run the Codex parity path if the environment is available.</action>
  <instructions>
    The agent has already refreshed the docs, created the verification fixtures, and staged evidence templates under `docs/generated/phase-58/subagent-evidence/`.

    You must do the non-automatable part:
    1. Open an authenticated Claude Code session in this repository.
    2. Delegate to `env-contract-verifier`.
    3. Paste the relevant transcript into `docs/generated/phase-58/subagent-evidence/claude-session.md`.
    4. Fill `docs/generated/phase-58/subagent-evidence/claude-subagent.json` with:
       - `attempted`
       - `available`
       - `commandTranscriptPath`
       - `retrievalEvidence`
       - `verdict` (`pass` or `fail`)
       - `blocker` (only if the path could not be executed)
    5. If Codex is available, repeat the optional parity path and fill `codex-session.md` plus `codex-subagent.json`.
    6. If Codex is unavailable, record the exact blocker or waiver reason in `codex-subagent.json`.
    7. Do not mark Claude as passed unless the transcript shows `mycodemap env-contract`, `node dist/cli/index.js env-contract`, or `codemap_env_contract` before any other substantive work.
  </instructions>
  <verification>The agent can continue only after `claude-session.md` and `claude-subagent.json` exist, and `claude-subagent.json` contains a concrete `verdict` plus either non-empty `retrievalEvidence` or non-empty `blocker`.</verification>
  <resume-signal>Type "approved" with the evidence file paths, or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 58-05-04: Roll the real evidence back into UAT, verification, and state</name>
  <files>.planning/phases/58-subagent-environment-contract-injection/58-UAT.md, .planning/phases/58-subagent-environment-contract-injection/58-VERIFICATION.md, .planning/STATE.md, docs/generated/phase-58/subagent-evidence/claude-subagent.json, docs/generated/phase-58/subagent-evidence/codex-subagent.json, docs/generated/phase-58/subagent-evidence/claude-session.md, docs/generated/phase-58/subagent-evidence/codex-session.md</files>
  <read_first>
    - docs/generated/phase-58/subagent-evidence/claude-subagent.json
    - docs/generated/phase-58/subagent-evidence/codex-subagent.json
    - docs/generated/phase-58/subagent-evidence/claude-session.md
    - docs/generated/phase-58/subagent-evidence/codex-session.md
    - .planning/phases/58-subagent-environment-contract-injection/58-UAT.md
    - .planning/phases/58-subagent-environment-contract-injection/58-VERIFICATION.md
    - .planning/STATE.md
  </read_first>
  <action>
Use the checkpoint evidence to update the phase status truthfully:

1. `58-UAT.md`
   - Mark S1 Claude as `pass` only if `claude-subagent.json` says `verdict: "pass"` AND the transcript/evidence shows retrieval before work.
   - Mark S1 Claude as `fail` or `blocked` otherwise, with the exact blocker copied from the JSON.
   - Mark S2 Codex as `pass`, `fail`, or `waived` based on `codex-subagent.json`.
   - Mark S3 preparation script as `pass` if Task 58-05-02 artifacts exist.

2. `58-VERIFICATION.md`
   - Update the SDC-05 narrative to cite the actual evidence file paths.
   - If Claude passed, state exactly which retrieval call was observed.
   - If Claude failed or was blocked, keep the phase open and explain why.
   - Treat Codex as optional parity: cite the pass if present, otherwise cite the explicit waiver/blocker.

3. `.planning/STATE.md`
   - If Claude passed and no other blockers remain, update the phase status text so Phase 58 is ready for close / milestone close.
   - If Claude did not pass, keep status `rework` and summarize the remaining blocker precisely.
   - Update `Last Activity Description` to mention the real-evidence outcome, not the pre-rework assumption.

Do not invent success. If the Claude evidence is ambiguous or late (retrieval happens after substantive work), keep the phase open.
  </action>
  <verify>
    <automated>
      rtk rg -n "claude-subagent.json|claude-session.md|codex-subagent.json" .planning/phases/58-subagent-environment-contract-injection/58-VERIFICATION.md
      rtk rg -n "S1|S2|S3|waived|blocked|pass|fail" .planning/phases/58-subagent-environment-contract-injection/58-UAT.md
      rtk rg -n "Phase 58|rework|ready for close|milestone close" .planning/STATE.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `58-UAT.md` records S1, S2, and S3 using the real evidence results
    - `58-VERIFICATION.md` cites `claude-subagent.json` and `claude-session.md`
    - `58-VERIFICATION.md` does not call SDC-05 passed unless Claude evidence shows retrieval-before-work
    - `.planning/STATE.md` reflects either `rework` with a concrete blocker or readiness for close with a concrete evidence summary
  </acceptance_criteria>
  <done>
    - Phase 58 status is derived from real evidence files
    - The repository has one authoritative answer about whether SDC-05 is closed
    - Codex parity is recorded without blocking closure when a waiver is legitimate
  </done>
</task>

</tasks>

<threat_model>
T-58-17 stale verification truth: phase docs may continue to describe blocker-only fake evidence as success. Mitigation: re-baseline the UAT, HUMAN-UAT, VERIFICATION, and VALIDATION docs before any new execution.
T-58-18 fixture drift: manual verification can fail because agent definitions, hook snippets, and evidence filenames do not match each other. Mitigation: one preparation script regenerates the fixtures and manifest.
T-58-19 false closure: a human may mark Phase 58 complete without proving retrieval-before-work. Mitigation: blocking checkpoint plus post-checkpoint rollup that requires explicit evidence in `claude-subagent.json` and `claude-session.md`.
</threat_model>

<verification>
- `rtk rg -n "Claude required|Codex optional" .planning/phases/58-subagent-environment-contract-injection/58-HUMAN-UAT.md` exits 0
- `rtk npm run build && rtk node scripts/verify-subagent-env-contract.mjs` exits 0
- `! rtk rg -n "claude -p|codex exec --agent" scripts/verify-subagent-env-contract.mjs` exits 0
- `.claude/agents/env-contract-verifier.md` and `.codex/agents/env-contract-verifier.toml` both exist
- After the checkpoint, `docs/generated/phase-58/subagent-evidence/claude-subagent.json` and `claude-session.md` both exist
- `58-VERIFICATION.md` cites the real evidence paths and does not over-claim SDC-05
</verification>

<success_criteria>
1. The only active Phase 58 work is represented by this plan; 58-01 through 58-04 remain historical only.
2. No repository artifact still claims fake blocker-only evidence closes SDC-05.
3. The preparation script generates concrete fixtures and evidence templates without pretending to execute a real subagent.
4. Claude verification is captured through a real manual path and cited in `58-UAT.md`, `58-VERIFICATION.md`, and `.planning/STATE.md`.
5. Codex parity is either captured or explicitly waived with an exact blocker, without blocking closure if Claude has already passed.
</success_criteria>

<output>
After completion, create `.planning/phases/58-subagent-environment-contract-injection/58-05-SUMMARY.md`
</output>
