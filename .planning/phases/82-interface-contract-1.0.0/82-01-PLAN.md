---
phase: 82-interface-contract-1.0.0
plan: 01
type: execute
wave: 1
autonomous: false
requirements:
  - POL-04
files_modified:
  - src/cli/interface-contract/types.ts
  - src/cli/interface-contract/schema.ts
  - src/cli/interface-contract/index.ts
  - src/cli/interface-contract/commands/*.ts
  - src/cli/interface-contract/__tests__/interface-contract.test.ts
  - src/cli/__tests__/index-schema.test.ts
  - src/server/mcp/__tests__/schema-adapter.test.ts
---

# Phase 82 Plan 01

## Objective

把当前 interface contract 收口为 `1.0.0` 稳定表面：核心命令显式 `stable: true`，meta-schema 将该字段视为必填，并用 focused tests 锁住版本号与稳定性声明。

## Tasks

1. 扩展 contract type/schema
   - verify: `CommandContract` 与 zod meta-schema 都要求 `stable`
2. 标记当前注册命令为稳定表面
   - verify: built-in command contracts 全部 `stable: true`
3. 提升 full contract 版本到 `1.0.0`
   - verify: `getFullContract()` 与 `--schema` 输出版本同步
4. 收紧 focused tests
   - verify: 缺失 `stable` 的 fixture 会被 meta-schema 拒绝；内建 contract 全量通过

## Acceptance

- 当前注册的 core command contracts 都完整声明 `outputShape`、`errorCodes`、`examples`、`stable: true`
- full interface contract 版本升级为 `1.0.0`
- `--schema` 与 MCP schema-adapter 相关测试保持绿色
