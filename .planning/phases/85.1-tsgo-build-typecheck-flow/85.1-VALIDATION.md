---
phase: 85.1
slug: tsgo-build-typecheck-flow
status: verified
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-12
---

# Phase 85.1 — Validation Strategy

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Frameworks** | `tsgo`, npm scripts, Vitest via `prepublishOnly`, shell no-op check |
| **Quick run command** | `npm run build && npm run typecheck` |
| **Full suite command** | `npm run prepublishOnly` |
| **Estimated runtime** | Quick: ~5s; full: ~60s |

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Secure Behavior | Test Type | Automated Command | Status |
|---------|------|------|-------------|-----------------|-----------|-------------------|--------|
| 85.1-01-01 | 01 | 1 | TOOLCHAIN-TS-01 | `build` / `typecheck` 与 Claude PostToolUse 统一复用 `tsgo` | automated | `npm run build && npm run typecheck` | ✅ green |
| 85.1-01-02 | 01 | 1 | TOOLCHAIN-TS-01 | lockfile 正确锁定 `@typescript/native-preview` 与平台包 | artifact check | `rg -n "@typescript/native-preview" package.json package-lock.json` | ✅ green |
| 85.1-01-03 | 01 | 1 | TOOLCHAIN-TS-01 | CLI entry JSON import 语法不会阻断构建链路 | integration | `npm run build` | ✅ green |
| 85.1-01-04 | 01 | 1 | TOOLCHAIN-TS-01 | `file_path` 缺失时，Claude PostToolUse 不会把非 TS 编辑误判成 typecheck 失败 | failure-path | `TOOL_INPUT='{}' bash -lc 'FILE=$(echo "$TOOL_INPUT" | jq -r ''.file_path // empty'' 2>/dev/null); [ -n "$FILE" ] && [[ "$FILE" == *.ts || "$FILE" == *.tsx ]] && node_modules/.bin/tsgo --noEmit 2>&1 | head -15 || true'` | ✅ green |
| 85.1-01-05 | 01 | 1 | TOOLCHAIN-TS-01 | 发布前主路径继续通过 docs guardrail、build 和全量 tests | smoke | `npm run prepublishOnly` | ✅ green |

## Wave 0 Requirements

Existing project scripts already cover the required surfaces:

- `npm run build`
- `npm run typecheck`
- `npm run prepublishOnly`
- shell no-op check for missing `file_path`

No new harness was needed.

## Failure Scenario Coverage

- `.claude/settings.json` PostToolUse hook receives missing `file_path` and stays no-op
- `prepublishOnly` still surfaces existing AGENTS.md structure warnings without blocking the toolchain migration
- `build` validates that the `with { type: 'json' }` import form does not break the CLI entry

## Validation Sign-Off

- [x] Validation includes success and failure-path coverage.
- [x] All tasks map to automated verification.
- [x] `prepublishOnly` provides closeout-grade evidence beyond quick script checks.
- [x] `nyquist_compliant: true` set.

**Approval:** verified 2026-05-12
