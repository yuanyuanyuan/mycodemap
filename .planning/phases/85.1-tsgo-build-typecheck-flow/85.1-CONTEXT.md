# Phase 85.1: tsgo Build and Typecheck Flow - Context

**Gathered:** 2026-05-12
**Status:** Closeout complete; prepublish validation passed
**Milestone:** v2.7.1 post-ship follow-up

<domain>
## Phase Boundary

把当前未提交的 TypeScript toolchain 改动作为一个独立 phase 维护：CLI 构建与 typecheck 从 `tsc` 切到 `tsgo`，同步引入 `@typescript/native-preview`，修正 `src/cli-new/index.ts` 的 JSON import 语法，并让 Claude 的 TS 编辑后检查与新 typecheck 真相保持一致。

只记录和维护这些已出现的 git changes，不顺手扩展到更多构建系统、发布流程或其他 IDE/agent 集成。

</domain>

<decisions>
## Implementation Decisions

### 归档位置
- **D-01:** 本 phase 挂在 `v2.7.1` 归档下，编号为 `85.1`，表示它是紧邻 hook/agent ergonomics 工作面的 post-ship follow-up，而不是新 milestone。

### 变更真相
- **D-02:** `package.json` 的 `build` / `typecheck` script 是 CLI/tooling 真相；Claude 的 PostToolUse 检查应跟随该真相，不再各自漂移。

### 范围约束
- **D-03:** 仅覆盖当前已存在的四处改动：`package.json`、`package-lock.json`、`src/cli-new/index.ts`、`.claude/settings.json`。
- **D-04:** 不在本 phase 内处理更大范围的 TypeScript 版本升级、构建产物差异审计、CI matrix 调整或发版策略。

</decisions>

<canonical_refs>
## Canonical References

- `package.json`
- `package-lock.json`
- `src/cli-new/index.ts`
- `.claude/settings.json`
- `.planning/milestones/v2.7.1-ROADMAP.md`

</canonical_refs>

<code_context>
## Existing Code Insights

### Tracked Git Changes
- `package.json`:
  - `build`: `tsc` -> `tsgo`
  - `typecheck`: `tsc --noEmit` -> `tsgo --noEmit`
  - devDependencies 新增 `@typescript/native-preview`
- `package-lock.json`:
  - 锁定 `@typescript/native-preview@7.0.0-dev.20260511.1` 与平台可选包
- `src/cli-new/index.ts`:
  - `import('../../package.json', { assert: { type: 'json' } })`
  - 改为 `import('../../package.json', { with: { type: 'json' } })`
- `.claude/settings.json`:
  - `PostToolUse` 新增对 `*.ts` / `*.tsx` 文件的 `tsgo --noEmit` 快速检查

### Integration Points
- CLI 构建入口依赖 `package.json` script 真相。
- `src/cli-new/index.ts` 的 JSON import 语法需要与目标 Node/toolchain 兼容。
- Claude 编辑后检查应复用同一条 typecheck 工具链，避免 `tsc` / `tsgo` 双轨漂移。

</code_context>

<specifics>
## Specific Ideas

- 这批改动更像“agent-facing TypeScript toolchain follow-up”，而不是 hook protocol 本身，但它仍然紧贴 `v2.7.1` 的 AI/agent 工作流表面。
- 首轮真实验证已经覆盖 `npm run build`、`npm run typecheck`，并补了一个 hook 缺失 `file_path` 时的 no-op 风险场景。
- closeout 阶段追加通过 `npm run prepublishOnly`，覆盖 docs guardrail、build 和全量 test。

</specifics>

<deferred>
## Deferred Ideas

- `tsgo` 与 `tsc` 的产物差异对比
- CI / prepublish 路径是否需要显式记录 toolchain 切换
- 更广泛的 editor/agent integration 统一

</deferred>

---
*Phase: 85.1-tsgo-build-typecheck-flow*
*Context gathered: 2026-05-12*
