---
phase: 79-complexity-truth-unification
plan: 01
type: execute
wave: 1
depends_on:
  - 72-python-complexity-truth
files_modified:
  - src/core/ast-complexity-analyzer.ts
  - src/core/__tests__/ast-complexity-analyzer.test.ts
  - src/core/__tests__/analyzer.test.ts
  - src/infrastructure/parser/implementations/TreeSitterParser.ts
  - src/infrastructure/parser/implementations/PythonTreeSitterParser.ts
  - src/infrastructure/parser/implementations/GoParser.ts
  - src/infrastructure/parser/interfaces/ParserBase.ts
  - src/infrastructure/parser/__tests__/PythonTreeSitterParser.test.ts
  - src/infrastructure/parser/__tests__/GoParser.test.ts
  - src/cli/commands/complexity.ts
  - src/cli/commands/__tests__/complexity-command.test.ts
  - src/parser/implementations/smart-parser.ts
  - src/parser/__tests__/smart-parser.test.ts
autonomous: true
requirements:
  - POL-01
must_haves:
  truths:
    - Active TS/JS/Python/Go parser flows all source complexity from one canonical analyzer seam.
    - Shared parser/module truth and the shipped Python symbol-level truth remain the consumer-facing contract.
    - The `complexity` CLI no longer emits silent estimates or silent recompute results as canonical truth.
    - A deprecated parallel seam is proven not to drift silently from the canonical analyzer.
  artifacts:
    - path: src/core/ast-complexity-analyzer.ts
      provides: canonical language-aware complexity entrypoint for active parser flows
    - path: src/infrastructure/parser/implementations/TreeSitterParser.ts
      provides: TS/JS parser adapter to the canonical analyzer
    - path: src/infrastructure/parser/implementations/PythonTreeSitterParser.ts
      provides: Python parser adapter plus preserved symbol-level writeback from canonical details
    - path: src/infrastructure/parser/implementations/GoParser.ts
      provides: Go parser canonical complexity writeback instead of absent truth
    - path: src/cli/commands/complexity.ts
      provides: canonical-only consumer behavior with explicit non-canonical handling
    - path: src/parser/implementations/smart-parser.ts
      provides: deprecated legacy seam behavior that can no longer silently drift
  key_links:
    - from: src/infrastructure/parser/implementations/TreeSitterParser.ts
      to: src/core/ast-complexity-analyzer.ts
      via: parser-level canonical complexity delegation
      pattern: analyze.*Complexity
    - from: src/infrastructure/parser/implementations/PythonTreeSitterParser.ts
      to: src/core/ast-complexity-analyzer.ts
      via: canonical function detail source used for ParseResult and symbol backfill
      pattern: complexity.*details|attachComplexity
    - from: src/infrastructure/parser/implementations/GoParser.ts
      to: src/core/ast-complexity-analyzer.ts
      via: includeComplexity writeback on active Go parser flow
      pattern: includeComplexity|complexity
    - from: src/cli/commands/complexity.ts
      to: src/core/analyzer.ts
      via: shared module complexity truth
      pattern: module\\.complexity|symbol\\.complexity
    - from: src/parser/implementations/smart-parser.ts
      to: src/core/ast-complexity-analyzer.ts
      via: deprecated seam drift proof
      pattern: analyze.*Complexity|deprecated
---

<objective>
Land `POL-01` by converging active TS/JS/Python/Go complexity production on one canonical analyzer seam, preserving the shipped shared-truth consumer contract, and eliminating silent estimate/recompute drift in downstream readers.

Purpose: make complexity metrics trustworthy across active runtime flows without reopening parser routing or creating a second complexity contract.
Output: canonical complexity analyzer API, active parser adapters, explicit CLI handling for missing canonical truth, and regression proof that deprecated parallel seams no longer silently diverge.
</objective>

<execution_context>
@/home/stark/.codex/get-shit-done/workflows/execute-plan.md
@/home/stark/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/79-complexity-truth-unification/79-CONTEXT.md
@.planning/phases/79-complexity-truth-unification/79-RESEARCH.md
@.planning/phases/72-python-complexity-truth/72-CONTEXT.md
@.planning/phases/72-python-complexity-truth/72-01-SUMMARY.md
@src/core/ast-complexity-analyzer.ts
@src/core/analyzer.ts
@src/infrastructure/parser/implementations/TreeSitterParser.ts
@src/infrastructure/parser/implementations/PythonTreeSitterParser.ts
@src/infrastructure/parser/implementations/GoParser.ts
@src/infrastructure/parser/interfaces/ParserBase.ts
@src/cli/commands/complexity.ts
@src/parser/implementations/smart-parser.ts
@src/interface/types/parser.ts
@src/interface/types/index.ts
</context>

<tasks>
<task type="auto">
  <name>Task 79-01-01: Lock the canonical analyzer contract and encode red drift tests</name>
  <files>src/core/ast-complexity-analyzer.ts, src/core/__tests__/ast-complexity-analyzer.test.ts, src/core/__tests__/analyzer.test.ts, src/infrastructure/parser/__tests__/PythonTreeSitterParser.test.ts, src/infrastructure/parser/__tests__/GoParser.test.ts, src/cli/commands/__tests__/complexity-command.test.ts, src/parser/__tests__/smart-parser.test.ts</files>
  <read_first>.planning/phases/79-complexity-truth-unification/79-CONTEXT.md, .planning/phases/79-complexity-truth-unification/79-RESEARCH.md, src/core/ast-complexity-analyzer.ts, src/core/__tests__/ast-complexity-analyzer.test.ts, src/infrastructure/parser/__tests__/PythonTreeSitterParser.test.ts, src/infrastructure/parser/__tests__/GoParser.test.ts, src/cli/commands/__tests__/complexity-command.test.ts, src/parser/__tests__/smart-parser.test.ts</read_first>
  <action>Define the target behavior before implementation. Extend the canonical analyzer surface just enough to express language-aware active-path complexity without yet wiring the parsers. Add failing tests that pin these concrete behaviors: (1) the canonical analyzer exposes a content/language-aware entrypoint that can produce `ComplexityMetrics` for TypeScript/JavaScript, Python, and Go inputs under one module; (2) Phase 72 Python persisted-truth behavior stays intact when the parser switches to the canonical source; (3) the active Go parser must return real complexity when `includeComplexity` is requested instead of omitting it; (4) `complexity` CLI must stop silently estimating module complexity or silently recomputing AST detail when canonical truth is missing; (5) the deprecated `smart-parser` seam must either delegate to the canonical analyzer or surface explicit non-canonical/deprecated behavior, but may not silently keep an unrelated formula. Keep the suite red until all active-path adapters and CLI behavior changes land.</action>
  <verify>rtk ./node_modules/.bin/vitest run src/core/__tests__/ast-complexity-analyzer.test.ts src/core/__tests__/analyzer.test.ts src/infrastructure/parser/__tests__/PythonTreeSitterParser.test.ts src/infrastructure/parser/__tests__/GoParser.test.ts src/cli/commands/__tests__/complexity-command.test.ts src/parser/__tests__/smart-parser.test.ts</verify>
  <acceptance_criteria>
    - The test suite encodes a single canonical analyzer API rather than parser-local expectations.
    - There is at least one failing test for each active language path: TS/JS, Python, Go.
    - There is at least one failing consumer test proving silent estimate/recompute drift is no longer acceptable.
    - There is at least one failing regression proof covering the deprecated `smart-parser` seam.
  </acceptance_criteria>
  <done>Canonical contract and drift expectations are pinned in red tests across active parsers, shared analyzer output, CLI consumer behavior, and the legacy seam.</done>
</task>

<task type="auto">
  <name>Task 79-01-02: Route active parser complexity production through the canonical analyzer</name>
  <files>src/core/ast-complexity-analyzer.ts, src/infrastructure/parser/implementations/TreeSitterParser.ts, src/infrastructure/parser/implementations/PythonTreeSitterParser.ts, src/infrastructure/parser/implementations/GoParser.ts, src/infrastructure/parser/interfaces/ParserBase.ts, src/core/__tests__/analyzer.test.ts, src/infrastructure/parser/__tests__/PythonTreeSitterParser.test.ts, src/infrastructure/parser/__tests__/GoParser.test.ts</files>
  <read_first>src/core/ast-complexity-analyzer.ts, src/infrastructure/parser/implementations/TreeSitterParser.ts, src/infrastructure/parser/implementations/PythonTreeSitterParser.ts, src/infrastructure/parser/implementations/GoParser.ts, src/infrastructure/parser/interfaces/ParserBase.ts, src/core/analyzer.ts, .planning/phases/72-python-complexity-truth/72-CONTEXT.md, .planning/phases/79-complexity-truth-unification/79-RESEARCH.md</read_first>
  <action>Turn `src/core/ast-complexity-analyzer.ts` into the only active-path complexity entrypoint. Keep the current TS/JS AST implementation as one adapter, then add the minimum language-aware entrypoints needed so active parsers can call the same module from in-memory content rather than by temp file. Replace `TreeSitterParser`'s regex-based complexity implementation with a canonical analyzer call. Replace Python's parser-local complexity formula with a canonical analyzer call while preserving Phase 72 symbol-level writeback and shared `ParseResult.complexity` behavior. Add canonical complexity writeback to `GoParser.parseFile()` when `includeComplexity` is requested instead of leaving Go without complexity truth. Keep `ParserBase` as a non-canonical stub only for inactive languages; active languages must never leak the placeholder `1/1/100` result. Do not redesign parser routing, create a second complexity contract, or broaden into preview/plugin/helper cleanup.</action>
  <verify>rtk ./node_modules/.bin/vitest run src/core/__tests__/ast-complexity-analyzer.test.ts src/core/__tests__/analyzer.test.ts src/infrastructure/parser/__tests__/PythonTreeSitterParser.test.ts src/infrastructure/parser/__tests__/GoParser.test.ts</verify>
  <acceptance_criteria>
    - Active TS/JS parser complexity comes from `src/core/ast-complexity-analyzer.ts`, not `TreeSitterParser` regex logic.
    - Python complexity source moves to the canonical analyzer without losing shared module truth or symbol-level writeback.
    - Go parser returns canonical complexity on the active path when requested.
    - Shared analyzer output still persists module complexity through `ParseResult.complexity`.
    - No active path depends on `ParserBase.calculateComplexity()` placeholder output.
  </acceptance_criteria>
  <done>All active parser flows produce complexity through the canonical analyzer and shared analyzer output remains intact.</done>
</task>

<task type="auto">
  <name>Task 79-01-03: Remove silent downstream drift and prove the deprecated seam is explicit</name>
  <files>src/cli/commands/complexity.ts, src/cli/commands/__tests__/complexity-command.test.ts, src/parser/implementations/smart-parser.ts, src/parser/__tests__/smart-parser.test.ts, src/core/__tests__/analyzer.test.ts</files>
  <read_first>src/cli/commands/complexity.ts, src/cli/commands/__tests__/complexity-command.test.ts, src/parser/implementations/smart-parser.ts, src/parser/__tests__/smart-parser.test.ts, src/core/__tests__/analyzer.test.ts, .planning/phases/79-complexity-truth-unification/79-CONTEXT.md, .planning/phases/79-complexity-truth-unification/79-RESEARCH.md</read_first>
  <action>Finish the unification at the consumer and legacy seams. Remove module-level estimate fallback from `getModuleComplexity()` and remove silent AST recompute as the default answer path in `complexityCommand()`. Replace those branches with explicit canonical-only behavior: use shared persisted truth when present; if canonical truth is missing, return a visible actionable warning or failure that clearly marks the result as non-canonical instead of synthesizing numbers. Preserve the existing file-scoped JSON contract and the shipped Python shared-truth proof path. Then handle the deprecated `smart-parser` seam deliberately: either delegate its complexity calculation to the canonical analyzer or make its non-canonical/deprecated status explicit and covered by tests, but do not leave an unrelated silent formula in place. Add at least one regression assertion that old and new seams cannot silently diverge on the same fixture.</action>
  <verify>rtk ./node_modules/.bin/vitest run src/cli/commands/__tests__/complexity-command.test.ts src/parser/__tests__/smart-parser.test.ts src/core/__tests__/analyzer.test.ts && rtk npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `complexity` CLI no longer returns silent estimates as canonical truth.
    - Missing canonical truth is explicit, actionable, and test-backed.
    - Existing shared-truth JSON/read contracts remain stable where data is canonical.
    - The deprecated `smart-parser` seam no longer silently diverges from the canonical analyzer.
    - Typecheck and focused regression tests pass.
  </acceptance_criteria>
  <done>Downstream consumer behavior is canonical-only, legacy drift is explicit or eliminated, and focused regression/type checks pass.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parser-local complexity logic -> shared parser truth | Parallel formulas can silently drift while still producing valid-looking `ComplexityMetrics`. |
| shared module truth -> CLI consumer | Missing persisted truth can be masked by heuristic estimates or recompute fallbacks. |
| deprecated compatibility parser -> active runtime expectations | Legacy callers can keep surfacing different numbers unless the seam is explicitly bridged or marked non-canonical. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01 | Integrity | active parser adapters | mitigate | Route every active parser through one canonical analyzer module and cover TS/JS/Python/Go with focused tests. |
| T-79-02 | Integrity | CLI consumer behavior | mitigate | Remove silent estimate/recompute branches and replace them with explicit canonical-only behavior plus actionable remediation. |
| T-79-03 | Integrity | deprecated smart-parser seam | mitigate | Add explicit regression proof that the legacy seam delegates to or is explicitly fenced from the canonical analyzer. |
| T-79-04 | Scope creep | broader complexity cleanup | mitigate | Keep preview/plugin/helper cleanup deferred unless it directly blocks the active-path canonical claim. |
</threat_model>

<verification>
- [ ] rtk ./node_modules/.bin/vitest run src/core/__tests__/ast-complexity-analyzer.test.ts
- [ ] rtk ./node_modules/.bin/vitest run src/core/__tests__/analyzer.test.ts
- [ ] rtk ./node_modules/.bin/vitest run src/infrastructure/parser/__tests__/PythonTreeSitterParser.test.ts
- [ ] rtk ./node_modules/.bin/vitest run src/infrastructure/parser/__tests__/GoParser.test.ts
- [ ] rtk ./node_modules/.bin/vitest run src/cli/commands/__tests__/complexity-command.test.ts
- [ ] rtk ./node_modules/.bin/vitest run src/parser/__tests__/smart-parser.test.ts
- [ ] rtk npx tsc --noEmit
- [ ] rtk rg -n "complexity|analyze.*Complexity|includeComplexity|deprecated" src/core/ast-complexity-analyzer.ts src/infrastructure/parser/implementations/TreeSitterParser.ts src/infrastructure/parser/implementations/PythonTreeSitterParser.ts src/infrastructure/parser/implementations/GoParser.ts src/cli/commands/complexity.ts src/parser/implementations/smart-parser.ts
</verification>

<success_criteria>
- All active TS/JS/Python/Go parser flows source complexity from one canonical analyzer seam.
- Shared parser/module truth and shipped Python symbol-level truth remain intact.
- `complexity` CLI no longer emits silent estimated or silently recomputed complexity as canonical truth.
- At least one deprecated parallel seam is proven not to drift silently from the canonical analyzer.
- Focused parser, analyzer, CLI, legacy regression tests and typecheck all pass.
</success_criteria>

<output>
After completion, create `.planning/phases/79-complexity-truth-unification/79-01-SUMMARY.md`
</output>
