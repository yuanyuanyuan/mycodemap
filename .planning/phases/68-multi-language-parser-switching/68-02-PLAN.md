---
phase: 68-multi-language-parser-switching
plan: 02
type: execute
wave: 2
depends_on:
  - "68-01"
files_modified:
  - src/infrastructure/parser/__tests__/ParserRegistry.test.ts
  - src/infrastructure/parser/__tests__/TypeScriptParser.test.ts
  - src/infrastructure/parser/__tests__/PythonTreeSitterParser.test.ts
  - src/core/__tests__/analyzer.test.ts
  - src/parser/__tests__/tree-sitter-parser.test.ts
  - tests/fixtures/python/*.py
autonomous: true
requirements:
  - PY-03
  - PY-04
---

<objective>
Prove the shared parser cutover with behavior-level regression tests. This plan exists so Phase 68 does not stop at implementation wiring: it must demonstrate that the same shared parser can parse `.ts` and `.py`, that TS/JS main-path routing actually changed, and that Python still fails closed instead of silently falling back.
</objective>

<must_haves>
- There is at least one focused test file that exercises the same shared `TreeSitterParser` implementation against both a `.ts` input and a `.py` input.
- Registry tests prove TS/JS and Python both resolve to shared Tree-sitter-backed parsing behavior on the active path.
- Analyzer-level tests no longer assume the active TS path is regex `TypeScriptParser`.
- Python failure-path verification proves missing grammar still raises an explicit error rather than silently using regex.
- Verification covers regression-sensitive outputs: imports, exports, symbols, and `parserUsed`.
</must_haves>

<tasks>
<task type="auto">
  <name>Task 68-02-01: Add shared parser cross-language proof tests</name>
  <files>src/parser/__tests__/tree-sitter-parser.test.ts, tests/fixtures/python/*.py</files>
  <read_first>
    - /data/codemap/src/parser/implementations/tree-sitter-parser.ts
    - /data/codemap/src/infrastructure/parser/__tests__/PythonTreeSitterParser.test.ts
    - /data/codemap/src/infrastructure/parser/__tests__/TypeScriptParser.test.ts
    - /data/codemap/tests/fixtures/python/comprehensive.py
    - /data/codemap/.planning/phases/68-multi-language-parser-switching/68-CONTEXT.md
  </read_first>
  <action>
Create a dedicated test file `src/parser/__tests__/tree-sitter-parser.test.ts` that instantiates the shared `TreeSitterParser` implementation directly and proves it can parse both a TypeScript input and a Python input using the same class. The TypeScript case must assert concrete structural output such as import/export/symbol presence; the Python case must assert concrete structural output such as nested definitions, imports, or decorators using the shared parser rather than the old Python-only parser test harness. If existing fixtures are insufficient for the `.ts` proof, add a minimal inline TS fixture or small dedicated fixture file. Include assertions on `parserUsed` if Phase 68 uses that field to prove cutover identity.
  </action>
  <acceptance_criteria>
    - `src/parser/__tests__/tree-sitter-parser.test.ts` exists
    - `src/parser/__tests__/tree-sitter-parser.test.ts` contains `TreeSitterParser`
    - `src/parser/__tests__/tree-sitter-parser.test.ts` contains both `.ts` and `.py`
    - `src/parser/__tests__/tree-sitter-parser.test.ts` asserts at least one concrete TS structural output
    - `src/parser/__tests__/tree-sitter-parser.test.ts` asserts at least one concrete Python structural output
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 68-02-02: Update registry and analyzer tests to reflect the TS/JS shared-parser cutover</name>
  <files>src/infrastructure/parser/__tests__/ParserRegistry.test.ts, src/infrastructure/parser/__tests__/TypeScriptParser.test.ts, src/core/__tests__/analyzer.test.ts</files>
  <read_first>
    - /data/codemap/src/infrastructure/parser/__tests__/ParserRegistry.test.ts
    - /data/codemap/src/infrastructure/parser/__tests__/TypeScriptParser.test.ts
    - /data/codemap/src/core/__tests__/analyzer.test.ts
    - /data/codemap/src/infrastructure/parser/index.ts
    - /data/codemap/src/infrastructure/parser/implementations/TypeScriptParser.ts
    - /data/codemap/src/parser/implementations/tree-sitter-parser.ts
  </read_first>
  <action>
Update the tests that currently encode the regex `TypeScriptParser` as the active TS path. In `ParserRegistry.test.ts`, add or update assertions so the registry-level active TS/JS resolution proves shared Tree-sitter-backed behavior rather than merely showing `TypeScriptParser` registration by name. In `TypeScriptParser.test.ts`, keep behavior-level assertions (imports, exports, stats) but ensure they now validate the shared Tree-sitter-backed implementation instead of the old regex assumptions. In `src/core/__tests__/analyzer.test.ts`, stop spying on `TypeScriptParser.prototype.parseFile` as the proof of TS parsing and instead spy on the actual active shared path or assert `parserUsed`/shared behavior through analyzer outputs, so the test genuinely detects whether the main runtime cutover happened.
  </action>
  <acceptance_criteria>
    - `src/core/__tests__/analyzer.test.ts` does not contain `TypeScriptParser.prototype.parseFile`
    - `src/infrastructure/parser/__tests__/ParserRegistry.test.ts` contains an assertion that distinguishes shared Tree-sitter-backed TS/JS routing from the old regex default
    - `src/infrastructure/parser/__tests__/TypeScriptParser.test.ts` still asserts imports and exports on TS inputs after the cutover
    - At least one updated test checks `parserUsed` or another concrete shared-parser identity signal
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 68-02-03: Lock Python strict-failure and no-regression verification into tests</name>
  <files>src/infrastructure/parser/__tests__/PythonTreeSitterParser.test.ts</files>
  <read_first>
    - /data/codemap/src/infrastructure/parser/__tests__/PythonTreeSitterParser.test.ts
    - /data/codemap/src/infrastructure/parser/implementations/PythonTreeSitterParser.ts
    - /data/codemap/src/parser/implementations/tree-sitter-loader.ts
    - /data/codemap/.planning/phases/67-tree-sitter-python-grammar/67-CONTEXT.md
    - /data/codemap/.planning/phases/68-multi-language-parser-switching/68-CONTEXT.md
  </read_first>
  <action>
Extend `src/infrastructure/parser/__tests__/PythonTreeSitterParser.test.ts` so Phase 68 has explicit proof that the Python path still fails closed when grammar is unavailable and does not silently delegate to regex fallback. Use mocking or dependency interception to simulate grammar-unavailable conditions if necessary, and assert the error message still contains the explicit no-fallback guidance. Keep the existing AST superiority proof from Phase 67, but update it where needed so it remains valid after the shared-capability cutover. If `parserUsed` changes to the shared identity string, update assertions accordingly while preserving the core semantic checks (nested definitions, decorators, async, multiline imports).
  </action>
  <acceptance_criteria>
    - `src/infrastructure/parser/__tests__/PythonTreeSitterParser.test.ts` contains a grammar-unavailable failure assertion
    - The failure assertion checks explicit no-fallback wording such as `No silent fallback` or an equivalent strict-error phrase
    - Existing nested-definition / multiline-import superiority checks remain present
    - The updated test file still asserts Python AST behavior beyond a trivial parse success
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
- Tests are the only reliable proof that main-path routing changed instead of only local implementation details changing.
- Python grammar failure semantics protect analysis integrity by preventing silent downgrade.

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-02-1 | Repudiation | cutover proof | mitigate | Add direct shared-parser cross-language tests and analyzer-level assertions that detect real main-path routing changes. |
| T-68-02-2 | Tampering | Python fallback behavior | mitigate | Add explicit tests for grammar-unavailable strict failure so silent regex fallback cannot return unnoticed. |
| T-68-02-3 | Integrity | TS/JS no-regression claim | mitigate | Keep behavior-level TS parser/analyzer assertions after cutover instead of only class-construction tests. |
</threat_model>

<verification>
- [ ] `rtk npx tsc --noEmit`
- [ ] `rtk ./node_modules/.bin/vitest run src/parser/__tests__/tree-sitter-parser.test.ts`
- [ ] `rtk ./node_modules/.bin/vitest run src/infrastructure/parser/__tests__/ParserRegistry.test.ts`
- [ ] `rtk ./node_modules/.bin/vitest run src/infrastructure/parser/__tests__/TypeScriptParser.test.ts src/infrastructure/parser/__tests__/PythonTreeSitterParser.test.ts`
- [ ] `rtk ./node_modules/.bin/vitest run src/core/__tests__/analyzer.test.ts`
</verification>

<success_criteria>
- One shared parser implementation is proven on both `.ts` and `.py`
- Registry/analyzer tests prove TS/JS main-path cutover actually happened
- Python strict no-fallback behavior is explicitly verified
- TS/JS parsing behavior remains covered at behavior level after the cutover
</success_criteria>

<output>
After completion, create `.planning/phases/68-multi-language-parser-switching/68-02-SUMMARY.md`
</output>
