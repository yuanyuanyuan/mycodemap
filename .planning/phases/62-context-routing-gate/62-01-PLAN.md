---
phase: 62-context-routing-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/mcp/server.ts
  - src/server/mcp/service.ts
  - src/server/mcp/types.ts
  - src/server/mcp/context-tool.ts
  - src/server/mcp/__tests__/context-tool.test.ts
  - src/server/mcp/__tests__/dynamic-server.test.ts
  - src/server/mcp/__tests__/CodeMapMcpServer.test.ts
autonomous: true
requirements:
  - CTX-01
  - CTX-02
user_setup: []
must_haves:
  truths:
    - "`codemap_context` ships as a real native MCP tool and accepts `review`, `debug`, and `default` task requests rather than falling back to `cli_redirect` or CLI prose."
    - "The routing payload always includes real `graphStats`, `riskSummary`, and `nextToolSuggestions`, with counts sourced from storage metadata/statistics instead of guessed constants."
    - "Suggested tools map to real executable MCP/contract tool names registered in the repo, and the task-to-tool mapping stays static-first with only small runtime adjustments."
  artifacts:
    - path: "src/server/mcp/server.ts"
      provides: "Native MCP registration for `codemap_context`"
      contains: "codemap_context"
    - path: "src/server/mcp/context-tool.ts"
      provides: "Transport-free routing builder that assembles graph stats, risk summary, and suggestions"
      contains: "nextToolSuggestions"
    - path: "src/server/mcp/types.ts"
      provides: "Structured result typing for the routing gate"
      contains: "riskSummary"
  key_links:
    - from: "src/server/mcp/server.ts"
      to: "src/server/mcp/context-tool.ts"
      via: "native tool handler delegates to the routing builder instead of embedding business logic in `registerTool()`"
      pattern: "codemap_context"
    - from: "src/server/mcp/context-tool.ts"
      to: "src/cli/interface-contract/index.ts"
      via: "suggestion generation validates against the real command/tool catalog"
      pattern: "getFullContract"
---

<objective>
Establish the Phase 62 routing truth: a native `codemap_context` MCP tool that returns a thin, structured routing payload with real graph stats, compact risk summary, and task-based next-step suggestions.

Purpose: The routing gate only has value if it sits on top of the real MCP execution surface and points agents at tools they can actually run.
Output: one transport-free routing builder, native MCP registration, and base success/failure proof for `review` / `debug` / `default`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-context-routing-gate/62-CONTEXT.md
@.planning/phases/62-context-routing-gate/62-RESEARCH.md
@src/server/mcp/server.ts
@src/server/mcp/service.ts
@src/server/mcp/types.ts
@src/cli/interface-contract/index.ts
@src/cli/interface-contract/commands/index.ts
@src/interface/types/storage.ts
@src/server/mcp/__tests__/dynamic-server.test.ts
@src/server/mcp/__tests__/env-contract-tool.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 62-01-01: Build the transport-free routing payload for review/debug/default</name>
  <files>src/server/mcp/context-tool.ts, src/server/mcp/service.ts, src/server/mcp/types.ts</files>
  <read_first>
    - src/server/mcp/service.ts
    - src/server/mcp/types.ts
    - src/interface/types/storage.ts
    - src/cli/interface-contract/index.ts
    - src/cli/interface-contract/commands/index.ts
  </read_first>
  <action>
Create a dedicated routing builder outside MCP transport registration. It should accept the current task (`review`, `debug`, or `default`) and use existing storage capabilities to assemble a thin payload: `graphStats` from `loadGraphMetadata()` + `getStatistics()`, compact `riskSummary` from graph health / cycle count / parse-failure facts, and `nextToolSuggestions` from a static-first task mapping validated against the real tool catalog. Keep the payload intentionally small; do not introduce full-snippet dossiers, community metadata, or a second execution layer. Missing graph truth should degrade visibly through warnings or reduced-confidence output instead of silent fallback.
  </action>
  <verify>
    rtk rg -n "review|debug|default|graphStats|riskSummary|nextToolSuggestions" src/server/mcp/context-tool.ts src/server/mcp/service.ts src/server/mcp/types.ts
    rtk npx vitest run src/server/mcp/__tests__/context-tool.test.ts
    rtk tsc --noEmit
  </verify>
  <acceptance_criteria>
    - routing logic lives outside `server.ts` transport registration
    - `graphStats` exposes modules, symbols, and edges from real storage-backed values
    - `riskSummary` includes compact factors instead of a raw opaque score only
    - suggestions are derived from real registered tools, not a doc-only list
    - missing graph truth is surfaced explicitly
  </acceptance_criteria>
  <done>
    - Phase 62 has one routing truth seam
    - the tool can answer three task families without deep analysis bloat
    - graph and tool facts are code-backed
  </done>
</task>

<task type="auto">
  <name>Task 62-01-02: Register `codemap_context` as a native MCP tool and prove base routing behavior</name>
  <files>src/server/mcp/server.ts, src/server/mcp/__tests__/context-tool.test.ts, src/server/mcp/__tests__/dynamic-server.test.ts, src/server/mcp/__tests__/CodeMapMcpServer.test.ts</files>
  <read_first>
    - src/server/mcp/server.ts
    - src/server/mcp/__tests__/dynamic-server.test.ts
    - src/server/mcp/__tests__/CodeMapMcpServer.test.ts
    - src/server/mcp/__tests__/env-contract-tool.test.ts
    - src/server/mcp/context-tool.ts
  </read_first>
  <action>
Wire the routing builder into `registerNativeTools()` as `codemap_context`. Keep the MCP return shape aligned with existing native tools: JSON `content`, `structuredContent`, and `isError` derived from the routing result. Add focused tests proving the tool is discoverable through MCP transport, supports the three task values, and returns explicit failure or warning semantics when graph truth is missing or input is invalid. Do not route this tool through `schema-adapter.ts`; Phase 62 should extend the native-tool pattern, not reintroduce redirect indirection.
  </action>
  <verify>
    rtk rg -n "codemap_context|review|debug|default" src/server/mcp/server.ts src/server/mcp/__tests__/context-tool.test.ts src/server/mcp/__tests__/dynamic-server.test.ts src/server/mcp/__tests__/CodeMapMcpServer.test.ts
    rtk npx vitest run src/server/mcp/__tests__/context-tool.test.ts src/server/mcp/__tests__/dynamic-server.test.ts src/server/mcp/__tests__/CodeMapMcpServer.test.ts
    rtk tsc --noEmit
  </verify>
  <acceptance_criteria>
    - `listTools()` exposes `codemap_context`
    - direct MCP calls return structured routing content rather than command-string suggestions
    - invalid-input or missing-graph behavior is covered by tests
    - registration stays in the native-tool layer, not the contract redirect layer
  </acceptance_criteria>
  <done>
    - agents can call `codemap_context`
    - routing behavior is observable end-to-end through MCP transport
    - the phase now has a stable base for detail-level and filtering work
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Tool suggestions are agent-consumed control signals; recommending a nonexistent or non-executable tool is a correctness failure.
- Graph health affects routing quality; missing graph truth must not be misrepresented as confident guidance.

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01-1 | Spoofing | suggested tool catalog | mitigate | Validate static task mappings against the real native + contract tool universe before returning suggestions. |
| T-62-01-2 | Repudiation | graph-backed routing confidence | mitigate | Surface missing/partial graph truth through explicit warnings or reduced-confidence routing output rather than silent success. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `codemap_context` is registered as a native MCP tool
- [ ] `review`, `debug`, and `default` all return structured routing output
- [ ] `graphStats` proves modules/symbols/edges from storage truth
- [ ] At least one failure or degraded-truth path is tested
- [ ] `rtk tsc --noEmit` passes after the new routing types/service work
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Agents can retrieve lightweight routing context through one MCP call
- Base routing payload is real, thin, and executable-tool-aware

</success_criteria>

<output>
After completion, create `.planning/phases/62-context-routing-gate/62-01-SUMMARY.md`
</output>
