---
phase: 62-context-routing-gate
plan: 02
type: execute
wave: 2
depends_on:
  - "62-01"
files_modified:
  - src/server/mcp/server.ts
  - src/server/mcp/service.ts
  - src/server/mcp/types.ts
  - src/server/mcp/context-tool.ts
  - src/server/mcp/__tests__/context-tool.test.ts
  - src/server/mcp/__tests__/dynamic-server.test.ts
  - src/server/mcp/__tests__/CodeMapMcpServer.test.ts
  - AI_GUIDE.md
  - ARCHITECTURE.md
autonomous: true
requirements:
  - CTX-03
  - CTX-04
user_setup: []
must_haves:
  truths:
    - "`codemap_context` supports at least `minimal` and `standard` detail levels, and `minimal` is observably shorter while preserving routing value."
    - "Tool filtering is strict: irrelevant tools are removed, but the response never filters out a tool required by its own suggestions."
    - "Focused verification covers invalid task input, missing/stale graph truth, filter regressions, and length differentiation rather than only happy-path MCP calls."
  artifacts:
    - path: "src/server/mcp/context-tool.ts"
      provides: "Detail-level branching and strict tool-filter enforcement"
      contains: "detailLevel"
    - path: "src/server/mcp/__tests__/context-tool.test.ts"
      provides: "Focused proof for minimal-vs-standard and filter invariants"
      contains: "minimal"
    - path: "AI_GUIDE.md"
      provides: "Agent-facing documentation for the new routing gate"
      contains: "codemap_context"
  key_links:
    - from: "src/server/mcp/context-tool.ts"
      to: "src/server/mcp/__tests__/context-tool.test.ts"
      via: "payload compression and filter invariants are locked by focused tests"
      pattern: "detailLevel|nextToolSuggestions"
    - from: "ARCHITECTURE.md"
      to: "src/server/mcp/server.ts"
      via: "docs describe the same native MCP routing surface shipped in code"
      pattern: "codemap_context"
---

<objective>
Finish the routing contract: add `minimal` / `standard` detail levels, strict tool filtering, focused failure-path verification, and documentation sync for the new public MCP surface.

Purpose: Phase 62 is not complete if the routing gate exists but still cannot prove compression, filtering correctness, or agent-facing contract clarity.
Output: detail-level-aware routing responses, filter invariants, verification evidence, and synchronized docs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-context-routing-gate/62-CONTEXT.md
@.planning/phases/62-context-routing-gate/62-RESEARCH.md
@.planning/phases/62-context-routing-gate/62-01-PLAN.md
@src/server/mcp/server.ts
@src/server/mcp/service.ts
@src/server/mcp/types.ts
@src/server/mcp/context-tool.ts
@src/server/mcp/__tests__/context-tool.test.ts
@src/server/mcp/__tests__/dynamic-server.test.ts
@AI_GUIDE.md
@ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 62-02-01: Add `minimal` / `standard` detail levels and enforce strict tool filtering</name>
  <files>src/server/mcp/context-tool.ts, src/server/mcp/service.ts, src/server/mcp/types.ts, src/server/mcp/server.ts</files>
  <read_first>
    - src/server/mcp/context-tool.ts
    - src/server/mcp/types.ts
    - src/server/mcp/service.ts
    - src/server/mcp/server.ts
    - src/execution/contract-tools/analyze.ts
  </read_first>
  <action>
Extend the routing contract with explicit `detailLevel` support for `minimal` and `standard`. `minimal` must stay aggressively small: short summary, key counts, compact risk summary, and 2-3 next-step suggestions only. `standard` may add rationale, warnings, and candidate-file or hotspot hints where they are cheaply available, but it must not turn into a dossier-style payload. Add strict tool filtering so callers can narrow the exposed suggestion set, and validate the invariant that no response recommends a tool the same filter has removed. If a filter would invalidate all required suggestions, return an explicit warning or structured error rather than a misleading payload.
  </action>
  <verify>
    rtk rg -n "detailLevel|minimal|standard|nextToolSuggestions|warnings" src/server/mcp/context-tool.ts src/server/mcp/service.ts src/server/mcp/types.ts src/server/mcp/server.ts
    rtk npx vitest run src/server/mcp/__tests__/context-tool.test.ts
    rtk tsc --noEmit
  </verify>
  <acceptance_criteria>
    - `minimal` and `standard` are explicit contract branches, not ad hoc text differences
    - `minimal` keeps only the thin routing payload
    - filtering removes irrelevant tools without breaking required suggestions
    - broken filters fail visibly instead of silently producing invalid guidance
  </acceptance_criteria>
  <done>
    - payload depth is controllable
    - tool exposure is strict and predictable
    - routing remains lightweight under agent token budgets
  </done>
</task>

<task type="auto">
  <name>Task 62-02-02: Add focused verification for compression/filtering/staleness and sync routing docs</name>
  <files>src/server/mcp/__tests__/context-tool.test.ts, src/server/mcp/__tests__/dynamic-server.test.ts, src/server/mcp/__tests__/CodeMapMcpServer.test.ts, AI_GUIDE.md, ARCHITECTURE.md</files>
  <read_first>
    - src/server/mcp/__tests__/context-tool.test.ts
    - src/server/mcp/__tests__/dynamic-server.test.ts
    - src/server/mcp/__tests__/CodeMapMcpServer.test.ts
    - AI_GUIDE.md
    - ARCHITECTURE.md
    - .planning/phases/62-context-routing-gate/62-RESEARCH.md
  </read_first>
  <action>
Add focused tests that prove the final routing contract rather than just basic tool registration. At minimum, cover: invalid task input, missing graph truth, stale graph truth, filters that would hide a necessary tool, and a measurable `minimal` vs `standard` size difference. Keep at least one end-to-end MCP transport test in addition to focused unit coverage. Then update `AI_GUIDE.md` and `ARCHITECTURE.md` only where needed so agents and maintainers can see the new `codemap_context` surface, supported tasks, detail levels, and tool-filtering behavior without implying deeper graph capabilities than Phase 62 actually ships.
  </action>
  <verify>
    rtk rg -n "codemap_context|detailLevel|minimal|standard|nextToolSuggestions" src/server/mcp/__tests__/context-tool.test.ts src/server/mcp/__tests__/dynamic-server.test.ts AI_GUIDE.md ARCHITECTURE.md
    rtk npx vitest run src/server/mcp/__tests__/context-tool.test.ts src/server/mcp/__tests__/dynamic-server.test.ts src/server/mcp/__tests__/CodeMapMcpServer.test.ts
    rtk npx vitest run src/server/mcp/__tests__/context-tool.test.ts -t "invalid|missing|stale|filter|minimal|standard"
    rtk tsc --noEmit
  </verify>
  <acceptance_criteria>
    - tests prove `minimal` is observably shorter than `standard`
    - tests prove stale or missing graph truth is surfaced explicitly
    - tests prove filter invariants, including the “necessary tool hidden” failure mode
    - docs describe the shipped routing contract and no more
  </acceptance_criteria>
  <done>
    - failure-path evidence exists for the routing contract
    - docs and runtime tell one story
    - Phase 62 can close with proof instead of inference
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Detail levels are agent-facing contract controls; if `minimal` silently expands, token-budget routing loses its point.
- Tool filtering changes what actions an agent can see next; over-filtering can make the response internally inconsistent.

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-02-1 | Denial of service | filtered routing suggestions | mitigate | Reject or warn on filters that remove tools required by the route’s own recommended next steps. |
| T-62-02-2 | Information disclosure | detail-level contract drift | mitigate | Lock `minimal` vs `standard` with focused tests and doc truth so payload growth is visible. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `detailLevel` supports `minimal` and `standard`
- [ ] `minimal` is observably shorter than `standard`
- [ ] Tool filtering is strict and cannot hide required suggestions silently
- [ ] Stale/missing graph truth is covered by focused tests
- [ ] `AI_GUIDE.md` and `ARCHITECTURE.md` reflect the shipped routing contract
- [ ] `rtk tsc --noEmit` passes after the final routing/doc sync
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Routing detail levels are explicit and testable
- Tool filtering prevents irrelevant exposure without breaking the route

</success_criteria>

<output>
After completion, create `.planning/phases/62-context-routing-gate/62-02-SUMMARY.md`
</output>
