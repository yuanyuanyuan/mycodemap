---
phase: "53-bootstrap-profiles-project-detection"
plan_id: "53-03"
wave: 2
depends_on: ["53-01", "53-02"]
requirements_addressed: ["FRC-01", "FRC-02", "FRC-03", "FRC-04"]
files_modified:
  - src/cli/init/__tests__/detect.test.ts
  - src/cli/init/__tests__/profile-loader.test.ts
  - src/cli/commands/__tests__/init-profile.test.ts
  - src/cli/first-run-guide.ts
  - src/cli/__tests__/first-run-guide.test.ts
autonomous: true
must_haves:
  truths:
    - "detect.test.ts covers all 4 marker files + no markers + multi-marker scenarios"
    - "profile-loader.test.ts covers all 5 profiles + validation failure + path traversal rejection"
    - "init-profile.test.ts covers preview mode, apply mode with -y, existing config skip, non-TTY refusal, --profile bypass, multi-marker interactive selection"
    - "first-run-guide.ts mentions that init will recommend a profile if project type is detected"
    - "All tests pass with npx vitest run"
  artifacts:
    - path: "src/cli/init/__tests__/detect.test.ts"
      provides: "Unit tests for marker detection"
      min_lines: 60
    - path: "src/cli/init/__tests__/profile-loader.test.ts"
      provides: "Unit tests for profile loading and validation"
      min_lines: 80
    - path: "src/cli/commands/__tests__/init-profile.test.ts"
      provides: "Integration tests for profile in init command"
      min_lines: 120
    - path: "src/cli/first-run-guide.ts"
      provides: "Updated first-run guide text"
      contains: "profile"
  key_links:
    - from: "init-profile.test.ts"
      to: "src/cli/commands/init.ts"
      via: "executeInitCommand calls"
      pattern: "executeInitCommand"
    - from: "detect.test.ts"
      to: "src/cli/init/detect.ts"
      via: "detectProjectType calls"
      pattern: "detectProjectType"
    - from: "profile-loader.test.ts"
      to: "src/cli/init/profile-loader.ts"
      via: "loadProfile calls"
      pattern: "loadProfile"
---

<objective>
Create comprehensive tests for the detection and profile system, and update the first-run guide text. This plan validates that all locked decisions (D-01 through D-17) are correctly implemented and that the integration works end-to-end.

Purpose: Without tests, the detection and profile system cannot be verified. This plan delivers the verification layer and the lightweight first-run guide update (D-15).
Output: 3 test files and updated first-run guide.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

## Existing test patterns to replicate

### From src/cli/commands/__tests__/init-command.test.ts
```typescript
function createTempProject(): string {
  return mkdtempSync(path.join(tmpdir(), 'codemap-init-command-'));
}
vi.mock('chalk', () => ({
  default: {
    blue: (text: string) => text,
    cyan: (text: string) => text,
    gray: (text: string) => text,
    // ...
  },
}));
```

### From src/cli/commands/__tests__/init-rules.test.ts
```typescript
const tempRoots: string[] = [];
let consoleLogSpy: ReturnType<typeof vi.spyOn>;
beforeEach(() => { consoleLogSpy = vi.spyOn(console, 'log').mockImplementation(() => {}); });
afterEach(() => {
  consoleLogSpy.mockRestore();
  while (tempRoots.length > 0) { const root = tempRoots.pop(); if (root) rmSync(root, { recursive: true, force: true }); }
});
```

### From src/cli/__tests__/first-run-guide.test.ts
```typescript
it('shows a concise init → generate → help guide without hook or rules setup', () => {
  showFirstRunGuide();
  const output = consoleLogSpy.mock.calls.flat().join('\n');
  expect(output).toContain('mycodemap init');
});
```

### TTY mocking pattern (from src/cli/output/mode.ts tests, if any)
Use `vi.spyOn(process.stdout, 'isTTY', 'get')` to mock TTY state.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create detect.test.ts — unit tests for marker detection</name>
  <files>src/cli/init/__tests__/detect.test.ts</files>
  <read_first>
    - src/cli/init/detect.ts (to understand exports and behavior)
    - src/cli/commands/__tests__/init-command.test.ts (for temp project and chalk mock patterns)
  </read_first>
  <action>
Create `src/cli/init/__tests__/detect.test.ts` with the following test cases:

1. **Setup:**
   - Import `describe, expect, it` from `vitest`.
   - Import `mkdtempSync, writeFileSync, rmSync` from `node:fs`, `tmpdir` from `node:os`, `path` from `node:path`.
   - Import `detectProjectType` from `../detect.js`.
   - Helper `createTempDir(): string` returns `mkdtempSync(path.join(tmpdir(), 'codemap-detect-'))`.
   - `const tempDirs: string[] = [];` with `afterEach` cleanup.

2. **Test cases:**
   - `it('detects nodejs from package.json', () => { ... })`: Create temp dir with `package.json`, call `detectProjectType`, expect `candidates` length 1, `candidates[0].type === 'nodejs'`, `candidates[0].markerFile === 'package.json'`, `recommended === 'nodejs'`.
   - `it('detects python from pyproject.toml', () => { ... })`: Same pattern for `pyproject.toml` -> `python`.
   - `it('detects go from go.mod', () => { ... })`: Same pattern for `go.mod` -> `go`.
   - `it('detects rust from Cargo.toml', () => { ... })`: Same pattern for `Cargo.toml` -> `rust`.
   - `it('returns no candidates when no markers exist', () => { ... })`: Empty temp dir, expect `candidates` empty, `recommended === null`.
   - `it('returns all candidates and null recommendation for multiple markers', () => { ... })`: Create temp dir with both `package.json` and `Cargo.toml`, expect `candidates` length 2, `recommended === null`.
   - `it('does not read file contents', () => { ... })`: Create `package.json` with invalid JSON content, expect detection still works (only checks existence).

3. No mocking of `console.log` needed for unit tests.
  </action>
  <verify>
    <automated>npx vitest run src/cli/init/__tests__/detect.test.ts</automated>
  </verify>
  <done>
    - File exists at `src/cli/init/__tests__/detect.test.ts`
    - All 7 test cases pass
    - Tests use real temp directories and real filesystem writes
    - No mocked detection logic — tests exercise actual `detectProjectType`
  </done>
</task>

<task type="auto">
  <name>Task 2: Create profile-loader.test.ts — unit tests for profile loading and validation</name>
  <files>src/cli/init/__tests__/profile-loader.test.ts</files>
  <read_first>
    - src/cli/init/profile-loader.ts (to understand exports and behavior)
    - src/cli/init/profiles/*.json (to know the actual profile data)
  </read_first>
  <action>
Create `src/cli/init/__tests__/profile-loader.test.ts` with the following test cases:

1. **Setup:**
   - Import `describe, expect, it` from `vitest`.
   - Import `loadProfile, resolveProfilePath` from `../profile-loader.js`.

2. **Test cases:**
   - `it('loads and validates nodejs profile', () => { ... })`: `const profile = loadProfile('nodejs');` expect `profile.parser.include` is array with length >= 1, `profile.parser.extensions` is array with length >= 1, `profile.ignore` is array, `profile.analysis_depth === 'standard'`.
   - `it('loads and validates python profile', () => { ... })`: Same pattern.
   - `it('loads and validates go profile', () => { ... })`: Same pattern.
   - `it('loads and validates rust profile', () => { ... })`: Same pattern.
   - `it('loads and validates generic profile', () => { ... })`: Same pattern, expect `analysis_depth === 'shallow'`.
   - `it('rejects unknown profile names', () => { ... })`: `expect(() => loadProfile('unknown')).toThrow('未知 profile')`.
   - `it('rejects path traversal in profile name', () => { ... })`: `expect(() => loadProfile('../etc/passwd')).toThrow('非法 profile 名称')`.
   - `it('rejects profile names with path separators', () => { ... })`: `expect(() => loadProfile('foo/bar')).toThrow('非法 profile 名称')`.
   - `it('resolveProfilePath returns correct path for nodejs', () => { ... })`: `const p = resolveProfilePath('nodejs');` expect `p.endsWith('nodejs.json')` and `p.includes('profiles')`.

3. No temp directories needed — profile files are bundled in the package.
  </action>
  <verify>
    <automated>npx vitest run src/cli/init/__tests__/profile-loader.test.ts</automated>
  </verify>
  <done>
    - File exists at `src/cli/init/__tests__/profile-loader.test.ts`
    - All 10 test cases pass
    - All 5 built-in profiles validate successfully
    - Unknown names and path traversal are rejected with clear errors
    - `resolveProfilePath` returns correct path structure
  </done>
</task>

<task type="auto">
  <name>Task 3: Create init-profile.test.ts — integration tests for profile in init command</name>
  <files>src/cli/commands/__tests__/init-profile.test.ts</files>
  <read_first>
    - src/cli/commands/init.ts (to understand executeInitCommand and resolveProfile)
    - src/cli/commands/__tests__/init-command.test.ts (for temp project, chalk mock, and receipt assertion patterns)
    - src/cli/commands/__tests__/init-rules.test.ts (for asset assertion patterns)
    - src/cli/init/reconciler.ts (for InitAssetStatus values)
  </read_first>
  <action>
Create `src/cli/commands/__tests__/init-profile.test.ts` with the following test cases:

1. **Setup (copy exact patterns from init-command.test.ts):**
   - Import `afterEach, beforeEach, describe, expect, it, vi` from `vitest`.
   - Import `existsSync, mkdtempSync, mkdirSync, readFileSync, rmSync, writeFileSync` from `node:fs`.
   - Import `tmpdir` from `node:os`, `path` from `node:path`.
   - Mock chalk identically to init-command.test.ts.
   - Import `executeInitCommand` from `../init.js`.
   - Helper `createTempProject(): string`.
   - `const tempRoots: string[] = []`, `consoleLogSpy` with beforeEach/afterEach.

2. **Test cases:**
   - `it('applies nodejs profile when package.json exists and --yes is used', async () => { ... })`:
     - Create temp dir, write `package.json` (`{}`), run `executeInitCommand({ yes: true, cwd: rootDir })`.
     - Expect receipt asset with `key: 'bootstrap-profile'`, `status: 'installed'`.
     - Expect `.mycodemap/config.json` exists and contains `"include": ["src/**/*.{ts,tsx,js,jsx,mjs,cjs}"]`.
   - `it('shows profile as skipped in preview mode', async () => { ... })`:
     - Create temp dir, write `package.json`, run `executeInitCommand({ cwd: rootDir })` (no --yes).
     - Expect receipt asset with `key: 'bootstrap-profile'`, `status: 'skipped'`.
     - Expect `.mycodemap/config.json` does NOT exist.
   - `it('skips profile when .mycodemap/config.json already exists', async () => { ... })`:
     - Create temp dir, create `.mycodemap/config.json` with `{"mode":"hybrid"}`, write `package.json`.
     - Run `executeInitCommand({ yes: true, cwd: rootDir })`.
     - Expect receipt asset with `key: 'bootstrap-profile'`, `status: 'already-synced'`.
   - `it('bypasses detection with --profile python', async () => { ... })`:
     - Create temp dir (no markers), run `executeInitCommand({ yes: true, profile: 'python', cwd: rootDir })`.
     - Expect receipt asset with `key: 'bootstrap-profile'`, `status: 'installed'`.
     - Expect `.mycodemap/config.json` contains `"py"` in extensions.
   - `it('throws when no markers and no --profile in non-TTY', async () => { ... })`:
     - Mock `process.stdout.isTTY` to `false` using `vi.spyOn(process.stdout, 'isTTY', 'get').mockReturnValue(false)`.
     - Create temp dir (no markers), run `executeInitCommand({ cwd: rootDir })`.
     - Expect rejection with message containing `--profile`.
     - Restore TTY mock in finally.
   - `it('throws when multiple markers in non-TTY', async () => { ... })`:
     - Mock `process.stdout.isTTY` to `false`.
     - Create temp dir with both `package.json` and `Cargo.toml`.
     - Run `executeInitCommand({ cwd: rootDir })`.
     - Expect rejection with message containing `package.json` and `Cargo.toml` and `--profile`.
     - Restore TTY mock.
   - `it('prompts for selection when multiple markers in TTY', async () => { ... })`:
     - Mock `process.stdout.isTTY` to `true`.
     - Mock `process.stdin.isTTY` to `true`.
     - Mock `node:readline/promises` to return `'1'` (select first candidate).
     - Create temp dir with `package.json` and `Cargo.toml`.
     - Run `executeInitCommand({ yes: true, cwd: rootDir })`.
     - Expect receipt asset with `key: 'bootstrap-profile'`, `status: 'installed'`.
     - Restore mocks.
   - `it('throws clear error for empty directory without markers', async () => { ... })`:
     - Create temp dir (no markers), run `executeInitCommand({ cwd: rootDir })`.
     - Expect rejection with message containing `未检测到项目类型标记` and `--profile`.

3. **Important:** For TTY mocking, use:
   ```typescript
   let ttySpy: ReturnType<typeof vi.spyOn>;
   beforeEach(() => { ttySpy = vi.spyOn(process.stdout, 'isTTY', 'get'); });
   afterEach(() => { ttySpy.mockRestore(); });
   ```

4. For readline mocking in test 7, use `vi.doMock` or mock the module before import. Alternatively, mock `promptForProfileSelection` directly by importing from `../../init/detect.js` and using `vi.spyOn`.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/__tests__/init-profile.test.ts</automated>
  </verify>
  <done>
    - File exists at `src/cli/commands/__tests__/init-profile.test.ts`
    - All 8 test cases pass
    - Tests use real temp directories and real filesystem writes
    - TTY mocking is properly restored after each test
    - Receipt assets are asserted with exact key and status values
  </done>
</task>

<task type="auto">
  <name>Task 4: Update first-run-guide.ts — add profile hint (D-15)</name>
  <files>src/cli/first-run-guide.ts, src/cli/__tests__/first-run-guide.test.ts</files>
  <read_first>
    - src/cli/first-run-guide.ts (full current file)
    - src/cli/__tests__/first-run-guide.test.ts (full current file)
  </read_first>
  <action>
Modify `src/cli/first-run-guide.ts` with a lightweight text-only update per D-15:

1. In `showFirstRunGuide()`, after the existing step 3 (`mycodemap --help`), add a new sentence before the closing separator:
   ```typescript
   console.log(chalk.gray('  提示: ') + chalk.white('运行 `mycodemap init` 时，若检测到 package.json / pyproject.toml / go.mod / Cargo.toml，将自动推荐对应项目的 bootstrap profile。'));
   console.log('');
   ```
   Place this between step 3 and the final `console.log(chalk.cyan('━'.repeat(48)));`.

2. Modify `src/cli/__tests__/first-run-guide.test.ts`:
   - In the test `it('shows a concise init → generate → help guide without hook or rules setup', () => { ... })`, add:
     ```typescript
     expect(output).toContain('bootstrap profile');
     expect(output).toContain('package.json');
     ```
   - Keep all other assertions unchanged.

3. Do NOT add any detection invocation to first-run-guide.ts. This is text-only per D-15.
  </action>
  <verify>
    <automated>npx vitest run src/cli/__tests__/first-run-guide.test.ts</automated>
  </verify>
  <done>
    - `first-run-guide.ts` contains the profile hint sentence
    - `first-run-guide.test.ts` asserts presence of `bootstrap profile` and `package.json` in output
    - No detection logic added to first-run-guide.ts
    - Test passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test filesystem -> detection logic | Tests create real marker files to exercise detection |
| Test TTY mock -> interactive flow | Tests mock TTY state to verify non-TTY refusal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-08 | Denial of Service | Test temp directories not cleaned up | mitigate | `afterEach` removes all `tempRoots` recursively; pattern copied from existing tests |
| T-53-09 | Information Disclosure | Test assertions leak absolute paths in failure messages | accept | Vitest failure messages may show absolute paths; this is test-only, not production output |
</threat_model>

<verification>
After all tasks complete:
1. `npx vitest run src/cli/init/__tests__/detect.test.ts` passes
2. `npx vitest run src/cli/init/__tests__/profile-loader.test.ts` passes
3. `npx vitest run src/cli/commands/__tests__/init-profile.test.ts` passes
4. `npx vitest run src/cli/__tests__/first-run-guide.test.ts` passes
5. `npm test` (full suite) passes
</verification>

<success_criteria>
- `detect.test.ts`: 7 tests covering all marker files, no markers, multi-marker, and content-independence
- `profile-loader.test.ts`: 10 tests covering all 5 profiles, unknown name rejection, path traversal rejection, and resolveProfilePath
- `init-profile.test.ts`: 8 tests covering preview, apply, existing config skip, --profile bypass, non-TTY refusal, multi-marker non-TTY, multi-marker TTY selection, and empty directory error
- `first-run-guide.test.ts` passes with new profile hint assertions
- Full suite `npm test` passes
</success_criteria>

<output>
After completion, create `.planning/phases/53-bootstrap-profiles-project-detection/53-03-SUMMARY.md`
</output>
