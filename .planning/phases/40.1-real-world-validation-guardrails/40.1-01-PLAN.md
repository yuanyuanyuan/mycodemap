---
phase: 40.1-real-world-validation-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - CLAUDE.md
  - docs/rules/testing.md
  - docs/rules/pre-release-checklist.md
autonomous: true
requirements: [VAL-01]

must_haves:
  truths:
    - "AGENTS.md Section 8 声明真实场景验证为宪法级硬约束（8.1 阈值 + 8.2 豁免条款）"
    - "CLAUDE.md 路由表标注 testing.md 含真实验证规则，编辑归宿区含真实验证规则目的地"
    - "testing.md 真实场景验证段落含适用范围、豁免条款、交叉引用 AGENTS.md 8.1"
    - "pre-release-checklist.md #12 含适用范围、豁免条款、交叉引用 AGENTS.md 8.1 和 testing.md"
  artifacts:
    - path: "AGENTS.md"
      provides: "Section 8.1 真实场景验证阈值 + Section 8.2 豁免条款"
      contains: "8.1"
    - path: "CLAUDE.md"
      provides: "路由表真实验证标注 + 编辑归宿条目"
      contains: "真实验证"
    - path: "docs/rules/testing.md"
      provides: "适用范围/豁免/交叉引用元信息层"
      contains: "适用范围"
    - path: "docs/rules/pre-release-checklist.md"
      provides: "#12 元信息层（适用范围/豁免/交叉引用）"
      contains: "适用范围"
  key_links:
    - from: "AGENTS.md"
      to: "docs/rules/testing.md"
      via: "Section 8.1 交叉引用"
      pattern: "testing\\.md"
    - from: "AGENTS.md"
      to: "docs/rules/pre-release-checklist.md"
      via: "Section 8.1 交叉引用"
      pattern: "pre-release-checklist\\.md"
    - from: "docs/rules/testing.md"
      to: "AGENTS.md"
      via: "元信息层反向引用"
      pattern: "AGENTS\\.md.*8\\.1"
    - from: "docs/rules/pre-release-checklist.md"
      to: "AGENTS.md"
      via: "元信息层反向引用"
      pattern: "AGENTS\\.md.*8\\.1"
---

<objective>
Phase 40.1 文档层修改 — 为四份治理文档添加真实场景验证的宪法级声明、路由标注和元信息层。

Purpose: 建立真实场景验证的宪法级硬约束基础，使规则在 AGENTS.md 声明意图，在 testing.md/pre-release-checklist.md 细化适用范围，在 CLAUDE.md 可被发现。Per D-01 (硬约束分级落地)、D-04 (41/42 豁免边界)、D-05 (元信息层策略)。

Output: 四份文档完成修改，交叉引用双向连通。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40.1-real-world-validation-guardrails/40.1-CONTEXT.md

@AGENTS.md
@CLAUDE.md
@docs/rules/testing.md
@docs/rules/pre-release-checklist.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AGENTS.md — 添加 Section 8.1 验证阈值 + Section 8.2 豁免条款</name>
  <files>AGENTS.md</files>
  <read_first>AGENTS.md lines 114-121 (Section 8 当前内容)</read_first>
  <action>在 AGENTS.md Section 8（line 114-121）末尾、Section 9 之前，插入两个子节：

**Section 8.1 真实场景验证阈值**（per D-01）：
- 声明最低阈值：真实 filesystem + 真实 subprocess 或真实 transport（非纯 mock）
- 禁止：仅 mock 依赖后断言函数返回值；仅 toEqual 断言无真实端到端触发；在已污染环境中声称"通过"；无失败场景验证
- 要求：每个修复/功能至少 1 个失败场景
- 分级执行控制：pre-commit warn-only → CI blocking → pre-release 必过

**Section 8.2 豁免条款**（per D-04）：
- Phase 41/42 已有实现（截至本规则生效日）不追溯合规
- Phase 41/42 后续任何修改必须合规
- Phase 43+ 从 inception 起必须合规
- Phase 42 测试已使用 StdioServerTransport + PassThrough 做真实 MCP transport，符合阈值

交叉引用：
- → docs/rules/testing.md（详细验证规则正文）
- → docs/rules/pre-release-checklist.md #12（发布门检查）
</action>
  <verify>
    <automated>grep -c "8\.1" AGENTS.md && grep -c "8\.2" AGENTS.md && grep -c "testing\.md" AGENTS.md && grep -c "pre-release-checklist\.md" AGENTS.md && grep -c "StdioServerTransport" AGENTS.md</automated>
  </verify>
  <done>
    - AGENTS.md 含 Section 8.1 声明阈值（grep -c "8.1" >= 1）
    - AGENTS.md 含 Section 8.2 豁免条款（grep -c "8.2" >= 1）
    - AGENTS.md 交叉引用 testing.md 和 pre-release-checklist.md
    - 豁免条款提及 StdioServerTransport + PassThrough
  </done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md — 路由表标注 + 编辑归宿条目</name>
  <files>CLAUDE.md</files>
  <read_first>CLAUDE.md lines 22-42 (路由表 + 编辑归宿区)</read_first>
  <action>两处修改：

1. 路由表（line 30）— 修改 `docs/rules/testing.md` 行：
   - 原: `| 测试规则与 fixture 边界在？                        | \`docs/rules/testing.md\`                                    |`
   - 改: `| 测试规则、fixture 边界、**真实场景验证规则**在？       | \`docs/rules/testing.md\`                                    |`

2. 编辑归宿区（line 36-42 之间）— 在 "改**验证 / hook / CI 规则**" 条目后插入：
   - `- 改**真实场景验证规则**：编辑 \`docs/rules/testing.md\` 与 \`docs/rules/pre-release-checklist.md\`（宪法声明在 \`AGENTS.md\` Section 8.1）`
</action>
  <verify>
    <automated>grep "真实场景验证" CLAUDE.md | grep -v '^#' | grep -c 'testing\.md'</automated>
  </verify>
  <done>
    - CLAUDE.md 路由表含"真实场景验证规则"标注
    - CLAUDE.md 编辑归宿区含真实场景验证规则条目（指向 testing.md + pre-release-checklist.md + AGENTS.md 8.1）
  </done>
</task>

<task type="auto">
  <name>Task 3: testing.md + pre-release-checklist.md — 元信息层</name>
  <files>docs/rules/testing.md, docs/rules/pre-release-checklist.md</files>
  <read_first>docs/rules/testing.md lines 63-76 (真实场景验证段落头部), docs/rules/pre-release-checklist.md lines 275-297 (#12 段落)</read_first>
  <action>对两份文件分别添加元信息层（per D-05）：

**docs/rules/testing.md** — 在 line 63 "## 真实场景验证规则（强制）" 之后、line 65 原则引用之前插入：

```
> **适用范围**：本规则适用于 Phase 41 之后的所有新开发及对已有代码的后续修改。
> **豁免条款**：Phase 41/42 已有实现（截至本规则生效日）不受追溯约束；后续任何修改必须合规。
> **宪法依据**：→ AGENTS.md Section 8.1（真实场景验证阈值）、Section 8.2（豁免条款）
```

**docs/rules/pre-release-checklist.md** — 在 line 275 "### 12. 真实场景验证检查（强制）" 之后、line 277 目标之前插入：

```
> **适用范围**：本检查项适用于 Phase 41 之后的所有新开发及对已有代码的后续修改。
> **豁免条款**：Phase 41/42 已有实现（截至本规则生效日）不受追溯约束；后续任何修改必须合规。
> **宪法依据**：→ AGENTS.md Section 8.1（真实场景验证阈值）、Section 8.2（豁免条款）
> **详细规则**：→ docs/rules/testing.md "真实场景验证规则（强制）"
```

重要：两处均只插入元信息层，不修改任何现有规则正文。
</action>
  <verify>
    <automated>grep -c "适用范围" docs/rules/testing.md && grep -c "适用范围" docs/rules/pre-release-checklist.md && grep -c "AGENTS\.md.*8\.1" docs/rules/testing.md && grep -c "AGENTS\.md.*8\.1" docs/rules/pre-release-checklist.md && grep -c "testing\.md" docs/rules/pre-release-checklist.md</automated>
  </verify>
  <done>
    - testing.md 含适用范围 + 豁免条款 + 交叉引用 AGENTS.md 8.1/8.2
    - pre-release-checklist.md 含适用范围 + 豁免条款 + 交叉引用 AGENTS.md 8.1/8.2 + 交叉引用 testing.md
    - 两份文件原有规则正文未修改
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → docs | 治理文档间交叉引用，无运行时信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.1-01 | Tampering | AGENTS.md | accept | 仓库受 git 保护，文档修改需 review |
| T-40.1-02 | Information Disclosure | CLAUDE.md | accept | 路由信息为公开知识，无敏感数据 |
</threat_model>

<verification>
- AGENTS.md Section 8.1/8.2 存在且含阈值声明和豁免条款
- CLAUDE.md 路由表含真实验证标注
- testing.md 元信息层含适用范围/豁免/交叉引用
- pre-release-checklist.md #12 元信息层含适用范围/豁免/交叉引用
- 交叉引用双向连通：AGENTS.md → testing.md → AGENTS.md, AGENTS.md → pre-release-checklist.md → AGENTS.md
</verification>

<success_criteria>
1. `grep "8\.1" AGENTS.md | grep -c "真实"` >= 1
2. `grep "8\.2" AGENTS.md | grep -c "豁免"` >= 1
3. `grep "真实场景验证" CLAUDE.md | wc -l` >= 2
4. `grep "适用范围" docs/rules/testing.md | wc -l` >= 1
5. `grep "适用范围" docs/rules/pre-release-checklist.md | wc -l` >= 1
6. `grep "AGENTS\.md.*8\.1" docs/rules/testing.md | wc -l` >= 1
7. `grep "AGENTS\.md.*8\.1" docs/rules/pre-release-checklist.md | wc -l` >= 1
</success_criteria>

<output>
After completion, create `.planning/phases/40.1-real-world-validation-guardrails/40.1-01-SUMMARY.md`
</output>
