# Phase 40.1: Real-World Validation Guardrails - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-30
**Phase:** 40.1-real-world-validation-guardrails
**Areas discussed:** 验证阈值与强制程度, 自动化检查策略, Phase 41/42 豁免边界, 文档增强策略

---

## 验证阈值与强制程度

| Option | Description | Selected |
|--------|-------------|----------|
| 分级强制 | AGENTS.md 8.1 明确阈值+豁免条款；违反时 docs:check 报 warning，pre-release 阻断发布；给团队适应期 | ✓ |
| 宪法级红线 | 写入 AGENTS.md 作为宪法红线，违反即发布拒绝，与 L3 同等严格，无适应期 | |
| 仅指引级 | 只在 testing.md 中定义规则，AGENTS.md 加交叉引用但不再级声明 | |

**User's choice:** 硬约束，但硬约束都需要有 CI 护栏来做控制，宪法说明只是先声明。
**Notes:** 用户明确表示"这是硬约束"，但同时强调宪法声明和 CI 护栏是双保险机制 — 宪法先声明意图，CI 负责执行控制。

---

## 自动化检查策略 — Pre-commit

| Option | Description | Selected |
|--------|-------------|----------|
| 启发式 grep | 用 grep 检查测试文件是否包含真实调用模式（spawn/execSync/InMemoryTransport等），warn-only | ✓ |
| 简化：只检查测试文件存在 | 不检查模式，只检查是否有测试文件被修改 | |
| 跳过 pre-commit | 不查 pre-commit，只依赖 CI 深度检查 | |

**User's choice:** 启发式 grep（PLAN 方案）
**Notes:** warn-only，位置在 "File headers passed" 之后、"Documentation guardrails" 之前。

---

## 自动化检查策略 — CI 证据

| Option | Description | Selected |
|--------|-------------|----------|
| [证据] 标签 | 检查 PR commit messages 或 PR body 中是否有 [证据] 标签，简单但依赖人工纪律，可渐进升级 | ✓ |
| CI 扫描测试文件模式 | 扫描新增/修改的测试文件检查真实调用模式，机器可验证但启发式仍会误判 | |
| 双层检查 | 模式 + 标签两步检查，更严格但增加复杂度 | |

**User's choice:** [证据] 标签（PLAN 方案）
**Notes:** warn-only 初始阶段，可渐进升级为 blocking。仅对 FEATURE/BUGFIX commit 类型触发。

---

## Phase 41/42 豁免边界

| Option | Description | Selected |
|--------|-------------|----------|
| 分层豁免 | 41/42 不追溯，后续修改合规。42 已用真实 transport 只缺标签；41 需补真实 CLI 调用测试 | ✓ |
| 完全豁免 | 41/42 全部豁免，不管后续修改 | |
| 立即合规 | 41/42 立即合规，限期整改 | |

**User's choice:** 分层豁免
**Notes:** 代码库分析发现 Phase 42 的 `dynamic-server.test.ts` 实际使用了真实 MCP transport（`StdioServerTransport` + `PassThrough`），符合真实 transport 验证阈值。Phase 41 的 interface-contract 测试仅为结构断言。

---

## 文档增强策略

| Option | Description | Selected |
|--------|-------------|----------|
| 加元信息层 | 在现有段落加适用范围头+豁免条款+交叉引用，不改规则正文 | ✓ |
| 重写规则段落 | 重写 testing.md 的真实验证段落，加入更具体阈值和示例 | |
| 仅加交叉引用 | 只加 AGENTS.md 引用，不加适用范围和豁免条款 | |

**User's choice:** 加元信息层
**Notes:** 保持现有规则正文稳定性，只增加元信息（适用范围、豁免、交叉引用）。

---

## Claude's Discretion

- Pre-commit grep 模式列表的具体词汇选择
- CI evidence 检查脚本的详细实现细节
- AGENTS.md Section 8.1/8.2 的具体措辞
- CLAUDE.md 路由标注的具体格式

## Deferred Ideas

None — discussion stayed within phase scope.
