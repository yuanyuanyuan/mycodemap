---
phase: 40.1-real-world-validation-guardrails
plan: 02
wave: 2
status: complete
completed: 2026-04-30
---

# Plan 02 Summary — 自动化层

## Tasks Completed

| # | Task | Files | Status |
|---|------|-------|--------|
| 1 | .githooks/pre-commit — 添加步骤 5a 真实调用模式检查 | `.githooks/pre-commit` | ✅ |
| 2 | .github/workflows/ci-gateway.yml — 添加 PR 证据验证步骤 | `.github/workflows/ci-gateway.yml` | ✅ |

## Verification Results

```
.githooks/pre-commit:
  - bash -n 语法检查: PASS
  - StdioServerTransport 命中: 3 hits
  - PassThrough 命中: 3 hits
  - InMemoryTransport 排除: 0 hits (verified absent)
  - "real filesystem" / "real subprocess" 排除: 0 hits (verified absent)
  - warn-only 确认: echo WARNING + exit 0 上下文验证

.github/workflows/ci-gateway.yml:
  - python3 yaml.safe_load: PASS
  - [证据] 标签检查: 4 hits
  - FEATURE/BUGFIX 触发条件: 2 hits
  - exit 0 (warn-only): 1 hit in evidence step context
```

## Key Design Decisions Preserved

- **D-02**: Pre-commit 使用启发式 grep 检查 `.test.ts` 文件中的真实调用模式（`spawn|execSync|mktemp|StdioServerTransport|PassThrough|createServer|listen`）
- **D-03**: CI 仅对 `[FEATURE]`/`[BUGFIX]` commit 类型触发 `[证据]` 标签检查
- **分级执行控制**: pre-commit warn-only → CI warn-only (可渐进升级) → pre-release 必过

## Threat Model Acceptance

| Threat ID | Disposition | Mitigation Verified |
|-----------|-------------|---------------------|
| T-40.1-03 | accept | Warn-only, fixed whitelist grep patterns |
| T-40.1-04 | accept | Branch protection applies |
| T-40.1-05 | accept | git log is lightweight, warn-only |
