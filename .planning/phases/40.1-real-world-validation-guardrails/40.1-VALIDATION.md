---
phase: 40.1
slug: real-world-validation-guardrails
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-30
---

# Phase 40.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest (已安装) |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `npx vitest run` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npm run docs:check && bash -n .githooks/pre-commit`
- **After every plan wave:** Run `npm run docs:check && bash -n .githooks/pre-commit && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci-gateway.yml'))"`
- **Before `/gsd-verify-work`:** Full docs:check + pre-commit syntax + YAML validation
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 40.1-01-01 | 01 | 1 | VAL-01 | — | AGENTS.md 包含 Section 8.1/8.2 | automated | `grep '8.1' AGENTS.md && grep '8.2' AGENTS.md` | ✅ | ✅ green |
| 40.1-01-02 | 01 | 1 | VAL-01 | — | CLAUDE.md 路由表标注真实验证 | automated | `grep '真实验证' CLAUDE.md` | ✅ | ✅ green |
| 40.1-01-03 | 01 | 1 | VAL-01 | — | testing.md 含元信息层 | automated | `grep '适用范围' docs/rules/testing.md` | ✅ | ✅ green |
| 40.1-01-04 | 01 | 1 | VAL-01 | — | docs:check 通过 | automated | `npm run docs:check` | ✅ | ✅ green |
| 40.1-02-01 | 02 | 2 | VAL-01 | — | Pre-commit 步骤 5a 存在且 warn-only | automated | `bash -n .githooks/pre-commit && grep -c "StdioServerTransport" .githooks/pre-commit` | ✅ | ✅ green |
| 40.1-02-02 | 02 | 2 | VAL-01 | — | CI 证据验证步骤存在且 warn-only | automated | `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci-gateway.yml'))"` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements. This is a docs/config-only phase — no new test stubs needed.*

验证依赖：
- `npm run docs:check` — 文档护栏已存在
- `bash -n` — shell 语法检查
- `python3 yaml.safe_load` — YAML 验证
- `grep` — 内容存在性检查

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| AGENTS.md Section 8.1/8.2 内容正确性 | VAL-01 | 需人工审核法律/合规措辞 | `grep -A5 '8.1' AGENTS.md` 审核阈值和豁免条款 |
| CLAUDE.md 路由标注位置合理 | VAL-01 | 文档结构需人工判断 | `cat CLAUDE.md` 确认路由表和编辑归宿的标注 |
| testing.md 元信息层不破坏现有内容 | VAL-01 | 需人工 diff 确认 | `git diff docs/rules/testing.md` 确认只添加未删除 |
| Pre-commit 步骤顺序和逻辑正确 | VAL-01 | 运行时行为需手动触发验证 | `bash -n .githooks/pre-commit` + 实际 commit 测试 |
| CI 步骤位置和逻辑正确 | VAL-01 | GitHub Actions 需实际 PR 触发验证 | YAML 语法检查 + 手动审查步骤位置 |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved
