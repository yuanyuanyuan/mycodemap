---
wave: 2
depends_on: [56-01]
files_modified:
  - README.md
  - docs/SETUP_GUIDE.md
  - docs/AI_ASSISTANT_SETUP.md
autonomous: true
requirements: [INI-03]
---

# Plan 56-03: Doc Sync — Unified Flow Description

## Objective

Update three documentation files to describe the unified setup flow (install → init → doctor → generate → connect agent) and explain the new two-section init receipt.

## Tasks

### Task 1: Update README.md quick start

<read_first>
- README.md — current quick start / installation section (first 100 lines)
- .planning/phases/56-init-receipt-next-steps/56-CONTEXT.md — D-13 to D-15 (doc sync)
</read_first>

<action>
In `README.md`, find the quick start section (after the Installation subsection). Add or update the init step to mention the receipt:

After the existing `mycodemap init` or `mycodemap init -y` instruction, add a brief note:

```markdown
### Quick Start

```bash
# Install
npm install -g @mycodemap/mycodemap

# Initialize in your project
mycodemap init -y

# Review the init receipt — it shows two sections:
#   Main Agent: context files to merge into CLAUDE.md / AGENTS.md
#   Subagent: platform configs to copy into .claude/settings.json / .codex/agents/

# Verify setup
mycodemap doctor

# Generate code map
mycodemap generate
```

The init receipt distinguishes **main-agent setup** (project context for your AI assistant) from **subagent setup** (platform hooks for delegated agents). See [AI Assistant Setup Guide](docs/AI_ASSISTANT_SETUP.md) for details.
```

Keep the change minimal — only add receipt explanation, don't rewrite the entire quick start.
</action>

<acceptance_criteria>
- `README.md` contains `Main Agent` in the quick start section
- `README.md` contains `Subagent` in the quick start section
- `README.md` contains `mycodemap doctor` in the quick start flow
- `README.md` contains a link to `docs/AI_ASSISTANT_SETUP.md`
- The quick start flow order is: install → init → doctor → generate
</acceptance_criteria>

### Task 2: Update docs/SETUP_GUIDE.md with receipt explanation

<read_first>
- docs/SETUP_GUIDE.md — current init section (around line 79-97)
- .planning/phases/56-init-receipt-next-steps/56-CONTEXT.md — D-14 (unified flow), D-15 (receipt explanation)
</read_first>

<action>
In `docs/SETUP_GUIDE.md`, find the 初始化配置 section (around line 79). After the existing init command examples, add a subsection explaining the receipt:

```markdown
#### 理解 Init 收据

`mycodemap init` 完成后会输出一份收据，分为两个部分：

**Main Agent（主 Agent 上下文）**
- 报告已生成的上下文文件路径（如 `claude-context.md`、`agents-context.md`）
- 指导你将相关内容手动合并到项目根目录的 `CLAUDE.md` / `AGENTS.md`
- 检测已同步状态：如果 `CLAUDE.md` 已包含 `.mycodemap/` 引用，标记为 `already-synced`

**Subagent（子 Agent 配置）**
- 报告平台配置示例文件（如 `claude-hook-example.json`、`codex-agent-example.toml`）
- 指导你将配置复制到对应平台设置目录

收据还会显示**个性化下一步**，基于实际安装的资产生成，而非固定的三步欢迎信息。

> 注意：`init` 不会自动改写 `CLAUDE.md`、`AGENTS.md` 等团队共管文件。所有需要手动操作的步骤都会在收据中明确标注。
```

Insert this after the existing init examples (around line 97). Keep surrounding content unchanged.
</action>

<acceptance_criteria>
- `docs/SETUP_GUIDE.md` contains `Main Agent（主 Agent 上下文）`
- `docs/SETUP_GUIDE.md` contains `Subagent（子 Agent 配置）`
- `docs/SETUP_GUIDE.md` contains `个性化下一步`
- `docs/SETUP_GUIDE.md` contains `already-synced`
- `docs/SETUP_GUIDE.md` contains `不会自动改写`
</acceptance_criteria>

### Task 3: Update docs/AI_ASSISTANT_SETUP.md with receipt guide

<read_first>
- docs/AI_ASSISTANT_SETUP.md — current structure (first 60 lines and Claude Code section)
- .planning/phases/56-init-receipt-next-steps/56-CONTEXT.md — D-13 to D-15 (doc sync)
</read_first>

<action>
In `docs/AI_ASSISTANT_SETUP.md`, after the 概述 section (after the quick selection table), add a new section:

```markdown
## 理解 Init 收据

运行 `mycodemap init` 后，收据会分为两个部分，帮助你快速连接 AI 助手：

### Main Agent 部分

这一部分处理主 AI 助手的项目上下文连接：

| 文件 | 用途 | 操作 |
|------|------|------|
| `claude-context.md` | Claude Code 项目上下文建议 | 复制到项目根目录 `CLAUDE.md` |
| `agents-context.md` | 通用 Agent 上下文建议 | 复制到项目根目录 `AGENTS.md` |

如果 `CLAUDE.md` 或 `AGENTS.md` 已包含 `.mycodemap/` 路径引用，收据会标记为 `already-synced`，无需重复操作。

### Subagent 部分

这一部分处理子 Agent 的平台配置：

| 文件 | 用途 | 操作 |
|------|------|------|
| `claude-hook-example.json` | Claude Code SubagentStart hook 示例 | 复制到 `.claude/settings.json` |
| `codex-agent-example.toml` | Codex agent developer_instructions 示例 | 复制到 `.codex/agents/` |

### 个性化下一步

收据底部的"下一步"基于实际安装状态动态生成：
1. 冲突解决（最高优先级）
2. 手动操作指引
3. 已安装资产的使用指导
4. 默认推荐步骤（`mycodemap doctor`、`mycodemap generate`）

> 所有团队共管文件（`CLAUDE.md`、`AGENTS.md`、`.claude/settings.json`）不会被自动改写。收据提供可复制的片段，由你决定何时以及如何集成。
```

Insert this after the 概述 section and before the per-platform sections (Kimi CLI 配置).
</action>

<acceptance_criteria>
- `docs/AI_ASSISTANT_SETUP.md` contains `## 理解 Init 收据`
- `docs/AI_ASSISTANT_SETUP.md` contains `### Main Agent 部分`
- `docs/AI_ASSISTANT_SETUP.md` contains `### Subagent 部分`
- `docs/AI_ASSISTANT_SETUP.md` contains `### 个性化下一步`
- `docs/AI_ASSISTANT_SETUP.md` contains `already-synced`
- `docs/AI_ASSISTANT_SETUP.md` contains `不会被自动改写`
- The section appears before the per-platform configuration sections
</acceptance_criteria>

## Verification Criteria

1. `grep -c 'Main Agent' docs/AI_ASSISTANT_SETUP.md` returns at least 1
2. `grep -c 'Subagent' docs/AI_ASSISTANT_SETUP.md` returns at least 1
3. `grep -c '个性化下一步' docs/SETUP_GUIDE.md` returns at least 1
4. `grep -c 'mycodemap doctor' README.md` returns at least 1
5. All three docs describe the flow: install → init → doctor → generate → connect agent

## must_haves

- truths:
  - All three docs describe the unified setup flow (D-14, INI-03)
  - Docs explain the two-section receipt and what each section means (D-15)
  - Docs make clear that team-owned files are never auto-rewritten (ABT-04)
  - Docs reference `already-synced` detection behavior (D-09)
