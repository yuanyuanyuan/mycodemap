---
phase: 60-storage-convergence
plan: 03
type: execute
wave: 2
depends_on:
  - "60-01"
  - "60-02"
files_modified:
  - src/infrastructure/storage/adapters/sqlite-loader.ts
  - src/infrastructure/storage/adapters/SQLiteStorage.ts
  - src/infrastructure/storage/__tests__/SQLiteStorage.test.ts
  - src/infrastructure/storage/__tests__/fallback-mechanism.test.ts
  - src/cli/output/error-codes.ts
  - src/cli/output/errors.ts
  - src/cli/output/wasm-fallback.ts
  - src/cli/output/__tests__/errors.test.ts
  - src/cli/doctor/check-native-deps.ts
autonomous: true
requirements:
  - STOR-03
  - STOR-04
user_setup: []
must_haves:
  truths:
    - "Native sqlite failure falls back only within the SQLite family (`better-sqlite3` → `node:sqlite` / `sql.js`) and never across backend families (covers D-06, D-07)."
    - "Fallback visibility remains observable to humans and, where shared output seams allow, to machines via a `WasmFallbackResult`-style metadata contract or actionable fields rather than silent warnings only (covers D-07, D-08)."
    - "Total SQLite loader failure returns actionable remediation that aligns with the new storage truth and doctor guidance (covers D-04, D-06)."
  artifacts:
    - path: "src/infrastructure/storage/adapters/sqlite-loader.ts"
      provides: "Native-first, SQLite-family-only fallback ladder"
      contains: "Native better-sqlite3 unavailable, using WASM fallback"
    - path: "src/cli/output/errors.ts"
      provides: "Canonical structured remediation surface for SQLite fallback/loader failures"
      contains: "DEP_NATIVE_MISSING"
    - path: "src/infrastructure/storage/__tests__/SQLiteStorage.test.ts"
      provides: "Focused proof that SQLite storage still works after fallback-contract tightening"
      contains: "governance.sqlite"
  key_links:
    - from: "src/infrastructure/storage/adapters/sqlite-loader.ts"
      to: "src/cli/output/errors.ts"
      via: "loader total-failure path uses actionable remediation instead of backend-family downgrade"
      pattern: "createActionableError"
    - from: "src/cli/doctor/check-native-deps.ts"
      to: "src/infrastructure/storage/adapters/sqlite-loader.ts"
      via: "doctor guidance and loader remediation point users at the same SQLite fallback story"
      pattern: "better-sqlite3|sql.js|node:sqlite"
---

<objective>
Prove and expose SQLite-family fallback semantics: keep native-first loading, verify fallback stays inside the SQLite family, and make failure/remediation visibility explicit.

Purpose: Storage convergence is not complete until native dependency degradation is proven and observable without reopening cross-backend fallback.
Output: fallback-path tests, actionable dual-failure remediation, and aligned doctor/output surfaces for SQLite dependency issues.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-storage-convergence/60-CONTEXT.md
@.planning/phases/60-storage-convergence/60-RESEARCH.md
@.planning/phases/60-storage-convergence/60-01-PLAN.md
@.planning/phases/60-storage-convergence/60-02-PLAN.md
@src/infrastructure/storage/adapters/sqlite-loader.ts
@src/infrastructure/storage/adapters/SQLiteStorage.ts
@src/infrastructure/storage/__tests__/SQLiteStorage.test.ts
@src/infrastructure/storage/__tests__/fallback-mechanism.test.ts
@src/cli/output/error-codes.ts
@src/cli/output/errors.ts
@src/cli/output/wasm-fallback.ts
@src/cli/output/__tests__/errors.test.ts
@src/cli/doctor/check-native-deps.ts
</context>

<tasks>

<task type="auto">
  <name>Task 60-03-01: Tighten SQLite-family fallback visibility and total-failure remediation</name>
  <files>src/infrastructure/storage/adapters/sqlite-loader.ts, src/cli/output/error-codes.ts, src/cli/output/errors.ts, src/cli/output/wasm-fallback.ts, src/cli/doctor/check-native-deps.ts</files>
  <read_first>
    - src/infrastructure/storage/adapters/sqlite-loader.ts
    - src/cli/output/error-codes.ts
    - src/cli/output/errors.ts
    - src/cli/output/wasm-fallback.ts
    - src/cli/doctor/check-native-deps.ts
  </read_first>
  <action>
Review the SQLite loader and surrounding output/doctor seams so fallback remains native-first but is no longer “just a console warning”. Keep the fallback ladder inside the SQLite family only, preserve actionable total-failure behavior, and expose fallback visibility through the strongest shared seam available in the repo, preferably by reusing the existing `WasmFallbackResult` pattern or an equivalent structured metadata contract that AI/human consumers can both observe. Do not reintroduce filesystem/Kuzu rescue behavior while making fallback visibility more explicit.
  </action>
  <verify>
    rtk rg -n "Native better-sqlite3 unavailable, using WASM fallback|createActionableError|DEP_NATIVE_MISSING|sql.js|node:sqlite" src/infrastructure/storage/adapters/sqlite-loader.ts src/cli/output/error-codes.ts src/cli/output/errors.ts src/cli/output/wasm-fallback.ts src/cli/doctor/check-native-deps.ts
    ! rtk rg -n "filesystem|kuzudb" src/infrastructure/storage/adapters/sqlite-loader.ts src/cli/doctor/check-native-deps.ts
  </verify>
  <acceptance_criteria>
    - fallback path still contains native-first SQLite-family behavior
    - touched fallback/doctor/output files do not mention filesystem/Kuzu rescue behavior
    - total-failure path still emits actionable remediation
    - fallback visibility is exposed in a testable shared seam, not only an ad hoc comment
  </acceptance_criteria>
  <done>
    - SQLite fallback remains inside one backend family
    - Users and automation can observe fallback status
    - Native dependency failures have one aligned remediation story
  </done>
</task>

<task type="auto">
  <name>Task 60-03-02: Add focused proof for SQLite fallback branches and post-cutover storage behavior</name>
  <files>src/infrastructure/storage/__tests__/SQLiteStorage.test.ts, src/infrastructure/storage/__tests__/fallback-mechanism.test.ts, src/cli/output/__tests__/errors.test.ts, src/infrastructure/storage/adapters/SQLiteStorage.ts</files>
  <read_first>
    - src/infrastructure/storage/__tests__/SQLiteStorage.test.ts
    - src/infrastructure/storage/__tests__/fallback-mechanism.test.ts
    - src/cli/output/__tests__/errors.test.ts
    - src/infrastructure/storage/adapters/SQLiteStorage.ts
    - src/infrastructure/storage/adapters/sqlite-loader.ts
  </read_first>
  <action>
Add targeted tests or fixtures that prove three branches: native sqlite load succeeds, native sqlite failure can still use a SQLite-family fallback path, and both native + fallback failure surfaces actionable remediation. Keep at least one real-ish storage behavior assertion through `SQLiteStorage` or its existing tests so the phase verifies more than mocked error formatting. If `fallback-mechanism.test.ts` still exists after 60-01, repurpose it toward SQLite-family fallback proof rather than cross-family backend switching.
  </action>
  <verify>
    rtk rg -n "better-sqlite3|sql.js|node:sqlite|DEP_NATIVE_MISSING|nextCommand|rootCause|remediationPlan" src/infrastructure/storage/__tests__/SQLiteStorage.test.ts src/infrastructure/storage/__tests__/fallback-mechanism.test.ts src/cli/output/__tests__/errors.test.ts
    rtk rg -n "governance.sqlite" src/infrastructure/storage/__tests__/SQLiteStorage.test.ts
  </verify>
  <acceptance_criteria>
    - tests cover native-fail → SQLite-family-fallback success
    - tests cover total loader failure with actionable remediation fields
    - tests no longer encode cross-family fallback expectations
    - at least one touched test still exercises real SQLiteStorage behavior, not only mocked formatting
  </acceptance_criteria>
  <done>
    - Failure-path evidence exists for the roadmap fallback promise
    - SQLiteStorage remains behaviorally verified after the convergence changes
    - No test still normalizes cross-backend downgrade as acceptable behavior
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Native dependency detection and fallback handling influence both runtime correctness and user recovery steps.
- Warning/error surfaces can hide fallback state from automation if they remain console-only.

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-03-1 | Denial of service | SQLite native dependency path | mitigate | Keep native-first then SQLite-family fallback behavior verified, and surface total-failure remediation when all loaders fail. |
| T-60-03-2 | Repudiation | fallback visibility | mitigate | Expose fallback status through shared output/doctor seams so it is observable in tests and automation, not only ad hoc console warnings. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Focused tests prove native success, SQLite-family fallback success, and total-failure remediation
- [ ] No fallback-path test still encodes filesystem/Kuzu rescue semantics
- [ ] Doctor/output guidance and loader remediation stay aligned on SQLite dependency recovery
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Native sqlite degradation stays inside the SQLite family
- Fallback and total-failure behavior are both visible and verified

</success_criteria>

<output>
After completion, create `.planning/phases/60-storage-convergence/60-03-SUMMARY.md`
</output>
