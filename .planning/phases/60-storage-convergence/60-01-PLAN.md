---
phase: 60-storage-convergence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/infrastructure/storage/StorageFactory.ts
  - src/interface/types/storage.ts
  - src/infrastructure/storage/index.ts
  - src/infrastructure/storage/__tests__/StorageFactory.test.ts
  - src/infrastructure/storage/__tests__/fallback-mechanism.test.ts
autonomous: true
requirements:
  - STOR-01
  - STOR-02
user_setup: []
must_haves:
  truths:
    - "`storage.type: \"auto\"` no longer selects or falls back to `filesystem` / `kuzudb`; persistent storage truth is SQLite-only (covers D-01, D-06)."
    - "Explicit `filesystem` / `kuzudb` requests fail deterministically instead of degrading across backend families (covers D-03, D-04)."
    - "Public storage type helpers stop advertising deprecated persistent backends as healthy runtime options (covers D-02, D-10)."
  artifacts:
    - path: "src/infrastructure/storage/StorageFactory.ts"
      provides: "Single runtime truth for storage backend selection"
      contains: "UNSUPPORTED_STORAGE_TYPE"
    - path: "src/interface/types/storage.ts"
      provides: "Narrowed storage contract aligned with SQLite-only persistence"
      contains: "StorageType"
    - path: "src/infrastructure/storage/__tests__/fallback-mechanism.test.ts"
      provides: "Failure-path proof that legacy backends no longer fall back across families"
      contains: "UNSUPPORTED_STORAGE_TYPE"
  key_links:
    - from: "src/infrastructure/storage/StorageFactory.ts"
      to: "src/interface/types/storage.ts"
      via: "factory selection logic and public storage contract agree on the only supported persistent family"
      pattern: "StorageType"
    - from: "src/infrastructure/storage/StorageFactory.ts"
      to: "src/infrastructure/storage/__tests__/fallback-mechanism.test.ts"
      via: "tests prove unsupported backend rejection and SQLite-only auto behavior"
      pattern: "UNSUPPORTED_STORAGE_TYPE"
---

<objective>
Collapse storage runtime truth onto SQLite: remove healthy filesystem/Kuzu execution paths, redefine `auto` as SQLite-family-only behavior, and make unsupported legacy backends fail explicitly.

Purpose: Phase 60 cannot succeed if the factory still silently preserves old backend families.
Output: one storage-factory truth, narrowed storage types, and tests that prove legacy backend requests fail instead of cross-family fallback.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-storage-convergence/60-CONTEXT.md
@.planning/phases/60-storage-convergence/60-RESEARCH.md
@src/infrastructure/storage/StorageFactory.ts
@src/interface/types/storage.ts
@src/infrastructure/storage/index.ts
@src/infrastructure/storage/__tests__/StorageFactory.test.ts
@src/infrastructure/storage/__tests__/fallback-mechanism.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 60-01-01: Rewrite StorageFactory selection truth to SQLite-only persistence</name>
  <files>src/infrastructure/storage/StorageFactory.ts, src/infrastructure/storage/index.ts</files>
  <read_first>
    - src/infrastructure/storage/StorageFactory.ts
    - src/infrastructure/storage/index.ts
    - src/infrastructure/storage/adapters/SQLiteStorage.ts
    - src/infrastructure/storage/adapters/sqlite-loader.ts
  </read_first>
  <action>
Refactor `src/infrastructure/storage/StorageFactory.ts` so persistent runtime truth only resolves within the SQLite family. Remove healthy runtime paths that instantiate `FileSystemStorage` or lazy-load `KuzuDBStorage` from the factory selection flow. Keep `memory` as an explicit non-persistent/testing backend, but make `storage.type: 'auto'` resolve to the SQLite path rather than repo-size or dependency-based backend switching. If callers explicitly request `filesystem` or `kuzudb`, reject immediately with deterministic `UNSUPPORTED_STORAGE_TYPE` semantics instead of warning and falling back. Update any storage export surface in `src/infrastructure/storage/index.ts` so it no longer presents deprecated backend support as active runtime truth.
  </action>
  <verify>
    ! rtk rg -n "new FileSystemStorage|loadKuzuDBStorage|falling back to filesystem|return 'filesystem'|return 'kuzudb'" src/infrastructure/storage/StorageFactory.ts
    rtk rg -n "UNSUPPORTED_STORAGE_TYPE|case 'sqlite'|case 'memory'|type === 'auto'" src/infrastructure/storage/StorageFactory.ts
  </verify>
  <acceptance_criteria>
    - `src/infrastructure/storage/StorageFactory.ts` does not instantiate `FileSystemStorage`
    - `src/infrastructure/storage/StorageFactory.ts` does not lazy-load `KuzuDBStorage`
    - `src/infrastructure/storage/StorageFactory.ts` contains `UNSUPPORTED_STORAGE_TYPE`
    - `src/infrastructure/storage/StorageFactory.ts` still supports `sqlite`
    - `src/infrastructure/storage/StorageFactory.ts` still supports `memory`
  </acceptance_criteria>
  <done>
    - StorageFactory has one persistent backend family
    - `auto` no longer means cross-backend magic
    - Legacy backend requests fail instead of silently mutating backend families
  </done>
</task>

<task type="auto">
  <name>Task 60-01-02: Narrow public storage types and replace legacy fallback tests</name>
  <files>src/interface/types/storage.ts, src/infrastructure/storage/__tests__/StorageFactory.test.ts, src/infrastructure/storage/__tests__/fallback-mechanism.test.ts</files>
  <read_first>
    - src/interface/types/storage.ts
    - src/infrastructure/storage/__tests__/StorageFactory.test.ts
    - src/infrastructure/storage/__tests__/fallback-mechanism.test.ts
    - .planning/phases/60-storage-convergence/60-CONTEXT.md
  </read_first>
  <action>
Update `src/interface/types/storage.ts` so public storage contracts align with the new runtime truth: `StorageType` should only represent supported active backends, and any remaining `auto` support should clearly mean SQLite-family auto-selection rather than repository-size heuristics. Remove or de-emphasize `autoThresholds` if it only exists to support the old graph-db switching model. Rewrite storage tests so they stop asserting `kuzudb`/`filesystem` success and instead prove three behaviors: `auto` resolves to SQLite-family storage, explicit `filesystem` rejects with unsupported-storage semantics, and explicit `kuzudb` rejects with the same deterministic failure contract. Preserve at least one failure-path assertion, not only the happy path.
  </action>
  <verify>
    rtk rg -n "export type StorageType = 'sqlite' \\| 'memory'|UNSUPPORTED_STORAGE_TYPE|auto" src/interface/types/storage.ts src/infrastructure/storage/__tests__/StorageFactory.test.ts src/infrastructure/storage/__tests__/fallback-mechanism.test.ts
    ! rtk rg -n "expect\\(\\['kuzudb', 'filesystem'\\]\\)|prefer kuzudb when available|fallback to filesystem when kuzudb unavailable" src/infrastructure/storage/__tests__/StorageFactory.test.ts src/infrastructure/storage/__tests__/fallback-mechanism.test.ts
  </verify>
  <acceptance_criteria>
    - `src/interface/types/storage.ts` does not expose `StorageType = 'filesystem' | 'kuzudb'`
    - fallback tests do not assert successful `kuzudb` or `filesystem` runtime selection
    - storage tests assert `UNSUPPORTED_STORAGE_TYPE` for deprecated explicit backends
    - at least one test asserts `auto` resolves inside the SQLite family
  </acceptance_criteria>
  <done>
    - Public types and runtime selection truth match
    - Legacy fallback expectations are removed from tests
    - Failure-path evidence exists for deprecated backend rejection
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Config/CLI/server inputs can still send stale backend names into the storage boundary.
- Storage selection determines persistence semantics and must not silently switch to an unintended backend family.

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01-1 | Tampering | storage backend input | mitigate | Reject explicit `filesystem` / `kuzudb` with deterministic unsupported-storage errors instead of executing deprecated backends. |
| T-60-01-2 | Repudiation | storage selection truth | mitigate | Collapse `auto` onto SQLite-family logic in one factory path so runtime truth is inspectable and testable. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Targeted storage-factory tests prove explicit `filesystem` / `kuzudb` rejection
- [ ] `rtk rg` on touched runtime files shows no active cross-family fallback messaging
- [ ] `auto` path is verified as SQLite-only rather than dependency-driven backend switching
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Storage runtime truth is SQLite-only for persistence
- Explicit deprecated backend requests fail deterministically

</success_criteria>

<output>
After completion, create `.planning/phases/60-storage-convergence/60-01-SUMMARY.md`
</output>
