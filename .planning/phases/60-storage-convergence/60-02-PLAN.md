---
phase: 60-storage-convergence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/config-loader.ts
  - src/interface/config/index.ts
  - src/cli/output/error-codes.ts
  - src/cli/output/errors.ts
  - src/cli/__tests__/config-loader.test.ts
  - src/cli/commands/generate.ts
  - src/cli/commands/__tests__/generate.test.ts
  - docs/ai-guide/OUTPUT.md
  - ARCHITECTURE.md
autonomous: true
requirements:
  - STOR-02
  - STOR-04
user_setup: []
must_haves:
  truths:
    - "Config defaults and validation now describe SQLite as the only recommended persistent backend, with `auto` as a SQLite-family alias rather than legacy backend roulette (covers D-01, D-02, D-10)."
    - "Deprecated `filesystem` / `kuzudb` config values fail with actionable unsupported-storage remediation instead of normalizing to healthy runtime state (covers D-03, D-04, D-05)."
    - "Docs, generate/runtime messaging, and error-code contracts all tell the same storage story after the cutover (covers D-04, D-09)."
  artifacts:
    - path: "src/cli/config-loader.ts"
      provides: "Canonical storage config validation and defaults"
      contains: "storage.type"
    - path: "src/cli/output/error-codes.ts"
      provides: "Canonical unsupported-storage remediation entry"
      contains: "UNSUPPORTED_STORAGE_TYPE"
    - path: "docs/ai-guide/OUTPUT.md"
      provides: "User-facing output contract aligned with SQLite-only persistence"
      contains: "SQLite"
  key_links:
    - from: "src/cli/config-loader.ts"
      to: "src/cli/output/errors.ts"
      via: "invalid storage config routes into actionable remediation rather than silent normalization"
      pattern: "UNSUPPORTED_STORAGE_TYPE"
    - from: "src/cli/commands/generate.ts"
      to: "src/cli/__tests__/config-loader.test.ts"
      via: "config defaults and generate-time storage messaging stay in sync"
      pattern: "sqlite"
---

<objective>
Move config/default/runtime truth onto SQLite-only persistence: reject deprecated storage config values, change defaults, and align user-facing output/docs with the converged backend contract.

Purpose: Storage convergence is incomplete if CLI/config/docs still present filesystem or Kuzu as healthy persistent choices.
Output: canonical config validation, unsupported-storage remediation, synchronized tests, and doc/runtime wording that consistently points to SQLite.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-storage-convergence/60-CONTEXT.md
@.planning/phases/60-storage-convergence/60-RESEARCH.md
@src/cli/config-loader.ts
@src/interface/config/index.ts
@src/cli/output/error-codes.ts
@src/cli/output/errors.ts
@src/cli/__tests__/config-loader.test.ts
@src/cli/commands/generate.ts
@src/cli/commands/__tests__/generate.test.ts
@docs/ai-guide/OUTPUT.md
@ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 60-02-01: Converge config validation and defaults on SQLite-only persistence</name>
  <files>src/cli/config-loader.ts, src/interface/config/index.ts, src/cli/__tests__/config-loader.test.ts</files>
  <read_first>
    - src/cli/config-loader.ts
    - src/interface/config/index.ts
    - src/cli/__tests__/config-loader.test.ts
    - src/interface/types/storage.ts
  </read_first>
  <action>
Update `src/cli/config-loader.ts` and any config-facing type surfaces in `src/interface/config/index.ts` so deprecated storage values are rejected at the config boundary instead of reaching runtime. Change the default storage config from filesystem-backed persistence to SQLite-backed persistence, and remove config validation language that still advertises `filesystem` or `kuzudb` as supported persistent backends. If `auto` remains valid, define it explicitly as a SQLite-family alias rather than dependency- or threshold-driven backend choice. Rewrite config-loader tests so the default expectation, accepted explicit values, and failure cases all match the new contract.
  </action>
  <verify>
    rtk rg -n "type: 'sqlite'|UNSUPPORTED_STORAGE_TYPE|deprecated|auto" src/cli/config-loader.ts src/cli/__tests__/config-loader.test.ts
    ! rtk rg -n "type: 'filesystem'.*outputPath|type: 'kuzudb'.*databasePath|仅支持 filesystem、sqlite、kuzudb、memory、auto" src/cli/config-loader.ts src/cli/__tests__/config-loader.test.ts
  </verify>
  <acceptance_criteria>
    - `src/cli/config-loader.ts` default storage is SQLite-based, not filesystem-based
    - config loader does not accept `filesystem` as a healthy persistent backend
    - config loader does not accept `kuzudb` as a healthy persistent backend
    - tests assert at least one deprecated-backend failure path
    - tests still cover valid SQLite config normalization
  </acceptance_criteria>
  <done>
    - Config boundary matches the new storage truth
    - Default config points users at SQLite
    - Legacy config values fail before runtime work begins
  </done>
</task>

<task type="auto">
  <name>Task 60-02-02: Align runtime messaging, generate path, and docs with unsupported-storage remediation</name>
  <files>src/cli/output/error-codes.ts, src/cli/output/errors.ts, src/cli/commands/generate.ts, src/cli/commands/__tests__/generate.test.ts, docs/ai-guide/OUTPUT.md, ARCHITECTURE.md</files>
  <read_first>
    - src/cli/output/error-codes.ts
    - src/cli/output/errors.ts
    - src/cli/commands/generate.ts
    - src/cli/commands/__tests__/generate.test.ts
    - docs/ai-guide/OUTPUT.md
    - ARCHITECTURE.md
  </read_first>
  <action>
Add or wire a canonical unsupported-storage remediation contract so deprecated storage inputs produce actionable failure text in both machine and human output modes. Update `src/cli/commands/generate.ts` and its tests so generate-time logging and storage wiring no longer describe filesystem/Kuzu as active MVP3 truth. Then sync `docs/ai-guide/OUTPUT.md` and `ARCHITECTURE.md` so documentation matches shipped runtime behavior: SQLite is the single persistent backend family, `auto` is SQLite-only, and deprecated backend requests fail with guidance instead of running.
  </action>
  <verify>
    rtk rg -n "UNSUPPORTED_STORAGE_TYPE|SQLite|auto" src/cli/output/error-codes.ts src/cli/output/errors.ts src/cli/commands/generate.ts docs/ai-guide/OUTPUT.md ARCHITECTURE.md
    ! rtk rg -n "MVP3 Storage \\(kuzudb\\)|MVP3 Storage \\(filesystem\\)|Use filesystem, sqlite, kuzudb, memory, or auto instead" src/cli/commands/generate.ts src/cli/commands/__tests__/generate.test.ts docs/ai-guide/OUTPUT.md ARCHITECTURE.md
  </verify>
  <acceptance_criteria>
    - error registry contains a canonical unsupported-storage remediation entry or equivalent explicit mapping
    - generate command tests do not assert successful Kuzu/filesystem persistent runtime messaging
    - docs state SQLite as the only persistent backend truth
    - at least one test proves actionable unsupported-storage output fields or formatted messaging
  </acceptance_criteria>
  <done>
    - Runtime messaging and docs tell one storage story
    - Deprecated backends fail with actionable remediation
    - Generate-time surfaces no longer reinforce removed backend families
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Config files and CLI surfaces are user-controlled and can continue to send stale backend names.
- User-facing docs/output can drift from runtime and create misleading remediation paths.

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-02-1 | Spoofing | config/docs/backend guidance | mitigate | Reject deprecated storage values at the config boundary and update docs/help to describe only SQLite persistence. |
| T-60-02-2 | Repudiation | error/remediation contract | mitigate | Add one canonical unsupported-storage remediation path so human/JSON outputs expose the same next-step guidance. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Config-loader tests prove default SQLite config and deprecated-backend rejection
- [ ] Generate/runtime messaging no longer presents filesystem/Kuzu as healthy persistent backends
- [ ] Docs and output contracts explicitly match the post-cutover storage truth
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Config defaults, runtime messaging, and docs all point to SQLite-only persistence
- Deprecated storage config values fail with actionable remediation

</success_criteria>

<output>
After completion, create `.planning/phases/60-storage-convergence/60-02-SUMMARY.md`
</output>
