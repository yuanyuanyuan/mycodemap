---
phase: 1000-evaluate-arcadedb-as-alternative-to-k-zudb
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/infrastructure/storage/adapters/ArcadeDBStorage.ts
  - src/infrastructure/storage/StorageFactory.ts
  - src/interface/types/storage.ts
  - mycodemap.config.schema.json
autonomous: true
requirements:
  - ARC-01
  - ARC-02
  - ARC-03
must_haves:
  truths:
    - ArcadeDBStorage 类实现 IStorage 接口所有方法
    - StorageFactory.create() 支持 'arcadedb' 类型
    - ArcadeDB 失败时自动降级到文件系统
    - 配置 schema 包含 arcadedb 选项
  artifacts:
    - path: src/infrastructure/storage/adapters/ArcadeDBStorage.ts
      provides: ArcadeDB 存储适配器实现
      min_lines: 200
      exports: [ArcadeDBStorage]
    - path: src/infrastructure/storage/StorageFactory.ts
      provides: 扩展的存储工厂
      contains: "case 'arcadedb':"
    - path: src/interface/types/storage.ts
      provides: 更新的存储类型
      contains: "'arcadedb'"
    - path: mycodemap.config.schema.json
      provides: 配置 schema
      contains: '"arcadedb"'
  key_links:
    - from: StorageFactory.create()
      to: ArcadeDBStorage
      via: "case 'arcadedb'"
    - from: ArcadeDBStorage
      to: StorageBase
      via: extends
    - from: StorageFactory fallback
      to: FileSystemStorage
      via: arcadedb unavailable
---

<objective>
实现完整的 ArcadeDB 存储适配器，替换现有的 KùzuDB 支持。

Purpose: 提供功能完整的 ArcadeDB 存储后端，保持与现有存储接口兼容，实现自动降级机制。
Output: ArcadeDBStorage 适配器、扩展的 StorageFactory、更新的类型定义和配置 schema。
</objective>

<execution_context>
@/data/codemap/.claude/get-shit-done/workflows/execute-plan.md
@/data/codemap/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-CONTEXT.md
@.planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-RESEARCH.md

## 参考实现 (KuzuDBStorage.ts)
位置: src/infrastructure/storage/adapters/KuzuDBStorage.ts:1-318

关键模式:
- 延迟加载可选依赖: `async function loadKuzuModule()`
- 继承 StorageBase 实现 IStorage
- 使用 graph-helpers.ts 中的函数处理图操作
- 嵌入式模式初始化
- 参数化查询防止注入

## StorageBase 接口要求
位置: src/infrastructure/storage/interfaces/StorageBase.ts:42-190

必须实现的抽象方法:
- doInitialize(): Promise<void>
- doClose(): Promise<void>
- saveCodeGraph(graph: CodeGraph): Promise<void>
- loadCodeGraph(): Promise<CodeGraph>
- deleteProject(): Promise<void>
- updateModule(module: Module): Promise<void>
- deleteModule(moduleId: string): Promise<void>
- findModuleById(id: string): Promise<Module | null>
- findModulesByPath(path: string): Promise<Module[]>
- findSymbolByName(name: string): Promise<Symbol[]>
- findSymbolById(id: string): Promise<Symbol | null>
- findDependencies(moduleId: string): Promise<Dependency[]>
- findDependents(moduleId: string): Promise<Dependency[]>
- findCallers(functionId: string): Promise<Symbol[]>
- findCallees(functionId: string): Promise<Symbol[]>
- detectCycles(): Promise<Cycle[]>
- calculateImpact(moduleId: string, depth: number): Promise<ImpactResult>
- getStatistics(): Promise<ProjectStatistics>

## 现有 StorageFactory 结构
位置: src/infrastructure/storage/StorageFactory.ts:1-197

降级机制已实现:
- auto 模式: 静默 fallback 到 filesystem
- 显式 arcadedb: 抛出错误由调用者处理降级

需要添加:
- 延迟加载 ArcadeDBStorage
- 'arcadedb' case 分支
- checkArcadeDBAvailability() 方法

## 类型定义
位置: src/interface/types/storage.ts:14

当前 StorageType: 'filesystem' | 'kuzudb' | 'memory'
需要添加: 'arcadedb'

## 配置 Schema
位置: mycodemap.config.schema.json:48-53

当前 storage.type enum: ["filesystem", "kuzudb", "memory", "auto"]
需要添加: "arcadedb"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ArcadeDBStorage adapter</name>
  <files>src/infrastructure/storage/adapters/ArcadeDBStorage.ts</files>
  <read_first>
    - src/infrastructure/storage/adapters/KuzuDBStorage.ts
    - src/infrastructure/storage/interfaces/StorageBase.ts
    - src/infrastructure/storage/graph-helpers.ts
  </read_first>
  <action>
创建 ArcadeDB 存储适配器，实现与 KuzuDBStorage 功能对等。

文件头必须包含:
```typescript
// [META] since:2026-03 | owner:architecture-team | stable:false
// [WHY] ArcadeDB storage adapter - Apache 2.0 licensed graph database backend
```

实现要求:
1. 延迟加载 arcadedb 模块:
   ```typescript
   async function loadArcadeDBModule() {
     return await import('arcadedb');
   }
   ```

2. 类定义:
   ```typescript
   export class ArcadeDBStorage extends StorageBase {
     readonly type = 'arcadedb' as const;
     private db: ArcadeDB | null = null;
     private conn: ArcadeDBConnection | null = null;
   }
   ```

3. doInitialize() 实现:
   - 使用嵌入式模式: `new ArcadeDB({ mode: 'embedded', path: dbPath })`
   - 数据库路径: `join(this.projectPath, this.config.databasePath || '.codemap/arcadedb')`
   - 创建 Snapshot 节点表 (使用 ArcadeDB 的 Cypher 语法)

4. 数据持久化策略 (与 KuzuDBStorage 相同):
   - 使用 Snapshot 节点存储序列化的 CodeGraph
   - saveCodeGraph: 删除旧快照，创建新快照
   - loadCodeGraph: 读取快照并反序列化

5. 所有查询方法使用 graph-helpers.ts 中的函数:
   - import { upsertModuleInGraph, deleteModuleFromGraph, ... } from '../graph-helpers.js'

6. 错误处理:
   - 使用 StorageError 类
   - 错误代码前缀: 'ARCADE_'

7. 参考 KuzuDBStorage.ts:107-133 的初始化逻辑
8. 参考 KuzuDBStorage.ts:149-162 的 saveCodeGraph 逻辑
9. 参考 KuzuDBStorage.ts:164-175 的 loadCodeGraph 逻辑
  </action>
  <acceptance_criteria>
    - ArcadeDBStorage.ts 文件存在且可编译
    - 文件包含 `export class ArcadeDBStorage extends StorageBase`
    - 包含 `readonly type = 'arcadedb'`
    - 包含 `doInitialize()` 方法实现
    - 包含 `doClose()` 方法实现
    - 包含所有必需的 IStorage 方法实现
    - 使用延迟加载模式导入 arcadedb
    - 错误代码使用 'ARCADE_' 前缀
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck -- --noEmit 2>&1 | grep -c "ArcadeDBStorage" || echo "0"</automated>
  </verify>
  <done>ArcadeDBStorage 适配器实现完成，通过类型检查</done>
</task>

<task type="auto">
  <name>Task 2: Extend StorageFactory for arcadedb type</name>
  <files>src/infrastructure/storage/StorageFactory.ts</files>
  <read_first>
    - src/infrastructure/storage/StorageFactory.ts
    - src/infrastructure/storage/adapters/ArcadeDBStorage.ts (Task 1 output)
  </read_first>
  <action>
扩展 StorageFactory 以支持 'arcadedb' 存储类型。

修改 src/infrastructure/storage/StorageFactory.ts:

1. 在文件顶部添加延迟加载器 (参考第 17-29 行 KuzuDBStorage 模式):
   ```typescript
   let ArcadeDBStorage: typeof import('./adapters/ArcadeDBStorage.js').ArcadeDBStorage | null = null;

   async function loadArcadeDBStorage(): Promise<typeof ArcadeDBStorage> {
     if (!ArcadeDBStorage) {
       const module = await import('./adapters/ArcadeDBStorage.js');
       ArcadeDBStorage = module.ArcadeDBStorage;
     }
     return ArcadeDBStorage;
   }
   ```

2. 在 create() 方法的 switch 语句中添加 'arcadedb' case (在第 79 行后添加):
   ```typescript
   case 'arcadedb': {
     const ArcadeStorage = await loadArcadeDBStorage();
     if (!ArcadeStorage) {
       throw new StorageError(
         'ArcadeDB storage adapter not available. Install with: npm install arcadedb',
         'ADAPTER_NOT_AVAILABLE'
       );
     }
     return new ArcadeStorage(config);
   }
   ```

3. 添加 checkArcadeDBAvailability() 方法 (参考第 111-122 行 checkKuzuAvailability):
   ```typescript
   private async checkArcadeDBAvailability(): Promise<boolean> {
     try {
       const arcadedb = await import('arcadedb');
       // 验证基本功能
       const db = new arcadedb.default({
         mode: 'embedded',
         path: ':memory:'
       });
       await db.close();
       return true;
     } catch {
       return false;
     }
   }
   ```

4. 修改 determineStorageType() 方法:
   - 将 KùzuDB 检查替换为 ArcadeDB 检查
   - 优先尝试 ArcadeDB，不可用时 fallback 到 filesystem
   - 更新错误消息: 'ArcadeDB is not available. Install with: npm install arcadedb'
   - 更新错误代码: 'ARCADE_NOT_AVAILABLE'

5. 修改 isStorageTypeAvailable() 静态方法 (第 165-181 行):
   - 添加 'arcadedb' case，使用 require.resolve('arcadedb') 检查
   - 移除 'kuzudb' case 或标记为已弃用

6. 修改 getAvailableStorageTypes() 静态方法 (第 187-190 行):
   - 更新数组: ['filesystem', 'memory', 'arcadedb']
   - 移除 'kuzudb'

7. 更新降级逻辑 (第 54-60 行):
   - 将 'kuzudb' 替换为 'arcadedb'
   - 更新警告消息: '[StorageFactory] ArcadeDB unavailable, falling back to filesystem'

8. 更新注释 (第 35-38 行):
   - 将 KùzuDB 相关注释替换为 ArcadeDB
   - 说明 ArcadeDB 使用 Apache 2.0 许可证
  </action>
  <acceptance_criteria>
    - StorageFactory.ts 包含 loadArcadeDBStorage 函数
    - create() 方法包含 'arcadedb' case 分支
    - determineStorageType() 优先检查 ArcadeDB
    - isStorageTypeAvailable() 包含 'arcadedb' case
    - getAvailableStorageTypes() 返回包含 'arcadedb' 的数组
    - 降级逻辑使用 'arcadedb' 而非 'kuzudb'
    - 所有 KùzuDB 相关注释已更新为 ArcadeDB
  </acceptance_criteria>
  <verify>
    <automated>grep -n "arcadedb" src/infrastructure/storage/StorageFactory.ts | head -20</automated>
  </verify>
  <done>StorageFactory 扩展完成，支持 arcadedb 类型和降级机制</done>
</task>

<task type="auto">
  <name>Task 3: Update type definitions and config schema</name>
  <files>
    src/interface/types/storage.ts
    mycodemap.config.schema.json
  </files>
  <read_first>
    - src/interface/types/storage.ts
    - mycodemap.config.schema.json
  </read_first>
  <action>
更新类型定义和配置 schema 以支持 'arcadedb' 存储类型。

修改 src/interface/types/storage.ts:

1. 更新 StorageType 定义 (第 14 行):
   ```typescript
   export type StorageType = 'filesystem' | 'arcadedb' | 'memory';
   ```
   - 添加 'arcadedb'
   - 移除 'kuzudb'

2. 更新 StorageConfig 注释 (第 74-75 行):
   - 将 "KùzuDB 配置" 改为 "ArcadeDB 配置"
   - databasePath 用于 ArcadeDB 嵌入式数据库路径

修改 mycodemap.config.schema.json:

1. 更新 storage.type enum (第 50 行):
   ```json
   "enum": ["filesystem", "arcadedb", "memory", "auto"]
   ```
   - 添加 "arcadedb"
   - 移除 "kuzudb"

2. 更新 storage.type description (第 52 行):
   ```json
   "description": "Storage backend type (KùzuDB is no longer supported, use ArcadeDB instead)"
   ```

3. 更新 databasePath description (第 61 行):
   ```json
   "description": "ArcadeDB database directory relative to project root"
   ```

4. 验证 JSON schema 有效性:
   - 确保 JSON 格式正确
   - 确保没有尾随逗号
  </action>
  <acceptance_criteria>
    - storage.ts 中 StorageType 包含 'arcadedb'
    - storage.ts 中 StorageType 不包含 'kuzudb'
    - mycodemap.config.schema.json storage.type enum 包含 "arcadedb"
    - mycodemap.config.schema.json storage.type enum 不包含 "kuzudb"
    - databasePath description 提及 ArcadeDB
    - JSON schema 格式有效
  </acceptance_criteria>
  <verify>
    <automated>grep -n "StorageType" src/interface/types/storage.ts | head -5 && grep -n "arcadedb" mycodemap.config.schema.json</automated>
  </verify>
  <done>类型定义和配置 schema 更新完成</done>
</task>

</tasks>

<verification>
1. 类型检查通过: `npm run typecheck`
2. 构建成功: `npm run build`
3. ArcadeDBStorage 导出正确: `grep "export class ArcadeDBStorage" src/infrastructure/storage/adapters/ArcadeDBStorage.ts`
4. StorageFactory 包含 arcadedb 支持: `grep "case 'arcadedb'" src/infrastructure/storage/StorageFactory.ts`
5. 配置 schema 有效: `node -e "JSON.parse(require('fs').readFileSync('mycodemap.config.schema.json'))"`
</verification>

<success_criteria>
- ArcadeDBStorage 适配器完整实现 IStorage 接口
- StorageFactory 支持 'arcadedb' 类型并正确降级
- 类型定义和配置 schema 更新
- 所有代码通过类型检查
- 降级机制正常工作 (ArcadeDB 不可用时 fallback 到 filesystem)
</success_criteria>

<output>
After completion, create `.planning/phases/1000-evaluate-arcadedb-as-alternative-to-k-zudb/1000-01-SUMMARY.md`
</output>
