---
phase: 69-pythontypeenhancer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/parser/enhancers/PythonTypeEnhancer.ts
  - src/parser/enhancers/__tests__/PythonTypeEnhancer.test.ts
  - tests/fixtures/python/type-enhancer-google.py
  - tests/fixtures/python/type-enhancer-numpy.py
  - tests/fixtures/python/type-enhancer-sphinx.py
  - tests/fixtures/python/type-enhancer-ambiguous.py
autonomous: true
requirements:
  - PY-05
must_haves:
  truths:
    - Python files with explicit annotations or supported docstring fields produce top-level type metadata without changing parser routing.
    - Google, NumPy, and Sphinx mainstream parameter, return, and attribute declarations are parsed through one bounded enhancer seam.
    - Ambiguous or weakly structured docstring prose fails soft and does not invent type facts.
  artifacts:
    - path: src/parser/enhancers/PythonTypeEnhancer.ts
      provides: Annotation-first Python post-parse type enrichment and targeted symbol backfill
    - path: src/parser/enhancers/__tests__/PythonTypeEnhancer.test.ts
      provides: Fixture-backed proof for supported docstring styles and fail-soft behavior
    - path: tests/fixtures/python/type-enhancer-google.py
      provides: Google-style fixture with Args, Returns, and Attributes coverage
    - path: tests/fixtures/python/type-enhancer-numpy.py
      provides: NumPy-style fixture with Parameters and Returns coverage
    - path: tests/fixtures/python/type-enhancer-sphinx.py
      provides: Sphinx-style fixture with :param, :type, :returns, and :rtype coverage
    - path: tests/fixtures/python/type-enhancer-ambiguous.py
      provides: Negative fixture proving ambiguity is skipped
  key_links:
    - from: tests/fixtures/python/type-enhancer-google.py
      to: src/parser/enhancers/PythonTypeEnhancer.ts
      via: direct enhancer fixture parsing
      pattern: Args:|Returns:|Attributes:
    - from: tests/fixtures/python/type-enhancer-numpy.py
      to: src/parser/enhancers/PythonTypeEnhancer.ts
      via: NumPy section parsing
      pattern: Parameters|Returns|Attributes
    - from: tests/fixtures/python/type-enhancer-sphinx.py
      to: src/parser/enhancers/PythonTypeEnhancer.ts
      via: Sphinx field parsing
      pattern: :param|:type|:returns|:rtype:
---

<objective>
Create the Python post-parse type enhancement seam for Phase 69. Deliver a `PythonTypeEnhancer` that converts explicit Python annotations and a stable subset of Google/NumPy/Sphinx docstring fields into top-level `result.typeInfo`, with targeted backfill for directly visible symbol fields and fail-soft handling for ambiguous prose.

Purpose: land `PY-05` without reopening parser routing, registry selection, or Phase 71 contract cleanup.
Output: new enhancer, focused fixtures, and dedicated tests that prove style support plus ambiguity refusal.
</objective>

<execution_context>
@/home/stark/.codex/get-shit-done/workflows/execute-plan.md
@/home/stark/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/69-pythontypeenhancer/69-CONTEXT.md
@.planning/phases/69-pythontypeenhancer/69-RESEARCH.md
@.planning/phases/69-pythontypeenhancer/69-PATTERNS.md
@src/parser/enhancers/TypeScriptTypeEnhancer.ts
@src/infrastructure/parser/implementations/PythonTreeSitterParser.ts
@src/parser/interfaces/IParser.ts
@tests/fixtures/python/comprehensive.py

<interfaces>
From `src/parser/interfaces/IParser.ts`:
```typescript
export interface ParseResult {
  path: string;
  exports: ExportInfo[];
  imports: ImportInfo[];
  symbols: ModuleSymbol[];
  dependencies: string[];
  type: 'source' | 'test' | 'config' | 'type';
  stats: { lines: number; codeLines: number; commentLines: number; blankLines: number };
  typeInfo?: TypeInfo;
  callGraph?: CallGraph;
  complexity?: ComplexityMetrics;
}
```

From `src/interface/types/index.ts`:
```typescript
export interface FunctionSignature {
  parameters: ParameterInfo[];
  returnType: string;
  genericParams?: string[];
  async: boolean;
}

export interface ModuleSymbol {
  name: string;
  kind: SymbolKind;
  signature?: FunctionSignature;
  members?: MemberInfo[];
  type?: string;
}
```

From `src/parser/enhancers/TypeScriptTypeEnhancer.ts`:
```typescript
async enhance(results: ParseResult[]): Promise<ParseResult[]>;
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 69-01-01: Create Python type-enhancement fixtures and failing tests for supported docstring styles</name>
  <files>src/parser/enhancers/__tests__/PythonTypeEnhancer.test.ts, tests/fixtures/python/type-enhancer-google.py, tests/fixtures/python/type-enhancer-numpy.py, tests/fixtures/python/type-enhancer-sphinx.py, tests/fixtures/python/type-enhancer-ambiguous.py</files>
  <read_first>
    - /data/codemap/src/parser/enhancers/__tests__/PythonTypeEnhancer.test.ts
    - /data/codemap/src/infrastructure/parser/__tests__/PythonTreeSitterParser.test.ts
    - /data/codemap/tests/fixtures/python/comprehensive.py
    - /data/codemap/.planning/phases/69-pythontypeenhancer/69-CONTEXT.md
    - /data/codemap/.planning/phases/69-pythontypeenhancer/69-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `src/parser/enhancers/__tests__/PythonTypeEnhancer.test.ts` contains `Google`
    - `src/parser/enhancers/__tests__/PythonTypeEnhancer.test.ts` contains `NumPy`
    - `src/parser/enhancers/__tests__/PythonTypeEnhancer.test.ts` contains `Sphinx`
    - `src/parser/enhancers/__tests__/PythonTypeEnhancer.test.ts` contains `fails soft`
    - `tests/fixtures/python/type-enhancer-google.py` contains `Args:`
    - `tests/fixtures/python/type-enhancer-numpy.py` contains `Parameters`
    - `tests/fixtures/python/type-enhancer-sphinx.py` contains `:param`
    - `tests/fixtures/python/type-enhancer-ambiguous.py` contains a prose-only docstring with no explicit field syntax
  </acceptance_criteria>
  <behavior>
    - Test 1: a Google-style fixture yields parameter, return, and attribute expectations through the enhancer per D-07, D-08, and D-10
    - Test 2: a NumPy-style fixture yields parameter and return expectations through the enhancer per D-07 and D-10
    - Test 3: a Sphinx-style fixture yields parameter and return expectations through the enhancer per D-07 and D-10
    - Test 4: an ambiguous fixture keeps `typeInfo` empty or partial without guessed facts per D-09
  </behavior>
  <action>
Create one dedicated enhancer test file at `src/parser/enhancers/__tests__/PythonTypeEnhancer.test.ts` and four fixture files under `tests/fixtures/python/`. The tests must instantiate the real `PythonTreeSitterParser`, convert the fixture into a legacy `ParseResult` shape only as needed for the enhancer test seam, then assert exact enriched outputs for:
1. Google `Args:/Returns:/Attributes:` sections.
2. NumPy `Parameters/Returns/Attributes` sections.
3. Sphinx `:param/:type/:returns/:rtype:` sections.
4. A negative fixture whose prose mentions words like "string" or "optional" but has no explicit supported field syntax.
Use exact expected strings such as `Optional[str]`, `list[User]` or `List[User]`, and a concrete class attribute field (for example `timeout: int`) so the implementation target is unambiguous. Keep these tests red until Task 69-01-02 lands. Do not wire the enhancer into analyzer or registry paths in this task; that belongs to Phase 69 plan 02 per D-03 and D-11.
  </action>
  <verify>
    <automated>rtk ./node_modules/.bin/vitest run src/parser/enhancers/__tests__/PythonTypeEnhancer.test.ts</automated>
  </verify>
  <done>Focused fixtures exist, the dedicated test file encodes all four required behaviors, and the initial test run fails because `PythonTypeEnhancer` is not implemented yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 69-01-02: Implement annotation-first PythonTypeEnhancer with bounded docstring parsing and targeted symbol backfill</name>
  <files>src/parser/enhancers/PythonTypeEnhancer.ts</files>
  <read_first>
    - /data/codemap/src/parser/enhancers/PythonTypeEnhancer.ts
    - /data/codemap/src/parser/enhancers/TypeScriptTypeEnhancer.ts
    - /data/codemap/src/infrastructure/parser/implementations/PythonTreeSitterParser.ts
    - /data/codemap/src/parser/interfaces/IParser.ts
    - /data/codemap/.planning/phases/69-pythontypeenhancer/69-CONTEXT.md
    - /data/codemap/.planning/phases/69-pythontypeenhancer/69-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `src/parser/enhancers/PythonTypeEnhancer.ts` exports `class PythonTypeEnhancer`
    - `src/parser/enhancers/PythonTypeEnhancer.ts` contains `async enhance(results: ParseResult[])`
    - `src/parser/enhancers/PythonTypeEnhancer.ts` writes to `typeInfo`
    - `src/parser/enhancers/PythonTypeEnhancer.ts` contains recognizers for `Args:`, `Parameters`, and `:param`
    - `src/parser/enhancers/PythonTypeEnhancer.ts` preserves existing annotation truth before docstring backfill
    - `src/parser/enhancers/PythonTypeEnhancer.ts` contains a fail-soft branch that skips ambiguous docstring content instead of guessing
  </acceptance_criteria>
  <behavior>
    - Test 1: PEP 484 parameter and return annotations populate `result.typeInfo` first per D-01 and D-08
    - Test 2: docstring facts only backfill missing fields and never overwrite stronger AST annotation truth per D-08 and D-09
    - Test 3: explicit class attribute types appear both in top-level metadata and targeted symbol/member fields per D-04, D-05, and D-06
  </behavior>
  <action>
Implement `src/parser/enhancers/PythonTypeEnhancer.ts` as the Python peer of `TypeScriptTypeEnhancer`, but keep the scope strictly post-parse. Export `class PythonTypeEnhancer` with `constructor(enabled = true)` or an equivalent enable gate and `async enhance(results: ParseResult[]): Promise<ParseResult[]>`. For `.py` files only, build top-level `result.typeInfo` as the authoritative output per D-01 and D-02 by merging:
1. Existing AST-visible parameter and return annotations already present on Python function signatures.
2. Stable-subset Google fields for `Args`, `Returns`, and `Attributes`.
3. Stable-subset NumPy sections for `Parameters`, `Returns`, and `Attributes`.
4. Stable-subset Sphinx fields for `:param`, `:type`, `:returns:`, and `:rtype:`.
Use annotation-first precedence: keep explicit signature types when present, and use docstrings only to fill missing parameter, return, and class-attribute facts per D-08 and the research recommendation. Backfill only clearly derivable symbol fields per D-04 through D-06; for example, update `signature.parameters[*].type`, `signature.returnType`, and explicit class member/property types when a high-confidence field exists. Fail soft on ambiguous prose per D-09: unsupported formats, free-text paragraphs, or conflicting weak hints must leave metadata empty rather than inventing a type. Do not move files, rename parser interfaces, or touch registry routing in this task per D-03, D-11, and D-12.
  </action>
  <verify>
    <automated>rtk ./node_modules/.bin/vitest run src/parser/enhancers/__tests__/PythonTypeEnhancer.test.ts</automated>
  </verify>
  <done>`PythonTypeEnhancer` exists, the dedicated fixture-backed tests pass, and Python annotation/docstring facts now land on top-level `result.typeInfo` with bounded symbol backfill and ambiguity refusal.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python source docstring -> enhancer parser | Untrusted prose crosses into type metadata truth and must be bounded to explicit field syntax only |
| AST signature -> docstring merge | Two sources of type truth can conflict; explicit annotations must win |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | Tampering | `PythonTypeEnhancer` docstring parsing | mitigate | Parse only anchored Google/NumPy/Sphinx field blocks and skip free-form prose per D-07 and D-09 |
| T-69-02 | Integrity | annotation/docstring merge | mitigate | Preserve AST annotation truth and allow docstrings to backfill only missing fields |
| T-69-03 | Denial of service | unsupported docstring variants | accept | Fail soft by returning partial/empty metadata rather than throwing on unfamiliar formatting |
</threat_model>

<verification>
- [ ] `rtk ./node_modules/.bin/vitest run src/parser/enhancers/__tests__/PythonTypeEnhancer.test.ts`
- [ ] `rtk rg -n "PythonTypeEnhancer|Args:|Parameters|:param|fails soft" src/parser/enhancers/__tests__/PythonTypeEnhancer.test.ts src/parser/enhancers/PythonTypeEnhancer.ts tests/fixtures/python/type-enhancer-*.py`
</verification>

<success_criteria>
- `PythonTypeEnhancer` exists and follows the existing post-parse enhancement seam
- Google, NumPy, and Sphinx mainstream field syntax each have at least one passing fixture-backed proof
- Explicit annotations are used before docstring backfill
- Ambiguous docstrings do not create guessed type metadata
</success_criteria>

<output>
After completion, create `.planning/phases/69-pythontypeenhancer/69-01-SUMMARY.md`
</output>
