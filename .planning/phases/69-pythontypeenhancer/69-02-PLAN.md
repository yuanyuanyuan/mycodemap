---
phase: 69-pythontypeenhancer
plan: 02
type: execute
wave: 2
depends_on:
  - 69-01
files_modified:
  - src/interface/types/parser.ts
  - src/parser/index.ts
  - src/parser/enhancers/__tests__/PythonTypeEnhancer.test.ts
  - src/core/analyzer.ts
  - src/core/__tests__/analyzer.test.ts
autonomous: true
requirements:
  - PY-06
must_haves:
  truths:
    - Python enhancement reaches the same top-level `typeInfo` surface that downstream TS readers already use.
    - Analyzer output for Python modules persists enriched type metadata into `module.typeInfo`.
    - A Python file with explicit annotations/docstrings produces richer graph metadata than a similar file without enhancement facts.
  artifacts:
    - path: src/interface/types/parser.ts
      provides: Registry parse-result shape that reuses the legacy `TypeInfo` contract for Python metadata
    - path: src/parser/index.ts
      provides: Legacy parser compatibility path that runs both TS and Python enhancers and preserves the same `TypeInfo` structure TS readers already consume
    - path: src/core/analyzer.ts
      provides: Main analysis path that forwards Python `typeInfo` into module and graph truth without contract drift
    - path: src/core/__tests__/analyzer.test.ts
      provides: Analyzer-facing proof that Python enrichment survives end-to-end and still satisfies the existing `TypeInfo` contract
  key_links:
    - from: src/parser/index.ts
      to: src/parser/enhancers/PythonTypeEnhancer.ts
      via: post-parse enhancer chaining
      pattern: PythonTypeEnhancer
    - from: src/core/analyzer.ts
      to: src/interface/types/index.ts
      via: convertToModuleInfo typeInfo assignment
      pattern: typeInfo: result.typeInfo
---

<objective>
Wire Python type enhancement into the active parser and analyzer paths so enriched Python metadata survives conversion seams and persists into module/graph truth using the same top-level `typeInfo` contract already established for TypeScript.

Purpose: close `PY-06` without turning Phase 69 into parser cleanup or routing work.
Output: propagation seam for registry results, enhancer wiring in legacy compatibility/analyzer paths, and analyzer-facing regression coverage that proves contract-level compatibility with the existing TS `TypeInfo` shape.
</objective>

<execution_context>
@/home/stark/.codex/get-shit-done/workflows/execute-plan.md
@/home/stark/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/69-pythontypeenhancer/69-CONTEXT.md
@.planning/phases/69-pythontypeenhancer/69-RESEARCH.md
@.planning/phases/69-pythontypeenhancer/69-01-PLAN.md
@.planning/phases/68-multi-language-parser-switching/68-01-SUMMARY.md
@.planning/phases/68-multi-language-parser-switching/68-02-SUMMARY.md
@src/parser/enhancers/PythonTypeEnhancer.ts
@src/parser/enhancers/TypeScriptTypeEnhancer.ts
@src/parser/index.ts
@src/core/analyzer.ts
@src/interface/types/parser.ts
@src/core/__tests__/analyzer.test.ts

<interfaces>
From `src/interface/types/parser.ts`:
```typescript
export interface ParseResult {
  filePath: string;
  language: LanguageId;
  module: Module;
  symbols: ModuleSymbol[];
  imports: ImportInfo[];
  exports: ExportInfo[];
  dependencies: Dependency[];
  callGraph?: CallGraphInfo;
  complexity?: ComplexityMetrics;
  parseTime: number;
  errors?: ParseError[];
  parserUsed?: string;
}
```

From `src/parser/interfaces/IParser.ts`:
```typescript
export interface TypeInfo {
  typeDefinitions: Array<...>;
  genericParams: Array<...>;
  crossFileRefs: Array<...>;
  unionTypes: string[];
  intersectionTypes: string[];
  typeAliases: Array<...>;
  conditionalTypes?: Array<...>;
  mappedTypes?: Array<...>;
  templateLiteralTypes?: Array<...>;
  indexedAccessTypes?: string[];
  inferredTypes?: string[];
}
```

From `src/interface/types/index.ts`:
```typescript
export interface ModuleInfo {
  path: string;
  symbols: ModuleSymbol[];
  complexity?: { cyclomatic: number; cognitive: number; maintainability: number };
  callGraph?: { calls: Array<{ caller: string; callee: string; line: number }>; recursive: string[]; callCounts: Record<string, number> };
  typeInfo?: {
    typeDefinitions: Array<{ name: string; kind: 'interface' | 'type' | 'enum' | 'class' | 'alias'; members: Array<{ name: string; type: string; optional: boolean }> }>;
    genericParams: Array<{ name: string; extends?: string; default?: string }>;
    crossFileRefs: Array<{ symbol: string; file: string; line: number }>;
    unionTypes: string[];
    intersectionTypes: string[];
  };
}
```
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 69-02-01: Propagate Python typeInfo through registry and legacy compatibility seams without reopening parser cleanup scope</name>
  <files>src/interface/types/parser.ts, src/parser/index.ts, src/parser/enhancers/__tests__/PythonTypeEnhancer.test.ts, src/core/analyzer.ts</files>
  <read_first>
    - /data/codemap/src/interface/types/parser.ts
    - /data/codemap/src/parser/index.ts
    - /data/codemap/src/core/analyzer.ts
    - /data/codemap/src/parser/enhancers/PythonTypeEnhancer.ts
    - /data/codemap/src/parser/enhancers/TypeScriptTypeEnhancer.ts
    - /data/codemap/.planning/phases/69-pythontypeenhancer/69-CONTEXT.md
    - /data/codemap/.planning/phases/71-parser-legacy-cleanup/71-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `src/interface/types/parser.ts` imports or references the legacy `TypeInfo` contract rather than introducing an ad-hoc inline Python-only shape
    - `src/interface/types/parser.ts` contains `typeInfo?: TypeInfo`
    - `src/parser/index.ts` imports `PythonTypeEnhancer`
    - `src/core/analyzer.ts` imports `PythonTypeEnhancer`
    - `src/parser/index.ts` forwards `typeInfo` inside `toLegacyParseResult` or the current compatibility mapper
    - `src/core/analyzer.ts` forwards `typeInfo` inside `convertRegistryResultToLegacyResult`
    - `src/core/analyzer.ts` still contains `typeInfo: result.typeInfo`
    - `src/parser/enhancers/__tests__/PythonTypeEnhancer.test.ts` contains a parser compatibility smoke test for `parseFile()`
    - `src/parser/enhancers/__tests__/PythonTypeEnhancer.test.ts` asserts the returned Python `typeInfo` includes contract-level fields `typeDefinitions`, `genericParams`, `crossFileRefs`, `unionTypes`, `intersectionTypes`, and `typeAliases`
  </acceptance_criteria>
  <action>
Keep Phase 69 narrow per D-03, D-11, and D-12: do not remove compatibility mappers, relocate enhancers, or touch parser routing. Instead, add the smallest propagation seam needed for `PY-06`. In `src/interface/types/parser.ts`, extend the registry `ParseResult` to carry optional `typeInfo` by reusing the existing legacy `TypeInfo` contract from `src/parser/interfaces/IParser.ts`; do not introduce a weaker inline object type or Python-only variant. In `src/parser/index.ts`, instantiate `PythonTypeEnhancer` beside `TypeScriptTypeEnhancer`, run both enhancers in a deterministic order on the legacy compatibility path, and make sure the compatibility mapper forwards `typeInfo` from registry results into the legacy `ParseResult`. In `src/core/analyzer.ts`, instantiate the Python enhancer beside the TS enhancer, run both on `parseResults` before `convertToModuleInfo`, and ensure `convertRegistryResultToLegacyResult` also forwards `typeInfo` for any future registry-side enrichment. Extend the existing Wave 0 test file at `src/parser/enhancers/__tests__/PythonTypeEnhancer.test.ts` with a parser-level smoke case that calls the compatibility `parseFile()` path on a supported Python fixture and asserts the returned legacy `ParseResult.typeInfo` satisfies the existing contract: `typeDefinitions`, `genericParams`, `crossFileRefs`, `unionTypes`, `intersectionTypes`, and `typeAliases` must all be present as fields; for Phase 69, these fields may legitimately be empty arrays except for the fields populated by the fixture under test. Use top-level `result.typeInfo` as the only authoritative read/write channel per D-01, D-02, and D-05.
  </action>
  <verify>
    <automated>rtk npx tsc --noEmit</automated>
    <automated>rtk ./node_modules/.bin/vitest run src/parser/enhancers/__tests__/PythonTypeEnhancer.test.ts</automated>
  </verify>
  <done>The registry/legacy propagation seam is connected, the parser compatibility path has a runtime smoke proof for Python `typeInfo`, and that smoke proof demonstrates reuse of the existing `TypeInfo` contract rather than a Python-specific shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 69-02-02: Add analyzer-facing A/B regression proof that Python enhancement reaches current graph truth output</name>
  <files>src/core/__tests__/analyzer.test.ts</files>
  <read_first>
    - /data/codemap/src/core/__tests__/analyzer.test.ts
    - /data/codemap/src/core/analyzer.ts
    - /data/codemap/src/parser/enhancers/PythonTypeEnhancer.ts
    - /data/codemap/tests/fixtures/python/type-enhancer-google.py
    - /data/codemap/tests/fixtures/python/type-enhancer-ambiguous.py
    - /data/codemap/.planning/phases/69-pythontypeenhancer/69-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `src/core/__tests__/analyzer.test.ts` contains a Python-specific analyzer case
    - the new test asserts `module.typeInfo` for a `.py` module as the current graph truth surface returned by `analyze()`
    - the new test compares the same Python fixture under enhancement-on vs enhancement-off conditions
    - the new test asserts at least one exact enriched value such as a parameter type, return type, or union/optional entry
    - the new test asserts the graph/output result becomes richer because enhancement is enabled, not because a different file was analyzed
    - the new test asserts the graph/output `typeInfo` still exposes the same contract-level fields that TS readers expect, even when some fields are empty arrays
  </acceptance_criteria>
  <behavior>
    - Test 1: analyzer output for a supported Python fixture includes non-empty `module.typeInfo` per D-01 and PY-06, and that field is treated as the current graph truth/output surface emitted by `analyze()`
    - Test 2: the same Python fixture yields strictly poorer or empty output metadata when the enhancement path is disabled or when the same source has its docstring type block removed, proving richer graph truth comes from enhancement rather than file variance
    - Test 3: the graph/output `typeInfo` object preserves the existing contract field set (`typeDefinitions`, `genericParams`, `crossFileRefs`, `unionTypes`, `intersectionTypes`, `typeAliases`) so TS-side readers can consume it without branching
    - Test 4: the pipeline still tolerates unsupported/ambiguous docstrings by leaving metadata absent rather than throwing
  </behavior>
  <action>
Extend `src/core/__tests__/analyzer.test.ts` with one Python-focused integration case that uses the same fixture/source as an A/B proof:
1. analyze one supported Python module with explicit annotations and a supported docstring type block while enhancement is enabled, and
2. analyze the same logical source with enhancement disabled, or analyze a copy of the same fixture with only the docstring type block removed while keeping the rest of the file identical.
Treat the `analyze()` return as the current graph truth/output surface for this phase: assert that the resulting Python `module.typeInfo` is non-empty, contains exact expected values, and is richer than the A/B control result for the same source. In the same test, assert contract-level compatibility: the graph/output object must expose `typeDefinitions`, `genericParams`, `crossFileRefs`, `unionTypes`, `intersectionTypes`, and `typeAliases`, with empty-array defaults where Phase 69 does not populate a field yet. Make the assertion text explicit that this proves enriched type metadata survives to the graph-facing analyzer output without drifting from the existing TS-readable `TypeInfo` shape, satisfying `PY-06` and the phase goal. Include one negative assertion showing ambiguous docstring prose did not generate a guessed union/optional field. Keep the test analyzer-facing rather than enhancer-unit-level so it proves end-to-end persistence into graph/output truth.
  </action>
  <verify>
    <automated>rtk ./node_modules/.bin/vitest run src/core/__tests__/analyzer.test.ts</automated>
  </verify>
  <done>The analyzer regression suite proves the current graph/output surface from `analyze()` carries enriched Python `module.typeInfo`, preserves the existing TS-readable `TypeInfo` field set, the same fixture becomes richer only when enhancement is active, and ambiguous text still fails soft.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry `ParseResult` -> legacy compatibility path | Optional metadata can be dropped at conversion seams |
| parse results -> analyzer module output | End-to-end graph truth depends on `typeInfo` surviving intermediate transforms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-04 | Tampering | `src/interface/types/parser.ts` propagation seam | mitigate | Add explicit optional `typeInfo` field using the same field name used by legacy/result consumers |
| T-69-05 | Integrity | analyzer compatibility conversion | mitigate | Forward `typeInfo` in both compatibility mappers and assert `module.typeInfo` in analyzer tests |
| T-69-06 | Repudiation | Python enhancement visibility | mitigate | Add analyzer-facing regression proof comparing enriched vs non-enriched Python modules |
</threat_model>

<verification>
- [ ] `rtk npx tsc --noEmit`
- [ ] `rtk ./node_modules/.bin/vitest run src/core/__tests__/analyzer.test.ts`
- [ ] `rtk rg -n "typeInfo\\?: TypeInfo|typeDefinitions|genericParams|crossFileRefs|unionTypes|intersectionTypes|typeAliases" src/interface/types/parser.ts src/parser/enhancers/__tests__/PythonTypeEnhancer.test.ts src/core/__tests__/analyzer.test.ts`
</verification>

<success_criteria>
- Registry and legacy parse-result seams can carry Python `typeInfo`
- Parser compatibility path and analyzer path both run the Python enhancer without changing routing semantics
- Analyzer output persists enriched Python metadata to the current graph/output surface exposed as `module.typeInfo` while preserving the existing TS-readable `TypeInfo` contract
- Tests prove the same Python source becomes richer only when enhancement is active or when its docstring type block is present
</success_criteria>

<output>
After completion, create `.planning/phases/69-pythontypeenhancer/69-02-SUMMARY.md`
</output>
