---
phase: 59-parser-cutover
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/analyzer.ts
  - src/core/file-discovery.ts
  - src/infrastructure/parser/index.ts
  - src/infrastructure/parser/implementations/TypeScriptParser.ts
  - src/infrastructure/parser/implementations/PythonParser.ts
  - src/infrastructure/parser/implementations/GoParser.ts
  - src/parser/enhancers/TypeScriptTypeEnhancer.ts
  - src/parser/index.ts
autonomous: true
requirements:
  - PAR-04
user_setup: []
must_haves:
  truths:
    - "TypeScript/JavaScript, Python, and Go all enter the same registry-backed parser orchestration flow in Wave 1 rather than staying TS-only (covers D-13, D-15)."
    - "Default file discovery covers TS/JS/Python/Go without requiring user include globs (covers D-14)."
    - "Type-enhancement logic is TS-only and does not block Python/Go structural parsing, with SmartParser reduced into an explicit enhancer seam (covers D-05, D-06, D-07, D-08)."
  artifacts:
    - path: "src/core/analyzer.ts"
      provides: "Multi-language parser orchestration"
      contains: "getParserByFile"
    - path: "src/core/file-discovery.ts"
      provides: "Default exclude patterns safe for TS/Python/Go repos"
      contains: ".venv/**"
    - path: "src/parser/enhancers/TypeScriptTypeEnhancer.ts"
      provides: "TS-only enhancement seam after structural parse"
      contains: "TypeScriptTypeEnhancer"
  key_links:
    - from: "src/core/analyzer.ts"
      to: "src/infrastructure/parser/registry/ParserRegistry.ts"
      via: "analyzer resolves parser per file extension through registry lookup"
      pattern: "getParserByFile"
    - from: "src/parser/enhancers/TypeScriptTypeEnhancer.ts"
      to: "src/infrastructure/parser/implementations/TypeScriptParser.ts"
      via: "enhancer only applies to TS/TSX results"
      pattern: "typescript|tsx|TypeScriptParser"
---

<objective>
Route TS/JS, Python, and Go through the same parser main path and establish the TypeScript-only enhancement seam without expanding Python/Go debt scope.

Purpose: The cutover is incomplete unless default discovery and per-language routing both move onto the shared registry path.
Output: analyzer/discovery defaults handle multi-language repos, and TypeScript enhancement becomes an explicit optional post-processing seam.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-parser-cutover/59-CONTEXT.md
@.planning/phases/59-parser-cutover/59-RESEARCH.md
@src/core/analyzer.ts
@src/core/file-discovery.ts
@src/infrastructure/parser/index.ts
@src/infrastructure/parser/registry/ParserRegistry.ts
@src/infrastructure/parser/implementations/TypeScriptParser.ts
@src/infrastructure/parser/implementations/PythonParser.ts
@src/infrastructure/parser/implementations/GoParser.ts
@src/parser/implementations/smart-parser.ts
</context>

<tasks>

<task type="auto">
  <name>Task 59-02-01: Expand default discovery and registry routing to TS/JS/Python/Go</name>
  <files>src/core/analyzer.ts, src/core/file-discovery.ts, src/infrastructure/parser/index.ts, src/infrastructure/parser/implementations/TypeScriptParser.ts, src/infrastructure/parser/implementations/PythonParser.ts, src/infrastructure/parser/implementations/GoParser.ts</files>
  <read_first>
    - src/core/analyzer.ts
    - src/core/file-discovery.ts
    - src/infrastructure/parser/index.ts
    - src/infrastructure/parser/registry/ParserRegistry.ts
    - src/infrastructure/parser/implementations/TypeScriptParser.ts
    - src/infrastructure/parser/implementations/PythonParser.ts
    - src/infrastructure/parser/implementations/GoParser.ts
  </read_first>
  <action>
Update analyzer/discovery defaults so `discoverProjectFiles` is called with a zero-config include set covering `src/**/*.{ts,tsx,js,jsx,py,go}` or the equivalent shared default, rather than TS-only discovery. In `src/core/file-discovery.ts`, extend `DEFAULT_DISCOVERY_EXCLUDES` with Python/Go noise directories such as `.venv/**`, `**/__pycache__/**`, and `vendor/**` so multi-language discovery does not regress into obvious junk inputs. Confirm the registry factory continues to register TS/JS, Python, and Go parsers, and adjust analyzer orchestration so each discovered file is resolved through `registry.getParserByFile(filePath)` before parse execution.
  </action>
  <verify>
    rtk rg -n "ts,tsx,js,jsx,py,go|\\.venv/\\*\\*|__pycache__|vendor/\\*\\*" src/core/analyzer.ts src/core/file-discovery.ts
    rtk rg -n "getParserByFile" src/core/analyzer.ts
  </verify>
  <acceptance_criteria>
    - `src/core/analyzer.ts` contains a default include covering `py` and `go`
    - `src/core/file-discovery.ts` contains `.venv/**`
    - `src/core/file-discovery.ts` contains `__pycache__`
    - `src/core/file-discovery.ts` contains `vendor/**`
    - `src/core/analyzer.ts` contains `getParserByFile`
  </acceptance_criteria>
  <done>
    - Multi-language files are discoverable by default
    - Per-file parser routing uses the registry instead of ad hoc language branches
    - Python/Go inputs are in the main path without promising deeper parity
  </done>
</task>

<task type="auto">
  <name>Task 59-02-02: Introduce TS-only enhancement seam without re-expanding SmartParser scope</name>
  <files>src/parser/enhancers/TypeScriptTypeEnhancer.ts, src/parser/index.ts, src/core/analyzer.ts</files>
  <read_first>
    - src/parser/implementations/smart-parser.ts
    - src/parser/index.ts
    - src/core/analyzer.ts
    - .planning/phases/59-parser-cutover/59-CONTEXT.md
  </read_first>
  <action>
Create `src/parser/enhancers/TypeScriptTypeEnhancer.ts` as the new seam for post-parse TypeScript enhancement. Do not make it implement `IParser`; instead, define an explicit method that accepts parse results and returns enhanced parse results only for `.ts`/`.tsx` files. Wire analyzer or the compatibility parser path so this enhancer is optional and defaults on, using a concrete config switch such as `parser.enhanceTypes: false` when disabled. Keep Python/Go results on structural parsing only and avoid any promise that they receive SmartParser-equivalent type/call-graph enrichment in Phase 59.
  </action>
  <verify>
    rtk rg -n "TypeScriptTypeEnhancer|enhanceTypes|\\.ts|\\.tsx" src/parser/enhancers/TypeScriptTypeEnhancer.ts src/parser/index.ts src/core/analyzer.ts
    ! rtk rg -n "PythonTypeEnhancer|GoTypeEnhancer" src/core/analyzer.ts src/parser/index.ts
  </verify>
  <acceptance_criteria>
    - `src/parser/enhancers/TypeScriptTypeEnhancer.ts` exists
    - `src/parser/enhancers/TypeScriptTypeEnhancer.ts` contains `TypeScriptTypeEnhancer`
    - The wiring contains `enhanceTypes`
    - The wiring scopes enhancement to `.ts` or `.tsx`
    - No touched file claims Python or Go use the same enhancement seam
  </acceptance_criteria>
  <done>
    - SmartParser shrinkage has a concrete landing seam
    - Type enhancement is explicit and TS-only
    - Python/Go stay in scope as registry-routed structural parsers only
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Discovery patterns decide which files enter parsing and can accidentally pull in junk directories.
- Enhancer wiring can accidentally apply TS-specific assumptions to non-TS languages.

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-02-1 | Denial of service | file discovery defaults | mitigate | Exclude `.venv/**`, `__pycache__/**`, and `vendor/**` to avoid obvious repo-noise explosions. |
| T-59-02-2 | Tampering | cross-language enhancement seam | mitigate | Restrict `TypeScriptTypeEnhancer` application to `.ts` and `.tsx` results only. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Multi-language discovery defaults are covered by tests or targeted fixture verification
- [ ] Registry lookup path is exercised for TS/JS, Python, and Go
- [ ] Type-enhancement seam is proven TS-only
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- TS/JS, Python, and Go share one parser orchestration path
- Type enhancement is an explicit TS-only post-processing seam

</success_criteria>

<output>
After completion, create `.planning/phases/59-parser-cutover/59-02-SUMMARY.md`
</output>
