---
phase: 59-parser-cutover
plan: 03
type: execute
wave: 2
depends_on:
  - "59-01"
  - "59-02"
files_modified:
  - src/cli/index.ts
  - src/cli-new/index.ts
  - src/interface/config/index.ts
  - src/server/routes/api.ts
  - src/server/handlers/AnalysisHandler.ts
  - src/parser/implementations/tree-sitter-loader.ts
  - src/cli/commands/__tests__/
  - src/cli/output/__tests__/
  - docs/ai-guide/OUTPUT.md
  - ARCHITECTURE.md
autonomous: true
requirements:
  - PAR-05
user_setup: []
must_haves:
  truths:
    - "Deprecated parser-mode requests fail with explicit migration guidance across CLI, config, and public API contracts, keeping the deprecation contract visible after the runtime cutover (reinforces D-09, D-10, D-11, D-12)."
    - "Native tree-sitter failure still reaches the existing WASM fallback path or actionable total-failure contract."
    - "Docs and public help text no longer present fast/smart/hybrid as the active analysis-flow truth."
  artifacts:
    - path: "src/parser/implementations/tree-sitter-loader.ts"
      provides: "Verified native-first WASM-fallback runtime behavior"
      contains: "Native tree-sitter unavailable, using WASM fallback"
    - path: "src/cli/index.ts"
      provides: "Updated parser-mode CLI contract"
      contains: "DEPRECATED_PARSER_MODE"
    - path: "docs/ai-guide/OUTPUT.md"
      provides: "Runtime truth and actionable error documentation aligned with code"
      contains: "DEPRECATED_PARSER_MODE"
  key_links:
    - from: "src/cli/index.ts"
      to: "src/parser/index.ts"
      via: "CLI mode parsing routes deprecated values into the shared actionable-error contract"
      pattern: "DEPRECATED_PARSER_MODE"
    - from: "src/parser/implementations/tree-sitter-loader.ts"
      to: "src/cli/output/errors.ts"
      via: "fallback or total failure uses actionable remediation formatting"
      pattern: "createActionableError|nextCommand"
---

<objective>
Prove the cutover with failure-path tests and sync all public contracts so runtime, help text, and docs tell the same parser story.

Purpose: Phase 59 is not done until deprecated modes fail explicitly, WASM fallback remains verified, and no user-facing surface still advertises the removed dual-path runtime.
Output: tests/verification for deprecated modes and fallback, plus doc/help/type cleanup across CLI, config, and server surfaces.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-parser-cutover/59-CONTEXT.md
@.planning/phases/59-parser-cutover/59-RESEARCH.md
@.planning/phases/59-parser-cutover/59-01-PLAN.md
@.planning/phases/59-parser-cutover/59-02-PLAN.md
@src/cli/index.ts
@src/cli-new/index.ts
@src/interface/config/index.ts
@src/server/routes/api.ts
@src/server/handlers/AnalysisHandler.ts
@src/parser/implementations/tree-sitter-loader.ts
@docs/ai-guide/OUTPUT.md
@ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 59-03-01: Update public parser-mode contracts and help text to deprecated-mode semantics</name>
  <files>src/cli/index.ts, src/cli-new/index.ts, src/interface/config/index.ts, src/server/routes/api.ts, src/server/handlers/AnalysisHandler.ts, docs/ai-guide/OUTPUT.md, ARCHITECTURE.md</files>
  <read_first>
    - src/cli/index.ts
    - src/cli-new/index.ts
    - src/interface/config/index.ts
    - src/server/routes/api.ts
    - src/server/handlers/AnalysisHandler.ts
    - docs/ai-guide/OUTPUT.md
    - ARCHITECTURE.md
    - .planning/phases/59-parser-cutover/59-CONTEXT.md
  </read_first>
  <action>
Clean up every public parser-mode surface so it matches the new runtime truth. In `src/cli/index.ts` and `src/cli-new/index.ts`, stop advertising `fast|smart|hybrid` as active analysis modes; instead document default parser behavior and the deprecated-mode error semantics. In `src/interface/config/index.ts`, narrow config mode typing so old values are no longer treated as valid active configuration. In `src/server/routes/api.ts` and `src/server/handlers/AnalysisHandler.ts`, stop exposing old parser-mode enums in public request/response types unless they are explicitly marked as deprecated-and-rejected inputs. Update `docs/ai-guide/OUTPUT.md` and `ARCHITECTURE.md` so they describe the single parser main path and the `DEPRECATED_PARSER_MODE` actionable failure contract.
  </action>
  <verify>
    ! rtk rg -n "fast\\|smart\\|hybrid|分析模式: fast, smart, hybrid|分析模式 \\(fast\\|smart\\|hybrid\\)" src/cli/index.ts src/cli-new/index.ts docs/ai-guide/OUTPUT.md ARCHITECTURE.md
    rtk rg -n "DEPRECATED_PARSER_MODE" src/cli/index.ts src/cli-new/index.ts docs/ai-guide/OUTPUT.md ARCHITECTURE.md
  </verify>
  <acceptance_criteria>
    - `src/cli/index.ts` does not contain `分析模式 (fast|smart|hybrid)`
    - `src/cli-new/index.ts` does not contain `分析模式: fast, smart, hybrid`
    - `docs/ai-guide/OUTPUT.md` contains `DEPRECATED_PARSER_MODE`
    - `ARCHITECTURE.md` contains parser single-path wording aligned with the cutover
    - Public type surfaces no longer present fast/smart/hybrid as healthy active modes
  </acceptance_criteria>
  <done>
    - Runtime and docs tell the same parser story
    - No public surface silently preserves the old mode model
    - Migration guidance is visible where users actually look
  </done>
</task>

<task type="auto">
  <name>Task 59-03-02: Add failure-path proof for deprecated modes and WASM fallback</name>
  <files>src/parser/implementations/tree-sitter-loader.ts, src/cli/commands/__tests__/, src/cli/output/__tests__/</files>
  <read_first>
    - src/parser/implementations/tree-sitter-loader.ts
    - src/cli/output/errors.ts
    - src/cli/output/error-codes.ts
    - src/cli/commands/__tests__/
    - src/cli/output/__tests__/
  </read_first>
  <action>
Add targeted tests or verification fixtures that prove two failure-path promises. First, deprecated CLI/config parser-mode requests (`fast`, `smart`, `hybrid`) must produce `DEPRECATED_PARSER_MODE` with actionable fields including `rootCause`, `remediationPlan`, and `nextCommand`. Second, `src/parser/implementations/tree-sitter-loader.ts` must retain the current native-first then WASM-fallback behavior, including the warning path when native fails but WASM succeeds and actionable total failure when both fail. Prefer focused unit/integration tests over broad end-to-end rewrites, and make at least one grep-verifiable assertion path for each promise.
  </action>
  <verify>
    rtk rg -n "DEPRECATED_PARSER_MODE|rootCause|remediationPlan|nextCommand" src/cli/commands/__tests__ src/cli/output/__tests__
    rtk rg -n "Native tree-sitter unavailable, using WASM fallback|createActionableError|DEP_NATIVE_MISSING" src/parser/implementations/tree-sitter-loader.ts src/cli/output/__tests__
  </verify>
  <acceptance_criteria>
    - Test files assert `DEPRECATED_PARSER_MODE`
    - Test files assert actionable fields including `nextCommand`
    - `src/parser/implementations/tree-sitter-loader.ts` still contains the WASM fallback warning text
    - Verification covers native-fail/WASM-success and total-failure branches
  </acceptance_criteria>
  <done>
    - Deprecated-mode failure semantics are proven, not just documented
    - WASM fallback remains verified after parser cutover changes
    - Failure-path evidence exists for both roadmap success criteria and phase verification
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Public CLI/config/server/docs surfaces can drift from runtime behavior.
- Native dependency failure handling is security- and UX-sensitive because it controls fallback behavior.

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-03-1 | Spoofing | user-facing parser docs/help | mitigate | Remove active fast/smart/hybrid messaging and replace it with explicit deprecated-mode guidance. |
| T-59-03-2 | Denial of service | tree-sitter runtime fallback | mitigate | Keep native-first/WASM-fallback tests and actionable total-failure contract in place. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Deprecated-mode tests fail before implementation and pass after
- [ ] WASM fallback verification covers both success-fallback and total-failure branches
- [ ] CLI help, config types, server types, and docs all match the post-cutover runtime truth
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Deprecated modes fail with migration guidance across public surfaces
- WASM fallback remains verified and documented

</success_criteria>

<output>
After completion, create `.planning/phases/59-parser-cutover/59-03-SUMMARY.md`
</output>
