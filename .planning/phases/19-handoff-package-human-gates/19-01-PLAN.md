---
phase: 19-handoff-package-human-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/interface/types/design-handoff.ts
  - src/interface/types/index.ts
  - src/cli/design-handoff-builder.ts
  - src/cli/__tests__/design-handoff-builder.test.ts
  - tests/fixtures/design-contracts/handoff-basic.design.md
autonomous: false
requirements:
  - HOF-01
  - HOF-02
user_setup: []
must_haves:
  truths:
    - Phase 19 有正式 handoff contract，而不是临时拼接的 markdown/json
    - human summary 与 machine JSON 都从同一份 canonical handoff truth 渲染
    - 默认 artifact path 复用 `.mycodemap` output helper，稳定落在 `handoffs/` 子目录
  artifacts:
    - src/interface/types/design-handoff.ts
    - src/interface/types/index.ts
    - src/cli/design-handoff-builder.ts
    - src/cli/__tests__/design-handoff-builder.test.ts
    - tests/fixtures/design-contracts/handoff-basic.design.md
  key_links:
    - `src/interface/types/design-handoff.ts` 定义 handoff machine contract 与 trace items
    - `src/cli/design-handoff-builder.ts` 提供 canonical truth、artifact path 与 markdown template
    - `tests/fixtures/design-contracts/handoff-basic.design.md` 作为包含 `Open Questions` 的 baseline fixture
---

<objective>
先交付 Phase 19 的 handoff 骨架：正式 handoff types、summary template、deterministic artifact path，以及可被后续 builder/command 复用的 canonical truth seam。

Purpose: 如果 handoff 从一开始没有正式 schema、固定模板与稳定输出路径，后面的 approval / assumption / open-question 追踪一定会在不同 render path 之间漂移。
Output: `DesignHandoff*` 类型、artifact path helper、markdown renderer baseline、baseline fixture 与 focused tests。
</objective>

<execution_context>
@/data/codemap/.codex/get-shit-done/workflows/execute-plan.md
@/data/codemap/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-handoff-package-human-gates/19-CONTEXT.md
@.planning/phases/19-handoff-package-human-gates/19-RESEARCH.md
@src/interface/types/design-contract.ts
@src/interface/types/design-mapping.ts
@src/cli/commands/design.ts
@src/cli/paths.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义正式 handoff contract 与 trace item 类型</name>
  <files>src/interface/types/design-handoff.ts, src/interface/types/index.ts</files>
  <read_first>.planning/phases/19-handoff-package-human-gates/19-CONTEXT.md, .planning/phases/19-handoff-package-human-gates/19-RESEARCH.md, src/interface/types/design-contract.ts, src/interface/types/design-mapping.ts, src/interface/types/index.ts</read_first>
  <action>新增 `src/interface/types/design-handoff.ts`，定义至少这些公开类型：`DesignHandoffApprovalStatus`（`approved` / `needs-review`）、`DesignHandoffApproval`、`DesignHandoffAssumption`、`DesignHandoffOpenQuestion`、`DesignHandoffArtifacts`、`DesignHandoffSummary`、`DesignHandoffPayload`、`DesignHandoffDiagnosticCode`、`DesignHandoffDiagnostic`、`DesignHandoffResult`。`DesignHandoffPayload` 必须至少包含 `goal`、`constraints`、`acceptanceCriteria`、`nonGoals`、`touchedFiles`、`supportingFiles`、`tests`、`risks`、`validationChecklist`、`approvals`、`assumptions`、`openQuestions`。`DesignHandoffResult` 必须至少包含 `ok`、`filePath`、`outputDir`、`readyForExecution`、`artifacts`、`summary`、`handoff`、`diagnostics`。在 `src/interface/types/index.ts` 中导出 `./design-handoff.js`。</action>
  <verify>rg -n "DesignHandoffResult|DesignHandoffPayload|DesignHandoffApproval|DesignHandoffOpenQuestion|DesignHandoffDiagnosticCode" src/interface/types/design-handoff.ts src/interface/types/index.ts</verify>
  <acceptance_criteria>
    - `src/interface/types/design-handoff.ts` 包含 `DesignHandoffResult`
    - `src/interface/types/design-handoff.ts` 中 `DesignHandoffPayload` 同时出现 `touchedFiles`、`tests`、`approvals`、`assumptions`、`openQuestions`
    - `src/interface/types/design-handoff.ts` 定义 `DesignHandoffApprovalStatus`
    - `src/interface/types/index.ts` 导出 `./design-handoff.js`
  </acceptance_criteria>
  <done>Phase 19 的 handoff output 不再依赖 ad-hoc 对象，而拥有正式共享 contract。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 deterministic artifact path 与 markdown renderer baseline</name>
  <files>src/cli/design-handoff-builder.ts</files>
  <read_first>.planning/phases/19-handoff-package-human-gates/19-CONTEXT.md, src/cli/paths.ts, src/interface/types/design-handoff.ts, src/interface/types/design-contract.ts, src/interface/types/design-mapping.ts</read_first>
  <action>新增 `src/cli/design-handoff-builder.ts`，提供至少两个可复用出口：1) `resolveDesignHandoffArtifacts({ filePath, outputDir? })`，默认通过 `resolveOutputDir(undefined, rootDir)` 得到 base output dir，并把 handoff artifact 放到 `${outputDir}/handoffs/` 下，文件名固定为 `{stem}.handoff.md` 与 `{stem}.handoff.json`；2) `renderDesignHandoffMarkdown(result)`，使用 `DesignHandoffResult` / `DesignHandoffPayload` 渲染固定 heading：`## Goal`、`## Scope`、`## Non-Goals`、`## Risks`、`## Validation Checklist`、`## Assumptions`、`## Open Questions`、`## Approval Gates`。当前阶段不要在这里实现完整 builder 逻辑，只实现 path 与 renderer baseline，供 Phase 19-02 继续填充。</action>
  <verify>rg -n "resolveDesignHandoffArtifacts|renderDesignHandoffMarkdown|handoffs|\\.handoff\\.md|## Goal|## Approval Gates" src/cli/design-handoff-builder.ts</verify>
  <acceptance_criteria>
    - `src/cli/design-handoff-builder.ts` 包含 `resolveDesignHandoffArtifacts`
    - `src/cli/design-handoff-builder.ts` 中同时出现 `handoffs`、`.handoff.md`、`.handoff.json`
    - `src/cli/design-handoff-builder.ts` 调用 `resolveOutputDir`
    - markdown renderer 输出包含 `## Goal` 与 `## Approval Gates`
  </acceptance_criteria>
  <done>handoff artifact 的默认路径与 markdown summary 模板已被固定下来。</done>
</task>

<task type="auto">
  <name>Task 3: 添加 baseline fixture 与 focused tests</name>
  <files>src/cli/__tests__/design-handoff-builder.test.ts, tests/fixtures/design-contracts/handoff-basic.design.md</files>
  <read_first>src/cli/design-handoff-builder.ts, docs/product-specs/DESIGN_CONTRACT_TEMPLATE.md, tests/fixtures/design-contracts/valid-basic.design.md</read_first>
  <action>新增 `tests/fixtures/design-contracts/handoff-basic.design.md`，要求包含 `Goal`、`Constraints`、`Acceptance Criteria`、`Non-Goals` 与 `Open Questions`。新增 `src/cli/__tests__/design-handoff-builder.test.ts`，至少断言：`resolveDesignHandoffArtifacts()` 默认落到 `handoffs/` 子目录；输出文件名以 `.handoff.md` / `.handoff.json` 结尾；`renderDesignHandoffMarkdown()` 返回文本包含固定 headings 与 `Open Questions` 区块。</action>
  <verify>pnpm exec vitest run src/cli/__tests__/design-handoff-builder.test.ts</verify>
  <acceptance_criteria>
    - `tests/fixtures/design-contracts/handoff-basic.design.md` 包含 `## Open Questions`
    - `src/cli/__tests__/design-handoff-builder.test.ts` 退出码为 `0`
    - 测试断言默认路径包含 `/handoffs/`
    - 测试断言 markdown 文本包含 `## Validation Checklist`
  </acceptance_criteria>
  <done>Phase 19 骨架已被 fixture 与 focused tests 锁住。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "DesignHandoffResult|DesignHandoffPayload|DesignHandoffApproval|DesignHandoffOpenQuestion" src/interface/types/design-handoff.ts src/interface/types/index.ts`
- [ ] `pnpm exec vitest run src/cli/__tests__/design-handoff-builder.test.ts`
- [ ] `rg -n "handoffs|\\.handoff\\.md|\\.handoff\\.json" src/cli/design-handoff-builder.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- handoff schema、summary template 与 artifact path 已成为正式 contract
- 默认 artifact path 复用 `.mycodemap` helper，不引入新的 path drift
- 后续 Phase 19-02/03 可以直接消费 canonical truth seam，而不是重写模板/路径约定
  </success_criteria>

<output>
After completion, create `.planning/phases/19-handoff-package-human-gates/19-01-SUMMARY.md`
</output>
