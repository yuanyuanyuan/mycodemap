---
phase: 71-parser-legacy-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/analyzer.ts
  - src/parser/index.ts
  - src/parser/interfaces/IParser.ts
  - src/interface/types/parser.ts
  - src/types/index.ts
autonomous: true
---

<objective>
Unify internal parser types so the entire active runtime speaks `ILanguageParser` / registry `ParseResult` natively. Remove `convertRegistryResultToLegacyResult` and `toLegacyParseResult` adapter shims. Legacy `IParser` is preserved only as a `@deprecated` public re-export for external compatibility, not used internally.
</objective>

<must_haves>
- `src/core/analyzer.ts` no longer imports `ParseResult` from `../parser/interfaces/IParser.js`.
- `src/core/analyzer.ts` no longer contains `convertRegistryResultToLegacyResult()`.
- `src/parser/index.ts` no longer contains `toLegacyParseResult()`.
- `RegistryBackedParser` (if kept) returns registry `ParseResult` directly without conversion.
- Internal runtime files contain zero references to Legacy `ParseResult` shape.
- Legacy `IParser` interface remains exportable from `src/parser/interfaces/IParser.ts` with `@deprecated` marker.
</must_haves>

<tasks>
<task id="1" name="Switch Core to registry ParseResult and delete convertRegistryResultToLegacyResult">
<description>
In `src/core/analyzer.ts`:
1. Change the `ParseResult` import from `../parser/interfaces/IParser.js` to `../interface/types/parser.js`.
2. Remove the `convertRegistryResultToLegacyResult()` function entirely.
3. Update the `parseResults` accumulation logic so it directly collects registry `ParseResult[]` from `parser.parseFile()`.
4. Update `convertToModuleInfo()` to accept registry `ParseResult` (fields: `filePath`, `module`, `exports`, `imports`, `symbols`, `dependencies`, `callGraph`, `complexity`, `parseTime`).
5. Ensure `tsEnhancer.enhance()` signature is compatible with registry `ParseResult[]` (it likely already is, since the enhancer was created in Phase 59-02).
6. Verify `buildDependencyGraph()` and `createGlobalIndex()` still work with the new shape.
</description>
<verify>
- `rtk rg "convertRegistryResultToLegacyResult" src/core/analyzer.ts` returns no matches.
- `rtk rg "import.*ParseResult.*parser/interfaces/IParser" src/core/analyzer.ts` returns no matches.
- `rtk npx tsc --noEmit`
- `rtk ./node_modules/.bin/vitest run src/core/__tests__/analyzer.test.ts`
</verify>
</task>

<task id="2" name="Delete toLegacyParseResult and simplify RegistryBackedParser">
<description>
In `src/parser/index.ts`:
1. Remove `toLegacyParseResult()` function (lines ~89-110).
2. Update `RegistryBackedParser.parseFile()` to return registry `ParseResult` directly (the enhancer may still need to run, but its input/output should already be registry shape).
3. If `RegistryBackedParser` becomes unnecessary because Core no longer calls `createParser()`, mark it for removal in a follow-up or simplify it to a pure passthrough.
4. Keep `createParser()` as a public compatibility wrapper but document it as deprecated.
</description>
<verify>
- `rtk rg "toLegacyParseResult" src/parser/index.ts` returns no matches.
- `rtk npx tsc --noEmit`
- `rtk ./node_modules/.bin/vitest run src/parser/__tests__` (if exists)
</verify>
</task>

<task id="3" name="Mark Legacy IParser as deprecated and audit remaining internal usage">
<description>
1. Add `@deprecated` JSDoc to `IParser`, `ParserOptions`, `ParseResult` in `src/parser/interfaces/IParser.ts`.
2. Run `rtk rg "from.*parser/interfaces/IParser" src/` to find any remaining internal imports.
3. Update or remove any non-`src/parser/` internal imports (server, CLI, etc.) to use `src/interface/types/parser.js` instead.
4. Ensure `src/types/index.ts` re-export does not reintroduce Legacy shape as the primary export.
</description>
<verify>
- `rtk rg "from.*parser/interfaces/IParser" src/` returns only `src/parser/` internal files (if any).
- No `@deprecated` warnings are triggered by internal active code (only by external consumers, which is expected).
</verify>
</task>
</tasks>

<success_criteria>
- All tasks completed
- Type check passes
- Analyzer tests pass
- No `convertRegistryResultToLegacyResult` or `toLegacyParseResult` in active runtime
- Internal code uses single `ParseResult` shape from `src/interface/types/parser.ts`
</success_criteria>

<output>
After completion, create `.planning/phases/71-parser-legacy-cleanup/71-01-SUMMARY.md`
</output>
