---
phase: 61-mcp-direct-execution
plan: 02
type: execute
wave: 2
depends_on:
  - "61-01"
files_modified:
  - src/server/mcp/schema-adapter.ts
  - src/server/mcp/server.ts
  - src/server/mcp/types.ts
  - src/server/mcp/__tests__/dynamic-server.test.ts
  - src/server/mcp/__tests__/schema-adapter.test.ts
  - src/server/mcp/__tests__/CodeMapMcpServer.test.ts
autonomous: true
requirements:
  - MCP-01
  - MCP-03
user_setup: []
must_haves:
  truths:
    - "The selected MCP family returns real structured execution results instead of `status: \"cli_redirect\"`."
    - "The MCP cutover preserves contract-driven tool discovery and output-schema exposure, rather than replacing the dynamic registry with hand-written one-offs."
    - "Success and failure both use one consistent outer envelope and explicit `isError` semantics."
  artifacts:
    - path: "src/server/mcp/schema-adapter.ts"
      provides: "Selective direct-execution handler path for contract-backed tools"
      contains: "cli_redirect"
    - path: "src/server/mcp/server.ts"
      provides: "native/contract tool registration with direct execution routing"
      contains: "registerTool"
    - path: "src/server/mcp/__tests__/dynamic-server.test.ts"
      provides: "integration proof that the family no longer returns cli_redirect"
      contains: "codemap_analyze"
  key_links:
    - from: "src/server/mcp/schema-adapter.ts"
      to: "src/execution/contract-tools/index.ts"
      via: "contract tool handlers route into the shared executor instead of command-line suggestion stubs"
      pattern: "structuredContent"
    - from: "src/server/mcp/__tests__/dynamic-server.test.ts"
      to: "src/server/mcp/__tests__/schema-adapter.test.ts"
      via: "both integration and adapter-level tests stop encoding cli_redirect as success truth"
      pattern: "cli_redirect"
---

<objective>
Convert the MCP adapter path for the selected family from CLI suggestion mode into real executor-backed structured execution, while preserving schema-driven discovery and one shared success/failure envelope.

Purpose: Phase 61 is not complete if MCP tool calls still only return a shell command string.
Output: direct-execution MCP handlers for `query` / `deps` / `analyze`, updated schemas, and integration tests that prove success and failure behavior without `cli_redirect`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-mcp-direct-execution/61-CONTEXT.md
@.planning/phases/61-mcp-direct-execution/61-RESEARCH.md
@.planning/phases/61-mcp-direct-execution/61-PATTERNS.md
@src/execution/contract-tools/index.ts
@src/server/mcp/schema-adapter.ts
@src/server/mcp/server.ts
@src/server/mcp/types.ts
@src/server/mcp/__tests__/dynamic-server.test.ts
@src/server/mcp/__tests__/schema-adapter.test.ts
@src/server/mcp/__tests__/CodeMapMcpServer.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 61-02-01: Replace cli_redirect handlers with selective executor-backed MCP handlers</name>
  <files>src/server/mcp/schema-adapter.ts, src/server/mcp/server.ts, src/server/mcp/types.ts</files>
  <read_first>
    - src/server/mcp/schema-adapter.ts
    - src/server/mcp/server.ts
    - src/server/mcp/types.ts
    - src/execution/contract-tools/index.ts
    - src/cli/interface-contract/commands/query.ts
    - src/cli/interface-contract/commands/deps.ts
    - src/cli/interface-contract/commands/analyze.ts
  </read_first>
  <action>
Refactor MCP registration so `query`, `deps`, and `analyze` use executor-backed handlers instead of `createCliAvailabilityHandler(...)`. Keep schema-driven input discovery and tool metadata generation, but give the selected family a real output contract: one outer envelope with explicit success/failure semantics and command-native payloads nested inside. Wire the selected family into the dedicated execution modules created for this phase, without moving business logic into `schema-adapter.ts` or `server.ts`. Handle the `query` naming conflict deliberately: preserve the existing native `codemap_query` surface or unify it through the new family routing, but do not leave a hidden dual-truth split between native query and contract-backed direct execution. All other commands may stay on `cli_redirect` in this phase.
  </action>
  <verify>
    rtk rg -n "codemap_query|codemap_deps|codemap_analyze|structuredContent|isError" src/server/mcp/schema-adapter.ts src/server/mcp/server.ts
    ! rtk rg -n "expect\\(structured\\.status\\)\\.toBe\\('cli_redirect'\\)" src/server/mcp/__tests__/dynamic-server.test.ts src/server/mcp/__tests__/schema-adapter.test.ts
    rtk npx vitest run src/server/mcp/__tests__/schema-adapter.test.ts src/server/mcp/__tests__/dynamic-server.test.ts src/server/mcp/__tests__/CodeMapMcpServer.test.ts
    rtk tsc --noEmit
  </verify>
  <acceptance_criteria>
    - `codemap_analyze` does not rely on a CLI-availability handler
    - `codemap_deps` does not rely on a CLI-availability handler
    - `query` direct execution is explicitly reconciled with the existing native MCP query surface
    - the selected family exposes a structured output schema richer than `cli_redirect`
  </acceptance_criteria>
  <done>
    - MCP calls now execute real business logic
    - schema-driven discovery survives the cutover
    - success/failure semantics are explicit and uniform
  </done>
</task>

<task type="auto">
  <name>Task 61-02-02: Rewrite MCP tests to prove structured success and failure instead of command-string suggestions</name>
  <files>src/server/mcp/__tests__/dynamic-server.test.ts, src/server/mcp/__tests__/schema-adapter.test.ts, src/server/mcp/__tests__/CodeMapMcpServer.test.ts</files>
  <read_first>
    - src/server/mcp/__tests__/dynamic-server.test.ts
    - src/server/mcp/__tests__/schema-adapter.test.ts
    - src/server/mcp/__tests__/CodeMapMcpServer.test.ts
    - src/server/mcp/__tests__/env-contract-tool.test.ts
    - .planning/phases/61-mcp-direct-execution/61-PATTERNS.md
  </read_first>
  <action>
Replace MCP test expectations that currently treat `cli_redirect` as success truth. Keep `listTools()` and schema-discovery assertions, but add direct-call proof that the selected family returns real structured results. For at least one failure path, assert explicit status/error/diagnostics semantics rather than transport-level string formatting. Reuse the native-tool testing style from `env-contract-tool.test.ts` and `CodeMapMcpServer.test.ts` where possible, rather than inventing a weaker test style around command-string presence.
  </action>
  <verify>
    ! rtk rg -n "expect\\(structured\\.status\\)\\.toBe\\('cli_redirect'\\)" src/server/mcp/__tests__/dynamic-server.test.ts src/server/mcp/__tests__/schema-adapter.test.ts
    rtk rg -n "status|error|diagnostics|structuredContent" src/server/mcp/__tests__/dynamic-server.test.ts src/server/mcp/__tests__/schema-adapter.test.ts src/server/mcp/__tests__/CodeMapMcpServer.test.ts
    rtk npx vitest run src/server/mcp/__tests__/dynamic-server.test.ts -t "structured"
    rtk npx vitest run src/server/mcp/__tests__/CodeMapMcpServer.test.ts -t "not found|ambiguous|GRAPH_NOT_FOUND|SYMBOL_NOT_FOUND"
    rtk tsc --noEmit
  </verify>
  <acceptance_criteria>
    - no selected-family MCP success-path test asserts `cli_redirect`
    - at least one selected-family failure-path test asserts structured error semantics
    - tests still cover tool discoverability and schema exposure after the cutover
    - tests prove direct execution, not only that a handler returned some text
  </acceptance_criteria>
  <done>
    - the old transitional contract is no longer encoded as truth
    - success and failure are both observable to automation
    - direct execution is covered end-to-end through MCP transport
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- MCP input schemas are machine-consumed discovery surfaces and must stay aligned with real runtime behavior.
- MCP transport errors and CLI prose must not leak into structured business-result contracts for the selected family.

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-02-1 | Spoofing | MCP success contract | mitigate | Remove `cli_redirect` as the success path for the selected family and require real structured results. |
| T-61-02-2 | Repudiation | failure-path evidence | mitigate | Add explicit structured failure tests so non-success states are observable beyond transport text. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Selected-family MCP handlers no longer return `status: "cli_redirect"`
- [ ] Discovery metadata and output schemas are still exposed through `listTools()`
- [ ] At least one failure-path test proves structured error semantics for the new direct-execution family
- [ ] Targeted MCP Vitest suites pass after replacing redirect handlers
- [ ] `rtk tsc --noEmit` passes after adapter/type changes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- MCP direct execution is real for the selected family
- Success and failure share one structured output contract

</success_criteria>

<output>
After completion, create `.planning/phases/61-mcp-direct-execution/61-02-SUMMARY.md`
</output>
