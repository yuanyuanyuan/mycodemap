---
phase: 61-mcp-direct-execution
plan: 03
type: execute
wave: 3
depends_on:
  - "61-02"
files_modified:
  - src/cli/commands/query.ts
  - src/cli/commands/deps.ts
  - src/cli/commands/analyze.ts
  - src/cli/commands/__tests__/query-output.test.ts
  - src/cli/commands/__tests__/deps-output.test.ts
  - src/cli/commands/__tests__/analyze-command.test.ts
  - src/server/mcp/__tests__/dynamic-server.test.ts
  - docs/ai-guide/OUTPUT.md
  - ARCHITECTURE.md
autonomous: true
requirements:
  - MCP-02
  - MCP-03
  - MCP-04
user_setup: []
must_haves:
  truths:
    - "`query` and `deps` finish the phase as thin wrappers: parse/input normalization, executor invocation, output rendering, and exit-code handling only."
    - "`analyze` is materially thinner than before and no longer owns MCP-specific execution truth, even if a smaller compatibility shell remains."
    - "The repo contains explicit success-path and failure-path evidence for both CLI and MCP direct execution after the cutover."
  artifacts:
    - path: "src/cli/commands/query.ts"
      provides: "Thin wrapper boundary for query"
      contains: "renderOutput"
    - path: "src/cli/commands/__tests__/query-output.test.ts"
      provides: "CLI evidence for query success/failure rendering around the shared seam"
      contains: "query"
    - path: "docs/ai-guide/OUTPUT.md"
      provides: "User/agent-facing output truth after direct-execution cutover"
      contains: "MCP"
  key_links:
    - from: "src/cli/commands/query.ts"
      to: "src/cli/commands/__tests__/query-output.test.ts"
      via: "wrapper slimming is protected by CLI output and failure-path tests"
      pattern: "renderOutput"
    - from: "ARCHITECTURE.md"
      to: "src/server/mcp/server.ts"
      via: "docs describe the same shared executor boundary shipped in code"
      pattern: "MCP"
---

<objective>
Finish the Phase 61 convergence with wrapper thinning, explicit failure-path proof, and any output/architecture doc sync required by the new direct-execution contract.

Purpose: Shared execution and MCP cutover are not enough if the CLI wrappers stay thick or if verification only proves happy-path MCP calls.
Output: materially thinner wrappers, CLI + MCP verification evidence, and synchronized runtime/docs truth for the direct-execution family.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-mcp-direct-execution/61-CONTEXT.md
@.planning/phases/61-mcp-direct-execution/61-RESEARCH.md
@.planning/phases/61-mcp-direct-execution/61-PATTERNS.md
@.planning/phases/61-mcp-direct-execution/61-01-PLAN.md
@.planning/phases/61-mcp-direct-execution/61-02-PLAN.md
@src/cli/commands/query.ts
@src/cli/commands/deps.ts
@src/cli/commands/analyze.ts
@src/cli/commands/__tests__/query-output.test.ts
@src/cli/commands/__tests__/deps-output.test.ts
@src/cli/commands/__tests__/analyze-command.test.ts
@src/server/mcp/__tests__/dynamic-server.test.ts
@docs/ai-guide/OUTPUT.md
@ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 61-03-01: Finish CLI wrapper thinning and preserve renderer-only responsibilities</name>
  <files>src/cli/commands/query.ts, src/cli/commands/deps.ts, src/cli/commands/analyze.ts</files>
  <read_first>
    - src/cli/commands/query.ts
    - src/cli/commands/deps.ts
    - src/cli/commands/analyze.ts
    - src/cli/commands/publish-status.ts
    - .planning/phases/61-mcp-direct-execution/61-01-PLAN.md
  </read_first>
  <action>
Use the shared executor seam from Wave 1 to finish the wrapper cleanup promised in the roadmap. `query` and `deps` should end this plan as clearly thin wrappers. `analyze` must be materially thinner than at phase start, but it may still retain a smaller compatibility shell if that shell is renderer/CLI-facing rather than the owner of direct-execution truth. Do not reintroduce business logic into wrappers merely to satisfy old tests; update tests to the new architecture instead.
  </action>
  <verify>
    rtk rg -n "renderOutput|createProgressEmitter|formatError" src/cli/commands/query.ts src/cli/commands/deps.ts src/cli/commands/analyze.ts
    rtk rg -n "loadCodeMap\\(|querySymbol\\(|getModuleDependencies\\(|createCliAvailabilityHandler" src/cli/commands/query.ts src/cli/commands/deps.ts src/cli/commands/analyze.ts
    rtk npx vitest run src/cli/commands/__tests__/query-output.test.ts src/cli/commands/__tests__/deps-output.test.ts src/cli/commands/__tests__/analyze-command.test.ts
    rtk tsc --noEmit
  </verify>
  <acceptance_criteria>
    - `query` wrapper no longer owns code-map loading and branch-heavy business execution
    - `deps` wrapper no longer owns code-map loading and primary data shaping
    - `analyze` wrapper is observably thinner than its pre-Phase-61 state
    - wrappers remain responsible for rendering/output-mode/exit-code behavior
  </acceptance_criteria>
  <done>
    - wrapper ownership is clear
    - Wave 1 shared seam is fully paying off
    - further MCP-family expansion has a repeatable wrapper pattern
  </done>
</task>

<task type="auto">
  <name>Task 61-03-02: Add CLI + MCP failure-path evidence and sync user-facing direct-execution truth</name>
  <files>src/cli/commands/__tests__/query-output.test.ts, src/cli/commands/__tests__/deps-output.test.ts, src/cli/commands/__tests__/analyze-command.test.ts, src/server/mcp/__tests__/dynamic-server.test.ts, docs/ai-guide/OUTPUT.md, ARCHITECTURE.md</files>
  <read_first>
    - src/cli/commands/__tests__/query-output.test.ts
    - src/cli/commands/__tests__/deps-output.test.ts
    - src/cli/commands/__tests__/analyze-command.test.ts
    - src/server/mcp/__tests__/dynamic-server.test.ts
    - docs/ai-guide/OUTPUT.md
    - ARCHITECTURE.md
  </read_first>
  <action>
Add targeted proof that the new architecture behaves correctly on both success and failure paths. On the CLI side, test the thin wrappers around the shared seam in machine/human/structured modes where appropriate, and include at least one non-success path per family or representative subset. On the MCP side, retain at least one transport-level integration test proving the direct-execution family still returns structured failure information after the cutover. Then update `docs/ai-guide/OUTPUT.md` and `ARCHITECTURE.md` only where needed so docs reflect the new “shared executor + thin wrapper + direct MCP execution” truth and no longer imply `cli_redirect` for the selected family.
  </action>
  <verify>
    ! rtk rg -n "expect\\(structured\\.status\\)\\.toBe\\('cli_redirect'\\)" src/server/mcp/__tests__/dynamic-server.test.ts
    rtk rg -n "shared executor|direct execution|thin wrapper|MCP" docs/ai-guide/OUTPUT.md ARCHITECTURE.md
    rtk npx vitest run src/cli/commands/__tests__/query-output.test.ts src/cli/commands/__tests__/deps-output.test.ts src/cli/commands/__tests__/analyze-command.test.ts
    rtk npx vitest run src/server/mcp/__tests__/dynamic-server.test.ts -t "error|not found|ambiguous|structured"
    rtk tsc --noEmit
  </verify>
  <acceptance_criteria>
    - CLI tests include at least one explicit failure-path assertion for the new shared seam behavior
    - MCP integration tests still prove direct execution after Wave 1, including a structured failure case
    - docs describe the shipped direct-execution architecture rather than the transitional redirect contract
    - doc changes are limited to Phase 61 truth sync, not general cleanup
  </acceptance_criteria>
  <done>
    - failure-path evidence exists, not just happy-path demos
    - docs and runtime tell one story
    - Phase 61 closes with proof instead of inference
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Thin wrappers can silently re-grow if tests only assert output text and not ownership boundaries.
- AI/user-facing docs can preserve stale redirect assumptions after code truth has changed.

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-03-1 | Repudiation | wrapper ownership | mitigate | Add CLI tests that exercise the wrapper around the shared seam instead of encoding old wrapper-local business logic. |
| T-61-03-2 | Information disclosure | docs/runtime drift | mitigate | Sync only the direct-execution/output architecture surfaces so users and agents see the same truth the code implements. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] CLI wrappers are materially thinner and backed by targeted tests
- [ ] At least one MCP failure-path test still proves structured direct execution after the cutover
- [ ] Docs reflect shared executor + thin wrapper + direct MCP execution truth for the selected family
- [ ] CLI failure-path tests pass for the thin wrappers
- [ ] `rtk tsc --noEmit` passes after the final wrapper/doc sync wave
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- CLI wrappers are thin enough to match the phase intent
- Success and failure evidence exists across both CLI and MCP surfaces

</success_criteria>

<output>
After completion, create `.planning/phases/61-mcp-direct-execution/61-03-SUMMARY.md`
</output>
