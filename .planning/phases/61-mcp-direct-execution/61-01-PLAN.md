---
phase: 61-mcp-direct-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/execution/contract-tools/index.ts
  - src/execution/contract-tools/types.ts
  - src/execution/contract-tools/query.ts
  - src/execution/contract-tools/deps.ts
  - src/execution/contract-tools/analyze.ts
  - src/cli/commands/query.ts
  - src/cli/commands/deps.ts
  - src/cli/commands/analyze.ts
  - src/orchestrator/tool-orchestrator.ts
  - src/cli/storage-runtime.ts
autonomous: true
requirements:
  - MCP-02
  - MCP-04
user_setup: []
must_haves:
  truths:
    - "Phase 61 introduces one dedicated shared executor/service seam for the `query` / `deps` / `analyze` family, and both CLI and MCP are able to call that seam instead of duplicating business execution."
    - "`query` is explicitly included in the convergence, even though `codemap_query` already exists as a native MCP tool; the native tool and the CLI command stop being separate execution truths."
    - "CLI wrappers begin shrinking toward parsing + executor invocation + rendering, while remaining compatible enough for Wave 1 landing."
  artifacts:
    - path: "src/execution/contract-tools/index.ts"
      provides: "Shared executor/service entrypoint or family-level execution seam"
      contains: "query"
    - path: "src/cli/commands/query.ts"
      provides: "CLI query wrapper redirected toward the shared seam"
      contains: "renderOutput"
    - path: "src/cli/commands/analyze.ts"
      provides: "Analyze command integration point onto the shared seam without transport logic leakage"
      contains: "ToolOrchestrator"
  key_links:
    - from: "src/execution/contract-tools/index.ts"
      to: "src/cli/commands/query.ts"
      via: "CLI query uses the same execution truth as MCP"
      pattern: "query"
    - from: "src/execution/contract-tools/index.ts"
      to: "src/cli/commands/analyze.ts"
      via: "shared analyze execution path exists outside the MCP transport and CLI renderer"
      pattern: "analyze"
---

<objective>
Establish the shared direct-execution seam for the first MCP family: centralize business execution, result-envelope shaping, and runtime bootstrap for `query`, `deps`, and `analyze` so both CLI and MCP can call one truth.

Purpose: Phase 61 cannot succeed if MCP direct execution is added on top of three separate CLI-local implementations.
Output: one executor/service layer, initial CLI convergence onto that layer, and a stable boundary that Wave 2 can thin further without re-opening architecture.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-mcp-direct-execution/61-CONTEXT.md
@.planning/phases/61-mcp-direct-execution/61-RESEARCH.md
@.planning/phases/61-mcp-direct-execution/61-PATTERNS.md
@src/server/mcp/server.ts
@src/cli/commands/query.ts
@src/cli/commands/deps.ts
@src/cli/commands/analyze.ts
@src/orchestrator/tool-orchestrator.ts
@src/cli/storage-runtime.ts
</context>

<tasks>

<task type="auto">
  <name>Task 61-01-01: Define the family-level executor/result seam for query, deps, and analyze</name>
  <files>src/execution/contract-tools/index.ts, src/execution/contract-tools/types.ts, src/execution/contract-tools/query.ts, src/execution/contract-tools/deps.ts, src/execution/contract-tools/analyze.ts, src/cli/storage-runtime.ts, src/orchestrator/tool-orchestrator.ts</files>
  <read_first>
    - src/server/mcp/service.ts
    - src/server/mcp/types.ts
    - src/cli/storage-runtime.ts
    - src/orchestrator/tool-orchestrator.ts
    - src/cli/commands/publish-status.ts
  </read_first>
  <action>
Create a dedicated shared execution seam in a new family-scoped module area outside MCP handlers and outside thick CLI wrappers. The seam must be scoped explicitly to the `query` / `deps` / `analyze` family for now; it does not need to become a universal abstraction for every command. Define a shared outer result contract that standardizes `status`, `error`, and diagnostics semantics while preserving command-native payloads inside a `result` field or equivalent. Reuse existing MCP-native patterns where practical (`createErrorResult`-style shaping, transport-free service logic, runtime bootstrap from `storage-runtime`), but keep the new executor ownership in the dedicated execution modules rather than `schema-adapter.ts` or `server.ts`.
  </action>
  <verify>
    rtk rg -n "status|error|diagnostics|result" src/execution/contract-tools src/server/mcp/types.ts
    rtk rg -n "createConfiguredStorage|ToolOrchestrator" src/execution/contract-tools src/cli/storage-runtime.ts src/orchestrator/tool-orchestrator.ts
    rtk npx vitest run src/server/mcp/__tests__/CodeMapMcpServer.test.ts src/server/mcp/__tests__/dynamic-server.test.ts
    rtk tsc --noEmit
  </verify>
  <acceptance_criteria>
    - a shared executor/service boundary exists outside MCP registration code
    - the boundary is explicitly usable for `query`, `deps`, and `analyze`
    - the outer result contract is unified without flattening all command-native payloads into one schema
    - runtime bootstrap and diagnostics shaping are not duplicated separately in CLI and MCP
  </acceptance_criteria>
  <done>
    - the family has one execution seam
    - result-envelope ownership is centralized
    - Wave 2 can focus on wrapper thinning instead of architecture invention
  </done>
</task>

<task type="auto">
  <name>Task 61-01-02: Move CLI query/deps/analyze onto the shared seam without overfinishing wrapper cleanup</name>
  <files>src/cli/commands/query.ts, src/cli/commands/deps.ts, src/cli/commands/analyze.ts</files>
  <read_first>
    - src/cli/commands/query.ts
    - src/cli/commands/deps.ts
    - src/cli/commands/analyze.ts
    - src/cli/commands/publish-status.ts
    - .planning/phases/61-mcp-direct-execution/61-PATTERNS.md
  </read_first>
  <action>
Refactor the three CLI command surfaces so they invoke the new shared executor/service seam for business execution and keep wrapper-local responsibilities limited to argument parsing, output-mode decisions, progress emission, result rendering, and exit-code setting. `query` and `deps` should land noticeably thinner in this plan. `analyze` must also move onto the shared seam in a meaningful way, but it may retain a compatibility shell around existing orchestration complexity as long as the true business execution no longer lives only inside the CLI wrapper. Avoid forcing all final cleanup into this plan; reserve polish-only wrapper trimming for `61-03-PLAN.md`.
  </action>
  <verify>
    rtk rg -n "renderOutput|createProgressEmitter|formatError" src/cli/commands/query.ts src/cli/commands/deps.ts src/cli/commands/analyze.ts
    rtk rg -n "shared|service|executor|execute" src/cli/commands/query.ts src/cli/commands/deps.ts src/cli/commands/analyze.ts
    rtk npx vitest run src/cli/commands/__tests__/query-output.test.ts src/cli/commands/__tests__/deps-output.test.ts src/cli/commands/__tests__/analyze-command.test.ts
    rtk tsc --noEmit
  </verify>
  <acceptance_criteria>
    - CLI `query` does not remain a self-contained business executor
    - CLI `deps` does not remain a self-contained business executor
    - CLI `analyze` uses the shared seam for MCP-relevant execution truth
    - wrappers still own rendering/output-mode behavior rather than pushing transport concerns into the service
  </acceptance_criteria>
  <done>
    - CLI and MCP now have a shared place to meet
    - `query`/`deps` are materially thinner
    - `analyze` no longer blocks Phase 61 on a full rewrite
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- CLI flags and MCP arguments are user-controlled and must not produce divergent execution semantics for the same command family.
- Shared execution types/services will be used by both transport surfaces, so envelope drift becomes a correctness risk.

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01-1 | Repudiation | query/deps/analyze execution truth | mitigate | Collapse business execution into one shared seam so CLI and MCP cannot silently diverge. |
| T-61-01-2 | Tampering | wrapper/service ownership boundary | mitigate | Keep rendering/process concerns in wrappers and result/error shaping in the shared service to prevent transport-specific mutation of business truth. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Shared executor/service code exists for the selected family outside MCP registration and outside wrapper-only code
- [ ] CLI `query`, `deps`, and `analyze` all invoke that shared seam
- [ ] No new business execution logic is introduced directly into MCP handlers
- [ ] Targeted Vitest proves the shared seam is exercised through both MCP and CLI-adjacent tests
- [ ] `rtk tsc --noEmit` passes after the shared-type/service refactor
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- CLI and MCP can now share the same execution truth for the selected family
- Wrapper complexity is trending downward rather than upward

</success_criteria>

<output>
After completion, create `.planning/phases/61-mcp-direct-execution/61-01-SUMMARY.md`
</output>
