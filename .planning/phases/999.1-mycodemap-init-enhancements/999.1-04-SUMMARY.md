---
phase: 999.1-mycodemap-init-enhancements
plan: 04
subsystem: cli
tags: [cli, init, rules, ai-context, drift-detection]
requires: [999.1-02]
provides:
  - generic built-in rules bundle under `.mycodemap/rules/`
  - conflict-based drift detection for user-modified rules files
  - manual AI context snippet detection without auto-editing team-owned files
affects: [init, rules-bundle, AI-context, docs-phase-999.1-05]
tech-stack:
  added: []
  patterns: [built-in-rules-bundle, rules-drift-preview, manual-ai-context-snippet]
key-files:
  created:
    - src/cli/init/rule-templates.ts
    - src/cli/init/rules.ts
    - src/cli/commands/__tests__/init-rules.test.ts
    - .planning/phases/999.1-mycodemap-init-enhancements/999.1-04-SUMMARY.md
  modified:
    - src/cli/init/reconciler.ts
key-decisions:
  - "Rules bundle is generic and built-in; it does not copy repo-private `docs/rules/` verbatim into target projects."
  - "User-modified rules files are treated as `conflict` with preview text, not silently overwritten."
  - "AI context integration stays manual: detect snippet presence, but never edit `CLAUDE.md` / `AGENTS.md`."
patterns-established:
  - "Rules sync pattern: missing → install, exact match → already-synced, drift → conflict with preview."
  - "Snippet satisfaction pattern: `.mycodemap/rules/` reference found in `CLAUDE.md` or `AGENTS.md` upgrades receipt to `already-synced`."
requirements-completed: [R20, R21, R22, R23, R24, R25, R26]
duration: 1 plan
completed: 2026-04-21
---

# Phase 999.1 Plan 04 Summary

**通用 rules bundle 与手动 AI context snippet 已接入 `init` 资产台账**

## Accomplishments
- 新增 `src/cli/init/rule-templates.ts`，内置 commit / test / lint / docs / validation 五类通用 rules 文件。
- 新增 `src/cli/init/rules.ts`，完成 rules 文件生成、hash-based already-synced 判断、drift 冲突检测与 preview 文本。
- 将 rules 与 AI context snippet 状态接入 `src/cli/init/reconciler.ts`，替换 Wave 2 的 placeholder。
- 新增 `src/cli/commands/__tests__/init-rules.test.ts`，覆盖首装、重复运行、用户手改 drift、AI context snippet 手动/已满足场景。

## Task Commits

No commits were created in this session.

## Verification
- `./node_modules/.bin/vitest run src/cli/commands/__tests__/init-command.test.ts src/cli/commands/__tests__/init-hooks.test.ts src/cli/commands/__tests__/init-rules.test.ts src/cli/__tests__/first-run-guide.test.ts` — PASS (`18` tests)
- `npm run typecheck` — PASS

## Acceptance Criteria Evidence
- PASS — `.mycodemap/rules/commit/`, `test/`, `lint/`, `docs/`, `validation/` 都会生成
- PASS — 生成内容不包含 `.planning/` 等 repo 私有路径
- PASS — 重跑可区分 `already-synced` 与 `conflict`
- PASS — AI context snippet 只以 manual action 暴露，不会改写 `CLAUDE.md` / `AGENTS.md`

## Issues Encountered
- 初版 `rule-templates.ts` 在模板字符串里未转义反引号，导致测试前的 transform 失败。
- 已通过转义规则文字中的 `` `any` `` / `` `unknown` `` 修复；逻辑实现无需返工。

## Deviations from Plan
None.

## Next Phase Readiness
- 文档与 docs guardrail 现在可以围绕真实的 `.mycodemap/rules/` / `init-last.json` / manual snippet contract 同步。
- Package smoke 可以直接验证 rules bundle 在 tarball 安装后仍然存在。

---
*Phase: 999.1-mycodemap-init-enhancements*
*Completed: 2026-04-21*
