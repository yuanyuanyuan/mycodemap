# Phase 999.1: mycodemap init enhancements — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-20
**Phase:** 999.1-mycodemap-init-enhancements
**Areas discussed:** 工作空间路径策略, Git hooks 安装策略, AI 护栏规则的内容与注入, init 命令的交互模式

---

## 工作空间路径策略

| Option | Description | Selected |
|--------|-------------|----------|
| mycodemap.config.json | 保持现有文件名，只是目录变了 | |
| config.json | 简化为 config.json，更短更干净 | ✓ |

**User's choice:** config.json
**Notes:** 放在 .mycodemap/ 命名空间下，冲突概率低

| Option | Description | Selected |
|--------|-------------|----------|
| 自动迁移（复制+提示） | 自动复制到 .mycodemap/ 并打印迁移提示 | ✓ |
| 提示用户手动迁移 | 打印迁移命令让用户执行 | |
| 同时读取新旧位置（兼容模式） | 两个位置都支持读取，优先新位置 | |

**User's choice:** 自动迁移（复制+提示）
**Notes:** 零用户操作，自动复制内容到 .mycodemap/ 并提示删除旧文件

| Option | Description | Selected |
|--------|-------------|----------|
| 全部统一子目录 | 所有文件按功能放入 .mycodemap/ 下的子目录 | ✓ |
| 保持现有内部结构 | 只把配置文件移进来，其他保持现有扁平结构 | |

**User's choice:** 全部统一子目录
**Notes:** logs/、workflow/、rules/、storage/ 等子目录

| Option | Description | Selected |
|--------|-------------|----------|
| 保留并扩展 | 保留 .codemap → .mycodemap 提示，新增配置迁移提示 | |
| 清理旧提示，只保留新迁移 | 清理旧提示，只保留新的配置迁移提示 | ✓ |

**User's choice:** 清理旧提示，只保留新迁移
**Notes:** .codemap → .mycodemap 的迁移已经完成很久了

---

## Git hooks 安装策略

| Option | Description | Selected |
|--------|-------------|----------|
| 配置 core.hooksPath | 运行 git config core.hooksPath .mycodemap/hooks/ | |
| 复制到 .git/hooks/ | 把 hooks 文件复制到目标项目的 .git/hooks/ | ✓ |
| 两者结合：core.hooksPath + shim | .git/hooks/ 放 shim 调用两边 | |

**User's choice:** 复制到 .git/hooks/，但要提示用户如何处理已有 hooks 的冲突
**Notes:** 用户明确：选择复制方式，但要有冲突检测和提示机制

| Option | Description | Selected |
|--------|-------------|----------|
| 检测并提示覆盖/跳过 | 扫描已有 hooks，提示用户选择覆盖、跳过或合并 | ✓ |
| 直接覆盖，不做提示 | 假设 mycodemap 的 hooks 是标准 | |
| 跳过，不做处理 | 只在确认没有 hooks 时才安装 | |

**User's choice:** 检测并提示覆盖/跳过

| Option | Description | Selected |
|--------|-------------|----------|
| 完整复制 | 复制 pre-commit 和 commit-msg 全部内容 | ✓ |
| 最小集合 | 只复制 commit-msg 和最小 pre-commit | |
| 可定制菜单 | 列出功能让用户勾选 | |

**User's choice:** 完整复制

| Option | Description | Selected |
|--------|-------------|----------|
| 每次 init 检查同步 | 运行时检测 hooks 是否最新 | ✓ |
| 不自动同步，文档提示 | 安装后不再自动管理 | |
| 通过命令触发同步 | 提供 sync 子命令手动同步 | |

**User's choice:** 每次 init 检查同步

---

## AI 护栏规则的内容与注入

| Option | Description | Selected |
|--------|-------------|----------|
| 从 CodeMap 自身 docs/rules/ 生成 | 根据项目类型从 docs/rules/ 选择 | |
| 内置精简规则集 | 通用精简规则，不依赖项目类型 | ✓ |
| 两者结合 | 动态生成 + 精简备选 | |

**User's choice:** 内置精简规则集

| Option | Description | Selected |
|--------|-------------|----------|
| 平铺（一个文件一种规则） | 如 commit-rules.md、test-rules.md | |
| 合并（一个总文件） | 一个 guardrails.md 包含所有 | |
| 分类子目录 | .mycodemap/rules/commit/、.mycodemap/rules/test/ 等 | ✓ |

**User's choice:** 分类子目录

| Option | Description | Selected |
|--------|-------------|----------|
| 在目标项目根目录生成/更新 CLAUDE.md | 自动修改用户 AI 上下文文件 | |
| 提示用户手动添加引用 | 输出提示信息，让用户自己操作 | ✓ |
| 设置 MYCODEMAP_RULES 环境变量 | 间接方式，可能不被所有工具支持 | |

**User's choice:** 提示用户手动添加引用
**Notes:** 不自动修改用户的 AI 上下文文件，保持用户控制权

| Option | Description | Selected |
|--------|-------------|----------|
| 每次 init 时检查同步 | 运行时检测规则是否最新 | ✓ |
| 独立的 sync 子命令 | 提供专门的 sync 命令 | |
| 不自动同步，文档提示 | 安装后不再自动管理 | |

**User's choice:** 每次 init 时检查同步

---

## init 命令的交互模式

| Option | Description | Selected |
|--------|-------------|----------|
| 逐步确认 | 每步单独确认 | |
| 一键选择 | 先展示概览，用户一次性选择 | ✓ |
| 默认全部 | 默认全做，用 --yes/--interactive 控制 | |

**User's choice:** 一键选择

| Option | Description | Selected |
|--------|-------------|----------|
| 扩展欢迎信息 | 在现有欢迎信息之后增加新步骤 | ✓ |
| 合并欢迎信息到 init | 首次运行时直接运行交互式 init | |
| 保持现有欢迎信息不变 | 两者解耦 | |

**User's choice:** 扩展欢迎信息

---

## Claude's Discretion

- 子目录的具体命名（遵循现有代码惯例）
- 精简规则集的具体内容（参考 docs/rules/ 提炼）
- 交互式提示的措辞和 CLI 输出格式（遵循现有 chalk + emoji + 中文风格）

## 遗漏点讨论

| Option | Description | Selected |
|--------|-------------|----------|
| 不更新其他命令，保持根目录兼容 | 根目录的 mycodemap.config.json 继续有效 | |
| 同步更新所有命令路径解析 | 所有命令统一使用 `.mycodemap/config.json`，init 自动迁移解决兼容 | ✓ |

**User's choice:** 同步更新所有命令路径解析
**Notes:** 旧根目录配置通过 init 自动迁移处理，无需运行时兼容

| Option | Description | Selected |
|--------|-------------|----------|
| A. 在已有 hooks 末尾追加 | 直接追加 mycodemap 的 hooks 调用 | |
| B. 用特定标记包裹 | `# MYCODEMAP-SECTION` 包裹，支持增量更新 | |
| C. shim 方案 | .git/hooks/ 放轻量 shim，调用 .mycodemap/hooks/ 的完整 hooks | ✓ |

**User's choice:** shim 方案
**Notes:** 不覆盖用户已有 hooks 逻辑，通过 shim 间接调用

| Option | Description | Selected |
|--------|-------------|----------|
| 静默跳过 hooks 安装 | 非 Git 项目只创建配置文件 | |
| 阻止 init 完成 | 输出错误，要求先初始化 Git | ✓ |

**User's choice:** 阻止 init 完成

| Option | Description | Selected |
|--------|-------------|----------|
| 不处理 gitignore | 由用户自行决定 | |
| 自动追加 `.mycodemap/logs/` | 确保本地日志不进入版本控制 | ✓ |

**User's choice:** 自动追加 `.mycodemap/logs/`

| Option | Description | Selected |
|--------|-------------|----------|
| 欢迎信息一次性展示全部步骤 | 首次运行就展示 hooks/rules 步骤 | |
| 两步流程：精简欢迎 + init 后展示完整步骤 | 未 init 时不显示无法执行的指引 | ✓ |

**User's choice:** 两步流程

---

## Deferred Ideas

- `mycodemap sync-rules` 独立子命令
- 根据项目类型动态生成不同规则集
- 自动检测并修改用户项目的 CLAUDE.md / AGENTS.md
- 非 Git 项目的 hooks 支持
