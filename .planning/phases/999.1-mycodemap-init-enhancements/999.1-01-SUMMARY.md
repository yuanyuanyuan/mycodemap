---
phase: 999.1-mycodemap-init-enhancements
plan: 01
subsystem: cli
tags: [cli, config, paths, migration, validation]
requires: []
provides:
  - canonical `.mycodemap/config.json` config discovery for shared CLI loaders
  - fresh default storage rooted at `.mycodemap/storage`
  - unit coverage for canonical-vs-root config precedence and legacy fallback behavior
affects: [init, generate, query, shared-config-loading, phase-999.1-02]
tech-stack:
  added: []
  patterns: [canonical-config-path, legacy-root-config-fallback, namespace-storage-default]
key-files:
  created:
    - .planning/phases/999.1-mycodemap-init-enhancements/999.1-01-SUMMARY.md
  modified:
    - src/cli/paths.ts
    - src/cli/config-loader.ts
    - src/cli/__tests__/paths.test.ts
    - src/cli/__tests__/config-loader.test.ts
key-decisions:
  - "`.mycodemap/config.json` becomes the shared canonical config path for runtime discovery."
  - "Root `mycodemap.config.json` and `codemap.config.json` remain readable only as legacy migration inputs."
  - "Fresh default storage moves under `.mycodemap/storage` so new projects stop recreating `.codemap/storage`."
patterns-established:
  - "Shared seam fix: update `resolveConfigPath()` / `loadCodemapConfig()` instead of patching commands one by one."
  - "Canonical-first precedence is locked by unit tests before `init` starts writing the new layout."
requirements-completed: [R7, R8, R9, R10, R34, R35, R36]
duration: 1 wave
completed: 2026-04-21
---

# Phase 999.1 Plan 01 Summary

**Canonical `.mycodemap` workspace/config foundation is now shared by CLI path resolution and config loading**

## Performance

- **Completed:** 2026-04-21
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Updated `src/cli/paths.ts` so shared config discovery prefers `.mycodemap/config.json`, falls back to root `mycodemap.config.json`, then root `codemap.config.json`, and defaults to the canonical path when no config exists yet.
- Updated `src/cli/config-loader.ts` so fresh default storage uses `.mycodemap/storage` instead of `.codemap/storage`.
- Extended `src/cli/__tests__/config-loader.test.ts` to cover canonical config precedence, root `mycodemap.config.json` fallback, root `codemap.config.json` fallback, and default storage normalization.
- Extended `src/cli/__tests__/paths.test.ts` to assert canonical config path reporting and fixed the fixture to create `.mycodemap/` before writing `config.json`.

## Task Commits

No commits were created in this session.

- Reason: current higher-priority session instructions explicitly prohibit autonomous `git commit`, so execution evidence is recorded in this summary instead of git history.

## Files Created/Modified
- `src/cli/paths.ts` - introduced canonical config helpers and canonical-first resolution order.
- `src/cli/config-loader.ts` - changed fresh default storage output to `.mycodemap/storage`.
- `src/cli/__tests__/paths.test.ts` - added canonical config assertions and fixed the canonical fixture directory setup.
- `src/cli/__tests__/config-loader.test.ts` - added canonical/root fallback cases and updated storage-path expectations.
- `.planning/phases/999.1-mycodemap-init-enhancements/999.1-01-SUMMARY.md` - captured Wave 1 execution evidence and handoff context.

## Decisions Made
- The runtime truth for new projects is `.mycodemap/config.json`, not any root config filename.
- Backward compatibility remains explicit: root config files are still readable, but only after canonical config is absent.
- Shared helpers remain the migration seam, which keeps downstream commands untouched until later plans actually need behavior changes.

## Verification
- `./node_modules/.bin/vitest run src/cli/__tests__/paths.test.ts src/cli/__tests__/config-loader.test.ts` — PASS (`13` tests)
- `./node_modules/.bin/vitest run src/cli/__tests__/generate.test.ts src/cli/commands/__tests__/generate.test.ts` — PASS (`21` tests)
- `npm run typecheck` — PASS

## Acceptance Criteria Evidence

### Task 1
- PASS — canonical `.mycodemap/config.json` outranks both root config files in `src/cli/config-loader.ts`
- PASS — no-config defaults report canonical config path in `src/cli/paths.ts`
- PASS — fresh default storage path is `.mycodemap/storage`
- PASS — explicit configured `output` / `storage.outputPath` behavior remains covered by existing config-loader tests

### Task 2
- PASS — tests fail if canonical config is missing or outranked by root config
- PASS — root `mycodemap.config.json` fallback is covered
- PASS — root `codemap.config.json` legacy fallback is covered
- PASS — adjacent `generate` tests still pass without command-specific rewrites

## Deviations from Plan

Minor verification expansion only:

- Added targeted `generate` test runs beyond the plan’s minimum verification set to confirm the shared config seam change did not regress downstream callers.

## Issues Encountered
- Initial test run failed before assertions because `src/cli/__tests__/paths.test.ts` wrote `.mycodemap/config.json` without creating the parent directory.
- Fixed by adding `mkdirSync(path.join(rootDir, '.mycodemap'), { recursive: true })` in the test fixture; production code did not require changes.

## User Setup Required
None.

## Next Phase Readiness
- Wave 2 can now safely make `init` write `.mycodemap/config.json` because shared loaders already understand that canonical location.
- Receipt/migration work in Plan 02 can treat root config files as legacy sources without redefining global config resolution.

## Self-Check: PASSED
- Summary file exists at `.planning/phases/999.1-mycodemap-init-enhancements/999.1-01-SUMMARY.md`
- Wave 1 verification commands completed successfully
- No autonomous commit was made, consistent with session policy

---
*Phase: 999.1-mycodemap-init-enhancements*
*Completed: 2026-04-21*
