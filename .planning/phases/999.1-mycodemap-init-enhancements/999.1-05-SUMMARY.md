---
phase: 999.1-mycodemap-init-enhancements
plan: 05
subsystem: docs
tags: [docs, guardrails, package-smoke, init-contract]
requires: [999.1-03, 999.1-04]
provides:
  - synced human + AI docs for the `.mycodemap/` init contract
  - docs guardrails that enforce canonical config / receipt / rules-snippet truth
  - tarball smoke evidence that packaged CLI ships hook and rules assets
affects: [README, AI_GUIDE, setup-docs, validate-docs, package-validation, planning-truth]
tech-stack:
  added: []
  patterns: [canonical-config-doc-sync, docs-guardrail-evidence, packaged-init-smoke]
key-files:
  created:
    - mycodemap.design.md
    - .planning/phases/999.1-mycodemap-init-enhancements/999.1-05-SUMMARY.md
  modified:
    - README.md
    - AI_GUIDE.md
    - docs/SETUP_GUIDE.md
    - docs/AI_ASSISTANT_SETUP.md
    - docs/ai-guide/COMMANDS.md
    - docs/ai-guide/QUICKSTART.md
    - docs/ai-guide/PATTERNS.md
    - docs/rules/validation.md
    - mycodemap.config.schema.json
    - scripts/validate-docs.js
    - src/cli/__tests__/validate-docs-script.test.ts
key-decisions:
  - "Public docs now present `.mycodemap/config.json` as canonical truth and root config files as legacy migration sources."
  - "Docs guardrails explicitly enforce receipt path, rules-bundle mention, and manual `CLAUDE.md` / `AGENTS.md` snippet behavior."
  - "Package smoke is validated from the packed tarball, not from the source checkout."
patterns-established:
  - "Docs sync pattern: README / AI_GUIDE / setup docs / AI guide docs / guardrail script all move together."
  - "Smoke pattern: `npm pack` → temp-prefix install → temp git repo → `mycodemap init --yes` → inspect `.mycodemap/` artifacts."
requirements-completed: [R4, R5, R23, R40, docs-sync, package-smoke]
duration: 1 plan
completed: 2026-04-21
---

# Phase 999.1 Plan 05 Summary

**文档、guardrail 与 tarball smoke 已对齐到新的 `init` 收敛契约**

## Accomplishments
- 同步 `README.md`、`AI_GUIDE.md`、`docs/SETUP_GUIDE.md`、`docs/AI_ASSISTANT_SETUP.md`、`docs/ai-guide/COMMANDS.md`、`docs/ai-guide/QUICKSTART.md`、`docs/ai-guide/PATTERNS.md`，统一使用 canonical `.mycodemap/config.json` / `.mycodemap/status/init-last.json` / `.mycodemap/rules/` 术语。
- 更新 `docs/rules/validation.md`、`mycodemap.config.schema.json` 与 `scripts/validate-docs.js`，让 docs guardrail 能抓到 root config canonical drift、receipt drift、rules snippet drift。
- 更新 `src/cli/__tests__/validate-docs-script.test.ts`，适配新的 canonical config contract。
- 补充 `mycodemap.design.md` 最小 repo-root contract，使 `docs:check` 在当前仓库重新具备可运行基线。
- 完成 tarball smoke：`npm pack` 后在临时 prefix 安装，再在临时 Git repo 执行 `mycodemap init --yes`，确认 config / receipt / hooks / rules 都能从安装包形态落地。

## Task Commits

No commits were created in this session.

## Verification
- `npm run docs:check` — PASS
- `./node_modules/.bin/vitest run src/cli/__tests__/validate-docs-script.test.ts` — PASS (`23` tests)
- `npm run build` — PASS
- `npm pack` → temp-prefix install → temp Git repo `mycodemap init --yes` — PASS

## Acceptance Criteria Evidence
- PASS — README / AI docs 现在都把 `.mycodemap/config.json` 写成 canonical init truth
- PASS — docs guardrail 已覆盖 `.mycodemap/status/init-last.json`
- PASS — docs 明确说明 `init` 不会自动改写 `CLAUDE.md` / `AGENTS.md`
- PASS — tarball smoke 证明安装包内含 hooks / rules 资产，并能落地 receipt

## Issues Encountered
- `npm run check:all` 未能完成收口，但 blocker 与本 phase 无关：`src/cli/__tests__/gsd-milestone-followup.test.ts` 依赖的 `.claude/get-shit-done/bin/gsd-tools.cjs` 在当前仓库不存在，导致该组既有测试失败。
- 已确认这不是本轮 init/docs 改动引入的问题；定点验证、docs:check、build、package smoke 均已通过。

## Deviations from Plan
- 额外更新了 `docs/rules/validation.md`、`mycodemap.config.schema.json` 与新增 `mycodemap.design.md`，因为它们分别属于 docs guardrail 真相、配置默认值真相与 `docs:check` 的现有仓库基线依赖。

## Next Phase Readiness
- Phase 999.1 的公开 contract、machine-readable receipt、hooks/rules pack shape 与 AI setup guidance 已全部闭环。
- 唯一剩余全量回归 blocker 是仓库既有 GSD 测试环境缺少 `gsd-tools.cjs`，需单独修复或恢复。

---
*Phase: 999.1-mycodemap-init-enhancements*
*Completed: 2026-04-21*
