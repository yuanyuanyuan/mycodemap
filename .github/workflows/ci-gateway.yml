name: CI Gateway

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  ci-gateway:
    name: CI Gateway Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Check commit format
        run: npx mycodemap ci check-commits --range origin/main..HEAD

      - name: Check commit size
        run: npx mycodemap ci check-commit-size --range origin/main..HEAD

      - name: Check file headers
        run: npx mycodemap ci check-headers

      - name: Generate AI feed and verify sync
        run: |
          npx mycodemap generate
          git diff --exit-code .mycodemap/ai-feed.txt || (echo "ERROR: AI feed is out of sync. Run 'mycodemap generate' and commit changes." && exit 1)

      - name: Assess risk
        run: npx mycodemap ci assess-risk --threshold=0.7

      - name: Check output contract
        run: npx mycodemap ci check-output-contract --schema-version v1.0.0 --top-k 8 --max-tokens 160
